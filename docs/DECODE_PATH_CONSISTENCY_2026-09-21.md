# Decode 调度与回退路径一致性定位

状态：独立MRoPE候选待验收，旧组合已撤下；更新 2026-09-22。父版 7364767。
代码和原始 HTTP 记录是事实来源，本报告不替代精度或性能验收。
所有证据目录位于 `.q4t-work/e2e/`，实验都使用 tools/evalscope
真实 HTTP，必要构建后没有前置单测、数值测试或 profile。

## 当前结论与历史记录边界（2026-09-22）

0511 组合候选已因九次独立启动的首请求 decode 回退而撤下，
运行时补丁和二进制完整归档于
`.q4t-work/prepared/linear-scratch-isolated-20260922/`。
其 FP32 GDN、packed ROWS4、GEMV/indexer 和 RoPE 改动均未接受；
下面使用“当前”“继续运行”的旧条目描述的是当时实验状态。
完整失败记录保留，不能转作现版本验收证据。

独立分配候选4d4d82df已完成严格质量11次、原矩阵15次及
三轮九服务135次HTTP原始核对，输出一致，但40项对照中8K及
200K档首请求decode对固定参考不利分离，未通过性能门禁。
对正式父版范围重叠，不能据此把差异归因于分配改动。
独立分配候选源码与二进制已归档并撤下，完整结果见
`linear-scratch-isolated-restarts-20260922/raw-response-review.json`，
决定见 `prepared/linear-scratch-isolated-20260922/rejection.json`。

当前仅重新引入批处理decode MRoPE坐标写入，二进制03ecfa15，
构建零警告、首项质量HTTP11/11原始核对通过，输出同正式参考。
性能PID2124707在1K完整输出不同后退出1，服务退出0；三次
输出256且彼此一致，但正式参考从第72个字符开始不同。其余
四档未执行。证据decode-rope-isolated-performance-20260922。
PID2126567正在进行同1K/256的正式父版回退、候选调度、候选
回退与调度重启HTTP对照，仅用于失败定位，未改参考或进入
低层测试。最新状态见 [STATUS](STATUS.md)。

**原 decode/MTP 一致性问题没有修复。** 恢复正式运行时也恢复了
已定位的原路径差异；独立分配实验只隔离内存所有权的影响，不证明
调度路径、MTP 或正式数值精度问题已经闭合。后续应逐项重新引入
有独立证据的修复，避免把整个组合候选的结果归因于其中单个改动。

## 单次分配 44K 与后续条件（2026-09-22）

单次分配候选 44K 三次 HTTP 已完成原始核对，TTFT 均值
29.166841 秒，decode 17.846997 tok/s；对正式/固定参考总体
及后续样本范围无不利分离，完整输出仍同直接前版、不同正式
参考。对上一未接受 ROWS8 后续 decode 的差异保留。200K
继续运行于原 PID 2064172，未修改运行时或并发启动服务。

已启动后续驱动 PID 2067359，等待完整矩阵终态及全部 15
原始响应审查，冻结总体/后续范围的全部疑点。如果没有 8K
以外的新疑点，才执行既定九次独立启动；如果其他档位也有
不利分离，则记录 additional_adverse_contexts 并停止，避免
只测 8K 遗漏问题。目录
`linear-scratch-single-allocation-restarts-20260922`，当前仅
等待，未执行复核请求。下一步核对 200K 终态和实际接续条件；
当前性能、正式精度、消费者与真实容量门禁均未全部闭合。

## 单次分配前三档结果（2026-09-22）

单次分配候选 0511 的前三档 9 HTTP 已原始核对：TTFT
均值 0.777063/2.519641/4.937801 秒，decode
18.724877/18.016616/18.051707 tok/s。完整输出同直接前版
packed ROWS4，仍不同正式参考。

1K/4K 后续 decode 低于直接前版，但对正式/固定参考无不利
范围分离。8K 三次 decode 17.916962/18.090362/18.147797；
后两次范围与正式父版重叠，仍低于固定参考后续样本。当前
不能宣称合并分配提速或消除了性能疑点，全部样本保留。

原 PID 2064172 继续 44K/200K，运行时未改。当前 SHA 专用的
九次独立启动方案已准备于
`prepared/linear-scratch-single-allocation-restarts-20260922`，
参考二进制 SHA 均核对，尚未运行；完整矩阵结束后再决定
相邻/首请求复核。未进行低层测试或 profile。

## 单次分配质量通过（2026-09-22）

单次分配候选 0511 的首项严格质量 HTTP 已完成，11/11
（含 200K）通过。SQLite 原始请求、响应全文、长度、finish
和公式对固定质量参考逐项核对通过，服务/驱动退出 0，原
PID 2063145 已退出。证据 `linear-scratch-single-allocation-20260922`。

串行后续 PID 2064172 已实际启动完整五档 HTTP，证据
`linear-scratch-single-allocation-performance-20260922`。记录
正式父版/固定参考与直接前版 packed ROWS4 的完整比较，
所有失败样本保留。运行时未改、SHA 未变，未做低层数值、
单测或 profile。下一步核对 15 次原始响应和首请求/decode
表现；质量通过不证明正式性能负载精度、所有消费者或峰值
内存验收，候选仍未接受。

## 四列终态与单次分配候选（2026-09-22）

packed 四列 a3a7 完整五档 15 HTTP 已结束，原始 SQLite
响应/长度/finish/公式核对通过，服务/驱动退出 0，原 PID
2057930 已退出。均值如下，全部原样本保留：

| 输入 token | TTFT 秒 | decode tok/s |
|---|---:|---:|
| 1024 | 0.782584 | 18.772660 |
| 4096 | 2.519741 | 18.089391 |
| 8192 | 4.943273 | 18.055086 |
| 45056 | 29.213521 | 17.849156 |
| 204800 | 153.517242 | 17.069304 |

8K 后续 decode 对正式/固定参考仍不利分离，四列本身未满足
验收；完整输出同此前 FP32 候选，仍不同正式参考。其独立
重启方案未执行，保留未闭合状态，不以其他档位收益抵消。

确认原进程及所有 q4t* serve 都已退出、核对源码/二进制后，
应用 `prepared/linear-scratch-single-allocation-20260922`。本次
仅把 LinearAttentionScratch 六块同作用域缓冲改为单个 owner
和六个不重叠的 256-byte 对齐 slice，仍独立于 cuBLASLt
workspace，保留 raw/norm/dump 尾部容量与同 stream 释放。
新增容量溢出检查；不改 kernel 算术、调用顺序或当前 packed
ROWS4 分派，以单独检验分配生命周期这一变量。源代码分配/
释放次数各 6→1，不等于已测开销或峰值收益。

零警告构建退出 0，第一项严格质量 HTTP 已启动，PID 2063145，
证据 `linear-scratch-single-allocation-20260922`，新二进制：
`0511c791c509fefb95ec66b215535147044098f0ad2d0de839433f2e0fe3a6d8`。
原源码、二进制和完整失败审查已归档，未进行前置数值、单测
或 profile。下一步核对质量原始响应，通过后完成五档及相邻/
首请求复核，后续再验证消费者、精度和真实容量；候选未接受。

## 四列 44K 与临时内存候选（2026-09-22）

packed 四列候选 44K 三次 HTTP 已原始核对，TTFT 均值
29.213521 秒、decode 17.849156 tok/s，完整输出同八列候选，
仍不同正式参考。对正式/固定参考未见总体或后续范围不利
分离；对上一未接受八列候选的后续 TTFT/decode 差异保留。
8K decode 疑点未消除，原 PID 2057930 继续 200K，运行时不变。

源码审查发现 LinearAttentionForward 六个临时数组在相同作用域
分别 cudaMallocAsync/cudaFreeAsync。已准备
`prepared/linear-scratch-single-allocation-20260922`：一个 RAII
owner、六个不重叠且起点按 256 字节对齐的 slice，仍与传给
cuBLASLt 的 GEMM workspace 分离。保留 raw/norm 复用与 dump
尾部需求、stream 顺序和全部 kernel 算术/调度；加字节容量
溢出检查。源码分配/释放各 6→1，但不宣称已测性能或峰值收益。
候选基于当前 a3a7，源文件 SHA、前后源码和 patch 已保存，
尚未应用、构建或运行。下一步先完成当前五档与原始审查，
再决定是否用该生命周期改动解决剩余开销，不提前做低层测试。

## Packed 四列前三档（2026-09-22）

packed 四列候选 a3a7 的前三档 9 HTTP 已完成原始响应与
公式核对：TTFT 均值 0.782584/2.519741/4.943273 秒，decode
18.772660/18.089391/18.055086 tok/s。完整输出同上一八列
融合候选，仍不同正式参考。

8K 后两次 decode 18.102568–18.108386，对正式父版原始后续
样本 18.124699–18.136413、固定参考 18.189877–18.226861
均不利分离，也低于上一八列完整矩阵后续样本。首请求为
17.954303 tok/s。当前不能声称四列消除了回退；4K/8K 后续
TTFT 对上一未接受八列的细微差异同样保留，不改正式参考。

原 PID 2057930 继续 44K/200K，未中断采集或修改运行时。
已准备当前 SHA 的原九次独立启动方案于
`prepared/gdn-packed-rows4-restarts-20260922`，未执行，需
完整矩阵终态及原始审查后再决定。下一步核对余下两档，
依据完整证据决定相邻复核或撤回；未进行低层测试/profile。

## Packed 四列质量通过，五档采集中（2026-09-22）

packed decode 四列候选 a3a7 的首项严格质量 HTTP 已结束，
11/11（含 200K）通过，原始 SQLite 请求、响应全文、长度、
finish 和时间公式对固定质量参考核对通过；服务/驱动退出 0，
原 PID 2056145 已退出。证据 `gdn-packed-rows4-20260922`。

串行后续 PID 2057930 已实际进入完整五档 HTTP，证据
`gdn-packed-rows4-performance-20260922`；SHA 未变，正式父版/
固定参考和全部旧失败样本保留，额外比较上一融合 ROWS8。
尚未进行低层数值、单测/profile。下一步核对五档 15 原始请求、
decode 性能及首请求疑点；质量题通过不覆盖正式性能负载的
输出差异，新二进制消费者仍待复验，候选未接受。

## 独立重启终态与 packed 四列候选（2026-09-22）

融合候选 7328 的独立重启复核已完成：九服务/27 HTTP 原始
响应、长度、finish 和公式核对通过，九服务/驱动退出 0，原
PID 2045937 已退出。但 decode 对正式父版和固定参考均回退：

| 样本 | 候选 tok/s | 正式父版 | 固定参考 |
|---|---:|---:|---:|
| 三次加载后首请求 | 17.867986–17.908244 | 17.945492–17.958143 | 18.019315–18.100586 |
| 六次后续请求 | 18.075791–18.103263 | 18.130026–18.178307 | 18.120634–18.184957 |

TTFT 保留改善，但不能抵消上述回退；不以之前五侧相邻运行
未见回退覆盖本次结果，不更换或删除任何样本。7328 不接受，
其已通过的 MTP 用例仍只是该二进制的证据。fallback 准备方案
未执行，性能门禁仍未闭合。

确认所有推理服务退出，核对源文件/二进制 SHA 并归档之后，
应用 `prepared/gdn-packed-rows4-20260922`：仅无显式环境选择
时的 packed decode 默认 ROWS8→4，prefill/causal 保持 ROWS8，
显式 Q4T_GDN_REG 保持原行为；共同 FP32 公式、每列递推和
checkpoint 不改。源码上每 lane 状态 32→16 个 float、目标
packed CTA 数翻倍，实际资源、数值和性能尚未证明。这与此前
全路径默认 ROWS4 导致 prefill 回退的实验范围不同。

零警告构建退出 0，首项严格质量 HTTP 已启动，PID 2056145，
证据 `gdn-packed-rows4-20260922`。新二进制 SHA：
`a3a7be9b7521b5851b2052d4eef69e214ffe6b912e4f5bd89953e9a64de3a2c6`。
未做前置数值、单测或 profile。下一步核对质量原始响应，通过
后完成完整五档及首请求复核，正式精度和新二进制消费者仍待
闭合；候选未提交、未接受，正式参考不变。

## 当前二进制 MTP HTTP 终态（2026-09-22）

当前 7328 二进制的 plain-before/MTP/plain-after 27 HTTP
全部完成，三档 1024/8192/45056 各三次、实际输出均 32 token。
SQLite 原始响应全文、长度、finish、公式核对通过，三组输出
全部一致且重复确定，三服务/驱动退出 0，原 PID 2040662 已
退出。启动/调度日志确认 MTP 实际加载并执行 B=1。

| 模式 | 1K TTFT 秒 / decode tok/s | 8K | 44K |
|---|---:|---:|---:|
| plain-before | 0.767435 / 18.705846 | 4.927153 / 17.998524 | 29.170072 / 17.851403 |
| MTP | 0.935550 / 32.209150 | 5.488467 / 31.246154 | 30.648607 / 27.636013 |
| plain-after | 0.762651 / 18.785542 | 4.923157 / 17.996559 | 29.142662 / 17.858342 |

以上仅为本次 32-token 消费者用例，不能混入输出 256 的正式
性能矩阵；MTP TTFT 较高，两模式分组保留，默认仍关闭 MTP。
没有临时层/状态追踪，不证明所有 checkpoint 分支或内部状态
等价，更不覆盖正式精度、批处理或视觉验收。证据
`gdn-packed-prep-fusion-mtp-http-20260922`。

串行驱动 PID 2045937 已实际开始独立重启首请求复核，三种
二进制各三个服务实例，每实例三个 8192/256 HTTP，轮换顺序，
区分首请求与后续请求，全部样本保留。证据
`gdn-packed-prep-fusion-restarts-20260922`。下一步核对全部
27 请求和九个服务终态，判断首请求疑点；运行时不变，未进行
低层测试/profile，候选仍未接受。

## 双参考相邻复测终态及首请求限制（2026-09-22）

8K 五组相邻 HTTP 全部完成，15 请求原始响应、输出长度和
公式核对通过，五服务/驱动退出 0，原 PID 2038070 已退出。
候选后两次 decode 18.187956–18.207145 tok/s；前侧固定参考
18.132552–18.165377、前侧父版 18.112219–18.115169、后侧
父版 18.148759–18.171393、后侧固定参考 18.164114–18.166066。
总体和后续样本范围对四侧均无不利分离，原 8K 后续回退未在
本次相邻运行重现；原始矩阵的差异依然保留，不替换基线。

另有未闭合限制：候选服务加载后的首请求 decode 17.930093，
四侧参考首请求分别 18.017675/17.980354/17.961421/18.012962，
候选均稍慢。每个服务只有一个加载后首请求，不能据总体范围
重叠或后续请求改善证明首请求无回退；需要独立重复启动的
真实 HTTP 复核，不能宣称完整性能验收已闭合。

按此前已设置的总体/后续范围条件，串行驱动 PID 2040662
已启动同二进制 plain/MTP/plain 27 HTTP，目前第一侧 plain
1K 已采集。此检查仍属 E2E 消费者对照，非低层数值/profile，
不覆盖上述首请求疑点或正式精度差异。证据
`gdn-packed-prep-fusion-mtp-http-20260922`。下一步核对全部
消费者响应，同时准备首请求独立重复启动对照，待当前服务
退出再执行；运行时保持不变，候选未接受。

## 融合候选完整五档与相邻复测（2026-09-22）

融合候选 7328 完整五档 15 HTTP 已结束，服务/驱动及后续
审查退出 0，原 PID 2035718 已退出。原始 SQLite 请求/全文/
长度/finish/时间公式逐项核对通过，所有原样本保留。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.782818 | 18.746364 |
| 4096 | 2.517908 | 18.069082 |
| 8192 | 4.938806 | 18.077800 |
| 45056 | 29.174687 | 17.923020 |
| 204800 | 153.487542 | 17.148885 |

200K TTFT 对正式父版均值降低 3.761%，44K 降低 4.250%。
对正式父版/固定参考，五档三次总体范围均无不利分离；后续
两次的疑点并集只有 8K decode 对固定参考。全部完整输出同
上一候选 e216，仍不同正式参考，质量 11/11 不能覆盖这个
正式精度缺口。完整矩阵完成不等于当前运行时验收通过。

串行驱动 PID 2038070 已冻结上述原始审查和选择原因，仅选
8192，实际启动五组相邻 HTTP：固定参考、正式父版、候选、
正式父版、固定参考，各三次输出 256。第一侧启动日志确认
max_seq=1/max_len=208896/MTP off，未并行运行其他推理服务。
证据 `gdn-packed-prep-fusion-reference-pair-20260922`。下一步
复核五侧全部原始响应和终态，再判断 8K 性能疑点；运行时
保持不变，未开始数值、单测或 profile，其他消费者仍待复验。

## 融合候选 44K 结果（2026-09-22）

融合候选 44K 三次 HTTP 已完成，SQLite 原始响应、256-token
输出和时间公式核对通过。TTFT 均值 29.174687 秒，decode
17.923020 tok/s；相对 e216，TTFT 范围重叠、decode 均值增加
约 0.64%。对正式父版/固定参考，总体及后续请求范围无不利
分离。完整输出同 e216，仍不同正式参考，不据性能改善宣称
精度通过。

原串行驱动 PID 2035718 确认存活，200K 三请求已开始，未重启
或修改运行时。8K 后续 decode 对固定参考的疑点仍保留，待
完整五档后执行已准备的两参考相邻复测。另外准备了当前 SHA
专用的 plain/MTP/plain 三侧 27 HTTP 脚本，未执行；位置
`prepared/gdn-packed-prep-fusion-mtp-http-20260922`，不会把
旧 a95 的 MTP 结果算作本候选通过。下一步核对 200K 终态、
全部 15 原始响应和性能疑点并集，再推进相邻复测。

## 融合候选前三档及固定参考疑点（2026-09-22）

融合候选已完成前三档各三次 HTTP，原始响应和公式逐项核对。
1K/4K/8K TTFT 均值 0.782818/2.517908/4.938806 秒，decode
18.746364/18.069082/18.077800 tok/s；完整输出同 e216，仍不同
正式参考。8K 后两次 decode 18.147563–18.166654，较 e216 的
18.078405–18.092208 提高，也高于正式父版原始后两次样本，
但低于固定 fcb5925 的 18.189877–18.226861。首请求较慢的
样本仍保留，不据此判定性能门禁通过。

完整矩阵仍运行于 PID 2035718；尚未做数值、单测或 profile。
为区分固定参考的历史样本和当下机器状态，准备了五组相邻
HTTP：fixed-before、parent-before、candidate、parent-after、
fixed-after。三份二进制 SHA 已核对；长度列表须完整五档结束
后按疑点并集确定，目前未启动、未替换原样本。脚本位于
`prepared/gdn-packed-prep-fusion-reference-pair-20260922`。
下一步完成 44K/200K 和原始复核，再决定执行相邻对照。

## 融合候选质量通过，性能采集中（2026-09-22）

融合候选 7328 的首项严格质量 HTTP 已结束，11/11（含 200K）
通过；SQLite 原始请求、响应全文、长度、finish 与固定 7364767
质量参考逐项核对通过，服务/驱动均退出 0，原 PID 2034704 已
退出。此结果不解决正式性能负载的精度差异，也不证明 MTP、
批处理或视觉路径全部通过。

串行驱动 PID 2035718 已实际进入完整五档性能 HTTP；证据
`gdn-packed-prep-fusion-performance-20260922`。二进制仍为
`7328bd2c9a7c9a70d1849367e481fbbe17cb331a3da5805fd597c92c28ca5373`，
运行时未改，未进行数值/单测/profile。下一步核对全部 15 次
原始响应、8K decode 及各档 TTFT；继续保留 e216 和正式参考
对照、全部样本及第一/后续请求区分，当前候选尚未接受。

## Packed decode 准备融合候选（2026-09-22）

共享系数 8K 三侧复测全部 9 HTTP 完成，三服务/驱动退出 0，
原始 SQLite 响应、完整输出和公式已复核。候选后两次 decode
18.073747–18.075165 tok/s，前侧父版 18.140078–18.163494，
后侧 18.161286–18.175768，两侧均不利分离；首请求也各自较慢。
三次总体范围重叠不能覆盖这个疑点。候选 TTFT 改善，但不能
抵消 decode 回退，当前共享系数 e216 不接受。证据：
`gdn-shared-coefficients-parent-pair-20260922`，所有样本保留。

确认驱动 PID 2030766 已退出且无推理服务后，应用下一候选
`prepared/gdn-packed-prep-fusion-20260922`：将 packed decode
的 norm/alpha/beta 准备合入 register scan，跳过该布局的独立
prep launch；prefill/causal 保留共享准备。抽取共同 FP32
归一化、alpha/beta device helper，保留每列递推、checkpoint、
slot 寻址、默认 ROWS8 与 fallback。减少启动是源码推导，
实际性能和数值等价未证明；也不能把先前 FP32 版本的 MTP
结果自动转移到新版本。原源码、二进制和 patch 已归档。

必要构建退出 0、零警告，首项测试已启动严格质量 HTTP，PID
2034704，证据 `gdn-packed-prep-fusion-20260922`。二进制：
`7328bd2c9a7c9a70d1849367e481fbbe17cb331a3da5805fd597c92c28ca5373`。
未运行前置数值、单测或 profile。下一步核对质量原始响应，
通过后完成五档性能；正式精度、MTP/批处理/视觉仍待闭合。

## 共享系数完整五档与相邻复测（2026-09-22）

共享系数候选 e216 的完整五档 15 HTTP 已结束，原驱动 PID
2026051 已退出，服务/驱动退出 0。SQLite 原始响应、实际输入/
输出长度、finish、完整文本和时间公式逐项复核通过。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.783585 | 18.671634 |
| 4096 | 2.518001 | 17.997374 |
| 8192 | 4.939127 | 18.008415 |
| 45056 | 29.180992 | 17.808605 |
| 204800 | 153.621633 | 17.134907 |

200K TTFT 均值比正式父版降低 3.677%，44K 降低 4.229%；不是
正式接受结论。三次总体范围对正式父版/固定参考无不利分离，
但 8K 后两次 decode 对两参考均不利分离。全部原样本保留，
不以首请求拉宽范围覆盖疑点。完整输出同此前 FP32 候选，仍
不同正式参考，正式精度门禁未闭合。

依据完整和后续请求范围的疑点并集，仅选 8192，已启动
`gdn-shared-coefficients-parent-pair-20260922`（PID 2030766）：
父版—候选—父版，每侧三次、输出 256，仍是实际 HTTP。原始
完整矩阵、选择原因及三份审查结果已冻结到复测目录。启动前
确认无推理服务并核对两侧二进制 SHA；不修改运行时、不做
前置数值/单测/profile。下一步复核三侧终态及所有响应，判断
8K decode 疑点；MTP 和正式精度审查仍待完成。

## 共享系数前四档及 decode 疑点（2026-09-22）

共享系数候选已完成 1K/4K/8K/44K 各三次原始 HTTP 复核：
TTFT 均值 0.783585/2.518001/4.939127/29.180992 秒，decode
18.671634/17.997374/18.008415/17.808605 tok/s。完整输出同
此前 FP32 候选，仍不同正式参考。44K TTFT 对正式父版均值
30.469618 秒约减少 4.2%，其三次范围有利分离；完整五档未结束，
不宣称稳定全面收益或正式接受。

前三次总体范围对正式父版/固定参考无不利分离，但单列后续
请求发现 8K decode 候选 18.078405–18.092208，正式父版
18.124699–18.136413 tok/s，不利分离，对固定参考也如此。
原三个样本均保留，首请求与后续请求另列，并标注每档首请求
是否是服务加载后的首请求；不让较慢首请求拉宽范围掩盖差异。
证据 ordered-samples-review.json/review_ordered.py，当前部分结果。

已准备（未执行）prepared/gdn-shared-coefficients-parent-pair-20260922，
长度暂空，须完整终态后由总体或后续请求的不利档位确定，再跑
旧→新→旧。另有同候选 27 请求 plain/MTP/plain 脚本仅准备。
200K 仍由原 PID 2026051 运行，无重叠推理、源码/二进制未变。
下一步收齐矩阵，复核 decode 疑点，精度门禁仍不以质量题通过覆盖。

## 共享系数候选质量终态（2026-09-22）

共享系数候选 `e216ddaf…` 首项质量 HTTP 已完成 11/11，含
200K；原始 SQLite 请求/响应、实际长度、finish、时间逐项复核，
完整质量输出同 7364767，服务/驱动退出 0。证据
`gdn-shared-coefficients-20260922/raw-quality-audit.json`。

完整五档性能采集已启动，PID 2026051，证据
`gdn-shared-coefficients-performance-20260922`。单流/MTP 关闭、
三次重复、输出 256、固定输入/正式参考不变，同时保留清理
八列、四列及其他失败候选对照。原始质量复核/相同二进制/
无其他服务是启动条件。暂不声称性能收益或正式精度通过，
未进行前置单测、数值测试或 profile。下一步收齐完整矩阵。

## 四列完整矩阵失败，应用共享系数候选（2026-09-22）

四列状态版本 `gdn-factor-rows4-performance-20260922` 的完整
15 HTTP 已结束，服务/驱动退出 0，原始 SQLite 响应及公式全部
复核通过，但性能门禁失败。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.820759 | 18.737798 |
| 4096 | 2.681665 | 18.041622 |
| 8192 | 5.266038 | 18.049966 |
| 45056 | 30.993977 | 17.836345 |
| 204800 | 161.692556 | 17.108040 |

4K/8K/44K/200K TTFT 对正式父版和八列清理版均不利分离；各档
decode 对这两组无不利分离。完整输出同此前 FP32 候选，仍不同
正式参考。四列方案不接受，全部样本保留，不更换基线。

确认原 PID 2021932 已退出且无推理服务后，应用
`prepared/gdn-shared-coefficients-20260922`：恢复八列，按
(t,key-head) 准备 norm、按 (t,value-head) 准备 FP32 alpha/beta，
在原准备 kernel 内完成，scan 共享系数。表达式及每列递推/
checkpoint 保持，数值等价尚未实测。复用同 stream raw QKV
最后消费者之后的存储；普通模式目标 T=8192 raw 分配 160 MiB，
4 MiB 系数包含于其中；dump 模式保留原数据，分配 164 MiB。
这些是源码容量/调用数推导，不是峰值、带宽、指令或速度实测。

factor-alias 单独方案未独立运行，作为本候选的存储布局组成；
不能把两者收益分开归因。原四列源码/二进制与完整组合 patch
已归档，构建正在进行，首项仍为正式质量 HTTP。未进行前置
单测、数值或 profile。下一步完成构建并按 E2E 验证，正式参考不变。

## 四列状态候选质量终态（2026-09-22）

四列状态候选 `e9b79d10…` 首项质量 HTTP 完成 11/11，含
200K；原始 SQLite 请求、响应、长度、finish 与时间逐项复核，
完整质量输出同 7364767，服务与驱动退出 0。证据
`gdn-factor-rows4-20260922/raw-quality-audit.json`。

完整五档性能采集已启动，PID 2021932，证据
`gdn-factor-rows4-performance-20260922`，每档三次输出 256，
单流/MTP 关闭，正式输入及参考不变；新增清理后 ROWS=8 版本
对照，同时保留之前各候选及其失败。尚无性能结论，不以质量
题通过覆盖性能负载输出差异或正式精度门禁；未进行前置单测/
数值/profile。下一步收齐完整矩阵并核对原始响应。

## 清理三侧复核失败，应用四列状态候选（2026-09-22）

清理版本 `gdn-factor-clean-parent-pair-20260922` 全 18 HTTP 已
完成，三服务/驱动退出 0，原始响应与时间公式逐项核对通过。
44K TTFT 候选 30.547738–30.554627 秒，前侧父版
30.467498–30.518459，后侧 30.445505–30.455592，两侧均不利。
8K 三次总体范围与两侧重叠，但后两次候选
5.181291–5.182085 秒，前侧 5.178698–5.179814，后侧
5.180119–5.180694，两侧均略慢，不以首请求拉宽范围判定无回退。
44K decode 总体范围均重叠；后两次对后侧父版也有微小不利
分离，保留全部样本，不设临时容忍比例。当前清理版不接受。

确认原驱动已退出且无推理服务后，应用
`prepared/gdn-factor-rows4-20260922`：仅把 Q4T_GDN_REG 未设置
时默认 ROWS=8 改为 4，显式环境选择保持。每列归一化/归约/
递推表达式不变，源码级每 lane 状态 32→16 个 float、目标每
序列 CTA 192→384；实际数值、资源和性能尚未验证。原二进制
与源码已归档。必要构建正在进行，首项仍为质量 HTTP，未进行
任何前置单测/数值/profile。下一步按完整 E2E 检验，正式参考不变。

## 清理完整矩阵终态与相邻复核（2026-09-22）

共享因子清理版本完整五档 15 HTTP 已结束，服务/驱动退出 0，
原始 SQLite 响应、长度、finish 与公式逐项核对通过；采集有效
不代表正式精度或性能验收通过。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.807824 | 18.736647 |
| 4096 | 2.647458 | 18.017536 |
| 8192 | 5.185603 | 18.066620 |
| 45056 | 30.566246 | 17.811999 |
| 204800 | 159.806960 | 17.158575 |

8K/44K TTFT 对正式父版不利范围分离，其他档位和各档 decode
对正式父版/固定参考无不利分离。全部输出同 a95 FP32 候选，
仍不同正式参考。44K decode 对已拒绝的物化方案不利这一比较
保留，不以该版本替换正式参考。证据
`gdn-factor-clean-performance-20260922`，原失败样本不替换。

确认原 PID 2010792 退出、无 q4t 服务且二进制摘要正确后，启动
`gdn-factor-clean-parent-pair-20260922`（PID 2016652）：正式
父版→当前清理版→正式父版，8K/44K、各三次输出 256，共 18
真实 HTTP。档位由完整矩阵对正式父版/固定参考的不利分离集合
确定并保存原审查结果。各版本对自己的原输出校验，跨版本输出
差异仍保留；不以性能相邻复核覆盖精度门禁。ROWS=4 候选仍未
应用，清理后 MTP 对照仍未启动；下一步先完成相邻复核。

## 共享因子清理版本质量终态（2026-09-22）

共享因子/诊断清理版本 `75bae82e…` 首项质量 HTTP 完成 11/11，
含 200K；原始 SQLite 请求、输出、长度、finish 与时间逐项复核，
完整质量输出同 7364767，服务和驱动退出 0。证据
`gdn-factor-clean-20260922/raw-quality-audit.json`。

完整五档性能已启动，PID 2010792，证据
`gdn-factor-clean-performance-20260922`；启动前确认原始质量
复核、相同二进制和无其他推理服务。固定参考和所有失败样本
保留，正式输出精度门禁未据质量题通过而解除。

另准备 `prepared/gdn-factor-clean-mtp-http-20260922`，尚未
执行：同二进制 plain-before/MTP/plain-after，1K/8K/44K 各
三次、输出 32、max_len=65536，直接比较真实 HTTP 完整输出，
不依赖已移除的临时追踪；不能用这组输出比较证明全部内部
checkpoint 状态。下一步先收齐五档性能并决定复核路径。

## 对齐向量完整矩阵失败，撤回与清理（2026-09-22）

对齐向量版本 `905c223f…` 完整五档 15 HTTP 已完成，服务/驱动
退出 0，全部原始响应核对通过，但性能门禁失败。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.814015 | 18.711855 |
| 4096 | 2.658408 | 18.035339 |
| 8192 | 5.224372 | 18.039808 |
| 45056 | 30.788699 | 17.839841 |
| 204800 | 160.949904 | 17.175777 |

4K/8K/44K/200K TTFT 对正式父版均不利，8K/44K/200K 对
checkpoint 候选也不利；decode 无不利分离。所有完整输出同
FP32 初版/checkpoint/归一化复用候选，仍不同正式参考。证据
`gdn-normalized-vector-performance-20260922`，失败样本不替换。

确认原 PID 2006219 退出且无推理服务后，按文件摘要核对并应用
`prepared/gdn-factor-clean-20260922`，归档原二进制与七文件
前后源码：撤回 FP32 q/k 物化及 raw 复用，恢复共享 FP32 因子/
编译期 checkpoint；删除临时 host token/层/状态/阶段追踪及其
接口参数，保留原 Q4T_LIN_DUMP、pooled RoPE 和其他一致性修复。
构建正在进行，首项测试仍为真实质量 HTTP；未运行前置单测/
数值/profile。当前不是正式接受，下一步完成必要构建及 E2E。

## 对齐向量候选质量终态（2026-09-22）

对齐向量候选 `905c223f…` 首项质量 HTTP 已完成 11/11，包含
200K；原始 SQLite 响应、实际 token 数、finish、时间与固定
7364767 质量输入/完整输出逐项核对通过，服务与驱动退出 0。
证据 `gdn-normalized-vector-20260922/raw-quality-audit.json`。
这不覆盖五档性能负载输出差异或其他路径的精度审查。

完整五档性能采集已启动（PID 2006219），证据
`gdn-normalized-vector-performance-20260922`。启动条件包括
原始质量复核通过、服务已退出、二进制摘要一致、无其他推理
服务；未运行前置单测、数值测试或 profile。下一步收齐三次
重复范围，与正式父版、固定参考及前两轮 FP32 候选一起比较。

## 归一化复用完整矩阵失败及向量候选（2026-09-22）

归一化前移/复用 raw 缓冲候选 d8f830d4… 的完整五档采集结束，
15 个原始响应核对通过，服务与驱动退出 0；这不代表验收通过。

| 输入 token | TTFT 均值秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.817977 | 18.780359 |
| 4096 | 2.680170 | 18.073821 |
| 8192 | 5.271033 | 18.086211 |
| 45056 | 31.002705 | 17.859784 |
| 204800 | 161.925582 | 17.185859 |

4K/8K/44K/200K TTFT 对正式父版和 checkpoint 候选均不利范围
分离；各档 decode 没有不利分离。全部完整输出同 FP32 初版及
checkpoint 候选，仍不同于正式参考。原始样本不替换，当前方案
不接受，未开展底层数值测试或 profile。证据
`gdn-normalized-reuse-performance-20260922`。

确认原驱动已退出且无 q4t 服务后，应用
`prepared/gdn-normalized-vector-20260922`：每 lane 原四次 float2
读写改为两次 float4，诊断尾部对齐提高到 16 字节；不改变 FP32
乘积、归约或递推公式，不宣称编译器实际指令减少。原源码和
二进制完整归档。正在必要构建，首项测试仍为正式质量 HTTP。
下一步若完整 E2E 仍回退，撤回归一化前移方向；共享递推一致性
修复与原精度门禁仍需闭合，正式基线不变。

## 归一化复用候选质量终态（2026-09-22）

归一化复用候选首项质量 HTTP 已完成 11/11（含 200K），原始
SQLite 响应、长度、finish、时间与完整输出参考逐项复核通过，
服务/驱动退出 0。证据 gdn-normalized-reuse-20260922。
完整五档每档三次性能采集已启动（PID 2001726），证据
 gdn-normalized-reuse-performance-20260922；会同时对照正式父版、
固定参考与上一候选，保持原样本，不把采集成功当作验收通过。
正式输出差异、44K 回退及其他调用路径复验仍未闭合。

## 共享 GDN HTTP 终态与归一化缓冲复用候选（2026-09-22）

共享 GDN 的 `gdn-shared-mtp-http-20260922` 已完成 24 HTTP，
四服务与驱动退出 0，原始 SQLite 响应、长度、公式及实际 token 链
复核通过。8K/44K 的 plain/MTP 各三次 32-token 输出相同，插桩
开关不改变输出，接受数覆盖 0/2/3。选定位置 8192/8193/8218、
45056/45057 的全部 48 层摘要、首层九阶段摘要相同；可比较的
首两步首层 SSM/conv 摘要相同。仅证明这些 HTTP 用例与采样位置，
不能外推全面精度；44K TTFT 回退及正式输出差异仍未关闭。

源码检查确认 raw QKV 的计算消费者（卷积、checkpoint、状态更新）
均在同 stream 的归一化前完成，后续仅诊断 dump 读取 raw。
新候选 `prepared/gdn-normalized-reuse-20260922` 将 FP32 q/k
乘法前移到准备 kernel，普通模式复用 raw 投影缓冲；dump 模式
另留对齐尾部以保留原数据。T=8192 时 raw 160 MiB 可容纳
128 MiB 归一化值，普通模式不增加分配次数/容量（较因子尾部版
减少 1 MiB）；dump 模式分配 288 MiB。预算源码没有精确描述
这些瞬时缓冲，不能据常量余量宣称峰值容量已验证。旧 +127 MiB
尾部方案未应用。新候选正在必要构建，首项运行必须为质量 HTTP，
尚无速度或精度结论。

## 共享 FP32 GDN 三侧性能复核终态（2026-09-22）

`gdn-checkpoint-parent-pair-20260922` 18 HTTP 完成，三服务/
驱动退出 0；原始 SQLite 输入、完整响应、长度、finish、公式
均核对。8K TTFT/decode 与两侧范围重叠。44K TTFT 候选
30.520660–30.553731 秒，前侧父版 30.454704–30.490928，
后侧 30.499532–30.507141，两侧均不利分离；decode 范围重叠。
当前版不接受，原始矩阵及正式输出差异保留。

同一 a95dbafd… 二进制已启动 `gdn-shared-mtp-http-20260922`
（PID 1989834）：8K/44K、输出 32、各三次，plain/MTP 层及
阶段插桩关闭/开启四组。插桩控制是同候选 trace-off，不替换
正式参考；按实际接受 token 行比较，输入分叉后的位置另标，
不误作同输入。仅真实 HTTP 日志定位，不做底层数值/profile。


该轮关闭阶段插桩的 8K 对照已完成：plain/MTP 各三次，32 个
实际输出 token 链及完整响应哈希全部一致；MTP 接受数覆盖
0/2/3（k=3），包含部分接受/回滚路径。原先该用例第 28 个
输出的分叉本次未出现。44K 及插桩开关控制仍未完成，不能
外推内部状态相同或全面 MTP 精度通过，也不覆盖 44K 回退。

下一步准备方案 `prepared/gdn-normalized-fp32-20260922` 把
FP32 归一化乘法前移并保留完整 FP32 q/k，递推算术/检查点
不变，但当前未应用/构建/测试。只读模型 config 确认 nkh=16、
kd=128；T=8192 时尾部从 1 MiB 到 128 MiB，增加 127 MiB。
不能只报省乘法而忽略容量代价；收益、输出、容量均需实际验收。
先完成当前 MTP 阶段定位，再决定应用该方案。

## 当前进展（2026-09-22）

正式已接受运行时仍为 7364767，固定性能参考为 fcb5925。
当前工作区候选未接受、未提交；过程与失败证据见
[decode 路径报告](DECODE_PATH_CONSISTENCY_2026-09-21.md)。

候选内容：补 pooled decode RoPE 位置写入；对齐 GDN 单行/打包
算术并优化状态访问；短 indexer 每 head BF16 舍入；2–4 行 BF16
共享权重投影；混合长短批次中为短行保留原打分和 top-k 排列。
清理版曾移除临时 GPU 摘要；当前为 MTP 分叉定位重新加入受开关
控制的少量层/状态读回，关闭时不执行，不纳入性能收益结论。

当前候选二进制 SHA-256：
`a95dbafdeb94e885b91f496d155d133844fd049992f1fd7d518cf17a5aacbc31`。
此前清理版计算候选为 `448d73d5…`，host 轨迹版为 `c7f5ab35…`；
上述轨迹版本仅新增诊断；当前共享 FP32 GDN 候选另有算术变更，见下文。
必要构建零警告；每轮首测均为真实 HTTP，无前置底层测试/profile。

已验证的范围：

- 正式父版自身也有串行/并发分叉；这不证明候选精度已通过。
- BF16 投影及混合索引修正后，1K/8K 同 prompt 的实际 B=2/3/4
  各三轮、串行前后共 66 HTTP 全部输出/token 链一致。8K 边界
  24 HTTP 的观察位置 8192–8196 全 48 层摘要也一致。
- 清理版正式质量 11/11（含 200K），完整输出、长度及 stop
  同已接受 7364767，服务/驱动退出 0。
- 清理版不同 prompt 的 1K/4K/8K/44K 混合与串行前后共 36 HTTP
  全部输出/token 链同各自串行；三轮各有 121 个实际四长度 B=4
  步。不是所有 slot 排列、批量 prefill、视觉或 MTP 的覆盖。

完整五档 `decode-consistent-performance-20260922` 已终态：15 请求，
服务/驱动退出 0。max_seq=1/MTP 关闭，诊断环境关闭。
1K/4K/8K/44K/200K TTFT 均值为
0.797/2.636/5.188/30.520/159.546 秒，decode 均值为
18.792/18.069/18.069/17.851/17.149 tok/s。
**44K TTFT 对正式父版未通过**：30.498487–30.539666 秒，对照
30.448455–30.486824 秒；其余档位对正式父版与固定参考无不利
范围分离。原始样本与比较见 performance-review.json。

响应保持上一 GDN 诊断候选，但五档均与正式旧输出不同。该诊断
控制不替换正式输出参考，精度门禁尚未关闭，不用 near-tie 泛化解释。
`decode-consistent-parent-pair-20260922` 三侧 9 请求已完成，服务/
驱动退出 0。44K 候选 TTFT 30.490284–30.557880 秒，前侧父版
30.477829–30.604356、后侧 30.487182–30.570807 秒；decode
范围也均与两侧重叠。不利分离本次未重现，原失败及样本保留，
不宣称稳定提速。15 个原始矩阵响应的长度、结束原因、内容哈希
与指标公式已独立核对，证据 raw-response-audit.json。

`decode-consistent-paths-20260922` 已终态：1K/4K/8K/44K 的
回退、调度重启共 24 请求输出均同此前候选调度，两个服务/驱动
退出 0。重启调度四档 TTFT/decode 对此前候选均无不利分离。
回退 decode 低于调度，但四档输出及 TTFT/decode 与正式父版
两份历史回退记录一致/无不利分离；跨路径差异保留，不混为
候选同路径退化。故障注入摘要和实际回退日志均已核对。
这仅覆盖上述四档；不替换正式输出参考或证明全面精度通过。

`decode-consistent-vision-paths-20260922` 三侧 36 HTTP 已完成，
三个服务/驱动退出 0。单图/双图/视频/raw-stop 的重复、回退、
重启后输出及 token 数均一致；生成数分别 102/128/109/1。
原始数据库请求/响应已独立复核；质量、完整性能及当前视觉
运行时补丁 10 文件一致、二进制摘要相同。仅显式模板路径定位，
不覆盖标准 messages，不证明全面视觉精度或父版性能持平。

`decode-consistent-mtp-http-20260922` 三侧 27 HTTP 已完成，服务/
驱动退出 0，各次 32 token 且重复稳定，实际 MTP B=1 覆盖已核对。
1K/8K 输出同两侧父版且无不利分离；**44K 输出不同于两侧父版**。
44K TTFT 候选 31.962053–31.978889 秒，前侧父版范围重叠，后侧
31.932994–31.961539 秒不利分离。采集成功不代表验收通过。
同 prompt 的普通 decode 历史输出前缀也不同，但容量/输出上限
不同，不能仅据此把差异归因于 MTP 或量化噪声。

`decode-consistent-mtp-switch-http-20260922` 同候选/同容量和输出
上限的 plain→MTP→plain 三侧 27 HTTP 已终态，服务退出 0。
plain 重启前后各档一致；MTP 在 1K 同 plain，在 8K/44K 均不同，
各自重复确定。MTP TTFT 含初始化，不混为父候选性能回退。
源码发现 GDN 归一化/归约和 T=1/多 token 稀疏 attention 实现
差异，只是候选原因，尚无根因证明。

`mtp-verify-host-trace-http-20260922` 四组 24 HTTP 已完成，
日志开关不改变各模式输出，四服务/驱动退出 0；原始响应和 token
链已独立复核。三次请求均同一首次分叉：

- 8K：输出索引 27，预测位置 8218，输入 55404；plain=13、
  MTP=11，MTP base=8216/local_row=2/accepted=2，为校正行。
- 44K：输出索引 2，预测位置 45057，输入 248068；plain=271、
  MTP=198，MTP base=45057/local_row=0/accepted=3。

prefill token 和分叉前输入 token 前缀一致，不证明内部状态一致。
被拒草稿/超出 HTTP 上限的预测已从实际输出链剔除。

`mtp-layer-trace-http-20260922` 四组 24 HTTP 已终态，四服务/
驱动退出 0；层/状态诊断开关不改变前一 host 轨迹版各模式的完整
输出。原始响应、实际接受 token 链及层摘要三份复核均 complete。
8K/44K 各三次在所有观察位置首次不同的 trunk 层均为第 0 层，
该层之前输入 trunk 摘要相同。首个 decode base=8192/45056 时
第 0 层初始 SSM/conv 摘要均相同；下一 base=8193/45057 时
conv 仍相同，SSM 已不同。证据 layer-state-review.json。
这把首次差异缩小到第 0 层内部，不能仅凭哈希判定 GDN 根因、
差异大小或精度好坏；下一步区分投影/卷积/GDN 首次分叉。

本轮只读参考源码核对发现：固定 Transformers 5.16.1 先做 q/k
归一化，再显式转 FP32；evalscope 环境 5.17.0 顺序相反，不能
混用。ref_dump.py 构造模型未显式指定低精度，加载时 copy_ 到
现有参数；源张量转 BF16 不等于参数或激活为 BF16。没有执行
参考推理或运行时 dtype 验证，不能由源码顺序推导实际归一化
精度。版本来源摘要保存在 reference-source-audit.json，固定参考
不变。实际 B=2 批量 prefill 暂缓；正式输出和 MTP 44K 性能
未通过记录保留，候选运行时不接受、不提交。

当前已新增第 0 层线性链路摘要诊断，二进制 `c1765e65…`，完整
SHA 见 mtp-linear-stage-trace-http-20260922/manifest.json。只在
原层摘要选中的 forward 传递 host 位置，观察 input、qkv_raw、z、
a、beta、conv、GDN、gate、output；默认关闭，无计算逻辑改动。
必要构建零警告，首项四组 24 HTTP 已完成，四服务/驱动退出 0；
raw-response/token-chain/layer-state/stage 四份复核均 complete。
插桩关闭/开启保持前一层摘要版各模式完整响应。8K/44K 各三次
在全部观察位置 input、qkv_raw、z、a、beta、conv 摘要均一致，
第一个不同阶段是 GDN，gate 和 output 随之不同。首步初始 SSM
摘要相同，因此该层首步分叉定位在 GDN 计算边界，而非此前
投影或卷积；后续状态差异可能累积，不证明所有层只此一处。
源码确认 register 路径先把归一化 q/k 写回 BF16，而 causal
路径保留 FP32 归一化值，归约及更新结合顺序也不同。尚未
分别量化这些差异，不把任何一路预定为高精度真值。下一步
设计共享递推与 checkpoint 语义，精度保持及完整 HTTP 门禁
不变。新增读回/同步只用于定位，不能作性能或精度结论。

当前新增共享 FP32 GDN 候选（未接受）：kd=vd=128 的普通/打包/
因果及 ragged 路径共用 register 递推，按实际 seq slot、local token
保存 checkpoint。q/k 保持原 BF16 输入，归一化因子与乘积保持
FP32，不再把归一化结果写回 BF16；归约与更新顺序统一。因子
存放在原 qkv_raw 分配的 8-byte 对齐尾部，不与原投影重叠，
无新增分配次数，容量增加 8*T*nkh 字节加最多 6 字节对齐。
这仍是待验收算术变更，不据 FP32 类型声称模型精度改善。

初版必要构建零警告，二进制 `e39f4cc7…`，完整摘要及可回退源/
二进制见 prepared/gdn-shared-fp32-20260922。首项运行测试
`gdn-shared-fp32-20260922/quality` 已完成：正式质量 11/11，
含 200K，完整输出与 prompt 摘要同 7364767，服务/驱动退出 0。
共享 FP32 GDN 初版完整五档已终态：15 个请求、服务/驱动退出 0，
全部原始响应长度、结束原因、哈希及指标公式已核对。1K/4K/8K/
44K/200K TTFT 均值分别 0.846/2.914/5.738/33.555/173.496 秒，
decode 为 18.757/18.092/18.123/17.846/17.142 tok/s。
4K/8K/44K/200K TTFT 对 7364767、fcb5925、上一候选均不利
范围分离，decode 无不利分离；五档完整输出均不同于三份对照。
**初版不接受**。质量 11/11 不覆盖性能和正式输出失败；所有
样本及对照保留于 gdn-shared-fp32-performance-20260922。

确认旧测试终态且无推理服务后，应用 checkpoint 需求编译期
分派候选：只将 ssm_ckpt 是否存在移到 host 分派/模板参数，
普通 prefill/decode 不再保留逐 token checkpoint 分支和地址
生命周期，归一化、递推算术、分配及 checkpoint 布局不变。
这只是待验证开销假设，不宣称根因或性能恢复。
prepared/gdn-checkpoint-specialization-20260922 保存旧二进制/
前后源。必要构建零警告，首项 quality HTTP 已完成于
`gdn-checkpoint-specialization-20260922`：11/11，含 200K，
完整输出同 7364767，服务/驱动退出 0。随后启动同二进制五档
`gdn-checkpoint-specialization-performance-20260922`（PID
1978180，已结束），每档三次、输出 256，正式父版/固定参考/
初版及前一候选分别比较。结果见下，MTP/checkpoint 行为未复验。

checkpoint 专门化完整五档已完成，15 HTTP，服务/驱动退出 0，
原始响应/长度/finish/哈希及公式均已核对。1K/4K/8K/44K/200K
TTFT 均值 0.802/2.643/5.188/30.554/159.832 秒，decode
18.748/18.064/18.045/17.822/17.179 tok/s。五档输出均同共享
FP32 初版，仍不同于正式参考。8K/44K TTFT 对正式父版不利
范围分离，44K 对前一候选也不利；200K 无不利分离，decode
各档无不利分离。相对初版的恢复不能覆盖正式验收未通过。

按完整矩阵实际不利档位，已启动 8K/44K 的旧→新→旧相邻
HTTP：gdn-checkpoint-parent-pair-20260922（PID 1981749），
三侧每档三次，共 18 请求；分别检查每个二进制自身的原输出，
对全部原始 SQLite 请求/响应及公式复核。原矩阵失败不替换，
正式参考不变。目前相邻复核尚未完成，候选仍未接受。
MTP 四组 HTTP 已准备于 prepared/gdn-shared-mtp-http-20260922，
尚未执行，同候选 trace-off 只用于插桩控制，不替代正式参考。
此前所有失败记录与正式参考保持不变；无前置底层数值/profile。



## 阶段记录快照（2026-09-22，截止四档性能采集）

正式已接受运行时仍为 7364767，固定性能参考仍为 fcb5925。
当前工作区候选未接受、未提交；完整定位与历史阶段证据见
[decode 路径报告](DECODE_PATH_CONSISTENCY_2026-09-21.md)。

**当前定位**：正式父版同配置 30 请求已全部完成，两个服务及
驱动退出 0。1K/8K 的串行前后保持自身输出；两档各三轮实际
B=2 均分叉，每轮记录 126 次成功双 token 读回。问题并非候选
才引入，但不能据此推断没有精度/状态问题或接受候选。

已归档当前二进制和差异，应用仅 host 的诊断日志：记录 prefill
首 token，以及已完成读回的 B/行/slot/逻辑位置/输入输出 token。
不增加 CUDA 复制或同步。已零警告构建，首项同一二进制关闭/
开启日志的 24 请求 HTTP 已完成，两个服务及驱动退出 0，证据
`decode-batch-host-trace-http-20260922`，不执行底层测试。
完整输出保持加日志前和日志关闭组；三轮均为 2 次 B=1、126 次
B=2。slot 0 先以位置 1024 独跑一步，随后位置 1025 与 slot 1
位置 1024 配对，两者相差一步。逐请求位置连续，输入 token
等于上一步输出，prefill 首 token 相同；slot 0/1 首次生成差异
分别是零基索引 66/78（第 67/79 个 token），位置 1089/1101，
串行 token 321/6761，对应并发 593/494。三轮重复，串行前后
128 个 token ID 相同。未见该范围内 host 跳步；不能据此排除
更早的张量/状态差异。下一步在实际 HTTP 内缩小层间分叉。
逐层 HTTP 定位已完成：`decode-batch-layer-trace-http-20260922`
的 24 请求及两个服务/驱动正常结束，关闭/开启诊断完整输出
均保持前版，三轮批次及 host 输入链有效。初始 trunk 摘要相同；
先行的 B=1 行（slot 0，位置 1024）48 层均匹配串行，进入 B=2
的 slot 0 位置 1025、slot 1 位置 1024/1025 均从第 0 层输出
开始不同，后续 48 层均不同。串行前后逐层摘要一致。
层内边界 HTTP `decode-batch-stage-trace-http-20260922` 已完整结束：
24 请求、两个服务及驱动退出 0，全部输出保持前版。三轮 B=2
目标行的 attn_trunk/attn_normed/attn_mixed 摘要与串行相同，
首次差异为 attn_out；先行 B=1 和串行复用全部边界一致。
因此下一步缩小 LinearAttentionForward 内部，而非先修改 MoE。
哈希仅用于定位，不能证明精度损失、状态污染或数值误差性质。

已归档并增加显式启用的线性注意力诊断：实际 slot 的初始/更新后
SSM 与卷积状态、四投影、卷积输出、归一化 qkv、递推和门控输出。
仅第 0 层位置 1024/1025，通过真实 HTTP 比较串行与 B=2。
必要构建零警告，首项 `decode-batch-linear-trace-http-20260922`
已完整结束：24 请求、两个服务及驱动退出 0，所有完整输出保持
上一诊断版，三轮真实 B=2 和串行前后步进链核对通过。
三轮均为：slot 0 位置 1025、slot 1 位置 1024 首次进入 B=2 时，
ssm_before/conv_before 相同，qkv_raw 首次不同；后续卷积与状态
出现差异。slot 1 位置 1025 的初始状态已受上一步差异影响。
先行 B=1 以及串行前后所有内部边界摘要一致。该证据将首次差异
缩小到投影，不能把后续状态差异直接当成池化污染。
代码核对：Bf16Gemm 的 M=1 使用 Bf16Gev，M=2 转 cuBLASLt；
不同归约路径是待验证原因，哈希不能衡量误差或证明精度通过。
已归档旧版并应用 2–4 行 BF16 小批量路径：一次权重加载供多行
独立 f32x2 累加，沿用单行 lane/shuffle/最终 BF16 舍入顺序，
不改变 M=1 实现。激活通过缓存读取，无 M*K shared 分配；
不支持形状/对齐仍走既有 cuBLASLt 回退。该路径经 Bf16Gemm
通用分发，影响范围包含小批量其他消费者，尚需全面验收。
必要构建零警告，首项 `bf16-small-m-http-20260922` 已完整结束：
24 请求、两个服务及驱动退出 0。关闭/开启诊断的全部请求输出
一致且保持旧串行输出；三轮真实 B=2 两请求的 128 token 链
都匹配串行。位置 1024/1025 的第 0 层内部状态/投影/卷积/递推
摘要以及全部 48 层输出摘要均与串行一致，原分叉在本用例消失。
这不代表其他位置、输入、M=3/4 或长上下文已通过；旧并发输出
变化明确保留，不将旧输出对照失败改写成完全兼容。
正式质量 HTTP `bf16-small-m-quality-20260922` 已完成 11/11，
包括 200K：完整输出与已接受 7364767 相同，长度/答案/stop
通过，服务及驱动退出 0，二进制摘要一致；无前置底层测试。
扩展 `bf16-small-m-batches-http-20260922` 已完整结束，66 请求、
服务及驱动退出 0；两档各三轮 B=2/3/4 均确认实际批次，逐 token
位置/输入链有效。1K 全部与串行一致。8K 各轮先行 slot 0 一致，
后加入 slot 1–3 均从零基索引 31（第 32 token）分叉，串行前后
仍一致；该一致性检查失败，不能接受候选。原始响应审计曾保留
旧 B=2 请求数假设，在 B=4 报错，已修正并保留旧脚本；请求未重跑。
`decode-8k-layer-trace-http-20260922` 的 24 HTTP 完整结束，两个
服务及驱动退出 0，关闭/开启诊断完整输出保持前版。三轮均为：
slot 1 位置 8192/8193 全层一致；位置 8194 与 slot 0 的 8195
配对时首次在第 3 层（首个 full attention）不同，之后位置
8195/8196 首次不同在第 4 层；先行 slot 0 及串行前后全部一致。
代码核对：批次最大位置选择 OnePass 长路径，短行也被带入不同
归约且无 BF16 head 舍入的打分过程。不是完整精度证明。

已归档并应用针对性候选：OnePassScoreKernel 中 pooled 行的
可见组数仍属短路径时，用原短路径的串行 dot 与每 head BF16
舍入；真正长行保持旧 warp 归约，输出跨度仍按批次总组数。
打分候选的 `indexer-mixed-boundary-http-20260922` 已完成全部
24 请求、两个服务及驱动退出 0；三轮仍在 slot 1 位置 8194
第 3 层分叉，串行保持，旧失败与输出变化保留，候选不通过。

继续基于该候选验证 top-k 排列：混合批次中的 pooled 短行在
最终选择时重新读取原始短窗口分数/索引，而非重排已选候选；
这样使用与短路径相同的排序输入顺序。<=512 可见组保持原密集
展开；长行/非 pooled 路径不变，不新增 kernel 启动。尚未接受。
必要构建零警告，首项 `indexer-mixed-topk-http-20260922` 已完整
结束：24 请求、两个服务及驱动退出 0；三轮实际 B=2 的完整
128 token 链同串行，关闭/开启诊断全部输出同原串行。观察位置
8192–8196 的全部 48 层摘要一致，串行前后复用一致；原边界
分叉在该用例消失。不能外推其他输入/位置或性能。
`indexer-mixed-topk-batches-http-20260922` 已完整结束：66 请求、
服务及驱动退出 0，1K/8K 各三轮 B=2/3/4 均有真实批次覆盖，
全部完整输出、128 token 链与串行一致，前后复用一致。已保存
final-review.json；只证明同 prompt 的这些组合，非整体精度验收。

在上述进程终止后归档并移除临时 GPU 诊断：逐层/层内/线性注意力
摘要 D2H/同步与接口参数清理，计算改动保留。保留默认关闭的
host-only Q4T_DECODE_BATCH_TRACE，用已读回 token 确认批次/slot/
输入链，不新增 GPU 同步；其他任务文件未纳入。
清理版必要构建零警告，首项正式 HTTP
`decode-gpu-trace-clean-quality-20260922` 完整 11/11，通过答案、
完整输出、实际长度、stop 与已接受 7364767 对照，服务/驱动退出 0，
二进制摘要一致；未运行前置底层测试/profile。
`decode-mixed-context-http-20260922` 已完整完成 36 HTTP，服务和
驱动退出 0。1K/4K/8K/44K 各自串行前后各三次，中间三轮四请求
混合；全部完整输出和 128 token 链与各自串行一致。每轮 121 个
实际 B=4 步均含四种长度，原始 SQLite 行数/请求身份及输入链核对
通过。实际 slot 由调度决定，不把文件排列当作所有 slot 排列覆盖。
MTP/批量 prefill 关闭，仍非全消费者或整体精度验收。

`decode-consistent-performance-20260922` 仍在运行，PID 1917510；
五档各三请求、输出 256、max_seq=1/MTP 关闭，全部诊断环境关闭。
已完成 1K/4K/8K/44K：TTFT 均值 0.797/2.636/5.188/30.520 秒，
decode 均值 18.792/18.069/18.069/17.851 tok/s。前三档相对
正式父版 7364767 与固定 fcb5925 均无不利范围分离。
**44K TTFT 相对正式父版不通过**：候选 30.498487–30.539666 秒，
父版 30.448455–30.486824 秒，范围不利分离；对固定参考无不利
分离，decode 也无不利分离。保留全部样本，不能用差值小抹去。
200K 未完成，不作整体性能结论。已准备父版→候选→父版相邻
复验，等待当前完整终态后覆盖所有不利分离档位，不替换原结果。

上述响应控制明确使用上一 GDN 状态访问候选，仅用于检测本轮
额外输出变化；四档保持该控制，但仍与正式旧输出不同。正式
7364767 输出差异门禁未关闭，性能参考也未重置。完整比较保存在
performance-review.json，未执行底层测试/profile。
当前候选二进制 SHA-256：
`448d73d5b1567430162bf7631db36c2881c2c6648bde605f0dd84afb43a7ac58`。
下一步核对完整五档结果及双参考范围，批量 prefill、其他消费者和
正式输出差异的精度处置仍待完成，候选尚未接受。

通用 pooled indexer 每 head BF16 舍入候选已零警告构建，并以
30 请求真实 HTTP 作首项测试，服务/驱动退出 0。串行前后完整
输出保持改动前；1K 三轮并发输出不变且仍分叉，8K 三轮并发
输出已改变但仍分叉。该候选未解决问题，尚未接受；原始响应与
逐案例比较见 indexer-pooled-dot-round-http-20260922。
不改递推、top-k 或长 OnePass，不能将局部算术统一当作精度通过。
加日志前二进制 SHA-256：
`4752eb4a4920aa7219b91b531ed41cb28f08221718a72491af99bba00dde202a`。

已确认但不能替代本候选验收的前置证据：

- 旧调度与分配失败回退输出不同。RoPE 写入、GDN 算术边界及
  短 T=1 indexer 舍入分别经过 HTTP 定位；此前候选五档保持
  旧回退输出，**正式旧调度输出门禁仍失败**，参考没有重置。
- GDN packed float4 状态访问版（SHA 2e8d2756…）质量 11/11，
  五档 15 请求完整输出保持其前一候选。五档 decode 相对前一
  候选提高；相对正式父版范围重叠。8K/44K TTFT 初次不利
  分离，以及之后 44K decode 单侧分离，相邻复核未重现。
  保留失败和所有样本，不据此宣称稳定提速或正式精度接受。
- `decode-batch-http-20260922` 的 30 请求全部完成，两个服务
  退出 0。1K/8K 每档三轮并发均记录 126 次成功双 token 读回，
  确认真实 B=2，但同 prompt 的两个输出分叉；串行前后一致。
- `decode-batch-unobserved-20260922` 的 24 请求及服务正常结束，
  十个案例的输出多重集合均与观察器版本相同，分叉不依赖
  观察器加载。不能仅凭分叉断言状态污染、slot bug 或量化噪声。

后续必须闭合：当前 B=2 分叉定位、正式输出差异的精度处置、
最终二进制质量/完整五档及双参考性能、不同 prompt/非零及复用
slot/mixed rope_delta/跨短长 indexer 边界、视觉视频/MTP 消费者，
再按门禁做参考精度分析。多序列 prefill/MTP 仍有共享 GDN 路径，
不能把 B=1 或相同 prompt 的 B=2 结果当作完整覆盖。

当前没有执行前置数值测试、单测或 profile，也没有伪造正式
消费者工具所需的 acceptance.json。旧二进制、失败及未启动记录
均保留在 `.q4t-work/e2e/` 和 `.q4t-work/prepared/`。


## 阶段记录：GDN 状态访问及 B=2 首轮定位

以下章节的“正在进行/尚未运行”描述保留定位当时的状态；本节和
STATUS.md 是当前进度入口，不能把早期候选结果套到最终二进制。

- 原 11 题质量 HTTP 已通过，答案、完整输出和输入摘要保持
  7364767。正式五档输出门禁仍失败：修复后的调度输出不同于
  原调度参考。此失败没有被独立性能定位覆盖，也没有重置参考。
- 旧回退→候选调度→旧回退五档 45 请求已完成，各版输出保持
  旧回退。临时日志清理、池基址寻址修正后重新经过质量与五档
  HTTP；prefill TTFT 回退得到修正，短档 decode 差距仍存在。
- 短 indexer 的四 BF16 向量读取因 8K TTFT 双侧不利分离已撤回。
  保留标量点积顺序，只改变 block 从 8×256 到 64×32 的候选
  五档完成，未发现相对标量版不利范围分离，仍非正式接受。
- 当前 GDN packed 状态 float4 加载/存储候选，质量 11/11、五档
  15/15 原始 HTTP 长度/停止原因/候选输出检查通过、服务退出 0。
  完整输出保持前一候选，正式父版输出差异仍在。它只改
  PackedDecode 且 ROWS=4/8 的状态访问，不改变递推计算体。

当前二进制 SHA-256：
`2e8d275667c85ceb0cd188a7f602878ee76da4409b74fb9f5984b72a363e4bf6`。
证据目录 `gdn-packed-state-vector-performance-20260922`，
`raw-response-review.json` 逐项核对原始响应和多个独立参考。

| 输入 token | TTFT 均值秒 | decode tok/s | 与正式父版原范围比较 |
|---:|---:|---:|---|
| 1024 | 0.798285 | 18.740671 | 两指标重叠 |
| 4096 | 2.638415 | 18.029645 | 两指标重叠 |
| 8192 | 5.180500 | 18.040102 | TTFT 不利分离，decode 重叠 |
| 45056 | 30.536025 | 17.843248 | TTFT 不利分离，decode 重叠 |
| 204800 | 159.466187 | 17.006620 | 两指标重叠 |

每档三次、输出 256；MTP off、max_seq=1、max_prefill=8192、
max_len=208896。TTFT 是客户端指标，不是纯 prefill 耗时。
与紧邻线程块候选相比，五档 decode 范围均提高；该结果不能
抵消正式父版 8K/44K TTFT 风险，也不能证明全面精度保持。

`gdn-packed-state-vector-parent-pair-20260922` 已完成
8K/44K 父版→候选→父版，各三次。两版各自对照原完整输出；
跨版本输出不同，不能将耗时差全部归因于某个 kernel。

三侧十八请求全部完成，驱动及三个服务退出 0，各版输出保持
自身参考。8K 两指标与双侧范围重叠；44K TTFT 与双侧重叠，
初次 TTFT 不利分离未复现。但 44K decode 候选最大值
17.856872638 tok/s，后侧父版最小值 17.857021871，形成单侧
不利分离（与前侧重叠）。差距小不能自动忽略，整体仍不接受。
原始审查明确标记该项；额外“全部范围通过”的汇总断言因此失败，
不影响十八个 HTTP 已完成的事实。已启动独立 44K 三侧复核
gdn-packed-state-vector-44k-confirm-20260922，PID 1827688；
源码/二进制不变，不替换之前样本，不设置新容忍百分比。

独立 44K 三侧九请求现已完成，三个服务及驱动退出 0，各版
输出保持自身参考。候选 TTFT 30.52213–30.61802 秒、decode
17.83736–17.85722 tok/s，与两侧父版范围均重叠；之前单侧
分离未复现，原失败完整保留，不据此宣称稳定提速。
正式输出门禁仍未闭合，候选不接受。随后确认无 q4t 存活，
启动 decode-batch-http-20260922 故障定位（PID 1833694），
先编译只读 host 观察器，首项运行仍为真实 HTTP；不运行
数值/单测/profile，也不使用正式消费者工具的接受入口。

B=2 定位的 1K 已出现可复现输出差异：无观察器串行及观察器下
串行前/后各三次均保持同一输出；并发二请求三轮每轮均记录
126 次成功双 token 读回，但产生两个不同输出，三轮的两个摘要
分别重复。与串行的共同字符前缀为 309/380（不是 token 定位）。
各请求长度/停止原因正确。这证明该请求组并发路径不保持串行
完整输出，不能据此直接判断状态污染、slot bug 或量化误差。
证据 decode-batch-http-20260922/one-k-divergence.json，
8K 继续执行，尚未结束；所有分叉保留，当前候选仍不接受。

本轮 30 个 HTTP 请求已全部完成，原始数据库逐请求审查通过，
两个服务及驱动退出 0。1K/8K 各三轮并发均记录 126 次成功双
token 读回且输出分叉，两档串行前/后均保持无观察器输出。
串行相等尚不能排除观察器对并发调度的影响，故在终态及无
q4t 存活核对后，启动 decode-batch-unobserved-20260922，
PID 1838520，禁用 LD_PRELOAD、同二进制/配置再跑 24 请求。
逐案例比较完整输出摘要多重集合；此无观察器组不单独声称
实际 B=2 覆盖，不使用耗时作为性能结论。原分叉和门禁保持。

尚未闭合：正式输出差异的精度处置、
真实 B>1、
非零/复用 slot、mixed rope_delta、跨短长 indexer 边界批处理、
视觉/视频与 MTP 消费者。现有消费者工具要求的正式完整门禁
尚未满足，不伪造 acceptance.json，也未开展低层数值/profile。

## 问题与范围

正常 SchedulerLoop 即使 B=1 也调用 ModelDecodeBatchMulti；
调度 logits 分配失败时的回退调用 ModelDecodeStepSeq。
使用已接受二进制，1K、8K 文本和显式模板单图分别三次，按
调度→回退→调度重启对照：路径各自稳定，三类输出均不相同。
故该问题不是本次候选才引入。不同路径输出相同也不证明模型
精度正确，需要另外通过固定质量和参考精度审查。

## 两个独立边界

1. RoPE：单序列 decode 写入当前 logical position + rope_delta
   的三行坐标；旧 ModelDecodeBatchMulti 没有对应写入，实际
   forward 却使用 pooled rope 表。最小候选按实际 seq_id 补写。
   该候选改变调度输出，保持回退输出，但未消除三类路径差异。
2. GDN：在补写坐标后的实际单图首步 decode 内，日志逐层缩小
   差异，最终定位在第 0 层 Gated DeltaNet recurrence。
   初始 SSM/conv 状态、四项投影和卷积输出摘要相同，recurrence
   输出与更新后的 FP32 状态首次不同。

摘要一致仅用于定位，并非完整张量逐位精度检验。插桩显式开启，
不用于性能结论。每轮都核对生成输出未被插桩改变。

## 证据链

| 目录（均有 -20260921 后缀） | 实验 | 已确认结果 |
|---|---|---|
| decode-rope-diagnostic | 父版三类请求、三侧共 27 请求 | 重启一致，三类调度与回退均不同 |
| decode-rope-fix | 最小 RoPE 补写、同组 27 请求 | 回退不变，调度改变，仍与回退不同 |
| decode-rope-trace | HTTP 内实际 logits/top1 日志 | GPU 选择等于 host top1；首步已不同，token 分叉晚于分布分叉 |
| decode-rope-shared-gdn | 两侧 Q4T_GDN_REG=0，单图 9 请求 | 仍不同，单靠去除 Q/K BF16 中间舍入不能解释全部差异 |
| decode-rope-layer-trace | prefill/首步逐层摘要，9 请求 | prefill 48 层摘要一致，首步输入一致，第 0 层输出开始不同 |
| decode-rope-stage-trace | 层内阶段摘要，9 请求 | PLE 后及 attention 输入相同，attention 输出首次不同 |
| decode-rope-linear-trace | 状态/投影/conv/SSM 摘要，9 请求 | 状态输入与 conv 输出相同，SSM 输出首次不同 |
| decode-rope-gdn-shared-order | 共享模式下统一归约与更新函数，9 请求 | 三侧输出及全部边界摘要一致 |
| decode-rope-gdn-register-multi | 默认模式复用寄存器计算体，9 请求 | 三侧输出与边界摘要一致，保持原默认回退输出 |

各已结束的三侧实验服务退出均为 0。后几轮的 complete-review.json
记录完整审查；partial-review.json 保留当时未完成重启的中间观察。

## 当前候选

除 RoPE 补写外，GatedDeltaNetRegKernel 新增编译期 packed decode
模式：grid.z 选择一个 packed token，d_seq_id 选择真实池状态，
每序列只推进一次。Q/K 使用与单序列相同的预归一化，复用相同
计算体，单序列保持原 token 循环。共享路径复用同一 norm 归约、
GdnKSum 和 GdnUpdateY。MTP 多 token causal 分支尚未改变。

这不是把 B=1 绕回单序列，也不是将 Q4T_GDN_REG=0 作为生产设置。
当前单图对照仅覆盖 B=1，不能证明实际 B>1 状态隔离或非零 slot。
默认寄存器与共享模式分别有不同 Q/K 舍入与累加顺序，不能以数学
公式相同或 near-tie 一词替代精度审查。

## 未完成的门禁

- 同二进制 1K/8K/单图三侧共 27 请求正在进行，目录
  decode-rope-gdn-register-text-20260921；仅保留 decode 日志。
- 首组调度三类均已完成，1K/单图匹配旧回退，但 8K 不匹配旧
  回退，记录 prior-fallback-partial.json。新回退及重启尚未结束，
  该变化必须保留，不能仅以新版本路径相等判定精度保持。
- 严格质量 11 题已安排依赖执行：必须先确认上述进程结束、三类
  路径及重启输出一致、二进制摘要不变。目录
  decode-rope-gdn-register-quality-20260921；清除日志/实验环境，
  对照 7364767 原质量输出，不更新参考。
- 五档输出 256 的完整矩阵、直接父版与固定性能参考对照未完成。
  旧调度路径输出可能变化，必须显式保存和解释，不能悄悄替换基线。
- 实际多序列、池 slot 复用、生命周期、视觉/视频、MTP HTTP 未完成。
- 仅在完整所需 HTTP 门禁通过后，才能开展参考精度及性能细分析。
- 临时定位接口与日志需在最终候选中清理或明确保留；任何运行时
  清理后重新构建，首项仍为真实 HTTP，再验收最终二进制。

在这些证据齐全前，不宣称生产接受、精度无回退或吞吐改善。

## 8K 扩展中的进一步定位

当前新回退 1K/8K 均保持旧默认回退输出；新调度仅 8K 不同，
所以不是将所有变化都归因于单序列寄存器计算体改动。
三侧 27 请求的重启组尚未收尾。质量依赖条件因 8K 路径不一致
预计拒绝启动，不能视为质量题已经执行或失败。

已安排同二进制 8K 三侧首步/完整 prefill 边界日志，目录
`decode-rope-gdn-register-8k-trace-20260921`，等待上述真实进程
及依赖驱动结束后才启动，不并发占用模型服务。

静态线索：FullAttentionForward 在 n_groups_max <= max_blocks
时，d_seq_id==nullptr 使用 Bf16Gemm + IndexerReduce，而调度
T=1 使用 IndexerDecodeScores；超过该边界的小 T 则共用
OnePassScoreKernel。这只是待验证假设，需逐层/阶段 HTTP
日志证明差异实际发生的位置，不能直接称作根因或量化噪声。

## Indexer 舍入的受控结果

full attention 内部日志九请求结束：第 3 层 query、gate、index
query、有效压缩 key 摘要相同，评分和选中位置序列不同；插桩
不改变输出，各路径重复/重启稳定。单凭位置摘要不推断集合差异。
证据 `decode-rope-full-attention-trace-20260921/complete-review.json`。

专用 T=1 评分仅增加每 head 点积的 BF16 舍入，原缩放和选择
过程保持。`decode-rope-indexer-dot-round-20260921` 的调度、
回退共六请求已完成，均匹配旧默认回退输出；调度的首步完整
主干/子阶段/linear 摘要亦与旧回退一致。评分摘要仍不同，不能
宣称全部数值逐位一致。重启组仍在运行。

严格质量驱动 `decode-rope-indexer-round-quality-20260921`
已安排：只在前一真实进程结束、全部路径和重启比较通过、
二进制摘要不变后，清除所有 Q4T_ 与 LD_PRELOAD 环境运行
原 11 题 HTTP，对照 7364767 原输出。不会改写质量基线。
此时质量题尚未运行；首轮 8K 修复前的未启动记录保持原状。

该舍入改动只用于验证已定位的 T=1 分支原因。尚未统一实际
多 token indexer 的精度边界，不作为最终多序列实现或生产
精度结论。所有原始分叉、失败和中间观察均保留。

## 2026-09-22 质量与严格性能门禁

点积舍入三侧九请求已完成且输出一致，服务退出 0。
无定位环境的原 11 道质量题全部精确通过，prompt/完整输出
与 7364767 相同，长度/停止原因正确；quality-review.json 留证。

严格性能门禁在 1K 首档退出：相同 prompt、三次稳定、各输出
256，但文本与旧调度参考不同，后四档未运行。失败位于
`decode-rope-indexer-round-performance-20260922/failure-review.json`。
这不作为性能回退数值结论，也不能因质量题通过而接受。

当前独立定位矩阵 `decode-path-parent-fallback-matrix-20260922`
比较旧版回退→候选调度→旧版回退，覆盖五档各三次、输出 256。
父版 binary 与已接受 7364767 摘要相同；候选与后侧父版均严格
对照前侧回退输出。不修改或替换旧调度参考，结果即使通过也
不自动接受；它用于确认修复是否保持已有回退路径的完整行为。
在所需 E2E 尚未闭合前，不进入底层数值或性能细分析。

## STATUS 过程记录存档（2026-09-22）

以下为精简 STATUS 前的过程原文，含当时进行中描述，仅用于追溯。
当前状态以本报告顶部及 STATUS.md 为准；原失败不覆盖。

### 当时的候选过程记录

父版 7364767 已推送 main；以下改动未接受、未提交。
完整定位链见 [报告](DECODE_PATH_CONSISTENCY_2026-09-21.md)，
各原始实验保存在 `.q4t-work/e2e/`，历史失败和中间观察保留。

已确认三个实现边界：

1. ModelDecodeStep 写入当前 position + rope_delta 的三行 RoPE
   坐标；旧 ModelDecodeBatchMulti 未写入，却把 pooled 表传给
   full attention。最小候选按实际 seq_id 补写，但单独补写
   并未消除调度/回退的输出差异。
2. 单图首步第 0 层，初始 SSM/conv 状态、投影和卷积输出摘要
   相同，GDN recurrence 输出首次不同。共享模式统一归约/更新
   后三侧一致；默认模式复用寄存器计算体、按 token/真实 slot
   选择状态后，九个单图 HTTP 输出和边界摘要均保持旧回退。
3. 扩展 1K/8K/单图 27 请求时，1K/单图一致，8K 分叉重启可复现，
   新回退保持旧输出。因此首次质量依赖驱动拒绝启动，未运行
   质量题。8K 逐层日志确认 prefill 全层和 decode 前三层相同，
   首个 full attention（第 3 层）的输入相同、输出不同。
   内部 query/gate/index query/压缩 key 摘要相同，评分与所选
   位置序列不同。专用评分每 head 点积保留 FP32，而 GEMM 路径
   先存 BF16；专用 T=1 只增加该舍入后，8K 三侧九请求完整输出
   和主干边界匹配旧回退，评分摘要仍不全等。

这些都是实际 HTTP 内的故障定位；摘要相同不等于完整张量
精度检验，位置序列摘要不同也不能推断选中集合一定不同。
插桩各轮已核对不改变对应输出，不用于性能结论。

质量已通过：`decode-rope-indexer-round-quality-20260921` 的
11/11 题答案精确正确，输入/完整输出与 7364767 相同，长度和
停止原因正确，服务退出 0，quality-review.json 留证。全部
定位环境关闭。这不证明全面模型精度，仍不能接受候选。

严格五档门禁未通过：`decode-rope-indexer-round-performance-20260922`
在首档 1K 因输出与旧调度参考不同退出；prompt 相同，三次
输出稳定、实际长度正确、服务退出 0，后四档未执行。
failure-review.json 留证，质量通过不覆盖此失败，参考不更新。
该首档候选 decode 为 17.778–17.924 tok/s，旧调度记录
18.529–18.616，初次不利范围分离；输出也不同，不能直接
归因于某个 kernel。initial-range-review.json 保留观察，需
相邻对照复核，不能以路径一致性替代性能接受。

当前独立定位：`decode-path-parent-fallback-matrix-20260922`，
旧版回退→候选调度→旧版回退，五档各三次、输出 256，固定
max_len=208896/max_prefill=8192/MTP off。父版二进制摘要核对
7364767 已接受记录；候选及后侧父版严格对照前侧父版回退的
完整输出，仍保留旧调度输出门禁失败。这不是正式性能接受，
不替代旧调度参考，也不允许开始底层数值/profile。
该目录 review.py 已核对全部 45 个原始 HTTP 响应，三组各五档
各三次均完成，三个服务退出 0，实际长度/停止原因正确，完整
输出均匹配前侧旧回退。raw-response-review.json 留证；旧调度
输出门禁失败仍保留，不更新正式参考。

三侧性能复核确认：候选 1K/4K decode、4K/8K/44K/200K TTFT
对前后两侧均不利范围分离，8K/44K/200K decode 范围重叠。
200K TTFT 候选 159.895–160.419 秒，前侧 159.032–159.594，
后侧 159.107–159.769。相同输出下回退仍存在，当前候选不接受。

三侧驱动正常结束后，已应用 .q4t-work/prepared/
decode-trace-cleanup-20260922 的六文件诊断清理，逐文件原始摘要
核对通过，旧二进制和源码差异已归档。仅删除临时日志/诊断接口，
保留 RoPE/GDN/indexer 修复；尚未证明能消除回退。构建已完成
且零警告，首项严格质量 HTTP 已通过 11/11，完整输出和输入
摘要匹配 7364767，服务退出 0，证据目录
decode-trace-clean-quality-20260922。独立五档性能 HTTP 已完成，
目录 decode-trace-clean-performance-20260922，严格检查清理前后
输出，并单列旧调度输出差异和前后旧回退性能范围。这是定位
复验，不替代原正式参考，不执行前置底层测试。
清理版五档 15 请求已全部完成，服务退出 0，完整输出保持清理前
候选和旧回退；相对原调度输出仍不同。1K/4K decode、8K/44K/
200K TTFT 对两侧旧回退仍不利分离，4K TTFT 仅对前侧分离，
对后侧重叠；长档 decode 与旧回退重叠。200K TTFT 为
160.06403–160.47229 秒。清理前后五档 TTFT/decode 均范围重叠，
没有证据说明删除诊断代码解决回退；清理版不接受。

五档正常结束后核对源文件和二进制摘要，归档清理版，然后应用
独立寻址候选 gdn-sequence-base-20260922：仅将 packed sequence
池偏移放到 ssm 基指针，恢复寄存器计算体原 int head 索引。
递推/归一化/归约顺序未作意图变更，池偏移仍用 size_t；它是
静态候选，不是已证实根因或收益。必要构建已成功且零警告，
首项原 7364767 十一题质量 HTTP 已通过，答案/完整输出/输入
摘要一致，服务退出 0，证据 gdn-sequence-base-quality-20260922。
五档 HTTP 性能复验已完成，gdn-sequence-base-performance-20260922，
严格检查改动前后输出，并单列原调度输出差异及各版性能范围。
五档十五请求已完成、服务退出 0，完整输出保持清理版：4K TTFT
2.62813–2.64899 秒，与两侧旧回退范围重叠；8K TTFT
5.16639–5.17282 秒，低于清理版 5.20583–5.21433 秒，并与
原调度范围重叠。1K/4K decode 仍低于两侧旧回退，寻址调整
未消除短档 decode 回退。44K TTFT 30.46071–30.50481 秒，
低于清理版 30.66260–30.69990，与两侧旧回退及原调度范围重叠；
decode 对旧回退范围重叠。200K TTFT 159.19227–159.71833 秒，
与两侧旧回退和原调度范围重叠，低于清理版；decode 对旧回退
仍重叠。短档回退未解决，整体不接受，正式参考不变。

同一二进制的路径定位已完成：gdn-sequence-base-path-pair-20260922，
以前一轮完整矩阵的调度结果作前侧，再依次强制回退、重启调度，
只复核 1K/4K/8K 各三次。这是短档故障定位，不替代完整五档
验收；使用原 tools/evalscope HTTP 驱动，仅包装长度列表，保存
包装器、完整命令和二进制摘要。核对真实调度/回退启动日志，
输出仍须匹配当前候选，原调度输出失败继续保留。无源码或
二进制修改，不执行数值/单测/profile。
同二进制三侧短档全部完成，三侧各九请求输出一致、服务均退出 0。
回退 1K/4K decode 为 18.03673–18.20607 / 17.36555–17.50058
tok/s，前后调度均低于这些范围；8K 三侧范围重叠。已确认
差异可在同一二进制重启后复现，不是仅由跨版本造成。
源码 short indexer 路径不同与边界相符，仍待独立因果验证。

三侧正常结束后核对源码和二进制摘要、归档旧版，应用单文件
indexer-dot-vector-load-20260922：专用 hd=128 短评分每次加载
四个 BF16，保持逐维累加顺序及每 head BF16 舍入，未改归约
或 top-k。仅改读取方式，不预设提速或因果结论。必要构建
成功且零警告，首项原十一题质量 HTTP 已通过，输入摘要、
完整输出与 7364767 相同，服务退出 0，证据
indexer-dot-vector-quality-20260922。五档性能 HTTP 已启动，
indexer-dot-vector-performance-20260922。前两档六请求输出保持
寻址版，1K/4K decode 均值 18.04992/17.38354 tok/s，
范围 17.86657–18.14442 / 17.19517–17.48174，已与同二进制
回退记录重叠；均值优于改动前，但尚未完成五档或相邻复核，
不宣称稳定收益。原调度输出差异与相对其 decode 差距仍保留。
现已完成五档十五请求、服务退出 0：8K/44K TTFT 为 5.17632–5.18727 /
30.50588–30.54332 秒，对紧邻寻址版原五档记录不利范围分离，
decode 则重叠。8K 与寻址版另一次调度重启记录重叠，仍待
相邻复核，不能直接按波动放行。200K TTFT 159.15763–159.68756
秒、decode 16.33799–16.51542 tok/s，均与寻址版范围重叠。审查补入
固定 fcb5925 的原始五档参考（short-topk-register-20260921），
五档相对固定参考 decode 仍不利分离且完整输出不同。
当前不接受。已启动 indexer-dot-vector-parent-confirm-20260922，
使用归档的寻址版二进制运行完整五档，补齐旧版→候选→旧版；
旧版源码差异、原记录与摘要留证，当前工作区源码/二进制未改。
原始三侧审查脚本已准备，不替换失败样本或正式参考。
后侧旧版 1K/4K/8K 已完成，输出一致；1K/4K 候选无不利
范围分离。8K TTFT 候选 5.17632–5.18727 秒，后侧旧版
5.16995–5.17580 秒，前后两侧均不利分离；decode 重叠。
即使差距小也不放行。后侧 44K 已完成，TTFT
30.45969–30.51290 秒，与候选重叠，44K 初次不利分离未在后侧
复现；8K 风险仍在。最后 200K 继续执行。
三侧五档 45 请求全部完成、服务退出 0、输出一致。后侧 200K
两指标均与候选重叠，8K 双侧 TTFT 回退仍在，向量读取不接受。
已核对摘要并归档该候选二进制/源码，应用 indexer-short-block-20260922：
撤掉向量读取，以标量寻址版为基底，仅将短评分 launch 从 8×256
改为 64×32。每线程仍独立计算同一顺序的一个评分，2048 个评分
槽覆盖不变。1K 首步有效评分 block 数由 1 增至 8 是静态映射
事实，不等于实测占用率或性能收益。必要构建已成功且零警告，
首项原十一题质量 HTTP 已通过，完整输出/输入摘要匹配 7364767，
服务退出 0，证据 indexer-short-block-quality-20260922。
五档性能 HTTP 已启动，indexer-short-block-performance-20260922，
输出对照标量寻址版，性能另列最近旧版、直接父版与固定参考。
五档十五请求已完成、服务退出 0，输出保持标量寻址版：1K/4K decode 均值
18.05034/17.35288 tok/s，4K 范围高于两组标量旧版记录，
1K/4K 均与同输出回退路径范围重叠。8K TTFT
5.17136–5.17968 秒，与两组标量旧版和 7364767 范围重叠，
未复现向量读取版的 8K 不利分离。44K TTFT
30.45311–30.52550 秒、decode 17.05695–17.19249 tok/s，
两指标均与两组标量寻址版范围重叠；TTFT 与 7364767 重叠。
200K TTFT 159.24216–159.80675 秒、decode
16.39066–16.51783 tok/s，与两组标量版均范围重叠。原调度输出
与 decode 差距仍保留；正式参考不变，不能以候选间不回退宣布
正式接受，消费者工具的完整门禁也不能据此伪造通过。

下一独立候选已应用：gdn-packed-state-vector-20260922，仅对
PackedDecode、ROWS=4/8 的 GDN 状态用 float4 加载/存储，
递推/归一化/归约计算体不变；非打包和 ROWS=1/2 保持标量。
固定 kd=vd=128、列组和池偏移确保 16 字节对齐。旧二进制/源码
已归档，必要构建零警告，首项原十一题质量 HTTP 已通过，
完整输出/输入摘要匹配 7364767，服务退出 0，证据
gdn-packed-state-vector-quality-20260922。五档性能 HTTP 已启动，
gdn-packed-state-vector-performance-20260922；不预设机器指令
或性能收益，不执行前置数值/单测/profile。
前四档十二请求已完成，输出保持线程块版；decode 均值分别为
18.74067/18.02965/18.04010/17.84325 tok/s，均高于上一候选
重复范围，与 7364767 对应范围重叠。但 8K/44K TTFT 为
5.17697–5.18491 / 30.51535–30.54670 秒，对正式父版原记录
不利范围分离，需相邻复核，不能以 decode 收益抵消。200K
仍在执行；旧调度输出差异未闭合，整体不接受。
五档十五请求现已全部完成、服务退出 0，完整输出保持上一候选。
200K TTFT 159.30507–159.77333 秒，decode 16.95967–17.08949
（均值 17.00662）tok/s，均与正式父版范围重叠；decode 高于
上一候选范围。五档 decode 对正式父版及固定参考均无不利
范围分离，但这不是输出门禁通过。已启动
gdn-packed-state-vector-parent-pair-20260922 的 8K/44K
父版→候选→父版相邻 HTTP；两版分别对照各自完整输出，
跨版本差异保留，不据此推断单个 kernel 因果。无数值/profile。
定位报告顶部已补当前五档结果与未闭合门禁，早期进行中描述
明确标为历史阶段，避免把中间候选误认作当前状态。
相邻前侧父版两档六请求已完成，输出检查通过、服务退出 0；
8K TTFT 5.17338–5.28111 秒、44K 30.47341–30.52564 秒。
首个 8K 请求为该服务加载后首请求，保留全部样本；该短矩阵
顺序与原完整五档不同，不替代完整五档结果。候选组继续执行。
候选组现已完成两档六请求、服务退出 0，输出保持候选原记录；
8K TTFT 5.16690–5.28093 秒、44K 30.47325–30.49295 秒，
两档 TTFT/decode 均与前侧父版范围重叠。后侧父版正在执行，
尚不判定相邻复核通过；正式输出差异继续保留。
三侧十八请求全部完成，驱动及三个服务退出 0，各版输出保持
自身参考。8K 两指标与双侧范围重叠；44K TTFT 与双侧重叠，
初次 TTFT 不利分离未复现。但 44K decode 候选最大值
17.856872638 tok/s，后侧父版最小值 17.857021871，形成单侧
不利分离（与前侧重叠）。差距小不能自动忽略，整体仍不接受。
原始审查明确标记该项；额外“全部范围通过”的汇总断言因此失败，
不影响十八个 HTTP 已完成的事实。已启动独立 44K 三侧复核
gdn-packed-state-vector-44k-confirm-20260922，PID 1827688；
源码/二进制不变，不替换之前样本，不设置新容忍百分比。
已准备 .q4t-work/prepared/decode-batch-http-20260922 的只读
D2H 观察器及 HTTP 定位协议，尚未编译/执行。计划在普通主模型、
MTP off、禁止 batched prefill 的明确路径内记录双 token 读回，
区分请求并发与实际 B=2；slot 编号/混合上下文覆盖另行验证。
这是故障定位准备，不伪造正式消费者验收或绕过输出门禁。
定位驱动已准备：1K/8K 各自无观察器串行三次，再在观察器下
串行三次、并发二请求三轮、串行三次，共 30 个 HTTP 请求。
直接使用 tools/evalscope，覆盖事件不足或输出不等明确留证，
HTTP 完成不等于一致性通过。仍未编译/启动，等待当前复核结束。
独立 44K 三侧九请求现已完成，三个服务及驱动退出 0，各版
输出保持自身参考。候选 TTFT 30.52213–30.61802 秒、decode
17.83736–17.85722 tok/s，与两侧父版范围均重叠；之前单侧
分离未复现，原失败完整保留，不据此宣称稳定提速。
正式输出门禁仍未闭合，候选不接受。随后确认无 q4t 存活，
启动 decode-batch-http-20260922 故障定位（PID 1833694），
先编译只读 host 观察器，首项运行仍为真实 HTTP；不运行
数值/单测/profile，也不使用正式消费者工具的接受入口。
B=2 定位的 1K 已出现可复现输出差异：无观察器串行及观察器下
串行前/后各三次均保持同一输出；并发二请求三轮每轮均记录
126 次成功双 token 读回，但产生两个不同输出，三轮的两个摘要
分别重复。与串行的共同字符前缀为 309/380（不是 token 定位）。
各请求长度/停止原因正确。这证明该请求组并发路径不保持串行
完整输出，不能据此直接判断状态污染、slot bug 或量化误差。
证据 decode-batch-http-20260922/one-k-divergence.json，
8K 继续执行，尚未结束；所有分叉保留，当前候选仍不接受。
本轮 30 个 HTTP 请求已全部完成，原始数据库逐请求审查通过，
两个服务及驱动退出 0。1K/8K 各三轮并发均记录 126 次成功双
token 读回且输出分叉，两档串行前/后均保持无观察器输出。
串行相等尚不能排除观察器对并发调度的影响，故在终态及无
q4t 存活核对后，启动 decode-batch-unobserved-20260922，
PID 1838520，禁用 LD_PRELOAD、同二进制/配置再跑 24 请求。
逐案例比较完整输出摘要多重集合；此无观察器组不单独声称
实际 B=2 覆盖，不使用耗时作为性能结论。原分叉和门禁保持。
无观察器 1K 十二请求已完成，串行前/后及三轮并发的完整输出
多重集合均与观察器版本相同，分叉不依赖加载观察器；8K 继续。
静态审查发现通用 IndexerLogitsKernel（pooled T>1 短评分及
分块评分）缺少 T=1/GEMM 已有的每 head BF16 舍入。已在
.q4t-work/prepared/indexer-pooled-dot-round-20260922 准备仅
统一该舍入点的单文件补丁，尚未应用/构建/测试。不改递推、
归约/选择顺序或长 OnePass 路径，不能预先认定分叉根因。
无观察器 24 请求及服务/驱动均已正常结束，十个案例的完整
输出多重集合全部匹配观察器版本，分叉不依赖加载观察器。
核对终态、无 q4t 存活、源码及二进制摘要后，已归档旧二进制
和完整差异，并应用通用评分每 head BF16 舍入的单文件候选。
必要构建进行中，首项计划为 30 请求的真实 HTTP 定位，串行
输出仍须匹配改动前；无前置数值/单测/profile，尚未验证候选。
构建已成功且零警告，首项 indexer-pooled-dot-round-http-20260922
真实 HTTP 已启动（PID 1842616）；当前二进制 SHA-256 为
4752eb4a4920aa7219b91b531ed41cb28f08221718a72491af99bba00dde202a。
仅编译链接构建目标，没有运行 bench 或低层测试。

首次质量依赖未启动记录仍保留于
`decode-rope-gdn-register-quality-20260921/not-started.json`。

尚未完成：最终候选五档性能及直接父版/固定参考对照；真实 B>1、
slot 复用、生命周期、视觉/视频与 MTP 消费者 HTTP；门禁后的
参考精度审查。诊断清理后的构建和首项质量 HTTP 已完成，五档
性能及其余消费者验收仍在后续范围内。
当前点积舍入仅改专用 T=1 分支，不能冒充完整多形状方案。
静态覆盖审查另记录 mixed-context 的 batch max_position 会选择
整个批次的评分分支，多序列 prefill/MTP 的 GDN 仍走共享实现；
需实际 B>1、跨 8192 边界和非零 slot 的 HTTP 证据。审查表为
当前五档定位目录 pending-branch-coverage.json，尚未运行这些场景。
不更新输出参考，不将路径一致性当作精度接受，不执行前置
数值测试或 profile，不宣称吞吐收益。

## 2026-09-22：单次分配候选独立重启对照未通过

九服务/27 HTTP 全部完成，原始请求、响应、长度、结束原因及
指标公式核对完整，驱动/后续和九服务均退出 0。PID 2067359
及条件消费者 PID 2075538 均已退出。三个服务首请求 decode
候选范围 17.873087–17.962282 tok/s，固定参考范围
18.007338–18.072623 tok/s，存在不利范围分离；后续六请求
候选 18.064545–18.166342、固定参考 18.150260–18.198257，
有重叠。对正式父版的首请求和后续范围均重叠。TTFT 更低，
但不能覆盖首请求 decode 回退，本候选不接受。

条件驱动记录 started=false / restart performance regression，
MTP 与 fallback 请求没有启动。完整输出精度仍未闭合；不能
把 11 质量题、采集退出 0 或后续请求重叠当成整体通过。
证据目录 `linear-scratch-single-allocation-restarts-20260922`
及 `linear-scratch-single-allocation-consumers-20260922`。
下一步处理当前实现或回退，保留全部失败样本；没有进行低层
数值、单测或 profile。运行时仍未接受、未提交。

## 2026-09-22：撤下未通过组合候选，隔离 scratch 分配改动

九次重启及条件驱动均已退出且无推理服务后，完整归档九个
运行时文件、rejected-runtime.patch 与旧 0511 二进制于
`prepared/linear-scratch-isolated-20260922`。确认 HEAD 这九个
文件与正式 7364767 相同后恢复，仅在原算术/分派上应用六块
临时缓冲的单次分配；不复用 raw 存储，也不带入 FP32 GDN、
ROWS4、QKV/GEMV、indexer 或 RoPE 组合改动。此举隔离内存
所有权改变的影响，decode/MTP 一致性问题仍待修复，未取消。
其他任务的文档未覆盖，失败记录和原始响应保持原样。

必要构建退出 0、零警告；没有运行构建生成的测试程序。
新二进制 SHA 为
`4d4d82dfdd3e534c8e673791789b5ad45fe013bb8152541567a27067bc3f231c`。
首项严格质量 HTTP PID 2080585，证据
`linear-scratch-isolated-20260922`；PID 2080586 等待该进程
真实退出、质量原始审查和 SHA 一致后才运行完整五档性能，
证据 `linear-scratch-isolated-performance-20260922`。此次
性能采集显式使用正式 7364767 完整输出 reference，不能仅
凭内部重复一致放行。下一步先完成质量及五档 E2E，再根据
结果复核性能/消费者；不做前置单测、数值测试或 profile。

### 2026-09-22：packed BF16寄存器四列完整HTTP诊断（未接受）

候选二进制SHA d85e74e7c3d2515214860a0c671b1a46aba1292858fbff30b1337bf7a201b781。
在独立MRoPE修复上，packed decode复用单序列BF16舍入和寄存器递推；
内联归一化，packed每warp四列，单序列prefill保持原配置，MTP因果
路径尚未统一。质量HTTP11/11通过；正式性能验收在1K完整输出
不同后失败。随后同二进制完整五档诊断采集结束，驱动/服务退出0，
PID2142872已退出，15次原始请求、响应、token数、finish和计时
公式核对完成。诊断没有强制参考输出相同，不能用采集成功代替验收。

| 输入token | 平均TTFT秒 | 平均decode tok/s | 与正式参考完整输出相同 |
|---|---:|---:|---|
| 1024 | 0.806370 | 17.889782 | 否 |
| 4096 | 2.652455 | 17.194271 | 否 |
| 8192 | 5.205392 | 17.444752 | 否 |
| 45056 | 30.672384 | 17.201496 | 否 |
| 204800 | 160.248305 | 16.503583 | 否 |

固定原prompt、每档三次、输出256、MTP关闭。五档后续两次decode
范围均低于正式父版7364767及固定参考fcb5925；首请求另列于
ordered-samples-review.json，仅1K首请求是服务加载首请求。
当前候选不接受，不以输出改变后的吞吐差异证明单个kernel因果。
44K样本候选空think段直接进入正文，而正式参考继续思考，不能
仅凭文字可读断言精度无损，也不能声称所有差异都是near-tie。

证据：`.q4t-work/e2e/decode-gdn-register-rows4-full-diagnostic-20260922/`
中的results、raw-response-audit、formal-output-comparison和
ordered-samples-review。原始样本保留，正式参考未修改。

下一步已启动已接受7364767二进制的实际fallback路径五档HTTP
诊断PID2147170，证据`accepted-fallback-full-diagnostic-20260922`。
使用原已验证scheduler分配故障注入库触发fallback，保存二进制/
注入库SHA，核对日志确认MTP关闭且scheduler没有启动；同原prompt
三次256输出，结束后比较当前候选与正式参考。此项仅定位路径
差异，不是接受候选、替换参考或低层数值测试。启动时前一服务
已经退出，无并行GPU测试。结果尚未完成。


### 正式回退完整五档对照结果

`accepted-fallback-full-diagnostic-20260922`已完成，驱动/服务0，
15次原始请求和响应核对通过。正式7364767二进制经既有分配
故障注入进入实际fallback；原prompt，每档三次，输出256。

| 输入token | 同d85e74e7候选调度输出 | 同原正式调度输出 |
|---|---|---|
| 1024 | 是 | 否 |
| 4096 | 否 | 否 |
| 8192 | 否 | 否 |
| 45056 | 是 | 否 |
| 204800 | 是 | 否 |

此结果证明1K对齐不能推广至4K/8K；不是全局正确性判定。
下一步同候选二进制4K/8K回退对照已启动（监督PID2151054，
`decode-gdn-register-rows4-fallback-20260922`），先排除跨版本
干扰。当前代码仍未接受，原正式与固定参考不变。

### 短indexer每head BF16舍入的受控干预

同d85二进制fallback的4K/8K六次HTTP输出已核对，同正式回退，
不同同二进制调度。随后只增加IndexerDecodeScores每head点积
的BF16舍入，再转float进行原ReLU/head求和；其余累加/缩放
保持。源码依据是无d_seq_id的GEMM将各head点积存为BF16，
IndexerReduce读取后再ReLU/求和，而原短decode直接使用FP32。

候选d0561816零警告构建后首项质量HTTP11/11原始核对通过。
完整五档诊断正在运行PID2154983，前三档九次原始核对完成：
1K/4K/8K全部输出同正式回退；对修改前d85，1K不变，4K/8K
由不同变为相同。支持舍入边界解释这些样本的差异，不能推广
至所有批量/MTP路径。前三档decode均值17.943441/17.192721/
17.411338 tok/s，且输出仍不同原正式调度参考，当前不接受。
44K/200K尚未完成。证据decode-indexer-bf16-round-full-diagnostic-20260922，
原参考及所有失败样本保留，未进行低层测试。

### d0561816完整五档诊断：输出对齐回退，仍不接受

PID2154983已退出，驱动/服务0，15次原始HTTP响应核对完成。
五档三次输出均同正式7364767回退，但均不同原正式调度参考。
对d85，只有4K/8K输出改变并对齐回退，其他三档输出保持。
此结果支持短indexer每head BF16舍入是本组4K/8K差异来源，
不能推广为所有模型输入/批量/MTP正确性的证明。

| 输入token | 平均TTFT秒 | 平均decode tok/s |
|---|---:|---:|
| 1024 | 0.802941 | 17.943441 |
| 4096 | 2.652957 | 17.192721 |
| 8192 | 5.208297 | 17.411338 |
| 45056 | 30.667568 | 17.187795 |
| 204800 | 160.294917 | 16.531844 |

五档后续两次decode范围均低于正式父版及固定参考，不接受。
证据decode-indexer-bf16-round-full-diagnostic-20260922的
raw-response-audit、formal-output-comparison、path-output-comparison、
ordered-samples-review；未更换正式参考。d056二进制归档于
prepared/decode-bf16-small-m-restored-20260922/q4t-before。

下一步恢复独立BF16 SmallM投影实现（只涉及gemv.cu、gemv.h、
linear.h），为M=2/3/4保留单行GEMV的逐行f32x2和shuffle算术。
这是此前真实并发HTTP定位的QKV差异对应修正；其他旧组合
改动不随之恢复，旧测试不作为新候选通过证据。

### 47aada1d小批量投影候选的单流诊断结束

质量11/11及完整五档15次原始HTTP核对结束，驱动/服务0。
五档输出同直接父候选d056及正式回退，均不同正式调度参考。
TTFT/decode均值依次为0.803770/17.885941、2.655870/17.179442、
5.210410/17.423041、30.701000/17.172479、160.413385/16.521353。
五档后续两次decode范围均低于正式父版及固定参考，未接受。
证据decode-bf16-small-m-full-diagnostic-20260922；未改参考。

下一步实际批次HTTP诊断已启动PID2169107，输出
decode-small-m-batches-observed-20260922。独立观察库记录调度
token缓冲读回行数，需校准ChatServer::Start分配调用者；先带
观察库再不带，各1K/8K、串行前后及B2/3/4三轮共132请求，
输出128，关闭MTP/批量prefill；不是正式性能或混合输入覆盖。
当前尚未证明观察库校准、实际批次覆盖或批处理输出一致。

### 实际批次观测首段校准与1K输出

观察库捕获唯一token缓冲，分配调用者解析为
q4t::server::ChatServer::Start。已完成1K串行前三次和实际
B2/3/4各三轮共30次原始响应核对，所有完整输出同串行；
每轮对应B的成功读回125–126次。监督PID2169107仍在执行，
后续串行、8K及不带观察库对照尚未完成；不作为性能证明。

### 47aada1d实际批次HTTP对照完整结果

`decode-small-m-batches-observed-20260922`共132次原始HTTP响应
核对完成，44个采集案例，两个服务及驱动退出0，PID2169107
已退出。精确二进制addr2line确认观察库捕获ChatServer::Start
的唯一调度token缓冲分配；各案例对应实际B成功读回覆盖成立。

| 输入 | 带观察库 | 不带观察库 |
|---|---|---|
| 1K | 串行前后及B2/3/4三轮全部输出一致 | 对应所有输出一致 |
| 8K | 串行前后一致；B2/3/4三轮九个案例均有输出不同串行 | 同样九个案例分歧 |

两组22个对应案例输出多重集全部一致，分歧不依赖观察库存在。
无观察库组不单独宣称实际批次覆盖。条件为max_seq4/max_len16384、
关闭MTP及批量prefill、输出128，同prompt；不是混合输入或
正式性能验收。原始失败和正式参考保留。

据此应用预备的三个文件修正：多行短indexer每head BF16舍入；
混合批次中短行保持独立时的打分顺序/BF16边界；短行top-k
保留原排序输入和稠密展开，避免同批较长行改变其算术路径。
原47aada1d二进制与源码已归档，旧测试不计为新候选通过。
必要构建后首项仍为固定正式参考质量HTTP，尚待本轮验证。

### 5cdf9ed3 pooled indexer修正的单流诊断结束

质量11/11和完整五档15次原始HTTP核对结束，服务/驱动0。
五档输出同47aada1d及正式回退，仍不同正式调度参考；五档
后续两次decode范围低于正式父版与固定参考，不接受。
TTFT/decode均值依次0.805422/17.878437、2.656462/17.189580、
5.214342/17.393482、30.691476/17.187493、160.403315/16.595749。
证据decode-pooled-indexer-full-diagnostic-20260922。

原进程2178882结束后，衔接驱动2183499核对原始记录及SHA，
串行启动132请求复验PID2186451。新二进制的观察库分配调用者
已再次解析为ChatServer::Start，实际批次及输出复验尚未结束。
证据decode-pooled-indexer-batches-observed-20260922；不重置参考。

### 修正后8K实际B2的首段证据

5cdf9ed3的132请求复验仍运行（PID2186451）。已核对40次
原始响应：1K全部33次一致，8K串行三次及B2前两轮七次
一致，每轮126次成功双行读回。1K/8K串行输出摘要同此前
47aada1d版本，比较目标未变；旧版相同8K B2案例分歧，
新候选前两轮对齐。仅证明这些样本，剩余批次/无观察库/
异长混合/MTP仍未闭合，性能与正式参考输出条件未通过。

### 修正后带观察库完整两档66请求

当前5cdf9ed3的观察组1K/8K各33请求全部与各自串行输出一致，
两档B2/3/4各三轮均确认126次对应批宽成功读回；串行前后相同。
原始响应审查共69次（含无观察库1K串行前三次）。旧47aada1d
8K九个失败批处理案例在本观察组不再分歧，但无观察库剩余
对照尚在运行，不能推广为132次通过或性能验收通过。
证据目录decode-pooled-indexer-batches-observed-20260922，
原PID2186451及衔接PID2183499继续运行，没有重启或修改二进制。

### 2026-09-23：当前批处理与异长混合诊断完成

132请求44案例全部与各自串行相同，观察/无观察22对输出
集合全相同。观察组1K/8K的B2/3/4各三轮均126次对应批宽
成功读回。36异长混合请求全同各自串行前后，1K/4K/8K/44K
三排列各121次实际B4。独立原始响应审查132/132、36/36，
驱动/三服务全部退出0，衔接控制器正常终态。二进制5cdf9ed3
未变。该请求集的批处理分歧得到修复，但正式输出/性能条件
仍不通过；不能扩大为全部精度/MTP正确性结论。

继续当前二进制MTP开关三侧HTTP诊断27请求，PID2249625，
证据decode-pooled-indexer-mtp-switch-http-20260923，尚未完成。

### 2026-09-23：因果寄存器统一候选五档完成，未接受

bb74466f候选将默认128×128 pooled causal与单序列checkpoint
复用BF16寄存器递推，保持真实seq/局部token检查点布局。
必要构建0零警告，首项固定质量HTTP11/11原始核对一致。
完整五档15条原始响应已核对，驱动/服务退出0：

| 输入 | TTFT均值（秒） | decode均值（tok/s） |
|---|---:|---:|
| 1024 | 0.804587 | 17.898625 |
| 4096 | 2.651664 | 17.212833 |
| 8192 | 5.210757 | 17.510955 |
| 45056 | 30.725207 | 17.214510 |
| 204800 | 160.491427 | 16.603204 |

输出均同5cdf9ed3/正式回退，但不同正式调度参考；五档后续
两次decode范围均低于正式/固定参考，TTFT对正式参考不利。
不接受、不重设参考，均值表仅描述采集，不替代有序范围检查。
证据decode-causal-register-full-diagnostic-20260923。
原父候选MTP27请求显示8K/44K不同普通前后，1K相同；当前
bb74466f同请求MTP三侧复验已启动PID2261423，尚未完成。

### 因果寄存器候选MTP三侧诊断完成

bb74466f的三档×三次×三个服务共27响应完成原始审查，
1K/8K/44K的MTP输出均同普通前后，全部重复确定，驱动及
三服务退出0。普通三档完整摘要同修改前5cdf9ed3，未更换
比较目标。该请求集原8K/44K分歧已消失，不推广为全面MTP
或回滚正确，不覆盖五档性能与正式输出未通过条件。

下一候选4f363cb仅将packed非因果ROWS4状态读写经shared tile
连续搬运，已零警告构建，首项固定质量HTTP PID2266893启动；
尚无结果。证据decode-register-state-staging-20260923。

### 状态搬运4f363cb五档完成，仍未接受

首项质量11/11和五档15条原始响应核对完成，驱动/服务0。
输出全同bb74466f/正式回退，但不同正式调度参考。TTFT均值
0.803081/2.655928/5.209116/30.671036/160.288323秒；decode
18.572476/17.884083/18.194274/17.898155/17.165730 tok/s。
五档decode高于直接父版；4K以上后续decode对正式/固定参考
无不利范围分离，仍有五档TTFT对正式不利和1K decode对固定
不利。原样本全部保留，不接受、不宣称全面精度/性能通过。

预先确定的正式→候选→固定→候选→正式75请求对照已启动
PID2270980，证据decode-state-staging-adjacent-20260923。
全部60项有序比较预定，首请求单列、后续范围不混合；该诊断
不能重设输出参考或覆盖旧失败。尚未完成。


### 2026-09-23：状态搬运相邻五服务终态

`decode-state-staging-adjacent-20260923`预定75条HTTP全部完成，
五服务和总驱动退出0，原始响应核对通过。60项比较中24项
后续范围不利：两轮候选对前后正式的全部五档TTFT（20项），
第一轮1K decode对三个参考、第二轮对后侧正式（4项）。
不同服务重复范围不合并，首请求单列，历史失败保留。
候选与正式/固定输出仍不同，当前不接受。下一步隔离普通
单序列GDN状态索引宽度，不将当前质量题通过替代精度验收。
