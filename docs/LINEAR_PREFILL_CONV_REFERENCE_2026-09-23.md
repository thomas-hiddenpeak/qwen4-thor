# 完整4K prefill短卷积参考

2026-09-23。生产运行时仍为2392f6b1，未改计算或调度。原4K题期望
360284，实际600440未修复；本报告不能替代整模型质量验收。

## HTTP先行与实际边界

新观测器零警告构建后，第一项测试为tools/evalscope原4K请求：
无观测→观测→无观测。三侧均4096输入、7输出、600440、stop，服务
退出0、质量驱动退出1；这是观测不改变原错的检查，不是质量通过，
观测耗时不作性能数据。

36层均捕获完整[4096,10240]卷积输入/输出，实际[10240,4]权重、
[10240,3]更新前/卷积后/更新后的历史，并在GDN归一化之前保存完整
实际输入。实际输入和卷积输出指针不同，conv_k=4、num_ckpt=0。
历史更新指针绑定同一份原始输入和历史；GDN前处理指针绑定卷积输出。
36份元数据全部完整，所有阶段检查通过。

36组权重逐字节匹配checkpoint中各层linear_attn.conv1d.weight，
存储形状[10240,1,4]。历史初态共1105920个BF16全为正零，卷积本身
不改变历史；更新后的1105920项均同原始投影输入最后三行的转置，
而非卷积/SiLU输出。全部首三个位置也纳入独立参考，非只比较末端窗口。

每层卷积输出同本轮GDN输入完整字节相同，还与此前完整GDN快照的
prefill-qkv-before.bf16逐文件摘要一致；旧artifact-binding核对通过。
因此此次完整卷积计算结果接通已核对的q/k归一化和全4096步递推。

## 全量数值结果

CPU独立按每个位置t与通道c重建四项因果窗口：负位置从实际更新前
历史读取，其余从原始输入读取。按k=0,1,2,3用显式FP32 fma累加，
仅把生成的累加值送给独立设备SiLU辅助程序，未调用生产卷积kernel。

| 参考 | 1509949440个BF16输出的差异数 |
|---|---:|
| CPU四次fma＋设备SiLU | 0 |
| CPU四次fma＋CPU exp/FP32加法除法 | 3252 |
| FP64四项点积＋稳定SiLU公式，再FP32/BF16舍入 | 10296 |

三种完整结果、每层差异索引、CPU卷积累加值全部保留。不设误差
容忍阈值，也不把不同数学函数来源的差异直接判为生产kernel错误。
设备辅助保留__expf和实际SiLU除法；这不是独立证明设备数学库精度。

构建均零警告，分析与全量核查退出0。核查再次比较完整输出、差异
索引、checkpoint权重、三处历史及GDN输入绑定。没有挑选token或
只比较均值；也未用实际卷积累加值替代独立CPU累加。

## 失败记录与未覆盖项

首版观测器局部变量index与系统字符串函数同名，编译失败并有未使用
变量警告，尚未运行任何HTTP或数值测试。原源码/日志保留；改名后
零警告编译，再开始本次HTTP。该编译问题不是运行时或正确性缺陷。

本请求完整卷积→q/k归一化→GDN递推在给定实际原始QKV及a/b投影
输入的条件下，均符合指定算术。**尚未独立生成这些全token投影输入**，
旧投影参考只覆盖末prefill/首decode及记录算法重放；也未覆盖多序列、
MTP checkpoint、分块边界或其他上下文。更不能据此认定原错来自MoE，
或排除未验证路径。已知E4M3尺度编码问题仍在，原题仍错误。

下一步核对完整prefill QKV/a/b投影来源与参数生成边界。任何计算
修正仍需重新以真实HTTP质量和五档性能作门禁，不以此报告代替。

## 证据

`.q4t-work/e2e/linear-prefill-conv-20260923/`包含三侧HTTP原始报告、
实际全量快照、checkpoint-binding、gdn-binding、convolution-reference、
三种参考及差异索引、失败构建记录、artifact-binding。
模板位于tools/verify/linear_prefill_conv/。生成独立prepared目录后
构建observer.so零警告，run.py先HTTP；必要离线工具replay/silu构建
零警告后，analyze.py→audit.py。新实验使用新证据目录，不覆盖原件。
