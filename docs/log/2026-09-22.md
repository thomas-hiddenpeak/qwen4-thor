# 2026-09-22 — 开发日志

## 2026-09-22：B=2 HTTP 定位驱动准备完成

- 准备的驱动复用 tools/evalscope 请求与原始响应解析，1K/8K
  各自先无观察器串行三次，再观察器下串行三次、并发二请求
  三轮、串行三次，共 30 请求，每请求输出 128。
- 有实际双 token 读回事件才记录 B=2 覆盖；观察器输出必须与
  无观察器对照比较。差异保留到独立 review，不生成正式
  acceptance.json，HTTP 完成和输出一致性分别报告。
- 尚未编译或执行，当前 44K 三侧驱动 PID 1827688 存活；
  前侧父版已正常结束，候选组继续。下一步收齐该组后再定位。

## 2026-09-22：准备真实 B=2 decode HTTP 覆盖观察

- 静态核对普通 SchedulerLoop 以 B*sizeof(int32_t) 读回 GPU
  argmax；prefill 读回词表行。设备端独立 full attention 的
  position 读回是另一路径，定位协议必须明确限定主模型普通
  decode、MTP off，不能将任意 8-byte D2H 都称作 B=2。
- .q4t-work/prepared/decode-batch-http-20260922 保存协议和
  只读观察器源码，未编译、未执行。计划用 serial/parallel-2/
  serial 同 prompt HTTP 检查路径与复用；无实际双行读回不得
  声称合批覆盖，不推断 seq_id 编号或混合上下文正确性。
- 为什么：仅有两并发请求不保证真实 B=2。下一步仍先收齐
  PID 1827688 的 44K 三侧复核，当前运行时和二进制冻结。

## 2026-09-22：TTFT 双侧重叠，44K decode 单侧差异继续复核

- 三侧十八 HTTP 全部完成，服务/驱动退出 0，各版保持自身输出。
  8K 两指标、44K TTFT 与双侧父版范围重叠。
- 44K decode 候选最大 17.856872638、后侧父版最小
  17.857021871 tok/s，单侧不利分离；前侧仍重叠。保留原始
  审查标记，额外全范围通过断言失败如实记录，不能以小差距放行。
- 已确认原 PID 1820700 消失、终态成功且无 q4t 服务后，启动
  gdn-packed-state-vector-44k-confirm-20260922，PID 1827688，
  同二进制、独立父版→候选→父版，每组 44K 三次。
- 下一步检查双侧及启动首请求差异，全部样本保留；正式输出
  门禁仍未闭合，无底层数值/profile，候选未接受。

## 2026-09-22：相邻候选两档完成，与前侧父版范围重叠

- 候选两档六个 HTTP 请求输出保持原候选，长度、停止原因与
  原始摘要核对通过，服务退出 0。8K TTFT 5.16690–5.28093
  秒，44K 30.47325–30.49295 秒；两档 TTFT/decode 均与
  前侧父版重叠，初次 TTFT 分离尚未在本侧复现。
- 下一步等待同一驱动 PID 1820700 的后侧父版完成，再检查
  双侧范围。保留首请求、旧失败和正式输出差异，不将局部
  性能重叠作为精度或生产接受，无底层数值/profile。

## 2026-09-22：TTFT 相邻对照前侧父版完成

- gdn-packed-state-vector-parent-pair 的 parent-before 两档六请求
  完整输出保持正式父版，原始长度、停止原因和摘要核对通过，
  服务退出 0；8K TTFT 5.17338–5.28111 秒，44K
  30.47341–30.52564 秒。
- 本矩阵从 8K 开始，该档首请求含服务加载后首次请求影响；
  不剔除任何样本，不与完整五档顺序混为一次测量。
- 下一步候选及后侧父版按相同顺序完成，比较两侧范围。驱动
  PID 1820700 仍存活，源码/二进制冻结，不启动额外 GPU 工作。

## 2026-09-22：定位报告补齐当前证据边界

- DECODE_PATH_CONSISTENCY 报告顶部新增当前候选二进制摘要、
  五档真实均值、各参考关系和未闭合门禁；明确下面的进行中
  描述属于当时定位阶段，避免旧状态误导后续开发。
- 为什么：质量通过、候选输出保持、正式输出失败和性能范围
  是不同证据，不能混成“E2E 已接受”。原始失败不覆盖。
- 下一步等待 PID 1820700 的相邻 HTTP 对照结束；本次仅文档，
  不触碰运行时或启动额外测试，模型和 reference 保持只读。

## 2026-09-22：状态向量访问五档结束，开始 TTFT 相邻复核

- 五档十五个原始响应核对通过，输出保持上一线程块候选，
  服务/驱动退出 0；200K TTFT 159.30507–159.77333 秒，
  decode 16.95967–17.08949、均值 17.00662 tok/s。
- 五档 decode 均高于上一候选范围，与正式父版/固定参考范围
  重叠；8K/44K TTFT 对正式父版原记录的不利分离仍待复核。
  正式旧调度输出差异保留，不更新参考、不宣布接受。
- 确认上一进程终止、服务退出且无 q4t 存活后，启动
  gdn-packed-state-vector-parent-pair-20260922，PID 1820700。
  8K/44K 各三次，父版→候选→父版；各版检查自身完整输出。
- 为什么：区分可复现的 TTFT 回退与初次范围分离。下一步收齐
  相邻原始响应及终态，决定保留或修正；不运行底层测试/profile。

## 2026-09-22：状态向量访问前四档 decode 改善，TTFT 待复核

- gdn-packed-state-vector-performance-20260922 已完成前四档十二
  个 HTTP 请求，原始响应长度、停止原因、重复输出核对通过，
  完整输出保持上一线程块候选；正式旧调度输出差异仍保留。
- decode 均值 18.74067/18.02965/18.04010/17.84325 tok/s，
  四档均高于上一候选范围，与正式父版范围重叠。
- 8K/44K TTFT 5.17697–5.18491 / 30.51535–30.54670 秒，
  对正式父版原记录不利分离。不能用 decode 改善覆盖 TTFT 风险。
- 下一步等待已确认存活的 PID 1815222 完成 200K，收齐证据后
  进行必要相邻复核；不接受候选，不执行前置数值或 profile。

## 2026-09-22：打包 GDN 状态向量访问质量通过，开始五档性能

- 原十一题质量 HTTP 全部通过，完整输出、输入摘要、答案、
  实际长度和停止原因核对符合原参考，服务退出 0。
- 二进制摘要核对后启动 gdn-packed-state-vector-performance-20260922，
  五档各三次，输出对照刚完成的线程块版，原正式参考性能及
  输出差异继续单列保留，无前置数值/单测/profile。
- 下一步完整收齐性能，尚未认定向量状态访问收益，候选不接受。

## 2026-09-22：线程块五档收尾，打包 GDN 状态向量访问首项 HTTP 启动

- 线程块候选五档十五请求输出保持标量寻址版，服务退出 0。
  200K 两指标与两组标量版重叠；候选间无不利分离不等于原
  正式基线通过，原调度输出与 decode 差距仍保留。
- 完整结束后归档该版源码/二进制，仅改 PackedDecode 且
  ROWS=4/8 的 GDN 状态加载/存储为 float4，递推计算体不变；
  非打包和 ROWS=1/2 保持原标量访问。cuda 分配及固定形状的
  head/列组/池偏移满足 16 字节对齐，无新增分配。
- 为什么：独立验证打包状态访问开销，不预设性能根因或收益。
  必要构建零警告成功，第一项质量 HTTP 已启动，证据
  gdn-packed-state-vector-quality-20260922。下一步核对原十一题，
  通过后五档性能；无前置数值/单测/profile，候选仍未接受。

## 2026-09-22：线程块候选 44K 两指标与标量版范围重叠

- 44K 三请求输出保持标量寻址版，TTFT 30.45311–30.52550
  秒、decode 17.05695–17.19249 tok/s，均与两组标量版
  范围重叠；TTFT 与已接受 7364767 也重叠。
- 四档十二请求原始响应已核对，剩余 200K 继续运行。
  相对原调度的输出与 decode 差距仍保留，不把候选间通过
  当作正式接受。下一步完整收齐最后一档，无前置底层测试。

## 2026-09-22：线程块候选前三档完成，8K TTFT 范围重叠

- 前三档九请求输出保持标量寻址版。1K/4K decode 均值
  18.05034/17.35288 tok/s，4K 三次范围高于两组标量旧版
  记录，1K/4K 均与同输出回退路径范围重叠。
- 8K TTFT 5.17136–5.17968 秒，与两组标量旧版及原接受
  7364767 范围重叠，未复现被拒向量读取版的范围回退。
- 这仍不是正式接受：原调度输出及 decode 差距保留，长档
  仍运行。下一步完整收齐 44K/200K，不执行底层测试/profile。

## 2026-09-22：短评分线程块候选质量通过，开始五档性能

- 十一题质量 HTTP 全部通过，原始响应核对答案、长度、停止
  原因、完整输出与输入摘要均符合原参考，服务退出 0。
- 二进制摘要核对后启动 indexer-short-block-performance-20260922，
  五档各三次，输出对照标量寻址版，性能单列最近旧版与原基线；
  被拒绝的向量读取已不在源码中，不以其作为通过标准。
- 下一步完整收齐性能，当前没有收益结论，不接受候选，
  不执行前置数值/单测/profile，不更新正式参考。

## 2026-09-22：短评分线程块候选构建完成，首项质量 HTTP 启动

- 必要构建零警告成功，未运行测试或 bench 目标。
- indexer-short-block-quality-20260922 已启动原十一题 HTTP，
  输入/输出参考仍为 7364767；命令、摘要及源码差异留证。
- 下一步核对质量结果，通过后完整五档性能；当前未接受候选。

## 2026-09-22：读取候选拒绝，恢复标量并调整短评分线程块

- 三侧五档 45 请求全部完成，三个服务退出 0，完整输出一致。
  后侧 200K 两指标与候选重叠，但 8K 双侧 TTFT 不利分离
  仍在，因此拒绝向量读取。原始记录和候选源码/二进制已归档。
- 正常结束后核对摘要，应用已准备的线程块候选：恢复标量
  读取，相对标量寻址版只改 launch 从 8×256 为 64×32，
  逐线程点积、舍入和评分槽映射覆盖保持原定义。
- 为什么：独立验证短可见前缀只有少量 block 承担评分的问题，
  不预设收益。下一步必要构建后首项原十一题质量 HTTP，
  通过后五档性能，不执行前置底层测试或更新正式参考。

## 2026-09-22：44K 后侧范围重叠，准备短评分线程块候选

- 后侧旧版 44K 完成、输出一致；TTFT 30.45969–30.51290 秒
  与候选重叠，44K 初次不利分离未在后侧复现；8K 双侧回退
  仍不允许接受读取候选。最后 200K 继续运行。
- 静态准备 indexer-short-block-20260922：先丢弃向量读取，
  相对标量寻址版仅改 launch 为 64 blocks×32 threads。
  总计 2048 个独立评分槽不变，无跨线程归约；1K 首步有效
  评分 block 从 1 增至 8，4K 从 4 增至 32。未实测占用率或收益。
- 补丁与前后摘要仅存准备目录，未应用/未构建。下一步收齐
  当前矩阵后实施，再按质量 HTTP→五档性能顺序验证。

## 2026-09-22：后侧旧版复现 8K TTFT 不利分离

- 后侧旧版前三档九请求完成，输出匹配候选。1K/4K 两项指标
  无不利分离，候选 decode 均值较高但范围仍有重叠。
- 8K TTFT 候选 5.17632–5.18727 秒，后侧旧版
  5.16995–5.17580 秒，前后两侧均不利分离；decode 重叠。
  差距小不构成豁免，不能用短档提升抵消，也不替换样本。
- 下一步继续完整收齐 44K/200K，然后修正或回退读取候选；
  当前不接受，不执行底层测试/profile。

## 2026-09-22：向量读取五档结束，启动归档旧版相邻复核

- 候选五档十五请求全部完成，服务退出 0，完整输出保持寻址版。
  200K TTFT 159.15763–159.68756 秒、decode
  16.33799–16.51542 tok/s，两指标均与寻址版范围重叠。
- 8K/44K TTFT 的初次不利分离继续保留；相对固定 fcb5925
  五档 decode 仍不利分离且输出不同，不能以候选间通过覆盖。
- 完整结果核对后启动 indexer-dot-vector-parent-confirm-20260922，
  用归档寻址版二进制跑完整五档，补齐旧版→候选→旧版。
  原摘要与旧版源码证据核对，当前源码/二进制不变，无并发 GPU
  任务。下一步完成相邻范围复核，候选仍不接受。

## 2026-09-22：向量读取候选 8K/44K TTFT 待相邻复核

- 四档十二请求输出保持寻址版；8K/44K TTFT
  5.17632–5.18727 / 30.50588–30.54332 秒，对寻址版
  原五档记录出现不利分离；decode 范围重叠。8K 与寻址版
  另一次重启记录重叠，但不能直接用它覆盖本次不利观察。
- 分析补入固定 fcb5925 对应 short-topk-register-20260921
  原始记录；已完成各档 decode 仍低于固定参考，完整输出
  不同，均单列保存，不以候选间改善冒充正式接受。
- 下一步完成 200K，然后使用归档旧版补相邻复核。当前不接受，
  不改源码/二进制，不执行底层测试，全部失败样本保留。

## 2026-09-22：向量读取候选前两档 decode 接近回退范围

- 1K/4K 六请求输出保持寻址版，TTFT 无不利范围分离。
  decode 均值 18.04992/17.38354 tok/s，范围
  17.86657–18.14442 / 17.19517–17.48174，已与同二进制
  回退记录重叠；原始首请求低值仍保留，不替换样本。
- 均值优于读取改动前，但尚不足以宣称稳定收益或接受候选，
  原调度输出差异及其吞吐差距不被覆盖。下一步完成剩余三档。
- 寻址与三侧路径对照的文档检查点 11d6e40 已推送 main，
  运行时代码未提交，其他任务的文档内容保持未暂存。

## 2026-09-22：评分向量读取候选质量通过，开始五档性能 HTTP

- 十一题质量 HTTP 全部通过，原始响应核对答案、实际长度、
  停止原因、输入摘要和完整输出均符合 7364767 参考，服务退出 0。
- 二进制摘要核对后启动 indexer-dot-vector-performance-20260922，
  五档各三次，输出对照寻址版，同时保留原基线和同二进制路径
  对照的性能范围；原调度输出差异继续保留，不改正式参考。
- 下一步收齐五档，判断向量读取是否改善短档；当前尚无性能
  结论，不接受候选，不执行前置底层测试。

## 2026-09-22：评分向量读取候选构建完成，首项质量 HTTP 启动

- 必要构建成功、零警告；未运行测试目标或 bench。
- 原十一题质量 HTTP 已在 indexer-dot-vector-quality-20260922
  启动，输出参考仍为 7364767；命令、摘要与源码差异留证。
- 下一步等待质量结果，通过后完整五档性能；当前未接受候选。

## 2026-09-22：同二进制三侧复现短档差异，应用评分向量读取候选

- 三侧各九个 HTTP 请求输出一致、三服务退出 0；后侧调度
  1K/4K decode 同样低于回退范围，8K 重叠。原始响应复核
  全部完成；确认同二进制重启可复现，尚未认定具体 kernel 根因。
- 三侧结束后核对源码/二进制摘要并归档，只改专用短评分
  kernel 的 q/k 读取为每次四个 BF16，逐维累加顺序、每 head
  舍入、top-k 与其他路径不变。当前唯一入口固定 hd=128，
  head/group/d 偏移满足 8 字节对齐。
- 下一步必要构建后首先跑原十一题质量 HTTP，再测五档。
  这是独立候选，不预设收益，不执行前置底层测试。

## 2026-09-22：同二进制回退侧完成，短档性能差异仍在

- 回退侧 1K/4K/8K 九请求均完成、服务退出 0，实际启动日志
  确认为回退，完整输出匹配前侧调度。1K/4K decode 范围
  18.03673–18.20607 / 17.36555–17.50058 tok/s，均优于
  前侧调度且不重叠；8K decode 范围重叠。
- 排除了仅由跨版本引起差异的解释。源码 short indexer 在
  调度/回退分别走逐维专用评分/GEMM，而 >8192 共用长分支，
  与现象相符但尚不是因果证明。未运行底层性能分析。
- 后侧重启调度已开始，继续等待完整结果；下一步优先通过
  独立 HTTP 变更验证短评分路径，不改正式参考。

## 2026-09-22：寻址版五档收尾，启动同二进制短档路径对照

- 五档十五请求完成、服务退出 0，输出保持清理版和旧回退。
  200K TTFT 159.19227–159.71833 秒，恢复到旧版范围；
  1K/4K decode 回退仍在，候选整体不接受。
- 为消除跨版本混杂，保持当前源码/二进制，使用已完成的调度
  记录作前侧，依次运行同二进制的强制回退、重启调度。
  1K/4K/8K 各三次实际 HTTP，检查输出和实际运行路径。
- 仅包装原 evalscope 驱动的长度列表，完整包装器/命令/摘要
  保存在 gdn-sequence-base-path-pair-20260922；这是短档排障，
  不替代五档验收。下一步比较各路径性能，继续禁止底层测试。

## 2026-09-22：寻址候选 44K TTFT 回到旧版范围

- 44K 三次输出保持清理版及旧回退；TTFT 30.46071–30.50481
  秒，与两侧旧回退及原调度范围重叠，低于清理版
  30.66260–30.69990 秒。decode 17.02510–17.15035 tok/s
  与两侧旧回退范围重叠，原调度输出差异仍单列保留。
- 原始响应复核已覆盖四档十二请求，200K 继续运行。已有
  1K/4K decode 回退不被长档 TTFT 改善抵消，仍不接受候选。
- 下一步完成 200K，再针对剩余短档路径差异安排 HTTP 验证；
  模板实例不同只作为源码线索，不据此宣称机器级性能根因。

## 2026-09-22：寻址候选短档 TTFT 改善，decode 回退仍在

- 前三档九个 HTTP 请求输出保持清理版。4K TTFT 均值
  2.63551 秒，与两侧旧回退范围重叠；8K 为
  5.16639–5.17282 秒，低于清理版 5.20583–5.21433，
  与原调度参考范围重叠。尚非完整五档接受。
- 1K decode 17.72100–17.88287、4K 17.03735–17.17635
  tok/s，仍低于两侧旧回退；8K decode 对旧回退范围重叠。
  寻址改动没有消除短档 decode 回退，不能用 TTFT 改善抵消。
- 下一步继续收齐 44K/200K；当前不运行底层测试/profile，
  不把源码变化推断为已证明的机器指令或寄存器占用根因。

## 2026-09-22：GDN 寻址候选质量通过，开始五档性能复验

- 原始质量 11/11 答案精确正确，完整输出/输入摘要与 7364767
  一致，实际长度和停止原因正确，服务退出 0，复核记录已保存。
- 摘要核对后启动 gdn-sequence-base-performance-20260922，
  五档各三次，输出对照清理版，性能单列清理前后与原基线，
  原调度输出差异不被覆盖。没有前置底层测试或 profile。
- 下一步收集五档结果，判断独立寻址改动是否改善回退；当前
  仅质量通过，未接受候选，不改变正式参考。

## 2026-09-22：GDN 寻址候选零警告构建，首项质量 HTTP 启动

- 必要构建成功且零警告，未运行构建出的测试或 bench。
- gdn-sequence-base-quality-20260922 保存候选摘要与启动命令，
  使用原 7364767 十一题输入和完整输出，清除实验环境后运行。
- 下一步等待实际 HTTP 结果，通过后再进行性能复验；候选不接受。

## 2026-09-22：清理版五档不接受，应用独立 GDN 寻址改动

- 五档 15 请求均完成，服务退出 0，输出匹配清理前候选和旧
  回退，原调度差异保留。200K TTFT 160.06403–160.47229 秒，
  对两侧旧回退均不利分离；清理前后全部档位两指标范围重叠。
- 诊断清理未解决回退，不接受该版本。驱动正常结束后核对
  源码/二进制摘要并归档，应用事先准备的单文件寻址候选：
  序列池偏移放到 ssm 基指针，head 索引恢复原 int。
- 为什么：把本次复用引入的公共索引变更单独验证，不假定其
  为已证实根因。下一步必要构建后首先运行原质量 HTTP，
  通过后再验证五档；不执行前置数值/单测/profile。

## 2026-09-22：清理版 44K 完成，TTFT 回退仍在

- 44K 三次完整输出保持清理前候选及旧回退，TTFT
  30.66260–30.69990 秒，高于两侧旧回退范围；decode
  17.06863–17.19740 tok/s，与两侧旧回退范围重叠。
- 清理前后 TTFT/decode 范围均重叠，没有证据表明清理解决
  性能回退。原始响应核对已覆盖四档十二请求，200K 仍运行。
- 下一步等待最后一档正常结束并留证，再验证准备好的独立
  寻址候选；当前源码与二进制未改，不运行底层测试。

## 2026-09-22：清理版前三档仍有回退，准备独立寻址候选

- 前三档九请求输出保持清理前候选。1K/4K decode 对两侧旧回退
  仍不利分离；8K TTFT 为 5.20583–5.21433 秒，两侧旧回退
  为 5.17406–5.18077 / 5.18085–5.18576 秒，仍不利分离。
  清理前后范围重叠，不能认为日志清理已解决回退。
- 源码审查发现寄存器 GDN 复用时共同 ss_base 从 int 改为
  size_t，也影响单序列路径。准备仅将池偏移置于基指针、恢复
  原 int head 索引的补丁；保持 recurrence 运算顺序，模型单
  序列状态为 48×128×128 元素，池偏移仍用 size_t。
- 补丁与前后摘要只存 gdn-sequence-base-20260922，未应用、
  未构建、无性能结论。当前五档驱动仍存活，先完成剩余长档，
  再决定下一版；禁止在运行中更换源码/二进制或插入 GPU 任务。

## 2026-09-22：诊断清理版质量 11/11 通过，五档性能复验启动

- 原始响应核对 11 题答案精确正确、输入摘要/完整输出与
  7364767 一致、实际长度与停止原因正确，服务退出 0。
- 必须完成质量和二进制摘要核对才启动性能；五档每档三次
  HTTP 继续对照清理前候选输出，并保留原调度输出门禁失败。
  原调度、清理前候选与两侧旧回退性能均单列，不改正式基线。
- 下一步收齐五档记录，确定清理是否改变回退；候选仍未接受，
  不运行数值/单测/profile。证据 decode-trace-clean-performance-20260922。

## 2026-09-22：诊断清理版构建完成，首项质量 HTTP 启动

- 必要构建成功且零警告；没有运行构建出的测试或 bench。
- 首项真实 HTTP 仍使用原 7364767 的 11 题输入和完整输出
  参考，清除 Q4T_* 与 LD_PRELOAD 环境，独立目录
  decode-trace-clean-quality-20260922 保存命令、源码和二进制摘要。
- 下一步等待质量结果，通过后再做性能 HTTP；当前不接受候选。

## 2026-09-22：三侧五档收尾，移除临时 decode 诊断代码

- 45 个 HTTP 请求全部完成，三服务退出 0，五档完整输出均
  匹配旧回退。200K TTFT 对前后父版均不利分离；短档回退
  同样保留。候选不接受，不更新原调度参考，不开始底层测试。
- 驱动正常退出后核对六文件摘要，归档旧二进制与源码差异，
  应用事先审查的日志/诊断参数清理，保留三项核心修复。
- 原因：去除定位阶段的额外复杂度，随后独立确认清理后行为；
  不预设删除日志就能修复性能。下一步零警告构建后首项质量
  HTTP，质量通过再复验五档性能，全部失败记录继续保留。

## 2026-09-22：44K 三侧对照亦复现 TTFT 回退

- 后侧旧版 44K 三请求结束，三侧输出一致；候选 TTFT
  30.6448–30.6654 秒，后侧 30.4817–30.4988 秒，与前侧
  一样不利分离。decode 对两侧均范围重叠。
- 剩余后侧 200K 继续执行；保留全部原始记录，当前版本
  不接受。下一步完整收尾后进行准备好的去插桩 HTTP 复验。

## 2026-09-22：三侧短档对照复现性能回退

- 后侧旧版 1K/4K/8K 共九请求完成，三侧完整输出一致。
  候选 1K/4K decode 与 4K/8K TTFT 对前后两侧均不利
  范围分离，8K decode 与两侧重叠。
- 这些回退不能按波动接受；raw-response-review.json 保留
  全部范围。后侧长上下文继续运行，当前二进制冻结。
- 下一步完整收尾后清理临时插桩并重新先 HTTP；不预设
  清理能消除回退，不执行底层测试/profile。

## 2026-09-22：候选五档 15 请求输出均匹配旧回退

- 候选 200K 也已完成，五档共 15 请求完整输出均匹配前侧
  旧回退，实际输入/输出长度与停止原因正确，服务退出 0。
- 后侧旧回退已实际启动，raw-response-review.json 更新；
  初次不利性能范围及原旧调度输出失败仍保留，候选未接受。
- 下一步完成后侧相邻复核再清理临时日志，运行时继续冻结，
  不进行前置底层测试或 profile。

## 2026-09-22：44K 输出匹配，准备未应用的定位日志清理补丁

- 候选 44K 三请求完整输出匹配旧回退，decode 范围重叠，
  TTFT 对前侧不利分离；200K 继续执行，后侧待运行。
- 六文件临时日志清理补丁仅保存于 .q4t-work/prepared/
  decode-trace-cleanup-20260922，尚未应用；原始文件摘要和
  变更前后内容留证，linear device 源码前缀未改变。
- 下一步完成当前相邻对照后再应用清理，并重新构建、首项
  HTTP 验证；不预设清理必然消除回退，不做前置底层测试。

## 2026-09-22：候选前三档输出一致，扩展记录初次不利范围

- 候选 1K/4K/8K 共九请求完整输出匹配旧回退，44K 正在执行。
- 前侧范围比较：1K/4K decode 与 4K/8K TTFT 不利分离；
  8K decode 范围重叠。raw-response-review.json 保存原始范围，
  后侧旧版尚未执行，不能据部分样本接受候选。
- 下一步完成剩余长上下文和后侧复核，不穿插其他 GPU 测试。

## 2026-09-22：候选 1K/4K 输出匹配旧回退，1K 速度仍不利

- 候选 1K/4K 各三次完整输出匹配旧回退，长度和停止原因
  通过；8K/44K/200K 及后侧父版尚未完成。
- 相同 1K 输出下，候选 decode 17.737–17.908 tok/s，
  前侧旧回退 18.032–18.282，不利范围分离；该观察不能
  仅由生成内容变化解释，raw-response-review.json 保留。
- 下一步完成后续对照并复核后侧范围，当前不接受候选，
  不做底层测试或 profile。

## 2026-09-22：旧回退前侧五档 15 请求完成，候选调度开始

- 1K/4K/8K/44K/200K 各三次、输出 256，全量原始响应
  长度/停止原因/重复摘要核对通过，旧回退服务退出 0。
- 同一定位驱动已启动候选调度，逐档严格比较前侧旧回退
  完整输出；后侧父版尚未开始，原旧调度门禁失败保留。
- 下一步核对候选全档及相邻范围，整体未接受，无底层测试。

## 2026-09-22：补充五档原始响应核对脚本

- 当前定位目录 review.py 仅读取已完成 HTTP 响应，逐条核对
  3 次重复、实际长度、length 停止原因、prompt 和文本摘要，
  并区分未开始/未完成/失败，不自动标记生产接受。
- raw-response-review.json 确认旧回退前四档 12 请求有效；
  200K 仍运行，候选和后侧父版未开始。
- 下一步沿用同一真实进程，完成后更新核对，不重启采样，
  没有底层测试或 profile。

## 2026-09-22：保留首档 HTTP decode 不利范围观察

- 严格门禁失败的 1K 原始数据中，候选 decode 17.778–17.924
  tok/s，旧调度 18.529–18.616，初次范围不利分离；TTFT 范围
  重叠。initial-range-review.json 留证。
- 两版输出不同，不能由此归因于某个 kernel，候选未接受；
  正在运行的旧回退五档对照保持不变。下一步结合相同输出
  路径和相邻对照核对，不用质量通过覆盖性能风险。

## 2026-09-22：核对真实多序列尚未覆盖的分支

- 当前旧回退 1K/4K/8K 三档 HTTP 完成，长上下文继续执行。
  不改变运行时源码或二进制。
- 静态覆盖表 pending-branch-coverage.json 明确非零/复用 slot、
  实际 B>1、混合上下文跨 8192 的整体评分分支，以及多序列
  prefill/MTP 的独立 GDN 实现尚待验收。
- 下一步完成正在运行的五档对照，单流结果不替代这些覆盖，
  不进行前置数值/profile。

## 2026-09-22：保存 decode 定位文档检查点

- 将已完成定位、严格质量通过和旧调度输出门禁失败保存到
  文档检查点；修正 STATUS 中 build/q4t 仍称已接受版的旧描述。
- 运行时代码仍未接受、未提交；保护其他任务的 MoE 文档改动。
  旧版回退五档 HTTP 对照继续运行，未改变当前二进制。
- 下一步核对完整旧回退与候选行为，不以文档提交表示接受修复。

## 2026-09-22：严格性能首档输出门禁失败，启动父版回退五档因果对照

- 严格性能在 1K 因旧调度输出不同退出；prompt 未变、
  三次稳定、长度正确、服务退出 0，后四档未运行。
  failure-review.json 留证，不以质量通过覆盖失败。
- 已启动旧版回退→候选调度→旧版回退的独立五档 HTTP
  对照，固定 256 输出、每档三次；旧二进制摘要已核对，
  候选严格比较旧回退输出，不改旧调度基线。证据
  decode-path-parent-fallback-matrix-20260922。
- 下一步核对全上下文行为；该实验是失败定位，不是接受
  替代，无底层数值测试或 profile。

## 2026-09-22：严格质量 11/11 通过，启动原五档输出对照门禁

- 11 题答案、实际长度、stop 原因均通过，prompt/完整输出
  与 7364767 相同，服务退出 0，quality-review.json 留证。
- 同二进制启动原五档严格输出对照，证据
  decode-rope-indexer-round-performance-20260922。
- 下一步保存全部失败/成功记录；质量通过不覆盖性能或
  生成变化门禁，无前置数值/profile，候选尚未接受。

## 2026-09-22：合并 decode 定位状态，明确候选未完成范围

- STATUS 将多轮过时的“正在运行”观察合并成三个已验证边界：
  RoPE 坐标、GDN recurrence、indexer 点积舍入；保留原实验和日志。
- 当前严格质量 HTTP 仍运行，原输出参考不变；列明五档、
  多序列/消费者、精度及去插桩复验尚未完成，不误报生产接受。
- 下一步等真实质量进程完成后核对全部响应，无前置数值测试。

## 2026-09-22：8K 舍入三侧对照完成，严格质量 HTTP 启动

- 点积 BF16 舍入候选三侧九请求输出一致，重启日志一致，
  三服务退出 0；decode-rope-indexer-dot-round-20260921/
  complete-review.json 留证。评分摘要仍不全等，不宣称完整精度接受。
- 严格质量依赖条件已满足，decode-rope-indexer-round-quality-20260921
  正在运行原 11 题真实 HTTP，对照 7364767，关闭所有定位环境。
- 下一步核对完整质量结果再推进所需五档/消费者 HTTP；
  没有前置数值或 profile，候选未提交、未接受。
