# 2026-09-22 — 开发日志

## 2026-09-22：独立MRoPE在1K完整输出门禁停止，启动HTTP路径定位

性能驱动PID2124707已退出1，服务退出0。1K三次原始响应核对：
输入1024、输出256、finish=length，候选重复一致，但与正式
输出第72个字符开始不同；完整证据保留在decode-rope-isolated-
performance-20260922/output-difference.json。质量11题通过不能
覆盖此项；其余四档未运行，不重置参考、不进行低层测试。

启动仅HTTP的失败定位PID2126567：相同1K/256输入输出设置，
正式父版单序列回退、候选调度、候选回退、候选调度重启各三次。
目的检查调度坐标修复是否对齐已有单序列行为，不是正式验收。
证据decode-rope-isolated-paths-20260922。下一步核对路径对照，
再决定修复方向；当前候选仍未接受。

## 2026-09-22：独立MRoPE质量11/11原始核对通过，启动五档性能

PID2123379已退出，质量服务/驱动均退出0；11次HTTP原始请求、
响应文本、长度、finish与计时字段核对通过，输出同未改的7364767
正式质量参考。证据decode-rope-isolated-20260922/raw-quality-audit.json。
同03ecfa15二进制启动完整五档性能E2E，PID2124707，证据
`decode-rope-isolated-performance-20260922`。无运行时改动，
无前置数值/单测/profile。下一步完整性能与相关路径覆盖，
当前不接受候选，也不把质量题通过当作原一致性问题全面修复。

## 2026-09-22：独立补齐批处理decode MRoPE，启动首项质量HTTP

源码确认ModelDecodeStep更新三轴当前位置，而ModelDecodeBatchMulti
遗漏写入；仅补齐真实seq_id池化槽位、position+rope_delta坐标。
没有携带已撤下的scratch/FP32/GEMV/indexer组合改动。构建退出0、
零警告，测试程序未执行。新二进制SHA03ecfa153a343e6b223fd39ad3cacf8955ec7a45f693d51c1669175d3d9db372。

首项运行测试为tools/evalscope固定质量HTTP，父版参考7364767
不变，PID2123379，证据decode-rope-isolated-20260922。当前未
验收，未运行低层测试；下一步核对完整质量，再五档性能和
相关路径覆盖，不转移旧组合实验结论。

## 2026-09-22：独立 scratch 全部135 HTTP完成，未通过并撤下

原PID2087760已退出，九服务和驱动均退出0；135次原始HTTP
核对通过，40项比较完整。8K与200K档首请求decode对固定参考
不利分离，分别17.980077–18.028356对18.047754–18.110051，
17.011772–17.052264对17.061105–17.082034 tok/s。正式父版
20项范围无不利分离，不能将差异归因于分配改动；固定参考
门禁仍未通过，不接受候选，不进入消费者或低层测试。

源码、补丁、二进制和拒绝决定保存在prepared/linear-scratch-
isolated-20260922，原始E2E全部保留。唯一运行时文件恢复HEAD，
src/include无差异；build/q4t仍为拒绝候选，下一次运行前重建。
下一步独立处理decode位置表遗漏，恢复推进原一致性目标，
不携带本次分配及此前FP32组合改动，构建后先HTTP E2E。

## 2026-09-22：独立 scratch 三轮8K档首请求未通过固定参考门禁

最后一组固定参考8K完成，累计129/135 HTTP已原始核对，
32项三轮比较中出现1项不利分离：8K档首请求decode候选
17.980077–18.028356，固定参考18.047754–18.110051 tok/s。
该请求不是服务加载首请求。后续请求范围重叠，对正式父版
五档20项未出现不利分离，但不能抵消固定参考未通过项。
PID2087760仍存活，继续原计划最后44K/200K，未终止或新增
重测替换样本。证据 `linear-scratch-isolated-restarts-20260922`。
当前不接受候选，不进入消费者测试；下一步收齐全部结果，
保留失败证据并处理独立分配候选，继续原一致性修复目标。

## 2026-09-22：独立 scratch 前两轮90 HTTP核对完成

三种二进制前两轮六组五档共90 HTTP已完成原始记录核对，
六服务均退出0，输出检查通过。第二轮正式父版200K TTFT
为159.200034/159.653762/159.607224秒，decode为
17.049783/17.130499/17.119145 tok/s。PID2087760仍存活，
继续第三轮；不能用前两轮替代计划中的三轮对照。证据
`linear-scratch-isolated-restarts-20260922`。未改运行时、
未构建或运行低层测试。下一步完成第三轮及全部40项比较，
保留原始不利样本，再判定独立分配候选是否满足门禁。

## 2026-09-22：独立 scratch 第二轮候选完成，修正状态残留描述

首轮三组与第二轮候选四组共60 HTTP已核对原始请求、响应、
完整输出、长度与计时公式，四服务退出0。第二轮候选200K
TTFT为159.182186/159.608403/159.575522秒，decode为
17.052264/17.122871/17.120850 tok/s。原PID2087760仍存活，
已进入第二轮固定参考；三轮九组尚未完成，不能判定无回退。
证据 `linear-scratch-isolated-restarts-20260922`。

修正STATUS“已定位问题”中残留的现候选表述：RoPE/GEMV/
indexer及FP32递推属于已撤下的组合实验，现候选只有scratch
分配改动。避免把旧修复误报为当前代码。未改运行时或构建；
下一步完成现有交错复核，随后逐项处理仍未闭合的一致性问题。

## 2026-09-22：独立 scratch 第一轮三组45 HTTP核对完成

固定参考、正式父版、候选第一轮五档各15 HTTP已完成原始
核对，三服务均退出0，完整输出同各自固定参考。首轮候选
1K/8K后续decode与44K TTFT仍有不利差异；200K样本表现
混合，不能由单轮判定稳定无回退。所有原始样本保留。
PID2087760仍存活，正按原顺序进入第二轮候选，计划剩余
两轮不变。证据 `linear-scratch-isolated-restarts-20260922`。
下一步完成全部九组及逐档比较；当前未验收，无运行时改动
或低层测试/profile。

## 2026-09-22：修正定位报告首页的当前状态边界

对照 PID2087760 仍在运行，未改运行时。定位报告首页此前
仍突出旧0511候选的中间进度，补充当前结论与历史边界：
组合候选已撤下，4d4d仅为正式运行时上的独立分配实验，
原decode/MTP一致性问题仍未修复。旧实验文本与失败样本
保留，不能当作当前候选已通过证据。下一步等待现有对照
并依据结果处理，后续逐项验证一致性修复。

## 2026-09-22：独立 scratch 全矩阵输出一致，五档相邻复核启动

完整五档15次 HTTP 原始请求、响应、长度、finish 与指标公式
全部核对，完整输出均同正式参考；服务/驱动/后续退出0，
PID2080586已退出。200K TTFT/decode 均值159.577933秒/
17.089400 tok/s，其后续TTFT也对正式参考不利范围分离。
证据 `linear-scratch-isolated-performance-20260922`。

PID2087760已冻结完整原矩阵报告并启动三轮交错九服务对照。
选中全部五档：1K服务首请求，4K/8K/44K/200K的TTFT疑点，
以及8K后续decode对固定参考疑点。每服务五档各三次，共
计划135 HTTP，按固定/正式/候选交错顺序串行执行，保留
全部样本及每档顺序，只有1K第一请求属于服务加载首请求。
证据 `linear-scratch-isolated-restarts-20260922`。
完整输出一致不代表性能通过；当前未接受，消费者未运行，
没有低层测试/profile。下一步等待同一驱动完成后逐档判断，
不得用总体均值覆盖后续请求回退。

## 2026-09-22：独立 scratch 44K 核对完成，接续全疑点复核

44K 三次原始响应核对通过，完整输出同正式/固定参考，
TTFT/decode 均值 30.529862 秒/17.851577 tok/s。后续 TTFT
对正式参考有不利范围分离，须加入复核，不能因总体重叠
忽略。原 PID 2080586 继续 200K，运行时不变。

后续 PID 2087760 等待该 PID/start_ticks 真实终态、全15次
原始核对和完整输出同正式参考，随后冻结原矩阵审查报告，
选择 1K 服务首请求及所有总体/后续不利分离档位，串行运行
固定/正式/候选三轮交错九服务。当前未启动复核请求，长度
尚未冻结；允许 200K 新疑点自动纳入。每档分别审查，只有
第一档是服务加载首请求。证据
`linear-scratch-isolated-restarts-20260922`。下一步仍先
完成200K；没有低层测试/profile，候选未接受。

## 2026-09-22：准备独立 scratch 多档重启复核

矩阵 PID 2080586 仍存活，44K 第一请求已完成，未并发启动
服务或改运行时。准备目录
`prepared/linear-scratch-isolated-restarts-20260922` 采用
固定/正式/候选三轮交错九服务，复用 HTTP 入口及原始 SQLite
审查，扩展为每档分别保留三个首请求样本、六个后续样本。
仅第一档 1K 对应服务加载首请求，其他档位只是该档首请求。

当前 lengths 为空、lengths_finalized=false，未执行请求。
须完整矩阵真实结束、原始审查完成后，根据正式/固定参考的
总体及后续不利分离确定全部档位，并包含 1K 服务首请求。
不能遗漏长上下文新疑点，不能转移上一 0511 的通过或失败。
下一步仍先完成44K/200K，再冻结复核范围；无低层测试/profile。

## 2026-09-22：独立 scratch 前三档输出一致，保留性能疑点

PID 2080586 仍存活，前三档九次原始 HTTP 响应已核对，
完整输出同正式与固定参考。1K/4K/8K TTFT 均值分别
0.797092/2.636882/5.184117 秒，decode 分别
18.597624/17.863468/18.110583 tok/s。4K 后续 TTFT 对
正式参考不利分离；8K TTFT 总体及后续对正式参考不利分离，
8K 后续 decode 对固定参考不利分离。1K 首请求 decode
较参考低，仍须单列复核。不得用总体范围重叠覆盖这些疑点。

44K/200K 继续运行，不改运行时、不做低层测试。证据
`linear-scratch-isolated-performance-20260922` 的原始响应
审查及顺序样本报告。下一步先完成完整矩阵，冻结所有疑点
后再安排相邻对照，不只检查有利档位；当前候选未接受。

## 2026-09-22：独立 scratch 严格质量 11/11 通过

当前 4d4d82df 候选首项质量 HTTP（含 200K）11/11 完成，
SQLite 原始请求、完整响应、实际长度、finish 和指标公式
核对通过，输出与未变的正式质量参考一致。服务与驱动退出
0，PID 2080585 已退出。证据
`linear-scratch-isolated-20260922/raw-quality-audit.json`。

等待驱动 PID 2080586 核对终态及 SHA 后已开始完整五档
HTTP，明确使用正式性能完整输出 reference。质量题通过不
代替五档性能/输出或消费者验证，候选仍未接受。下一步
核对各档三次原始响应及首请求/后续表现；不改运行时、不
做低层测试或 profile。

## 2026-09-22：独立 scratch 静态容量预算明确

严格质量 HTTP PID 2080585 仍在运行，后续 PID 2080586
等待，无运行时改动。只读模型 config 确认 key heads=16、
value heads=48、kd=vd=128，in_qkv=10240、v_dim=6144。
按当前源码切片/对齐公式核算，T=8192 时六块数据仍为
538443776 bytes（513.5 MiB），无对齐增量；T=1 为
65728 bytes 有效数据、66048 bytes 单次分配，增加 320
bytes padding。因此只能说源码调用次数 6→1，不能说减少
实际内存驻留、峰值或已获得性能提升。静态预算不是运行时
测试，存于 `prepared/linear-scratch-isolated-20260922/`
`source-capacity-accounting.json`。下一步仍等待质量及五档
HTTP；分配失败路径、实际容量和消费者尚未验证。

## 2026-09-22：撤下未通过组合候选，隔离 scratch 分配改动

九次重启及条件驱动均已退出且无推理服务后，完整归档九个
运行时文件、rejected-runtime.patch 与旧 0511 二进制于
`prepared/linear-scratch-isolated-20260922`。确认 HEAD 这九个
文件与正式 7364767 相同后恢复，仅在原算术/分派上应用六块
临时缓冲的单次分配；不复用 raw 存储，也不带入 FP32 GDN、
ROWS4、QKV/GEMV、indexer 或 RoPE 组合改动。此举隔离内存
所有权改变的影响，decode/MTP 一致性问题仍待修复，未取消。
其他任务的文档未覆盖，失败记录和原始响应保持原样。

必要构建退出 0、零警告；没有运行构建生成的测试程序。
新二进制 SHA 为
`4d4d82dfdd3e534c8e673791789b5ad45fe013bb8152541567a27067bc3f231c`。
首项严格质量 HTTP PID 2080585，证据
`linear-scratch-isolated-20260922`；PID 2080586 等待该进程
真实退出、质量原始审查和 SHA 一致后才运行完整五档性能，
证据 `linear-scratch-isolated-performance-20260922`。此次
性能采集显式使用正式 7364767 完整输出 reference，不能仅
凭内部重复一致放行。下一步先完成质量及五档 E2E，再根据
结果复核性能/消费者；不做前置单测、数值测试或 profile。

## 2026-09-22：单次分配候选独立重启对照未通过

九服务/27 HTTP 全部完成，原始请求、响应、长度、结束原因及
指标公式核对完整，驱动/后续和九服务均退出 0。PID 2067359
及条件消费者 PID 2075538 均已退出。三个服务首请求 decode
候选范围 17.873087–17.962282 tok/s，固定参考范围
18.007338–18.072623 tok/s，存在不利范围分离；后续六请求
候选 18.064545–18.166342、固定参考 18.150260–18.198257，
有重叠。对正式父版的首请求和后续范围均重叠。TTFT 更低，
但不能覆盖首请求 decode 回退，本候选不接受。

条件驱动记录 started=false / restart performance regression，
MTP 与 fallback 请求没有启动。完整输出精度仍未闭合；不能
把 11 质量题、采集退出 0 或后续请求重叠当成整体通过。
证据目录 `linear-scratch-single-allocation-restarts-20260922`
及 `linear-scratch-single-allocation-consumers-20260922`。
下一步处理当前实现或回退，保留全部失败样本；没有进行低层
数值、单测或 profile。运行时仍未接受、未提交。

## 2026-09-22：串接带重启门禁的当前 SHA 消费者 HTTP

九次重启驱动 PID 2067359 仍存活，未改运行时。新增等待驱动
PID 2075538，记录原 PID start_ticks，等待原进程真实退出，
要求完整原始审查及八项比较均齐全。任何不利范围分离都保存
原因并不启动消费者；仅门槛通过后串行执行当前 0511 SHA
的 plain/MTP/plain 27 HTTP，再执行 fallback/scheduled-after
24 HTTP。各自检查二进制 SHA、服务实际模式和无并发服务。

MTP 完整原始响应核对且六项跨模式输出、重复确定性通过后
才继续 fallback；采集失败仍尝试保存原始审查。结果不代表
正式精度验收，低层测试/profile 未运行。消费者当前尚未开始，
后续目录 `linear-scratch-single-allocation-consumers-20260922`。
下一步仍先判断完整重启对照，失败则修复或回退。

## 2026-09-22：单次分配完整矩阵核对完成，开始独立重启对照

当前 0511 候选五档 15 HTTP 已全部完成；重新核对原始 SQLite
请求、完整响应、实际 token 数、finish 与 TTFT/decode 公式。
服务、驱动、后续均退出 0，原 PID 2064172 已退出。
200K 三次均值为 TTFT 153.536039 秒、decode 17.168570 tok/s。
完整输出同此前 FP32 候选、不同正式参考，精度仍待闭合。

完整矩阵及按顺序样本审查仅留下 8K 后续 decode 对固定参考
不利分离；没有新增其他档位疑点。后续 PID 2067359 已实际
启动九次独立服务/27 HTTP，首组 fixed 完成。下一步完成
固定参考、正式父版、候选的三轮交错对照，分别检查服务首
请求和后续请求；不能用总体均值掩盖回退。运行时没有改动，
消费者方案未运行，没有低层测试/profile。证据：
`linear-scratch-single-allocation-performance-20260922` 与
`linear-scratch-single-allocation-restarts-20260922`。

## 2026-09-22：为单次分配当前 SHA 准备消费者复验

原矩阵 PID 2064172 与等待后续 PID 2067359 均确认存活，
200K 仍在运行，没有修改二进制或启动并发推理。

已为当前 0511 SHA 准备 plain/MTP/plain 27 HTTP 与同二进制
scheduled/fallback/after 四档 HTTP：
`prepared/linear-scratch-single-allocation-mtp-http-20260922`、
`prepared/linear-scratch-single-allocation-paths-20260922`。
均要求当前完整矩阵及九次独立启动原始审查通过且无不利范围
分离，再核对 SHA 和无并发服务；当前未执行。旧 MTP 通过
记录不转移到单次分配版本。下一步仍先核对 200K 完整终态，
按冻结的全部疑点决定实际复核范围；未进行低层测试/profile。

## 2026-09-22：单次分配 44K 核对完成，接入有条件重启复核

单次分配候选 44K 三次 HTTP 已完成原始核对，TTFT 均值
29.166841 秒，decode 17.846997 tok/s；对正式/固定参考总体
及后续样本范围无不利分离，完整输出仍同直接前版、不同正式
参考。对上一未接受 ROWS8 后续 decode 的差异保留。200K
继续运行于原 PID 2064172，未修改运行时或并发启动服务。

已启动后续驱动 PID 2067359，等待完整矩阵终态及全部 15
原始响应审查，冻结总体/后续范围的全部疑点。如果没有 8K
以外的新疑点，才执行既定九次独立启动；如果其他档位也有
不利分离，则记录 additional_adverse_contexts 并停止，避免
只测 8K 遗漏问题。目录
`linear-scratch-single-allocation-restarts-20260922`，当前仅
等待，未执行复核请求。下一步核对 200K 终态和实际接续条件；
当前性能、正式精度、消费者与真实容量门禁均未全部闭合。

## 2026-09-22：单次分配前三档核对完成，性能疑点仍在

单次分配候选 0511 的前三档 9 HTTP 已原始核对：TTFT
均值 0.777063/2.519641/4.937801 秒，decode
18.724877/18.016616/18.051707 tok/s。完整输出同直接前版
packed ROWS4，仍不同正式参考。

1K/4K 后续 decode 低于直接前版，但对正式/固定参考无不利
范围分离。8K 三次 decode 17.916962/18.090362/18.147797；
后两次范围与正式父版重叠，仍低于固定参考后续样本。当前
不能宣称合并分配提速或消除了性能疑点，全部样本保留。

原 PID 2064172 继续 44K/200K，运行时未改。当前 SHA 专用的
九次独立启动方案已准备于
`prepared/linear-scratch-single-allocation-restarts-20260922`，
参考二进制 SHA 均核对，尚未运行；完整矩阵结束后再决定
相邻/首请求复核。未进行低层测试或 profile。

## 2026-09-22：单次分配质量 11/11，五档性能已启动

单次分配候选 0511 的首项严格质量 HTTP 已完成，11/11
（含 200K）通过。SQLite 原始请求、响应全文、长度、finish
和公式对固定质量参考逐项核对通过，服务/驱动退出 0，原
PID 2063145 已退出。证据 `linear-scratch-single-allocation-20260922`。

串行后续 PID 2064172 已实际启动完整五档 HTTP，证据
`linear-scratch-single-allocation-performance-20260922`。记录
正式父版/固定参考与直接前版 packed ROWS4 的完整比较，
所有失败样本保留。运行时未改、SHA 未变，未做低层数值、
单测或 profile。下一步核对 15 次原始响应和首请求/decode
表现；质量通过不证明正式性能负载精度、所有消费者或峰值
内存验收，候选仍未接受。

## 2026-09-22：四列完整矩阵归档，应用线性 scratch 单次分配

packed 四列 a3a7 完整五档 15 HTTP 已结束，原始 SQLite
响应/长度/finish/公式核对通过，服务/驱动退出 0，原 PID
2057930 已退出。均值如下，全部原样本保留：

| 输入 token | TTFT 秒 | decode tok/s |
|---|---:|---:|
| 1024 | 0.782584 | 18.772660 |
| 4096 | 2.519741 | 18.089391 |
| 8192 | 4.943273 | 18.055086 |
| 45056 | 29.213521 | 17.849156 |
| 204800 | 153.517242 | 17.069304 |

8K 后续 decode 对正式/固定参考仍不利分离，四列本身未满足
验收；完整输出同此前 FP32 候选，仍不同正式参考。其独立
重启方案未执行，保留未闭合状态，不以其他档位收益抵消。

确认原进程及所有 q4t* serve 都已退出、核对源码/二进制后，
应用 `prepared/linear-scratch-single-allocation-20260922`。本次
仅把 LinearAttentionScratch 六块同作用域缓冲改为单个 owner
和六个不重叠的 256-byte 对齐 slice，仍独立于 cuBLASLt
workspace，保留 raw/norm/dump 尾部容量与同 stream 释放。
新增容量溢出检查；不改 kernel 算术、调用顺序或当前 packed
ROWS4 分派，以单独检验分配生命周期这一变量。源代码分配/
释放次数各 6→1，不等于已测开销或峰值收益。

零警告构建退出 0，第一项严格质量 HTTP 已启动，PID 2063145，
证据 `linear-scratch-single-allocation-20260922`，新二进制：
`0511c791c509fefb95ec66b215535147044098f0ad2d0de839433f2e0fe3a6d8`。
原源码、二进制和完整失败审查已归档，未进行前置数值、单测
或 profile。下一步核对质量原始响应，通过后完成五档及相邻/
首请求复核，后续再验证消费者、精度和真实容量；候选未接受。

## 2026-09-22：四列 44K 核对完成，准备线性 scratch 单次分配

packed 四列候选 44K 三次 HTTP 已原始核对，TTFT 均值
29.213521 秒、decode 17.849156 tok/s，完整输出同八列候选，
仍不同正式参考。对正式/固定参考未见总体或后续范围不利
分离；对上一未接受八列候选的后续 TTFT/decode 差异保留。
8K decode 疑点未消除，原 PID 2057930 继续 200K，运行时不变。

源码审查发现 LinearAttentionForward 六个临时数组在相同作用域
分别 cudaMallocAsync/cudaFreeAsync。已准备
`prepared/linear-scratch-single-allocation-20260922`：一个 RAII
owner、六个不重叠且起点按 256 字节对齐的 slice，仍与传给
cuBLASLt 的 GEMM workspace 分离。保留 raw/norm 复用与 dump
尾部需求、stream 顺序和全部 kernel 算术/调度；加字节容量
溢出检查。源码分配/释放各 6→1，但不宣称已测性能或峰值收益。
候选基于当前 a3a7，源文件 SHA、前后源码和 patch 已保存，
尚未应用、构建或运行。下一步先完成当前五档与原始审查，
再决定是否用该生命周期改动解决剩余开销，不提前做低层测试。

## 2026-09-22：packed 四列前三档完成，8K decode 疑点仍在

packed 四列候选 a3a7 的前三档 9 HTTP 已完成原始响应与
公式核对：TTFT 均值 0.782584/2.519741/4.943273 秒，decode
18.772660/18.089391/18.055086 tok/s。完整输出同上一八列
融合候选，仍不同正式参考。

8K 后两次 decode 18.102568–18.108386，对正式父版原始后续
样本 18.124699–18.136413、固定参考 18.189877–18.226861
均不利分离，也低于上一八列完整矩阵后续样本。首请求为
17.954303 tok/s。当前不能声称四列消除了回退；4K/8K 后续
TTFT 对上一未接受八列的细微差异同样保留，不改正式参考。

原 PID 2057930 继续 44K/200K，未中断采集或修改运行时。
已准备当前 SHA 的原九次独立启动方案于
`prepared/gdn-packed-rows4-restarts-20260922`，未执行，需
完整矩阵终态及原始审查后再决定。下一步核对余下两档，
依据完整证据决定相邻复核或撤回；未进行低层测试/profile。

## 2026-09-22：packed 四列质量 11/11，完整五档已开始

packed decode 四列候选 a3a7 的首项严格质量 HTTP 已结束，
11/11（含 200K）通过，原始 SQLite 请求、响应全文、长度、
finish 和时间公式对固定质量参考核对通过；服务/驱动退出 0，
原 PID 2056145 已退出。证据 `gdn-packed-rows4-20260922`。

串行后续 PID 2057930 已实际进入完整五档 HTTP，证据
`gdn-packed-rows4-performance-20260922`；SHA 未变，正式父版/
固定参考和全部旧失败样本保留，额外比较上一融合 ROWS8。
尚未进行低层数值、单测/profile。下一步核对五档 15 原始请求、
decode 性能及首请求疑点；质量题通过不覆盖正式性能负载的
输出差异，新二进制消费者仍待复验，候选未接受。

## 2026-09-22：packed 四列候选接续质量后完整五档

原质量 PID 2056145 确认存活，10/11 请求已采集，200K 尚未
结束，不宣称通过。串行后续 PID 2057930 已等待原 PID 的
start-time/终态，质量及 11 条原始响应对固定参考审查通过后
才启动完整五档，否则保留失败并停止。证据
`gdn-packed-rows4-performance-20260922`。额外比较上一融合
ROWS8，正式/固定参考与原始样本不替换；检查所有 q4t* serve
进程以覆盖归档二进制名称，避免并发服务。未修改运行时或
执行其他推理/低层测试。下一步核对质量终态和五档实际启动。

## 2026-09-22：重启复核确认 decode 回退，应用 packed 四列候选

融合候选 7328 的独立重启复核已完成：九服务/27 HTTP 原始
响应、长度、finish 和公式核对通过，九服务/驱动退出 0，原
PID 2045937 已退出。但 decode 对正式父版和固定参考均回退：

| 样本 | 候选 tok/s | 正式父版 | 固定参考 |
|---|---:|---:|---:|
| 三次加载后首请求 | 17.867986–17.908244 | 17.945492–17.958143 | 18.019315–18.100586 |
| 六次后续请求 | 18.075791–18.103263 | 18.130026–18.178307 | 18.120634–18.184957 |

TTFT 保留改善，但不能抵消上述回退；不以之前五侧相邻运行
未见回退覆盖本次结果，不更换或删除任何样本。7328 不接受，
其已通过的 MTP 用例仍只是该二进制的证据。fallback 准备方案
未执行，性能门禁仍未闭合。

确认所有推理服务退出，核对源文件/二进制 SHA 并归档之后，
应用 `prepared/gdn-packed-rows4-20260922`：仅无显式环境选择
时的 packed decode 默认 ROWS8→4，prefill/causal 保持 ROWS8，
显式 Q4T_GDN_REG 保持原行为；共同 FP32 公式、每列递推和
checkpoint 不改。源码上每 lane 状态 32→16 个 float、目标
packed CTA 数翻倍，实际资源、数值和性能尚未证明。这与此前
全路径默认 ROWS4 导致 prefill 回退的实验范围不同。

零警告构建退出 0，首项严格质量 HTTP 已启动，PID 2056145，
证据 `gdn-packed-rows4-20260922`。新二进制 SHA：
`a3a7be9b7521b5851b2052d4eef69e214ffe6b912e4f5bd89953e9a64de3a2c6`。
未做前置数值、单测或 profile。下一步核对质量原始响应，通过
后完成完整五档及首请求复核，正式精度和新二进制消费者仍待
闭合；候选未提交、未接受，正式参考不变。

## 2026-09-22：独立重启复核进行中，准备回退消费者复验

原 PID 2045937 存活，round1-fixed 三请求原始核对通过，其余
独立启动继续；尚无足够候选首请求样本，不能判断回退。

本候选将准备融合限定在 packed decode，因此还需同一二进制
调度路径与 allocation-failure 回退路径的 HTTP 输出一致性。
已准备 `prepared/gdn-packed-prep-fusion-paths-20260922`：复用
已有分配失败 shim（摘要已核对），fallback、scheduled-after
各四档三次输出 256，与当前完整矩阵 scheduled-before 比较；
增加 SQLite 原始响应/参数/时间公式审查，完成条件要求驱动
退出 0。脚本要求独立重启审查完整且无不利范围分离后才能
执行，目前未启动。下一步继续核对九次重启，性能疑点闭合后
依次复验消费者；运行时未变，未运行低层测试/profile。

## 2026-09-22：MTP 27 HTTP 一致性通过，开始独立重启复核

当前 7328 二进制的 plain-before/MTP/plain-after 27 HTTP
全部完成，三档 1024/8192/45056 各三次、实际输出均 32 token。
SQLite 原始响应全文、长度、finish、公式核对通过，三组输出
全部一致且重复确定，三服务/驱动退出 0，原 PID 2040662 已
退出。启动/调度日志确认 MTP 实际加载并执行 B=1。

| 模式 | 1K TTFT 秒 / decode tok/s | 8K | 44K |
|---|---:|---:|---:|
| plain-before | 0.767435 / 18.705846 | 4.927153 / 17.998524 | 29.170072 / 17.851403 |
| MTP | 0.935550 / 32.209150 | 5.488467 / 31.246154 | 30.648607 / 27.636013 |
| plain-after | 0.762651 / 18.785542 | 4.923157 / 17.996559 | 29.142662 / 17.858342 |

以上仅为本次 32-token 消费者用例，不能混入输出 256 的正式
性能矩阵；MTP TTFT 较高，两模式分组保留，默认仍关闭 MTP。
没有临时层/状态追踪，不证明所有 checkpoint 分支或内部状态
等价，更不覆盖正式精度、批处理或视觉验收。证据
`gdn-packed-prep-fusion-mtp-http-20260922`。

串行驱动 PID 2045937 已实际开始独立重启首请求复核，三种
二进制各三个服务实例，每实例三个 8192/256 HTTP，轮换顺序，
区分首请求与后续请求，全部样本保留。证据
`gdn-packed-prep-fusion-restarts-20260922`。下一步核对全部
27 请求和九个服务终态，判断首请求疑点；运行时不变，未进行
低层测试/profile，候选仍未接受。

## 2026-09-22：MTP 三档与前侧输出一致，接入首请求串行复核

当前 MTP 的 1K/8K/44K 各三次 32-token 原始 HTTP 输出与
前侧普通解码一致，重复确定；后侧普通解码仍待完成。MTP 的
TTFT 高于 plain，两种模式分组保留，不混入普通性能基线。
原 PID 2040662 仍存活，没有改变运行时或进行低层测试。

首请求复核后续驱动 PID 2045937 已启动等待，只有当前三服务
及全部 27 原始响应、plain/MTP 完整输出检查通过才执行九次
独立启动；任一步失败即停止。证据目录
`gdn-packed-prep-fusion-restarts-20260922`，目前尚未执行请求。
下一步完成后侧 plain 和终态核对，再查看实际首请求复测。

## 2026-09-22：MTP 对照进行中，准备独立重启首请求复核

当前同二进制 plain/MTP/plain 对照的前侧普通解码三档共
9 HTTP 已完成原始核对，1K/8K/44K 各三次输出 32 token、
重复一致。MTP 服务已启动，日志确认实际 MTP step B=1；
其输出尚待完整对照，原 PID 2040662 继续运行。

为闭合加载后首请求疑点，准备
`prepared/gdn-packed-prep-fusion-restarts-20260922`：三轮顺序
fixed/parent/candidate、candidate/fixed/parent、parent/candidate/fixed，
三种二进制各独立启动三次，每次 8K 同输入三请求、输出 256，
共 9 服务/27 HTTP。统计三个独立首请求与六个后续请求，保留
原顺序/全部样本；不设置临时容忍比例，不替换原矩阵。脚本
要求当前 MTP 三服务终态及原始输出一致后才可执行，目前仅
准备、未启动。下一步先核对 MTP 全部响应，再执行首请求复核。

## 2026-09-22：8K 相邻后续回退未重现，首请求仍待复核

8K 五组相邻 HTTP 全部完成，15 请求原始响应、输出长度和
公式核对通过，五服务/驱动退出 0，原 PID 2038070 已退出。
候选后两次 decode 18.187956–18.207145 tok/s；前侧固定参考
18.132552–18.165377、前侧父版 18.112219–18.115169、后侧
父版 18.148759–18.171393、后侧固定参考 18.164114–18.166066。
总体和后续样本范围对四侧均无不利分离，原 8K 后续回退未在
本次相邻运行重现；原始矩阵的差异依然保留，不替换基线。

另有未闭合限制：候选服务加载后的首请求 decode 17.930093，
四侧参考首请求分别 18.017675/17.980354/17.961421/18.012962，
候选均稍慢。每个服务只有一个加载后首请求，不能据总体范围
重叠或后续请求改善证明首请求无回退；需要独立重复启动的
真实 HTTP 复核，不能宣称完整性能验收已闭合。

按此前已设置的总体/后续范围条件，串行驱动 PID 2040662
已启动同二进制 plain/MTP/plain 27 HTTP，目前第一侧 plain
1K 已采集。此检查仍属 E2E 消费者对照，非低层数值/profile，
不覆盖上述首请求疑点或正式精度差异。证据
`gdn-packed-prep-fusion-mtp-http-20260922`。下一步核对全部
消费者响应，同时准备首请求独立重复启动对照，待当前服务
退出再执行；运行时保持不变，候选未接受。

## 2026-09-22：相邻复测进行中，设置 MTP HTTP 后续门禁

五组 8K 相邻复测的第一侧 fixed-before 已结束，三请求原始
响应/长度/公式核对通过、服务退出 0，其余组继续；原驱动
PID 2038070 存活，没有改运行时或并行服务。

启动条件后续驱动 PID 2040662，等待该 PID/start-time 终态。
只有五组全部完成、原始审查通过、总体与后续样本对所有侧均
无不利分离，才运行当前同一二进制的 plain/MTP/plain 27 HTTP。
若仍有性能疑点，记录 remaining_performance_concerns 并停止，
不执行 MTP、不宣称门禁通过。目录
`gdn-packed-prep-fusion-mtp-http-20260922`，当前仅等待。
下一步核对五侧原始输出和速度；正式精度验收仍独立未闭合。

## 2026-09-22：融合候选五档结束，启动 8K 双参考相邻复测

融合候选 7328 完整五档 15 HTTP 已结束，服务/驱动及后续
审查退出 0，原 PID 2035718 已退出。原始 SQLite 请求/全文/
长度/finish/时间公式逐项核对通过，所有原样本保留。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.782818 | 18.746364 |
| 4096 | 2.517908 | 18.069082 |
| 8192 | 4.938806 | 18.077800 |
| 45056 | 29.174687 | 17.923020 |
| 204800 | 153.487542 | 17.148885 |

200K TTFT 对正式父版均值降低 3.761%，44K 降低 4.250%。
对正式父版/固定参考，五档三次总体范围均无不利分离；后续
两次的疑点并集只有 8K decode 对固定参考。全部完整输出同
上一候选 e216，仍不同正式参考，质量 11/11 不能覆盖这个
正式精度缺口。完整矩阵完成不等于当前运行时验收通过。

串行驱动 PID 2038070 已冻结上述原始审查和选择原因，仅选
8192，实际启动五组相邻 HTTP：固定参考、正式父版、候选、
正式父版、固定参考，各三次输出 256。第一侧启动日志确认
max_seq=1/max_len=208896/MTP off，未并行运行其他推理服务。
证据 `gdn-packed-prep-fusion-reference-pair-20260922`。下一步
复核五侧全部原始响应和终态，再判断 8K 性能疑点；运行时
保持不变，未开始数值、单测或 profile，其他消费者仍待复验。

## 2026-09-22：接入完整矩阵后双参考串行复测

为避免完整矩阵结束后停留在未执行计划，已启动相邻复测的
串行后续驱动 PID 2038070，等待当前矩阵 PID 2035718 的原
start-time 对应进程退出。仅在 matrix/continuation/server 终态
正常、15 条原始响应全部核对后，冻结完整与后续样本审查，
按正式父版/固定参考的疑点并集选择长度，不只硬编码 8K。
核对三份二进制 SHA、无推理服务后才运行五组 HTTP；任一步
失败保留错误并停止，不重启原任务。当前仅等待，复测尚未开始。

执行目录 `gdn-packed-prep-fusion-reference-pair-20260922`，
顺序为 fixed-before、parent-before、candidate、parent-after、
fixed-after，各档每组三次输出 256。无疑点则记录无需复测，
不宣称自动验收。下一步核对 200K 完整终态及实际选出的疑点，
保持正式精度与其他消费者门禁，未进行低层测试或 profile。

## 2026-09-22：融合候选 44K 核对完成，200K 采集中

融合候选 44K 三次 HTTP 已完成，SQLite 原始响应、256-token
输出和时间公式核对通过。TTFT 均值 29.174687 秒，decode
17.923020 tok/s；相对 e216，TTFT 范围重叠、decode 均值增加
约 0.64%。对正式父版/固定参考，总体及后续请求范围无不利
分离。完整输出同 e216，仍不同正式参考，不据性能改善宣称
精度通过。

原串行驱动 PID 2035718 确认存活，200K 三请求已开始，未重启
或修改运行时。8K 后续 decode 对固定参考的疑点仍保留，待
完整五档后执行已准备的两参考相邻复测。另外准备了当前 SHA
专用的 plain/MTP/plain 三侧 27 HTTP 脚本，未执行；位置
`prepared/gdn-packed-prep-fusion-mtp-http-20260922`，不会把
旧 a95 的 MTP 结果算作本候选通过。下一步核对 200K 终态、
全部 15 原始响应和性能疑点并集，再推进相邻复测。

## 2026-09-22：融合后 8K decode 改善，固定参考仍待复核

融合候选已完成前三档各三次 HTTP，原始响应和公式逐项核对。
1K/4K/8K TTFT 均值 0.782818/2.517908/4.938806 秒，decode
18.746364/18.069082/18.077800 tok/s；完整输出同 e216，仍不同
正式参考。8K 后两次 decode 18.147563–18.166654，较 e216 的
18.078405–18.092208 提高，也高于正式父版原始后两次样本，
但低于固定 fcb5925 的 18.189877–18.226861。首请求较慢的
样本仍保留，不据此判定性能门禁通过。

完整矩阵仍运行于 PID 2035718；尚未做数值、单测或 profile。
为区分固定参考的历史样本和当下机器状态，准备了五组相邻
HTTP：fixed-before、parent-before、candidate、parent-after、
fixed-after。三份二进制 SHA 已核对；长度列表须完整五档结束
后按疑点并集确定，目前未启动、未替换原样本。脚本位于
`prepared/gdn-packed-prep-fusion-reference-pair-20260922`。
下一步完成 44K/200K 和原始复核，再决定执行相邻对照。

## 2026-09-22：收敛当前状态入口，保留完整失败历史

STATUS 的候选段从 198 行整理为 58 行，以当前 7328 融合候选、
质量通过/性能采集中、待闭合条件和失败索引为主，清除已被后续
结果取代的“下一步构建/待质量”表述。完整实验未从报告或日志
删除，旧 STATUS 另存 prepared/gdn-packed-prep-fusion-20260922。
保护其他任务的 T4000/MoE 条目。没有改运行时代码或重启测试。
五档第一档三次原始响应已核对，输出同 e216、不同正式参考；
后续继续核对原 PID 2035718 的性能采集与未闭合门禁。

## 2026-09-22：融合候选质量 11/11，通过后开始五档性能

融合候选 7328 的首项严格质量 HTTP 已结束，11/11（含 200K）
通过；SQLite 原始请求、响应全文、长度、finish 与固定 7364767
质量参考逐项核对通过，服务/驱动均退出 0，原 PID 2034704 已
退出。此结果不解决正式性能负载的精度差异，也不证明 MTP、
批处理或视觉路径全部通过。

串行驱动 PID 2035718 已实际进入完整五档性能 HTTP；证据
`gdn-packed-prep-fusion-performance-20260922`。二进制仍为
`7328bd2c9a7c9a70d1849367e481fbbe17cb331a3da5805fd597c92c28ca5373`，
运行时未改，未进行数值/单测/profile。下一步核对全部 15 次
原始响应、8K decode 及各档 TTFT；继续保留 e216 和正式参考
对照、全部样本及第一/后续请求区分，当前候选尚未接受。

## 2026-09-22：融合候选接入质量后五档串行评估

质量 PID 2034704 已确认仍在运行，未重启。为保持评估连续且
避免并发服务，启动 PID 2035718 等待原 PID/start-time 终态，
核对 quality driver/server、固定参考与 11 条 SQLite 原始响应，
再核对二进制 SHA 和无推理服务，满足后才启动完整五档 HTTP。
任一检查失败记录失败并停止。候选性能对照额外保留 e216，
正式父版和固定参考不变；首请求/后续样本仍分别报告。
证据 `gdn-packed-prep-fusion-performance-20260922`。此条不是
测试通过记录；下一步核对质量终态和五档实际采集进度。

## 2026-09-22：8K decode 两侧复测回退，尝试融合 packed 准备

共享系数 8K 三侧复测全部 9 HTTP 完成，三服务/驱动退出 0，
原始 SQLite 响应、完整输出和公式已复核。候选后两次 decode
18.073747–18.075165 tok/s，前侧父版 18.140078–18.163494，
后侧 18.161286–18.175768，两侧均不利分离；首请求也各自较慢。
三次总体范围重叠不能覆盖这个疑点。候选 TTFT 改善，但不能
抵消 decode 回退，当前共享系数 e216 不接受。证据：
`gdn-shared-coefficients-parent-pair-20260922`，所有样本保留。

确认驱动 PID 2030766 已退出且无推理服务后，应用下一候选
`prepared/gdn-packed-prep-fusion-20260922`：将 packed decode
的 norm/alpha/beta 准备合入 register scan，跳过该布局的独立
prep launch；prefill/causal 保留共享准备。抽取共同 FP32
归一化、alpha/beta device helper，保留每列递推、checkpoint、
slot 寻址、默认 ROWS8 与 fallback。减少启动是源码推导，
实际性能和数值等价未证明；也不能把先前 FP32 版本的 MTP
结果自动转移到新版本。原源码、二进制和 patch 已归档。

必要构建退出 0、零警告，首项测试已启动严格质量 HTTP，PID
2034704，证据 `gdn-packed-prep-fusion-20260922`。二进制：
`7328bd2c9a7c9a70d1849367e481fbbe17cb331a3da5805fd597c92c28ca5373`。
未运行前置数值、单测或 profile。下一步核对质量原始响应，
通过后完成五档性能；正式精度、MTP/批处理/视觉仍待闭合。

## 2026-09-22：共享系数五档采集完成，启动 8K 三侧复测

共享系数候选 e216 的完整五档 15 HTTP 已结束，原驱动 PID
2026051 已退出，服务/驱动退出 0。SQLite 原始响应、实际输入/
输出长度、finish、完整文本和时间公式逐项复核通过。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.783585 | 18.671634 |
| 4096 | 2.518001 | 17.997374 |
| 8192 | 4.939127 | 18.008415 |
| 45056 | 29.180992 | 17.808605 |
| 204800 | 153.621633 | 17.134907 |

200K TTFT 均值比正式父版降低 3.677%，44K 降低 4.229%；不是
正式接受结论。三次总体范围对正式父版/固定参考无不利分离，
但 8K 后两次 decode 对两参考均不利分离。全部原样本保留，
不以首请求拉宽范围覆盖疑点。完整输出同此前 FP32 候选，仍
不同正式参考，正式精度门禁未闭合。

依据完整和后续请求范围的疑点并集，仅选 8192，已启动
`gdn-shared-coefficients-parent-pair-20260922`（PID 2030766）：
父版—候选—父版，每侧三次、输出 256，仍是实际 HTTP。原始
完整矩阵、选择原因及三份审查结果已冻结到复测目录。启动前
确认无推理服务并核对两侧二进制 SHA；不修改运行时、不做
前置数值/单测/profile。下一步复核三侧终态及所有响应，判断
8K decode 疑点；MTP 和正式精度审查仍待完成。

## 2026-09-22：共享系数 44K TTFT 改善，8K 后续 decode 待复核

共享系数候选已完成 1K/4K/8K/44K 各三次原始 HTTP 复核：
TTFT 均值 0.783585/2.518001/4.939127/29.180992 秒，decode
18.671634/17.997374/18.008415/17.808605 tok/s。完整输出同
此前 FP32 候选，仍不同正式参考。44K TTFT 对正式父版均值
30.469618 秒约减少 4.2%，其三次范围有利分离；完整五档未结束，
不宣称稳定全面收益或正式接受。

前三次总体范围对正式父版/固定参考无不利分离，但单列后续
请求发现 8K decode 候选 18.078405–18.092208，正式父版
18.124699–18.136413 tok/s，不利分离，对固定参考也如此。
原三个样本均保留，首请求与后续请求另列，并标注每档首请求
是否是服务加载后的首请求；不让较慢首请求拉宽范围掩盖差异。
证据 ordered-samples-review.json/review_ordered.py，当前部分结果。

已准备（未执行）prepared/gdn-shared-coefficients-parent-pair-20260922，
长度暂空，须完整终态后由总体或后续请求的不利档位确定，再跑
旧→新→旧。另有同候选 27 请求 plain/MTP/plain 脚本仅准备。
200K 仍由原 PID 2026051 运行，无重叠推理、源码/二进制未变。
下一步收齐矩阵，复核 decode 疑点，精度门禁仍不以质量题通过覆盖。

## 2026-09-22：共享系数质量 11/11，启动完整五档 HTTP

共享系数候选 `e216ddaf…` 首项质量 HTTP 已完成 11/11，含
200K；原始 SQLite 请求/响应、实际长度、finish、时间逐项复核，
完整质量输出同 7364767，服务/驱动退出 0。证据
`gdn-shared-coefficients-20260922/raw-quality-audit.json`。

完整五档性能采集已启动，PID 2026051，证据
`gdn-shared-coefficients-performance-20260922`。单流/MTP 关闭、
三次重复、输出 256、固定输入/正式参考不变，同时保留清理
八列、四列及其他失败候选对照。原始质量复核/相同二进制/
无其他服务是启动条件。暂不声称性能收益或正式精度通过，
未进行前置单测、数值测试或 profile。下一步收齐完整矩阵。

## 2026-09-22：共享系数零警告构建，首项质量 HTTP 启动

共享系数候选已零警告构建，首项正式质量 HTTP 已启动，PID
2024812，证据 gdn-shared-coefficients-20260922。当前二进制 SHA-256：
`e216ddaf0ca526bc411553a898502cef96897851c4595f47c3f7460199899bbd`。
仍使用 7364767 固定质量输入和完整输出参考，没有前置单测、
数值测试或 profile。下一步核对质量终态，通过后完整五档性能。

## 2026-09-22：四列完整矩阵四档回退，应用共享系数候选

四列状态版本 `gdn-factor-rows4-performance-20260922` 的完整
15 HTTP 已结束，服务/驱动退出 0，原始 SQLite 响应及公式全部
复核通过，但性能门禁失败。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.820759 | 18.737798 |
| 4096 | 2.681665 | 18.041622 |
| 8192 | 5.266038 | 18.049966 |
| 45056 | 30.993977 | 17.836345 |
| 204800 | 161.692556 | 17.108040 |

4K/8K/44K/200K TTFT 对正式父版和八列清理版均不利分离；各档
decode 对这两组无不利分离。完整输出同此前 FP32 候选，仍不同
正式参考。四列方案不接受，全部样本保留，不更换基线。

确认原 PID 2021932 已退出且无推理服务后，应用
`prepared/gdn-shared-coefficients-20260922`：恢复八列，按
(t,key-head) 准备 norm、按 (t,value-head) 准备 FP32 alpha/beta，
在原准备 kernel 内完成，scan 共享系数。表达式及每列递推/
checkpoint 保持，数值等价尚未实测。复用同 stream raw QKV
最后消费者之后的存储；普通模式目标 T=8192 raw 分配 160 MiB，
4 MiB 系数包含于其中；dump 模式保留原数据，分配 164 MiB。
这些是源码容量/调用数推导，不是峰值、带宽、指令或速度实测。

factor-alias 单独方案未独立运行，作为本候选的存储布局组成；
不能把两者收益分开归因。原四列源码/二进制与完整组合 patch
已归档，构建正在进行，首项仍为正式质量 HTTP。未进行前置
单测、数值或 profile。下一步完成构建并按 E2E 验证，正式参考不变。

## 2026-09-22：四列 44K 回退，准备按 token/head 共享系数

四列状态 44K 原始 HTTP 已核对：TTFT 均值 30.993977 秒、
decode 17.836345 tok/s，TTFT 对正式父版和八列清理版本均回退。
200K 仍由 PID 2021932 运行，尚无完整终态；不改变测量中的
源码/二进制，不以部分结果替换完整矩阵。

源码显示共享 GDN scan 的不同 value-column warp 重复计算相同
(t, value-head) 的 softplus/指数/sigmoid 系数。另准备
prepared/gdn-shared-coefficients-20260922（未应用/构建/测试）：
在原 norm 准备 kernel 中按 token/head 计算一次 FP32 alpha/beta，
scan 读取系数，表达式/每列递推/checkpoint 保持；准备 kernel
仍一次，不物化整份 q/k。它基于尚未应用的 factor-alias 方案。
T=8192 的 norm+alpha/beta 共 4 MiB，普通模式复用 160 MiB raw
区域；dump 模式保留 raw 并另留 4 MiB 尾部。源码只支持容量/
依赖推导，不能据此声称实际指令、精度、峰值或速度已验证。
下一步先完成四列完整矩阵，再选择已准备的撤回及系数方案按
必要构建→真实质量 HTTP→完整性能继续，不进行前置数值/profile。

## 2026-09-22：四列 4K/8K 回退，准备仅因子缓冲复用

四列状态候选前三档原始 HTTP 已核对：1K/4K/8K TTFT 均值
0.820759/2.681665/5.266038 秒，decode 均值
18.737798/18.041622/18.049966 tok/s。4K/8K TTFT 对正式
父版及清理后 ROWS=8 均不利，输出同八列 FP32 候选，仍不同
正式参考。减少每 warp 状态列数未在已完成档位带来收益。
44K/200K 由原 PID 2021932 继续，运行时保持不变。

仅准备 prepared/gdn-factor-alias-20260922：若四列完整矩阵
失败，恢复八列，只将 FP32 norm 因子复用同 stream 卷积/
checkpoint/状态更新后不再消费的 raw QKV 区域，不物化 q/k，
不改变递推。Q4T_LIN_DUMP 仍通过保留原尾部保护 raw 诊断值。
目标 T=8192/nkh=16 时普通分配请求从 161→160 MiB，dump 模式
仍 161 MiB；无新增分配次数。这是源码容量推导，不是实测
峰值/带宽或提速。未应用、构建或测试，下一步先收齐当前矩阵。

## 2026-09-22：四列状态质量 11/11，启动完整五档 HTTP

四列状态候选 `e9b79d10…` 首项质量 HTTP 完成 11/11，含
200K；原始 SQLite 请求、响应、长度、finish 与时间逐项复核，
完整质量输出同 7364767，服务与驱动退出 0。证据
`gdn-factor-rows4-20260922/raw-quality-audit.json`。

完整五档性能采集已启动，PID 2021932，证据
`gdn-factor-rows4-performance-20260922`，每档三次输出 256，
单流/MTP 关闭，正式输入及参考不变；新增清理后 ROWS=8 版本
对照，同时保留之前各候选及其失败。尚无性能结论，不以质量
题通过覆盖性能负载输出差异或正式精度门禁；未进行前置单测/
数值/profile。下一步收齐完整矩阵并核对原始响应。

## 2026-09-22：四列状态零警告构建，首项质量 HTTP 启动

每 warp 四列状态候选已零警告构建，首项正式质量 HTTP 启动，
PID 2020602，证据 gdn-factor-rows4-20260922。当前二进制 SHA-256：
`e9b79d103a646fa40218336d438547490821daae716d886c3b8a43829d375bc6`。
使用原 7364767 质量输入及完整输出参考，没有前置单测、数值
测试或 profile。下一步先确认质量终态，通过后完整五档性能。

## 2026-09-22：清理三侧 44K 回退，应用每 warp 四列状态

清理版本 `gdn-factor-clean-parent-pair-20260922` 全 18 HTTP 已
完成，三服务/驱动退出 0，原始响应与时间公式逐项核对通过。
44K TTFT 候选 30.547738–30.554627 秒，前侧父版
30.467498–30.518459，后侧 30.445505–30.455592，两侧均不利。
8K 三次总体范围与两侧重叠，但后两次候选
5.181291–5.182085 秒，前侧 5.178698–5.179814，后侧
5.180119–5.180694，两侧均略慢，不以首请求拉宽范围判定无回退。
44K decode 总体范围均重叠；后两次对后侧父版也有微小不利
分离，保留全部样本，不设临时容忍比例。当前清理版不接受。

确认原驱动已退出且无推理服务后，应用
`prepared/gdn-factor-rows4-20260922`：仅把 Q4T_GDN_REG 未设置
时默认 ROWS=8 改为 4，显式环境选择保持。每列归一化/归约/
递推表达式不变，源码级每 lane 状态 32→16 个 float、目标每
序列 CTA 192→384；实际数值、资源和性能尚未验证。原二进制
与源码已归档。必要构建正在进行，首项仍为质量 HTTP，未进行
任何前置单测/数值/profile。下一步按完整 E2E 检验，正式参考不变。

## 2026-09-22：相邻 8K 首请求影响单列，后续请求差异保留

清理版相邻复核正在运行（PID 2016652）。前侧父版 8K/44K
六请求完成且服务退出 0，候选 8K 三请求完成。8K 总体范围与
前侧重叠，但首请求 TTFT 分别约 5.276/5.305 秒；后两次父版
5.178698–5.179814、候选 5.181291–5.182085 秒仍不利分离。
不能仅用总体重叠掩盖后续请求差异；后侧尚未完成，不作最终结论。

原始复核脚本新增按顺序保留全部样本、每档首请求及后续范围，
同时标注是否是加载后首请求；原三次总体比较和原样本保留，
不替换样本或新增容忍百分比。整轮 complete 必须驱动退出 0
且三组完整，每个原始响应/输出/metric 均要求三项，避免仅由
服务局部终态提前判定。下一步继续完成剩余两侧 HTTP。

## 2026-09-22：清理完整矩阵终态，启动 8K/44K 三侧复核

共享因子清理版本完整五档 15 HTTP 已结束，服务/驱动退出 0，
原始 SQLite 响应、长度、finish 与公式逐项核对通过；采集有效
不代表正式精度或性能验收通过。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.807824 | 18.736647 |
| 4096 | 2.647458 | 18.017536 |
| 8192 | 5.185603 | 18.066620 |
| 45056 | 30.566246 | 17.811999 |
| 204800 | 159.806960 | 17.158575 |

8K/44K TTFT 对正式父版不利范围分离，其他档位和各档 decode
对正式父版/固定参考无不利分离。全部输出同 a95 FP32 候选，
仍不同正式参考。44K decode 对已拒绝的物化方案不利这一比较
保留，不以该版本替换正式参考。证据
`gdn-factor-clean-performance-20260922`，原失败样本不替换。

确认原 PID 2010792 退出、无 q4t 服务且二进制摘要正确后，启动
`gdn-factor-clean-parent-pair-20260922`（PID 2016652）：正式
父版→当前清理版→正式父版，8K/44K、各三次输出 256，共 18
真实 HTTP。档位由完整矩阵对正式父版/固定参考的不利分离集合
确定并保存原审查结果。各版本对自己的原输出校验，跨版本输出
差异仍保留；不以性能相邻复核覆盖精度门禁。ROWS=4 候选仍未
应用，清理后 MTP 对照仍未启动；下一步先完成相邻复核。

## 2026-09-22：清理后 44K TTFT 仍分离，准备状态列数候选

清理版本 44K 三次请求已完成并核对原始响应：TTFT 均值
30.566246 秒，decode 均值 17.811999 tok/s。TTFT 对正式父版
及此前 BF16 候选仍不利，和 a95 checkpoint 版本范围重叠；
44K decode 对已拒绝的归一化复用版不利，该比较保留，不以
被拒绝版本替换正式性能参考。200K 仍由 PID 2010792 运行。

仅准备 prepared/gdn-factor-rows4-20260922，未应用/构建/测试。
若完整矩阵及相邻复核确认回退仍在，考虑默认每 warp 状态列数
8→4：源码级每 lane 状态浮点数 32→16，目标形状每序列 CTA
192→384；每列 FP32 归一化、归约和递推表达式保持，显式环境
选择不变。未采集编译资源或 profile，不能宣称占用率或速度
改善，更多 CTA 也可能增加读流量与 decode 开销。先完成当前
矩阵与复核，不再以整份 FP32 q/k 物化作为当前优化方向。

## 2026-09-22：清理版本 8K TTFT 仍分离，准备相邻复核

清理版本完整矩阵前三档已完成并复核原始响应：1K/4K/8K
TTFT 均值 0.807824/2.647458/5.185603 秒，decode 均值
18.736647/18.017536/18.066620 tok/s。1K/4K 对正式父版无
不利范围分离，8K TTFT 仍不利；全部输出同 a95 FP32 候选，
仍不同正式参考。44K/200K 由原 PID 2010792 继续，未改源码。

已准备但未执行 prepared/gdn-factor-clean-parent-pair-20260922，
长度列表暂空并由脚本要求显式完成选择：完整五档终态后，根据
所有不利分离档位安排旧→新→旧，每档三次。原失败样本保留，
不重置参考，不以相邻复核覆盖正式精度审查。下一步先收齐矩阵。

## 2026-09-22：清理版本质量 11/11，启动完整五档

共享因子/诊断清理版本 `75bae82e…` 首项质量 HTTP 完成 11/11，
含 200K；原始 SQLite 请求、输出、长度、finish 与时间逐项复核，
完整质量输出同 7364767，服务和驱动退出 0。证据
`gdn-factor-clean-20260922/raw-quality-audit.json`。

完整五档性能已启动，PID 2010792，证据
`gdn-factor-clean-performance-20260922`；启动前确认原始质量
复核、相同二进制和无其他推理服务。固定参考和所有失败样本
保留，正式输出精度门禁未据质量题通过而解除。

另准备 `prepared/gdn-factor-clean-mtp-http-20260922`，尚未
执行：同二进制 plain-before/MTP/plain-after，1K/8K/44K 各
三次、输出 32、max_len=65536，直接比较真实 HTTP 完整输出，
不依赖已移除的临时追踪；不能用这组输出比较证明全部内部
checkpoint 状态。下一步先收齐五档性能并决定复核路径。

## 2026-09-22：撤回与清理零警告构建，首项质量 HTTP 启动

撤回/清理版本必要构建退出 0、零警告，首项正式质量 HTTP 已
启动，PID 2009404，证据 gdn-factor-clean-20260922。二进制
SHA-256 为 75bae82e45dc2f8fd8c581101903a72577645b491487980215a32201950a93b5。
继续使用 7364767 固定质量输入及完整输出参考，未运行前置单测、
数值测试或 profile。下一步确认质量终态，再采集完整五档性能；
原 a95 的 44K 回退和正式输出差异仍为未通过项。

## 2026-09-22：向量完整矩阵四档回退，撤回物化并清理追踪

对齐向量版本 `905c223f…` 完整五档 15 HTTP 已完成，服务/驱动
退出 0，全部原始响应核对通过，但性能门禁失败。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.814015 | 18.711855 |
| 4096 | 2.658408 | 18.035339 |
| 8192 | 5.224372 | 18.039808 |
| 45056 | 30.788699 | 17.839841 |
| 204800 | 160.949904 | 17.175777 |

4K/8K/44K/200K TTFT 对正式父版均不利，8K/44K/200K 对
checkpoint 候选也不利；decode 无不利分离。所有完整输出同
FP32 初版/checkpoint/归一化复用候选，仍不同正式参考。证据
`gdn-normalized-vector-performance-20260922`，失败样本不替换。

确认原 PID 2006219 退出且无推理服务后，按文件摘要核对并应用
`prepared/gdn-factor-clean-20260922`，归档原二进制与七文件
前后源码：撤回 FP32 q/k 物化及 raw 复用，恢复共享 FP32 因子/
编译期 checkpoint；删除临时 host token/层/状态/阶段追踪及其
接口参数，保留原 Q4T_LIN_DUMP、pooled RoPE 和其他一致性修复。
构建正在进行，首项测试仍为真实质量 HTTP；未运行前置单测/
数值/profile。当前不是正式接受，下一步完成必要构建及 E2E。

## 2026-09-22：对齐向量 44K 仍回退，准备撤回及诊断清理

对齐向量候选 44K 三次 HTTP 已完成，原始响应复核通过：
TTFT 均值 30.788699 秒，decode 均值 17.839841 tok/s。完整
输出同前两轮 FP32 候选，仍不同正式参考；TTFT 对正式父版及
checkpoint 候选均不利范围分离。200K 仍由 PID 2006219 采集。

已准备但未应用 `prepared/gdn-factor-clean-20260922`：撤回
归一化 q/k 物化，恢复 a95 共享 FP32 因子/编译期 checkpoint
算术，同时删除临时 HTTP token/层/阶段诊断及其接口参数。
逐文件保存前后源码和摘要；保留 ModelDecodeBatchMulti 的 pooled
RoPE 更新、共享递推/checkpoint 及其他 decode 修复。现有
Q4T_LIN_DUMP 保留。该清理不预设性能收益，后续仍必须先构建
及质量 HTTP，旧 trace 脚本也不能直接用于清理后版本。
下一步等完整矩阵终态并复核，再应用撤回/清理方案；测量中
未修改源码或二进制，未运行底层数值测试或 profile。

## 2026-09-22：对齐向量 4K/8K TTFT 仍回退，继续长上下文

对齐向量候选完整矩阵前三档已完成并核对原始响应：1K/4K/8K
TTFT 均值 0.814015/2.658408/5.224372 秒，decode 均值
18.711855/18.035339/18.039808 tok/s。完整输出同前两轮 FP32
候选，仍不同正式参考。4K/8K TTFT 对正式父版不利范围分离，
8K 对 checkpoint 候选也不利；因此尚不能接受。相对上一轮
归一化复用的均值改善不抵消正式门禁失败。

44K/200K 仍由原 PID 2006219 运行，期间未修改运行时代码或
二进制，未运行额外推理服务。下一步收齐长上下文；如果完整
结果仍回退，按计划撤回归一化前移，保留共享递推修复与全部
失败样本。证据 gdn-normalized-vector-performance-20260922。

## 2026-09-22：对齐向量候选质量 11/11，启动完整五档

对齐向量候选 `905c223f…` 首项质量 HTTP 已完成 11/11，包含
200K；原始 SQLite 响应、实际 token 数、finish、时间与固定
7364767 质量输入/完整输出逐项核对通过，服务与驱动退出 0。
证据 `gdn-normalized-vector-20260922/raw-quality-audit.json`。
这不覆盖五档性能负载输出差异或其他路径的精度审查。

完整五档性能采集已启动（PID 2006219），证据
`gdn-normalized-vector-performance-20260922`。启动条件包括
原始质量复核通过、服务已退出、二进制摘要一致、无其他推理
服务；未运行前置单测、数值测试或 profile。下一步收齐三次
重复范围，与正式父版、固定参考及前两轮 FP32 候选一起比较。

## 2026-09-22：对齐向量候选零警告构建，首项质量 HTTP 启动

对齐向量候选已零警告构建，首项正式质量 HTTP 已启动（PID
2004904），证据 gdn-normalized-vector-20260922。当前二进制 SHA-256：
`905c223fdc14f47a9114e7ebb845cd7dd532ec588851d0714cda4ca4a1415857`。
仍使用 7364767 的固定质量输入和完整输出参考，未运行前置测试。
下一步先核对质量终态，通过后再采集完整五档性能。

## 2026-09-22：归一化复用完整矩阵失败，应用对齐向量候选

归一化前移/复用 raw 缓冲候选 d8f830d4… 的完整五档采集结束，
15 个原始响应核对通过，服务与驱动退出 0；这不代表验收通过。

| 输入 token | TTFT 均值秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.817977 | 18.780359 |
| 4096 | 2.680170 | 18.073821 |
| 8192 | 5.271033 | 18.086211 |
| 45056 | 31.002705 | 17.859784 |
| 204800 | 161.925582 | 17.185859 |

4K/8K/44K/200K TTFT 对正式父版和 checkpoint 候选均不利范围
分离；各档 decode 没有不利分离。全部完整输出同 FP32 初版及
checkpoint 候选，仍不同于正式参考。原始样本不替换，当前方案
不接受，未开展底层数值测试或 profile。证据
`gdn-normalized-reuse-performance-20260922`。

确认原驱动已退出且无 q4t 服务后，应用
`prepared/gdn-normalized-vector-20260922`：每 lane 原四次 float2
读写改为两次 float4，诊断尾部对齐提高到 16 字节；不改变 FP32
乘积、归约或递推公式，不宣称编译器实际指令减少。原源码和
二进制完整归档。正在必要构建，首项测试仍为正式质量 HTTP。
下一步若完整 E2E 仍回退，撤回归一化前移方向；共享递推一致性
修复与原精度门禁仍需闭合，正式基线不变。

## 2026-09-22：归一化前移 44K TTFT 仍回退，200K 继续

44K 三次 HTTP 已完成且原始响应复核通过：TTFT 均值
31.002705 秒，decode 均值 17.859784 tok/s；输出同 checkpoint
FP32 候选，仍不同正式参考。TTFT 对正式父版、此前 BF16 候选
及 checkpoint FP32 候选均不利范围分离。归一化前移未消除
44K 回退，当前方案不接受。200K 三次仍由同一 PID 2001726
继续采集，未重启、未更改正在测量的源码或二进制。

该轮已经否定“减少重复 FP32 乘法即带来 E2E 收益”的假设；
具体额外时间归因尚未知，不以访存量推导冒充实测 profile。
下一步保留完整矩阵终态，再检验已准备的对齐向量读写候选，
若仍退化则撤回归一化前移方向，保留共享递推一致性修复。

## 2026-09-22：归一化复用 4K/8K TTFT 回退，完整矩阵继续

归一化复用候选完整矩阵仍在运行（PID 2001726）。已完成的
1K/4K/8K 各三次原始响应均核对通过，完整输出与上一 FP32
候选一致，仍不同于正式参考。TTFT 均值分别为
0.817977/2.680170/5.271033 秒，decode 为
18.780359/18.073821/18.086211 tok/s。4K/8K TTFT 对正式父版
及 checkpoint 候选均不利范围分离；不能接受该方案，不以局部
算术减少推断提速。44K/200K 尚未完成，保持原源码/二进制采集。

prepared/gdn-normalized-vector-20260922 仅准备 float4 读写方案，
未应用、构建或测试；保留 FP32 乘积与递推算术，诊断尾部对齐
提高到 16 字节。编译器实际指令和性能尚未知，未进行 profile。
后续 MTP 24 HTTP 复核脚本也仅准备，尚未启动。下一步先收齐
完整矩阵，保留失败样本后再决定修改或撤回；正式基线不变。

## 2026-09-22：归一化复用质量 11/11，开始完整五档 HTTP

归一化复用候选首项质量 HTTP 已完成 11/11（含 200K），原始
SQLite 响应、长度、finish、时间与完整输出参考逐项复核通过，
服务/驱动退出 0。证据 gdn-normalized-reuse-20260922。
完整五档每档三次性能采集已启动（PID 2001726），证据
 gdn-normalized-reuse-performance-20260922；会同时对照正式父版、
固定参考与上一候选，保持原样本，不把采集成功当作验收通过。
正式输出差异、44K 回退及其他调用路径复验仍未闭合。


## 2026-09-22：归一化复用候选零警告构建，首项质量 HTTP 启动

- gdn-normalized-reuse-20260922 首项质量 HTTP 已启动，PID 2000380；
  仍使用 7364767 原质量输入及完整输出参考，未运行任何前置测试。
- 必要构建退出 0、零警告，二进制 SHA-256 为
  d8f830d405e4d905cb82dba81bd234613cc383d2be98571fbb7ee8bf04ef8e58。
- 下一步等待质量结果；通过后采集完整五档性能，原失败和固定参考
  均保留。缓冲生命周期推导不替代 E2E、精度或容量实测。

## 2026-09-22：共享 GDN 24 HTTP 终态，准备归一化缓冲复用

共享 GDN 的 `gdn-shared-mtp-http-20260922` 已完成 24 HTTP，
四服务与驱动退出 0，原始 SQLite 响应、长度、公式及实际 token 链
复核通过。8K/44K 的 plain/MTP 各三次 32-token 输出相同，插桩
开关不改变输出，接受数覆盖 0/2/3。选定位置 8192/8193/8218、
45056/45057 的全部 48 层摘要、首层九阶段摘要相同；可比较的
首两步首层 SSM/conv 摘要相同。仅证明这些 HTTP 用例与采样位置，
不能外推全面精度；44K TTFT 回退及正式输出差异仍未关闭。

源码检查确认 raw QKV 的计算消费者（卷积、checkpoint、状态更新）
均在同 stream 的归一化前完成，后续仅诊断 dump 读取 raw。
新候选 `prepared/gdn-normalized-reuse-20260922` 将 FP32 q/k
乘法前移到准备 kernel，普通模式复用 raw 投影缓冲；dump 模式
另留对齐尾部以保留原数据。T=8192 时 raw 160 MiB 可容纳
128 MiB 归一化值，普通模式不增加分配次数/容量（较因子尾部版
减少 1 MiB）；dump 模式分配 288 MiB。预算源码没有精确描述
这些瞬时缓冲，不能据常量余量宣称峰值容量已验证。旧 +127 MiB
尾部方案未应用。新候选正在必要构建，首项运行必须为质量 HTTP，
尚无速度或精度结论。

## 2026-09-22：共享 GDN 8K HTTP 输出分叉本次未出现，含部分接受

- gdn-shared-mtp-http-20260922 的 plain-off/mtp-off 8K 各三次
  已完成，原始响应核对通过，完整文本哈希和 32 个实际输出
  token 链一致。原先第 28 个 token 分叉本次未出现。
- off-token-chain-review.json 按实际接受行重建；三次均覆盖
  accepted=0/2/3（k=3），包含 checkpoint 恢复边界，并验证
  correction 与下一步输入衔接。没有比较被拒草稿作为实际输出。
- 44K 与阶段插桩开关控制仍在运行，不能据输出相同推断内部
  状态相同或全面精度通过。下一步完成剩余 HTTP 与层/阶段
  对照；44K 性能回退、正式输出差异仍未通过，正式基线不变。

## 2026-09-22：44K 三侧复核仍回退，启动共享 GDN MTP HTTP 定位

- gdn-checkpoint-parent-pair-20260922 全 18 请求完成，三服务/
  驱动退出 0，原始响应与公式复核通过。8K 两指标与两侧父版
  均重叠；44K TTFT 候选 30.520660–30.553731，前侧父版
  30.454704–30.490928，后侧 30.499532–30.507141 秒，两侧
  均不利。原样本/失败保留，当前版不接受。
- 确认无推理服务后，同二进制启动 gdn-shared-mtp-http-20260922，
  PID 1989834；8K/44K、输出 32、plain/MTP 插桩关/开各三次。
  仅真实 HTTP 排障，检查共享 GDN 的首层分叉和接受/回滚行为，
  不以此覆盖性能/正式输出未通过，不做底层测试/profile。
- prepared/gdn-normalized-fp32-20260922 仅准备 FP32 乘法前移
  方案，尚未应用/构建/测试。模型 config 实际 nkh=16/kd=128，
  T=8192 临时尾部增加 127 MiB（1→128 MiB），无新增分配次数。
  下一步先完成当前 MTP 定位，再决定应用并按 E2E 检验收益，
  不隐去容量代价，正式基线不变。

## 2026-09-22：收拢 STATUS 候选入口，明确当前证据与历史边界

- 等待相邻 HTTP 期间，仅收拢 STATUS 的 decode 候选段：当前
  二进制、修改原因、完整五档结果、未通过项及运行中 PID 集中
  展示；旧候选的验证不自动转移到共享 FP32 候选。
- 详细过程与所有失败仍完整保留在 decode 路径报告和历史日志，
  原入口快照保存在 prepared/status-decode-summary-20260922。
  模型/运行时代码/二进制及其他任务的 T4000 条目均未修改。
- 原因：入口累积多轮“当前/尚未完成”叙述，容易把历史阶段与
  当前状态混读。下一步仍是相邻性能及 MTP HTTP，不改变门禁，
  本次纯文档整理不增加推理测试或宣称运行时通过。

## 2026-09-22：checkpoint 专门化五档采集完成，启动 8K/44K 三侧复核

- gdn-checkpoint-specialization-performance-20260922 完整 15 HTTP
  终态，服务/驱动退出 0，所有原始响应/指标复核通过。TTFT
  均值 0.802/2.643/5.188/30.554/159.832 秒，decode
  18.748/18.064/18.045/17.822/17.179 tok/s。五档输出同
  FP32 初版，仍不同正式参考，不能据此声称精度已接受。
- 8K/44K TTFT 对正式父版不利分离；44K 对前一候选也不利，
  200K 无不利分离，decode 各档无不利分离。完整矩阵及所有
  失败样本保留，不以相对 FP32 初版的明显恢复抵消正式门禁。
- 已启动 gdn-checkpoint-parent-pair-20260922，PID 1981749，
  8K/44K 旧→新→旧，每侧每档三次，共 18 HTTP。父版归档
  二进制 0c3f3914… 与候选 a95dbafd… 摘要已核对，各版检查
  自身输出，原始 SQLite 复核也已准备，未计通过。
- 下一步完成相邻复核，再进行已准备的 MTP/阶段 HTTP；
  当前无底层测试/profile，正式参考及工作区其他任务内容不变。

## 2026-09-22：checkpoint 专门化 8K 对正式父版仍不利，准备相邻复核

- 8K 三次 HTTP/原始响应复核完成，TTFT 均值 5.188 秒，
  范围 5.184510–5.192201，相对正式父版 5.161910–5.173672
  不利分离；相对固定参考/上一候选无不利分离，输出同 FP32
  初版。保留为未通过，不用相对初版收益覆盖或设置新容忍率。
- prepared/gdn-checkpoint-parent-pair-20260922 已准备旧→新→旧
  HTTP 驱动，未执行。完整矩阵结束后选择所有实际不利档位，
  对各版验证自身完整输出，保留原始与相邻样本，正式参考不变。
- 下一步先完成剩余长上下文采集，再相邻复核与 MTP HTTP。
  当前进程继续，源码/二进制不变；无底层测试/profile。

## 2026-09-22：checkpoint 专门化 4K TTFT 回到父版范围，继续完整矩阵

- 前两档 1K/4K HTTP 各三次完成，原始响应/长度/finish/公式
  已核对。TTFT 均值 0.802/2.643 秒，decode 18.748/18.064
  tok/s，输出同 FP32 初版，仍不同于正式参考。
- 两档相对正式父版/固定参考/上一候选均无不利分离；4K 初版
  2.914 秒降至 2.643 秒。本次只有 checkpoint 编译期分派变化，
  结果支持该开销假设，但不证明整轮性能恢复或精度已接受。
- 已准备 gdn-shared-mtp-http-20260922 四组实际 HTTP 驱动，
  8K/44K 各三次、输出 32、plain/MTP 插桩关/开，尚未执行。
  同候选 trace-off 控制不替代正式参考，按实际接受 token 行
  比较阶段；输入 token 已分叉的位置单独标记，不误作同输入。
- 下一步完成当前五档，再复验 MTP/GDN/checkpoint 行为；
  原始失败记录保留，没有前置底层测试/profile。

## 2026-09-22：checkpoint 专门化质量 11/11，启动完整性能对照

- gdn-checkpoint-specialization-20260922 首项质量 HTTP 已终态，
  11/11（含 200K），输入及完整输出同正式 7364767，服务/
  驱动退出 0。二进制 a95dbafd… 保持不变。
- 启动 gdn-checkpoint-specialization-performance-20260922，
  PID 1978180，五档各三次、输出 256、MTP/诊断关闭。对初版
  FP32、正式父版、固定参考及前一候选分别保留原始比较，
  采集成功不等同精度/性能接受。
- 下一步完成完整矩阵，检验单独 checkpoint 编译期分派是否
  恢复性能，再安排 MTP/checkpoint HTTP。质量通过不覆盖
  旧失败；未做底层数值、单测或 profile，正式基线不变。

## 2026-09-22：共享 FP32 初版完整性能不接受，启动 checkpoint 专门化首测

- gdn-shared-fp32-performance-20260922 五档 15 HTTP 全部完成，
  服务/驱动退出 0，原始响应/指标复核全通过，仅代表采集有效。
  TTFT 均值 0.846/2.914/5.738/33.555/173.496 秒，decode
  18.757/18.092/18.123/17.846/17.142 tok/s。4K 起四档 TTFT
  对正式父版/固定参考/上一候选均不利分离；decode 无不利
  分离，五档输出均不同。初版不接受，失败与全样本保留。
- 确认进程终态且无推理服务后，应用已准备的 checkpoint 编译期
  分派：仅改变 checkpoint 需求选择，递推算术与 FP32 因子、
  分配、checkpoint 布局不变。旧二进制/源/补丁已归档。
- 必要构建零警告；a95dbafd… 首项运行测试为正式质量 HTTP，
  gdn-checkpoint-specialization-20260922，PID 1976772，沿用
  7364767 输入及完整输出参考。尚未计通过，未做底层测试。
- 下一步质量通过后完成完整性能并对初版与正式参考分别比较，
  检验 checkpoint 分支开销假设，不提前声称恢复；MTP 与
  checkpoint 行为仍需真实 HTTP 复验。正式基线不变。

## 2026-09-22：8K 同样 TTFT 回退，准备 checkpoint 需求编译期分派

- 同一性能进程前 3 档完成，8K TTFT 均值 5.738 秒，相对
  正式父版/固定参考/上一候选均不利分离；decode 尚无不利
  分离。三档完整输出均不同于三份对照，继续保留完整矩阵。
- 只读源码发现共享 register kernel 即便普通 prefill 不保存
  checkpoint，仍有逐 token 的运行时指针/边界判断及 checkpoint
  地址生命周期。其开销尚未证实，不能直接认定为全部回退根因。
- prepared/gdn-checkpoint-specialization-20260922 已准备前后源，
  仅将 checkpoint 需求在 host 分派为编译期模板参数；保持
  FP32 因子、递推算术、分配及 checkpoint 布局。未应用、未
  编译、未测试，运行中源码/二进制不变。
- 下一步等待现有矩阵终态，再以单独 HTTP 候选检验该假设。
  正式参考与失败证据不变，无底层测试/profile。

## 2026-09-22：共享 FP32 候选 4K TTFT 回退，完整矩阵继续保留

- 性能前两档 1K/4K 各三次 HTTP 完成；输出各自确定，但均
  不同于正式父版、固定参考和上一候选。4K TTFT 均值 2.914
  秒，重复范围对三份对照均不利分离，见 performance-review。
- 当前候选不接受。质量 11/11 不覆盖性能和正式输出失败；
  原始质量 SQLite 的 11 请求、输入/输出/stop 与二进制/源
  摘要已独立核对，raw-quality-audit.json 保存结果。
- 下一步让同一进程完成五档原始采集，保留失败，不修改运行中
  二进制；完成后处理回退，再以真实 HTTP 验证。未运行底层
  数值、单测或 profile，正式参考不变。

## 2026-09-22：共享 FP32 GDN 首项质量 11/11，通过后启动五档性能

- gdn-shared-fp32-20260922 首项正式质量 HTTP 完成，11/11，
  含 200K，服务/驱动退出 0；完整文本、prompt 摘要、长度和
  stop 均按原 7364767 参考核对通过。二进制 e39f4cc7… 不变。
- 因质量门禁已通过，启动同二进制五档真实 HTTP 性能采集：
  gdn-shared-fp32-performance-20260922，PID 1972427。保持
  原 prompt、单流、输出 256、每档三次，关闭 MTP/诊断。
- 此轮保留所有档位的完整输出及性能样本，再独立与 7364767、
  fcb5925 和前一候选比较；不以采集成功替代正式输出或性能
  验收，不更换参考。下一步完成矩阵、MTP/checkpoint 复验；
  当前仍未接受，未做底层数值、单测或 profile。

## 2026-09-22：共享 FP32 GDN 候选构建完成，首测正式质量 HTTP

- 根据真实 HTTP 首层 GDN 分叉证据，统一目标 kd=vd=128 的
  单序列、打包 decode、因果/ragged 多 token register 递推。
  使用同一 FP32 归一化因子及更新顺序，按实际 slot/local token
  保存 checkpoint，单序列传入切片仍按局部 slot 0 寻址。
- 保留源 BF16 q/k，消除 register 路径归一化结果的 BF16 写回；
  因子在 qkv_raw 原分配的对齐尾部，无新增分配次数。原 raw
  内容和调试 dump 保留，增加容量 8*T*nkh 加最多 6 字节对齐。
  不能把更宽中间类型直接当作最终模型精度或性能已改善。
- prepared/gdn-shared-fp32-20260922 保存前后源、旧二进制、
  补丁及摘要。必要构建零警告；首项真实 EvalScope quality
  HTTP 启动于 gdn-shared-fp32-20260922（PID 1969142），
  沿用 7364767 输入和完整输出参考，所有诊断环境关闭。
- 下一步完成质量与完整性能门禁，并重新验证 MTP/checkpoint
  路径；若失败先保留原始记录处理，不进行前置底层测试。
  候选未接受，正式基线不变。

## 2026-09-22：24 HTTP 阶段对照完成，首层首次差异定位到 GDN

- mtp-linear-stage-trace-http-20260922 四组 24 HTTP 全部完成，
  四服务/驱动退出 0，四份原始响应/接受 token/层/阶段复核
  complete=true。开关插桩均保持上一层摘要版同模式完整输出。
- 8K 三次 × 位置 8192/8193/8218，以及 44K 三次 × 位置
  45056/45057：input、qkv_raw、z、a、beta、conv 摘要全部
  相同，首次不同阶段均为 GDN，gate/output 随之不同。结合
  首步初始 SSM 相同，排除该层首步此前投影/conv 作为首次
  分叉边界；不外推所有层，也不由哈希推导误差大小或精度。
- 源码 register 路径归一化后写 BF16，causal 路径保留 FP32，
  且归约/更新结合顺序不同。尚不能将某一项单独认定为全部
  输出分叉原因；原正式输出与性能未通过记录继续保留。
- 下一步围绕共享递推及 checkpoint 边界设计修正，避免仅为
  一致性随意降低某一路精度；必要构建后仍先 HTTP，完整门禁
  通过之前不做底层数值或 profile。运行时候选仍未接受。

## 2026-09-22：启动第 0 层线性阶段 HTTP 摘要

- 因前轮已将首次差异缩小到第 0 层，增加显式 host 位置诊断参数，
  仅从模型层选中的第 0 层传入线性 attention；记录投影、卷积、
  GDN、gate、输出及输入的逐行摘要，默认关闭。计算与接受逻辑
  不变，临时接口和插桩将在定位后清理，不作架构收益。
- 源码/旧二进制保存于 prepared/mtp-linear-stage-trace-20260922；
  必要构建零警告。首项运行测试启动为真实 EvalScope HTTP：
  mtp-linear-stage-trace-http-20260922，PID 1964097，8K/44K
  每档三次、输出 32、plain/MTP 插桩关/开四组。新增读回与
  同步不计性能，必须保持前一层摘要版完整输出，目前未计通过。
- 下一步完成原始响应/实际接受行核对，比较各阶段首个差异；
  不把被拒草稿和不同位置状态混比，不用哈希替代精度验收。

## 2026-09-22：MTP 层摘要完成，首次 decode 的第 0 层已分叉

- mtp-layer-trace-http-20260922 四组 24 HTTP 全部完成，四服务/
  驱动退出 0；raw-response、token-chain、layer-state 三份复核
  complete=true，开关插桩不改变各模式控制输出。未做底层测试。
- 8K/44K 各三次的所有观察位置，输入 trunk 相同，首个不同层
  均为第 0 层。首步 SSM/conv 相同，下一步 conv 相同而 SSM
  不同；不是仅在最终预测分叉位置才出现状态差异。哈希不提供
  数值误差或精度结论，尚不能确定具体算子根因。
- 更正参考来源解释：evalscope 的 Transformers 5.17.0 在归一化
  前转 FP32，固定 5.16.1 则在归一化之后转；不能切换参考。
  ref_dump.py 的参数构造/copy_ 也不能用源张量 dtype 推断实际
  激活 dtype。本次只读核对并保存源码 SHA，没有运行参考推理。
- 原因：先把真实 HTTP 的差异限定到可检验边界，避免把数学
  顺序不同直接当成根因。下一步观察第 0 层投影、conv 和 GDN
  边界；任何新增诊断必要构建后的首测仍为 HTTP，检查开关
  不改变输出。候选仍未接受，正式基线和所有失败记录保留。

## 2026-09-22：host 控制全部完成，44K 首次预测分叉定位并启动层摘要

- mtp-verify-host-trace-http-20260922 四组 24 HTTP 完成，四服务/
  驱动退出 0；日志关闭/开启保持各模式插桩前完整输出。原始
  响应和 token 链复核 complete=true，仍非生产接受。
- 44K 三次首个不同输出索引均为 2（第 3 个），预测位置
  45057、输入 248068，plain 预测 271，MTP 预测 198；MTP
  base=45057/local_row=0/accepted=3。prefill token 相同、
  分叉前 token 前缀一致。8K 首次预测差异仍为 8218，证据
  token-chain-review.json；不将输入相同当作递推状态相同。
- 旧进程终止且确认无 q4t* serve 后，应用选择性层摘要诊断：
  只在 max_seq=1、T<=4 观察 8192/8193/8218/45056/45057，
  记录 trunk 输入与各层输出哈希，并在匹配 forward base 记录
  第 0 层初始 SSM/conv 原始字节摘要。旧源/二进制/补丁保存于
  prepared/mtp-layer-trace-20260922。新增诊断 D2H/同步显式
  受 Q4T_DECODE_LAYER_TRACE 控制；计算、接受与回滚逻辑未改。
- q4t 必要构建零警告，当前 SHA 为
  7070dc6bc0d98a3767cf2677572f78604a8ecb49135ef639b647b4cbc7fd65b8。
  首项运行测试 mtp-layer-trace-http-20260922，PID 1958923：
  8K/44K、输出 32，plain-off/MTP-off/plain-on/MTP-on 各三次；
  host token 日志全程开启，层摘要只在 on 组开启。必须保持
  c7f5ab35… 版同模式完整 HTTP 输出，无前置数值或 kernel 测试。
- 下一步按真实已接受的 token/步内行匹配层摘要，找到首次不同
  层；拒绝草稿行不得混入比较。哈希仅用于定位，不证明精度，
  插桩耗时不承担性能验收。原失败保留，批量 prefill 仍未启动。

## 2026-09-22：8K 首次不同预测落在位置 8218 的 MTP 校正行

- mtp-verify-host-trace-http-20260922 原进程 1954161 仍运行。
  日志关闭四用例、plain-on 两档和 MTP-on 8K，共 21 请求已
  完成且保持插桩前各自模式输出；最后 MTP-on 44K 未完成。
  原始数据库复核见 raw-response-review.json。
- review_chains.py 从完整 HTTP 日志重建 token 链，8K 三次
  结果一致：prefill token=271 相同，前 27 个输出 token 相同，
  首个不同输出索引 27（第 28 个，逻辑输出位置 8219）。
  在预测位置 8218，两侧输入都是 55404；plain 预测 13，MTP
  主模型验证预测 11。对应 MTP step base=8216，第 3 行
  local_row=2，k=3、accepted=2，是校正预测，下一轮作为 b。
- base 连续、已接受输入与上一预测衔接均通过；已拒草稿不计
  输出，最后一步超出 HTTP 32-token 限制的尾部已裁掉。输入
  token 前缀相同不证明递推状态相同，更不能据此认定 near-tie
  或哪一个 kernel 是根因。证据 token-chain-review.json。
- 已核对 MTP 实际 k=3；单步验证 4 token 与 plain 单 token
  分派不同。下一步收齐 44K，再按真实位置做层级 HTTP 定位。
  本轮未改运行时，未做数值测试/profile，不宣布全控制完成。

## 2026-09-22：host 轨迹关闭组前三用例保持原输出，准备 token 链解析

- mtp-verify-host-trace-http-20260922 原进程 1954161 仍运行。
  plain-off 的 8K/44K 以及 MTP-off 的 8K，共九 HTTP 已完成，
  token 数、结束原因与完整输出哈希均同插桩前同模式控制，当前
  二进制摘要匹配。control-output-review.json 保存这部分结果；
  MTP-off 44K 与日志开启组未完成，不认定全控制通过。
- 阅读 serve 与 MTP 返回值代码确认：每步流输出为当前 b 加已
  接受草稿，校正预测作为下一轮 b。review_chains.py 按实际已
  接受输入重建链，校验 prefill token、每步 base、accepted、
  输入等于上一预测，裁掉最后一轮超过实际 HTTP 32 token 的
  尾部；被拒行不算输出。完成的日志开启用例才参与比较，保存
  首次不同输出索引、预测位置与 MTP 步内行/接受数量。
- 本轮仅编写原始 HTTP 日志解析，未运行模型/数值微测试，未改
  运行时。下一步继续采集剩余控制与日志组，再依据真实轨迹定位。

## 2026-09-22：同配置分叉终态确认，host 轨迹首项 HTTP 已启动

- decode-consistent-mtp-switch-http-20260922 三侧 27 HTTP 完成，
  三服务/驱动退出 0。相同候选/容量 65536/输出上限 32/输入，
  plain 重启前后完全一致；MTP 1K 同 plain，8K/44K 均不同。
  各组重复确定。原始数据库复核 complete=true，精度仍未接受。
- 源码同时存在 GDN 归一化/归约差异及 T=1 QsaDecodeSplit 对
  多 token SparseAttentionKernel 差异，尚不认定根因。已保存
  source-hypotheses.json；不直接修改怀疑的计算路径。
- 确认旧监督进程结束、全部 q4t* serve 不存活后，按源码摘要
  应用 prepared/mtp-verify-host-trace-20260922：只在 MTP verify
  已有 D2H 及接受/回滚之后打印 host token/预测/接受数。默认
  Q4T_DECODE_BATCH_TRACE 关闭，无新增 GPU 拷贝/同步。旧源码、
  二进制 448d73d5… 和 worktree patch 已归档。计算逻辑未改。
- 必要构建 q4t 零警告，当前 SHA 为
  c7f5ab35f872363b1311b8df4754b44bdc1665d934c5759c3669482fe7e33e11。
  首项运行测试 mtp-verify-host-trace-http-20260922，PID 1954161：
  8K/44K，plain-off/MTP-off/plain-on/MTP-on 各三次、输出 32，
  每组必须保持插桩前同模式 HTTP 完整输出。没有前置低层测试。
- 下一步核对开关控制后，从主机 token 轨迹找首次不同预测。
  被拒草稿及最后一轮超出输出上限的预测不得冒充流输出；批量
  prefill 诊断继续暂不启动。原性能/正式输出失败记录不替换。

## 2026-09-22：同配置开关对照在 8K 复现输出分叉

- decode-consistent-mtp-switch-http-20260922（PID 1949448）仍
  运行。plain 前侧三档完成；MTP 1K/8K 完成，各自三次确定，
  1K 同 plain，8K 完整输出不同。8k-output-difference.json 保存
  文本差异；尚不提供 token 层定位或数值根因证明。
- 两侧使用同一二进制、相同容量 65536/输出上限 32/输入，MTP
  开关是运行配置差异。其 TTFT 相对 plain 较高包含 MTP 初始化，
  与前一父候选 44K 单侧回退属于不同对照，不能混算。
- 文档检查点 b2aee75 已推送 main，本条为后续新证据。运行时
  未改；等待 44K 和后侧完整终态，再决定应用已准备 host trace。

## 2026-09-22：同配置 plain 三档完成，准备 host 验证 token 定位

- decode-consistent-mtp-switch-http-20260922 原进程 1949448
  仍运行，plain-before 三档各三次、32 token 已完成且重复稳定，
  MTP 侧尚在采集。新增原始数据库复核脚本，统一请求参数、
  二进制摘要、长度、结束原因与输出比较，未重启现有进程。
- 源码排查：普通 pooled decode 走 GdnRegPrepNormKernel +
  GatedDeltaNetRegKernel，q/k 归一化写回 BF16；MTP verify 的
  multi_seq_causal 走 GatedDeltaNetMultiSeqCausalKernel，归一化
  留在 FP32，归约/递推顺序也不同。这是可验证候选原因，未断言
  为本次输出分叉根因，没有运行数值测试或 profile。
- prepared/mtp-verify-host-trace-20260922 仅准备补丁，未应用。
  使用 MtpSpeculativeStepMulti 已读回的 vtok/m_argmax/accepted
  输出位置及保留行，复用默认关闭的 Q4T_DECODE_BATCH_TRACE。
  无额外 GPU 拷贝/同步；若同配置差异重现，再做真实 HTTP 定位。
  被拒草稿行和超出流输出上限的预测不能计入实际 token 链。
- 下一步等待同配置终态，根据证据决定是否应用诊断补丁；当前
  运行时代码和二进制保持不变，正式输出/性能失败证据保留。

## 2026-09-22：MTP 三侧终态暴露 44K 差异，转入同配置开关对照

- decode-consistent-mtp-http-20260922 三侧 27 HTTP 完成，三个
  服务/驱动退出 0，各请求 32 token 且各组三次确定。日志证明
  实际 MTP B=1。raw-response-review.json 独立核对数据库请求、
  响应、长度、结束原因、指标公式及二进制；capture_complete=true
  但 outputs_equal_parent=false，production_accepted=false。
- 1K/8K 候选输出同两侧父版，TTFT/decode 无不利分离。44K
  候选输出不同于两侧父版；TTFT 31.962053–31.978889 秒，与
  前侧 31.961117–32.001622 重叠，对后侧 31.932994–31.961539
  单侧不利分离。原数据完整保留，不能用范围差距小放行。
- 44k-output-difference.json 记录首个不同字符在偏移 10，且与
  既有普通 decode 输出的前缀关系不同。prompt 已核对一致，
  但普通矩阵 max_len=208896/output=256，本组 65536/32；文本
  比较不提供 token/数值精度证明，也不足以隔离 MTP 开关原因。
- 因此启动 decode-consistent-mtp-switch-http-20260922，PID
  1949448：同一候选二进制、同容量/上限/输入，plain→MTP→plain，
  1K/8K/44K 各三次。全部仍为真实 HTTP 定位；原失败不替换。
  批量 prefill 准备驱动标记暂不启动，先处置 MTP 差异，不用新
  消费者采集掩盖失败。运行时代码未改，无低层测试或 profile。

## 2026-09-22：MTP 候选前两档同父版，准备实际批量 prefill 诊断

- decode-consistent-mtp-http-20260922 原进程 1941891 仍运行。
  前侧父版 1K/8K/44K 九请求完成，均 32 token 且重复确定；候选
  1K/8K 已完成，完整输出同前侧父版，TTFT/decode 无不利分离。
  候选 44K 与后侧父版未完成，不宣称全 MTP 验收。
- 新增本轮 raw-response-review.json，从原始数据库核对实际
  prompt、max_tokens/温度/stream、token 数、结束原因、内容哈希
  及性能公式，按两侧分别比较，不混入 MTP-off 正式矩阵。
- 准备 decode-consistent-batch-prefill-http-20260922（未启动），
  复用既有 HTTP 排队方法和已构建的只观察读回库：长请求挡住
  prefill，排入 1K/4K 两短请求，要求实际 B=2/正确行距，三侧
  各三轮。驱动独立标记诊断用途，重复稳定与跨版本差异分别记录，
  不制造 acceptance.json，不改正式验收驱动。待 MTP 终态审查后
  再决定执行；本轮未编辑运行时或执行低层测试。

## 2026-09-22：视觉三侧 36 HTTP 一致，启动独立 MTP 三侧采集

- decode-consistent-vision-paths-20260922 已终态，三服务/驱动
  退出 0。调度→分配失败回退→调度，四用例各三次共 36 请求。
  单图/双图/视频/raw-stop 输出数分别 102/128/109/1，所有
  重复与路径/重启间的完整输出及 token 数一致。自然 EOS 样本
  未伪装成 128-token 等长性能。raw-response-review.json 从
  原始数据库核对请求 messages/max_tokens/温度/stream、长度、
  结束原因、文本和哈希；complete=true，production_accepted=false。
- runtime-provenance.json 核对正式质量、完整性能矩阵与当前
  运行时补丁的 10 文件完全相同，二进制摘要相同。此检查不是
  数值精度证明；显式视觉模板不能替代标准 messages 接入验证。
- 随后启动 decode-consistent-mtp-http-20260922，PID 1941891，
  父版→候选→父版，各 1K/8K/44K 三次、输出上限 32。使用已有
  evalscope 请求，必须有实际 MTP B=1 步日志，不接受 plain 回退
  冒充覆盖；采集完成和父版输出相等分别记录，不能混入 MTP-off
  正式矩阵。独立诊断驱动不伪造或修改 acceptance 门禁。
- 下一步收齐 MTP 原始响应、状态与差异，再推进批量 prefill
  消费者及正式旧输出精度处置。本轮没有运行时编辑或底层测试。

## 2026-09-22：四档回退与重启完整终态，启动视觉路径 HTTP

- decode-consistent-paths-20260922 监督进程已结束，driver-exit=0。
  四档回退及调度重启 24 个新 HTTP 请求的完整输出均同先前候选
  调度，两个服务退出 0。重启调度四档 TTFT/decode 均无不利分离；
  跨路径回退 decode 较低的原始结果保留，父版历史同路径对照
  四档输出一致且两项指标无不利分离。raw-response-review.json
  complete=true，仍明确 production_accepted=false。
- 随后启动 decode-consistent-vision-paths-20260922，PID 1937369。
  同一二进制调度→回退→调度，单图、双图、视频、raw-stop，
  每组三次，输出上限 128。检查实际路径、二进制/故障库摘要、
  重复输出与重启一致性；仅显式模板定位，不声明标准 messages
  正确性，不伪造正式 acceptance 门禁。
- 准备 decode-consistent-mtp-http-20260922（未启动）：父版→候选
  →父版，1K/8K/44K 三次各输出上限 32，要求实际 MTP B=1 日志，
  与默认 MTP-off 性能矩阵隔离。等待视觉终态核对后再决定启动。
- 本轮没有运行时修改、重建或底层测试。下一步继续视觉、批量
  prefill/MTP 消费者覆盖及正式旧输出差异的精度处置。

## 2026-09-22：四档回退完整输出一致，继续调度重启对照

- decode-consistent-paths-20260922 四档回退 12 请求已完成，
  服务退出 0；所有输出保持候选调度。原监督进程 1930547 仍存活，
  已进入 scheduled-after，未提前终止或重启测量。
- 补齐四档与正式父版两份历史 fallback 对照：完整输出一致，
  TTFT 和 decode 均无不利范围分离。四档 fallback 相比候选调度
  的 decode 范围分离仍原样保留，不能混成“候选回退更慢”的结论。
  historical-fallback-review.json 现含四档两指标及父版摘要核对。
- 视觉准备驱动补上异常终态记录，仍未启动；没有改动运行时代码。
  下一步等待调度重启四档完成，保持正式精度参考不变。

## 2026-09-22：回退前三档输出一致，速度按相同路径比较

- decode-consistent-paths-20260922 原进程 1930547 仍运行，回退
  1K/4K/8K 各三次输出同候选调度，实际输入/输出长度与内容哈希
  已复核。44K、调度重启尚未完成，不宣称全路径验收。
- 回退 decode 均值 18.157/17.438/17.351 tok/s，均低于候选
  调度且范围不重叠。另与 decode-path-parent-fallback-matrix-
  20260922 两份正式父版历史回退对照：三档完整输出一致，decode
  范围均重叠；父版二进制摘要和完整 HTTP 终态已核对。这是历史
  同路径比较，不是新的相邻复验，也不替换原始跨路径差异。
  证据 raw-response-review.json、historical-fallback-review.json。
- 准备 decode-consistent-vision-paths-20260922：原有单图/双图/
  视频与 raw-stop 四组，调度→回退→调度，每组三次，输出上限
  128。仅定位用途，未启动，不调用正式消费者 acceptance 门禁，
  不伪造 acceptance.json；显式视觉模板不覆盖标准 messages。
- 下一步等待当前四档终态和重启对照，再按证据推进视觉/其他
  消费者与正式输出差异。运行时代码和二进制未改。

## 2026-09-22：44K 三侧复验终态，启动清理版回退路径 HTTP

- decode-consistent-parent-pair-20260922 三侧各三次 44K 请求
  全部完成，三个服务/驱动退出 0；原始响应长度、结束、输出哈希
  与版本摘要复核，父版两侧保持正式输出，候选保持自身诊断输出。
- 候选 TTFT 范围 30.490284–30.557880 秒，前侧父版
  30.477829–30.604356，后侧 30.487182–30.570807，均重叠。
  decode 候选 17.799644–17.858757 tok/s，也与两侧范围重叠。
  不利分离本次未重现；原矩阵失败与样本仍保留，不认定稳定提速，
  更不认定正式输出精度门禁通过。证据 raw-response-review.json。
- 随后启动 decode-consistent-paths-20260922（PID 1930547），
  同一二进制四档 1K/4K/8K/44K，先分配失败回退，再调度重启，
  每组每档三次，对照先前完整矩阵。故障注入摘要已加断言，实际
  回退日志与调度启用状态检查保留；没有运行时修改或底层前置测试。
- 下一步核对回退路径完整输出、消费者范围和正式旧输出差异。

## 2026-09-22：独立复核完整矩阵原始响应，补强后续驱动空闲检查

- 对 decode-consistent-performance-20260922 的 15 个原始响应逐项
  核对实际输入长度、输出 256、length 结束、内容 SHA-256、TTFT
  与首 token 后 decode 公式，并确认当前二进制摘要同采集版本。
  证据 raw-response-audit.json；这不是正式输出差异的精度通过。
- 三侧相邻复验 PID 1925052 仍运行：首侧父版三次 HTTP 完成，
  输出保持正式参考且服务退出 0；候选侧采集中。未改运行时。
- 后续 prepared/decode-consistent-paths-20260922 尚未启动。
  增加故障注入库摘要断言；空闲检查改为检查 /proc 下实际 q4t*
  可执行服务，涵盖 q4t-before，避免仅匹配 q4t 漏掉归档父版。
- 下一步等待当前候选和后侧父版完整结果；不重启仍活跃的进程，
  不使用不完整结果替代原 44K 回退记录。

## 2026-09-22：清理版五档 HTTP 完成，启动 44K 三侧相邻复验

- 原进程 1917510 已结束；decode-consistent-performance-20260922
  服务/驱动退出 0，五档各三次请求完成。200K TTFT 均值
  159.546233 秒、decode 17.148532 tok/s，无不利范围分离。
- 完整矩阵仅 44K TTFT 对正式父版历史样本不利分离，其他档位
  对正式父版/固定参考均无不利分离。五档输出同上一 GDN 诊断
  候选、不同于正式旧输出；不宣布精度或生产验收通过。
- 从已准备驱动启动 decode-consistent-parent-pair-20260922，
  PID 1925052，44K 父版→候选→父版，每侧三次真实 HTTP。
  原始 performance-review.json 冻结保存，父版与候选分别对照
  自身完整输出参考；验证二进制摘要并确认无其他 q4t 服务后启动。
- 本轮未改运行时、未重建、未运行前置底层测试。下一步收齐
  相邻复验，再处理回退路径/消费者与正式输出差异门禁。

## 2026-09-22：收束当前状态入口，保留完整阶段证据

- 性能采集仍在原进程运行期间，仅整理文档：STATUS 的 decode
  候选段改为当前实现、已验证范围、44K TTFT 未通过项与后续门禁。
  详细过程原文保留在 decode 路径报告的带日期阶段快照中，开发
  历史日志未修改，所有失败、正式输出差异及未覆盖范围均保留。
- 这样避免当前入口堆积旧“下一步/正在运行”表述，不把阶段
  诊断误读为验收。运行时/二进制未改，下一步仍等待 200K 完整结果。

## 2026-09-22：四档性能已采集，44K TTFT 对正式父版不利分离

- decode-consistent-performance-20260922 仍运行，PID 1917510；
  1K/4K/8K/44K 各三次输出保持上一 GDN 候选，正式旧输出差异仍在。
  TTFT 均值 0.797/2.636/5.188/30.520 秒，decode 均值
  18.792/18.069/18.069/17.851 tok/s；200K 未完成。
- 前三档相对正式父版 7364767/固定 fcb5925 均无不利范围分离。
  44K TTFT 候选 30.498487–30.539666 秒，高于正式父版
  30.448455–30.486824 秒，记为未通过；对固定参考及 decode
  无不利分离。原始样本完整保留，不设置新容忍率。
- 已准备 decode-consistent-parent-pair-20260922，但尚未执行；
  当前完整终态后，按全部不利分离档位做父版→候选→父版相邻复验，
  各自维持完整输出控制。没有前置底层测试/profile，候选未接受。

## 2026-09-22：混合上下文 36 HTTP 一致，启动完整五档性能复验

- decode-mixed-context-http-20260922 的 36 请求、服务和驱动正常
  结束，1K/4K/8K/44K 串行前后及三轮不同 prompt 并发全部输出/
  128 token 链匹配各自串行。三轮均含 121 个实际四长度 B=4 步；
  原始 SQLite 行数与输入身份/长度集合、host 链核对通过。实际
  slot 并不等于文件排列，不宣称所有排列或全部消费者覆盖。
- 同二进制启动 decode-consistent-performance-20260922，PID 1917510，
  五档各三次输出 256、max_seq=1/MTP 关闭、所有诊断环境关闭。
- 响应控制为上一 GDN 状态访问候选，明确仅检测本轮新增变化；
  7364767 正式输出差异门禁未关闭，固定参考 fcb5925 不变。
  性能将对照两份正式参考的重复范围，不拼接样本或设置新容忍率。
- 下一步核对全部五档，批量 prefill、其他消费者及精度处置仍待完成；
  没有前置底层测试/profile，候选尚未接受。

## 2026-09-22：清理版质量 11/11，启动不同 prompt 混合上下文 HTTP

- decode-gpu-trace-clean-quality-20260922 完整 11/11，含 200K；答案、
  完整输出、实际长度、stop 与已接受 7364767 一致，服务和驱动退出 0，
  二进制摘要核对通过。没有前置底层测试/profile。
- 同二进制启动 decode-mixed-context-http-20260922，PID 1909422：
  1K/4K/8K/44K 各自串行前后各三次，加三轮不同排列四请求并发，
  共 36 HTTP，max_seq=4/max_len=65536/max_prefill=8192，MTP/批量
  prefill 关闭。逐请求核对原始 prompt 身份/长度与各自串行输出，
  从 host 日志验证实际 batch/slot/位置/input 链，不预设发送顺序。
- 下一步检查混合长短行和不同输入的隔离，尤其短行密集 top-k 与
  长行多级选择共享批次的行为；完整五档性能及其他消费者仍待验收。

## 2026-09-22：66 HTTP 全部一致，清理 GPU 诊断后首测正式质量

- indexer-mixed-topk-batches-http-20260922 的 66 请求、服务及驱动
  正常结束，1K/8K 各三轮 B=2/3/4 真实覆盖；完整输出和 128 token
  链全部同串行，前后复用一致。final-review.json 保留范围限制，
  不宣称不同输入、全部消费者或性能已通过。
- 确认进程终止后归档并应用已审查的清理补丁：删除临时 GPU 摘要
  D2H/同步及 trace 接口参数。保留默认关闭的 host-only token 日志，
  用已有读回结果验证实际批次，不新增 GPU 同步，计算修正保留。
- 必要构建零警告，第一项正式 HTTP 11 题
  decode-gpu-trace-clean-quality-20260922 已启动，PID 1906867。
  SHA-256 448d73d5b1567430162bf7631db36c2881c2c6648bde605f0dd84afb43a7ac58。
- 下一步核对质量、清理后批量及不同 prompt/混合长度，最终五档
  性能和其他消费者仍待完成；没有前置底层测试/profile。

## 2026-09-22：混合 top-k 候选 24 HTTP 一致，启动 B=2/3/4 复验

- indexer-mixed-topk-http-20260922 完整 24 请求、两个服务和驱动
  正常结束；关闭/开启诊断的全部完整输出同原串行，三轮实际 B=2
  的两个 128 token 链同串行。位置 8192–8196 全部 48 层摘要
  一致，串行前后复用一致，final-review.json 保留验收范围限制。
- 原 8K 边界分叉在该用例消失；不能当成整体精度/性能通过。
  同二进制启动 indexer-mixed-topk-batches-http-20260922，PID 1901062，
  1K/8K 各三轮实际 B=2/3/4，加串行前后共 66 HTTP，仅 host 日志。
- 下一步核对完整 token 链与实际批量，保留所有旧失败；正式质量、
  五档性能及其他消费者仍待最终版验证，无前置底层测试/profile。

## 2026-09-22：保留短窗口 top-k 排列候选首轮 8K 输出一致

- indexer-mixed-topk-http-20260922 正在运行（PID 1896918），首轮
  关闭诊断的两个并发完整输出均为 f124ca62…，匹配串行和原串行输出。
- 初步支持保留短窗口排序输入的方向，但三轮重复、开启诊断的
  逐层摘要与串行复用尚未全部完成；候选不接受，继续同一测试进程。

## 2026-09-22：单改打分完整 HTTP 仍失败，验证短窗口 top-k 排列

- indexer-mixed-boundary-http-20260922 完整 24 请求、两个服务及驱动
  退出 0，三轮仍在 slot 1 8194 第 3 层分叉，串行保持；保留失败。
- 静态检查短路径一次排序展开、长路径再次排序候选，等分索引顺序
  可能变化。归档后修改 pooled 短行的最终选择输入为原始短窗口
  logits/顺序，使用同一排序网络；密集行保留直接展开，长行不变。
  不新增 kernel，不预判因果或精度通过。
- 必要构建零警告，首项 indexer-mixed-topk-http-20260922 已启动，
  PID 1896918，同配置 24 HTTP；没有前置底层测试/profile。
  SHA-256 fee26e7f04c54a58e664de021276c8909e4c5996ce9e420e2c3a4403dfe2979c。
- 下一步检查响应、实际步进和早期逐层摘要，失败即继续定位/回退，
  当前所有运行时修改仍未接受。

## 2026-09-22：混合边界打分候选首轮仍未闭合

- indexer-mixed-boundary-http-20260922 仍运行（PID 1891909）。首轮
  关闭诊断的串行保持 f124ca62…；并发 slot 0 同串行，slot 1 从
  旧 f02b1c09… 改变为 b7c49cad…，仍不一致，不能接受该候选。
- 不替换原始样本，不把算术修改当作修复成功；继续同一轮 24 HTTP，
  下一步依据层间摘要判断是否还涉及 top-k 选择/排列。暂无底层测试。

## 2026-09-22：8K 第 3 层切换边界复现，启动按行保留短打分候选

- decode-8k-layer-trace-http-20260922 共 24 请求完整结束，两个服务
  和驱动退出 0；日志关闭/开启输出均保持前版。三轮均为 slot 1
  8192/8193 全层一致，8194 首次在第 3 层不同（配对 slot 0 8195），
  8195/8196 首次在第 4 层不同；slot 0 和串行前后全部一致。
- 静态核对短路径每 head BF16 舍入、长 OnePass warp 归约不舍入，
  批次最大位置让尚短的行提前选长路径。保留上述证据但不直接当作
  精度通过。归档后仅修改 OnePass 的 pooled 短行打分，保留其原
  短路径 dot 顺序和 BF16 舍入，真正长行计算不变。
- 必要构建零警告，首项 indexer-mixed-boundary-http-20260922
  已启动，PID 1891909，同配置日志关闭/开启 24 HTTP，无前置测试。
  SHA-256 2572a711a9f107da64ff622a2eea58ed9ab5e652445744a1f5e46a3f17c25f69。
- 下一步检查串行完整输出保持、边界逐层差异是否消失。正式质量/
  五档性能/所有消费者仍待最终二进制验证，候选未接受。

## 2026-09-22：启动 8K 切换边界逐层 HTTP 定位

- 确认扩展 66 HTTP 完整终态且无 q4t 存活，归档二进制及源码；
  仅将显式日志位置筛选改为 8192–8196，计算实现不变。
- 必要构建零警告，首项 decode-8k-layer-trace-http-20260922
  已启动，PID 1886942；max_seq=4，日志关闭/开启各十二 HTTP。
  SHA-256 ac11d2adbce5592f4d61db9a97f54e668f3d86cfb21c786ce765593b1991d88d。
- 下一步先检查完整输出保持前版及实际批次/输入链，再寻找逐层
  首次差异；覆盖短/长索引路径切换只是诊断假设，不是根因结论。
  仍未运行底层测试/profile，不作正式精度或性能接受。

## 2026-09-22：扩展 66 HTTP 完成，1K 一致、8K 保留分叉

- bf16-small-m-batches-http-20260922 全部 66 请求、服务及驱动正常
  结束，两档 B=2/3/4 各三轮实际批次覆盖，逐 token 位置和输入链有效。
- 1K 全部同串行。8K 每轮先行 slot 0 同串行，后加入 slot 1–3
  均从第 32 个生成 token 分叉；串行前后仍一致。保留完整请求、
  输出与 token 重建记录，final-review.json 明确 production_accepted=false。
- 原始响应审计器复制了旧 B=2/串行计数假设，首次 B=4 检查报错；
  已按实际并发数修复并保留旧脚本及 audit-repair.json。未重跑请求。
- 下一步真实 HTTP 观察位置 8192–8196 的逐层边界，覆盖按批次最大
  位置选择短/长索引路径的切换点；当前只是定位假设，不能据此断言
  池化错误或数值噪声，更不能接受候选或进入底层测试/profile。

## 2026-09-22：BF16 小批量质量 11/11，扩大实际批量 HTTP

- bf16-small-m-quality-20260922 完整 11/11，包含 200K；答案、
  完整输出、prompt 摘要、实际长度、stop 与已接受 7364767 相同，
  服务和驱动退出 0，运行二进制核对一致；无前置低层测试。
- 同二进制启动 bf16-small-m-batches-http-20260922，PID 1881466：
  1K/8K 各三轮 B=2/3/4，前后串行各三次，共 66 HTTP；max_seq=4，
  MTP/批量 prefill 关闭，仅 host 日志，不增加诊断 D2H 同步。
- 必须核对实际 B 和逐 token 连续输入链，不能以客户端并发数替代；
  保留全部失败/输出变化。不同 prompt、其他消费者与完整五档性能
  尚未覆盖，候选仍未接受。下一步核对 M=3/4 和 8K 输出一致性。

## 2026-09-22：BF16 小批量首轮 HTTP 一致，启动正式质量题

- bf16-small-m-http-20260922 的 24 请求全部完成，两个服务及驱动
  退出 0。关闭/开启诊断全部完整输出同旧串行，三个并发轮次均
  实际 B=2，两个请求的 128 token 链与串行一致。
- 第 0 层内部边界和全部 48 层输出在所观察位置 1024/1025 均
  与串行摘要一致。此前每轮两个并发输出分叉已在本用例消失，
  旧并发输出变更对照保留。不能外推所有输入/位置或 M=3/4。
- 同二进制启动 bf16-small-m-quality-20260922 正式 11 题 HTTP，
  PID 1876497，仍对照已接受 7364767 输出；没有前置底层测试。
- 下一步先核对质量题，再扩展实际批量、上下文和消费者；全部
  验收完成前不接受运行时、不把此诊断当作性能提升证明。

## 2026-09-22：BF16 小批量共享权重候选，首项 HTTP 启动

- 根据三轮 HTTP 首次投影分叉，归档旧版源码/二进制后增加
  Bf16SmallMGemm：M=2/3/4 模板，权重加载共享，各行按单行 GEMV
  顺序执行 f32x2 累加、shuffle 和 BF16 舍入，M=1 原 kernel 不变。
  不额外分配 shared memory，形状/对齐不支持仍走原 cuBLASLt。
- 经通用 Bf16Gemm 分发，小批量消费者均可能受影响，尚未接受。
  必要构建零警告，首项 bf16-small-m-http-20260922 已启动，
  PID 1872548，串行/并发/串行复用共 24 HTTP；无前置低层测试。
  SHA-256 22fa722857c49e1167b6a79dafd8da4106822562a2c1dda317bb0eca33803c66。
- 下一步核对串行完整输出是否保持、并发投影差异是否消失，以及
  后续层是否仍分叉。完整质量/五档/消费者精度和性能门禁仍待完成。

## 2026-09-22：线性注意力 HTTP 诊断完成，最早差异为 qkv 投影

- decode-batch-linear-trace-http-20260922 的 24 请求、两个服务及驱动
  全部正常结束；原始请求/完整响应控制通过，三轮实际 B=2 以及
  每 slot 连续位置、输入等于上一步输出均通过，串行前后保持一致。
- 首次 B=2 的 slot 0/1 行初始 SSM 与卷积状态同串行，qkv_raw
  首次不同，之后卷积及更新状态不同；slot 1 第二步初始状态已不同。
  三轮重复。先行 B=1 与串行前后内部边界全部相同。报告 linear-review.json。
- 代码确有 M=1 手写 BF16 GEMV 与 M=2 cuBLASLt 路径切换；这还只是
  待验证原因，不用哈希推断误差大小、精度通过或全面排除状态问题。
- 下一步验证逐行算术顺序一致且复用权重的批量 BF16 投影。继续
  必要构建后首项真实 HTTP；不运行前置底层测试，不重置正式输出参考。

## 2026-09-22：HTTP 分叉缩小到线性注意力，启动内部边界定位

- 上轮层内诊断 24 请求完整结束，两个服务及驱动退出 0；实际
  B=2 三轮均首次在 attn_out 分叉，其前主干/归一化/混合输入
  相同，串行前后与先行 B=1 相同；关闭/开启日志完整输出保持前版。
- 原始状态及投影边界尚未观察，不能把差异直接归因于量化或状态。
  归档旧源码/二进制后增加显式 Q4T_DECODE_LINEAR_TRACE：仅第 0 层
  1024/1025 位置读取实际 slot 状态、投影、卷积、递推及门控摘要。
- 必要构建零警告，第一项为 decode-batch-linear-trace-http-20260922
  的 24 个真实 HTTP，PID 1868491；没有前置单测/数值测试/profile。
  二进制 SHA-256 459f86be510b44bba0bf25125c0ba9d164461a81a59536c3595903bfbb356810。
- 下一步先验完整响应控制与实际步进，再比较内部边界；本轮仅诊断，
  正式输出门禁和性能验收未关闭，固定参考不变。

## 2026-09-22：层内边界版零警告构建，首项 HTTP 启动

- 必要构建成功且零警告，二进制 SHA-256：
  677e6f61c74328610d65ee613476591dcdec93bc6ce3c6d06f4096a3fcfcc07c。
- 首项 decode-batch-stage-trace-http-20260922 已启动，PID 1864780，
  关闭/开启诊断各十二个真实 HTTP；没有前置低层测试/profile。
- 下一步先核对完整输出和实际步进，再将层内边界摘要映射到
  同位置/输入 token 的行，寻找最早差异；不作精度或性能接受。

## 2026-09-22：应用第 0 层内部边界日志

- 前一逐层 HTTP 完整终态及输出控制核对通过，无 q4t 存活后
  归档旧二进制、源码及差异，应用 decode-batch-stage-trace-20260922。
- 仅显式开启且第 0 层目标位置时，记录 PLE embedding、attention
  trunk/normed/mixed/output、combined、MLP normed/mixed/MoE
  output 与最终层输出的按行摘要。新增默认空的诊断 host 位置
  参数，不改计算公式；开启诊断增加读回同步，非性能测量。
- 必要构建进行中，首次运行仍是关闭/开启诊断的二十四个真实
  HTTP。下一步核对原输出/批次后定位首个不同边界，不预判原因。

## 2026-09-22：逐层 HTTP 收尾，首次摘要差异在第 0 层

- 二十四 HTTP 请求全部完成，两个服务及驱动退出 0。关闭/
  开启诊断完整输出均保持前版，三轮 host 步进和实际 B=2
  覆盖成立；原始数据库、host/layer 审查均已保存。
- 相同位置/输入 token 的初始 trunk 摘要相同；先行 B=1
  slot 0 位置 1024 全 48 层匹配串行。B=2 slot 0 位置 1025、
  slot 1 位置 1024/1025 均从第 0 层输出开始不同，后续全部
  48 层不同，三轮重复；串行前后逐层摘要均保持。
- 下一步在真实 HTTP 内检查第 0 层 PLE、GRRead、线性注意力、
  MoE 边界。摘要只定位差异，不作为完整数值/精度结论；
  当前候选与所有历史失败仍未接受，无底层测试或 profile。

## 2026-09-22：逐层日志版构建成功，首项 HTTP 启动

- 必要构建成功且零警告，二进制 SHA-256：
  358e938bcbfc31ed7205bad9753a0c13b77121b44917dc7586f0dfb85bad0ccb。
- 首项 decode-batch-layer-trace-http-20260922 已启动，PID 1860284，
  关闭/开启诊断的真实 HTTP 各十二请求。仅作故障定位，无
  前置数值/单测/profile，不把 host-only 版本结果冒充本版通过。
- 下一步核对完整输出和批次，再将相同位置/输入 token 的行
  映射到实际 slot，比较初始及逐层摘要；hash 相等不代替精度。

## 2026-09-22：应用早期 decode 逐层摘要补丁

- 核对前一 HTTP 完整终态、无 q4t 存活、源码及二进制摘要后，
  归档旧版并应用 decode-batch-layer-trace-20260922；仅显式
  开启时记录 1024/1025 位置的初始 trunk 及每层输出摘要。
- 必要构建进行中，首项仍为真实 tools/evalscope HTTP：关闭/
  开启诊断各十二请求，检查旧输出控制及实际 slot/位置/B。
- 诊断含 D2H/同步，只用于已有 HTTP 分叉定位；不作性能结论，
  不把摘要相等当作精度通过，不运行前置底层测试或 profile。

## 2026-09-22：准备早期 decode 逐层摘要定位

- 在 .q4t-work/prepared/decode-batch-layer-trace-20260922 保存
  未应用补丁：仅显式开启且 T<=2、逻辑位置 1024/1025 时，
  对初始 trunk 及各层完成结果按行记录摘要与 T/行/位置/token。
- 为什么：生成 token 到第 67/79 个才分叉不能证明此前内部
  状态相同，应从最早相同输入的 decode 行缩小差异出现位置。
- 尚未应用/构建/运行。该诊断开启会增加 D2H 与同步，下一步
  首项仍为真实 HTTP，必须保持完整输出且确认批次；摘要一致
  不作为精度验收，不跑数值单测或性能 profile。

## 2026-09-22：host 步进 HTTP 收尾，生成分叉定位到第 67/79 token

- 24 请求、两个服务及驱动正常结束，日志开关两组完整输出均
  保持加日志前。host-trace-review.json 和逐案例 reconstructed-
  tokens.json 保留原始日志重建、128 token 与 127 decode step。
- 三轮均为 2 次 B=1、126 次 B=2；slot 0 位置 1024 先独跑，
  随后 slot 0/1 位置 1025/1024 配对。各自位置连续，输入
  等于上一步输出，prefill 首 token 一致，无已见 host 跳步。
- slot 0/1 首次生成分叉零基索引 66/78，对应逻辑位置
  1089/1101：串行 321/6761，并发 593/494；三轮相同。
  串行前后 token ID 完全相同。token 一致不证明内部张量相同。
- 下一步在真实 HTTP 内定位更早的层间差异，不把生成分叉时刻
  当作内部差异起点；保留正式输出失败，不进入数值测试/profile。

## 2026-09-22：host 日志版零警告构建，首项 HTTP 启动

- 必要 q4t 构建成功、零警告，二进制 SHA-256：
  5ce9580d91aeb299c819db4118297587c401d7f69cf0cec0bb87f9bb2b79e03c。
- 首项 decode-batch-host-trace-http-20260922 已启动，PID 1854936。
  同一二进制关闭/开启日志各十二个 1K HTTP，核对日志添加和
  开关是否保持完整输出，再按 slot 和逻辑位置分析 token 推进。
- 下一步先检查原始请求、响应、终态及控制组；不使用日志耗时
  作性能结论，不将 token 相同当作张量精度通过。

## 2026-09-22：正式父版对照结束，应用 host 步进日志

- 正式父版 30 请求及两个服务正常结束；两档串行前后一致，
  每档三轮实际 B=2 均分叉，各 126 次成功双 token 读回。
  已测行为在父版已存在，不表示精度或状态没有问题。
- 终态核对且无 q4t 存活后，归档候选二进制/完整差异，应用
  decode-batch-host-trace-20260922 的 host-only 日志补丁，
  只记录现有 prefill argmax 与 checked readback 后的每行
  B/slot/位置/输入输出 token，不增加 CUDA 工作。
- 必要 q4t 构建进行中，下一步首项为 tools/evalscope HTTP
  关闭/开启日志各十二请求，核对输出控制及真实步进；无
  前置单测/数值/profile，插桩不用于性能或生产接受。

## 2026-09-22：正式父版 1K 实际 B=2 也分叉

- 归档正式父版的 1K 三轮并发均记录 126 次成功双 token 读回，
  每轮两个输出不同，且不保持其自身串行输出；已测 1K 行为
  并非当前候选新引入。父版 8K 及完整终态仍待收齐。
- 准备 decode-batch-host-trace-20260922 补丁，仅记录现有 host
  prefill 首 token 及 decode 的 B/行/slot/逻辑位置/输入输出
  token，不增加 CUDA 复制、同步或 profile。尚未应用/编译。
- 补丁生成初次宽泛锚点匹配到 prefill 和 fallback 两处而断言
  停止，未改运行时；改用含 TTFT 调用的唯一锚点后准备完成。
- 下一步等待 PID 1849564 收齐父版，随后据证据决定日志定位；
  插桩必须核对完整输出和实际批次，不以 token 一致代替张量精度。

## 2026-09-22：舍入候选 HTTP 全部结束，启动正式父版对照

- 候选三十请求、两个服务及驱动正常结束；串行前后保持改动前。
  1K 三轮并发输出不变，8K 三轮并发输出改变，但两档每轮
  两个输出均仍不同；实际双 token 读回各 126 次且 copy/sync 成功。
- 已收齐原始响应与 before-rounding-comparison.json，候选未
  解决全部分叉，不宣布接受或进入低层测试。
- 确认 PID 1842616 消失、终态及无 q4t 存活后启动归档正式
  父版 7364767 同配置对照，decode-batch-accepted-parent-20260922，
  PID 1849564。当前源码/二进制不变，下一步判断既有行为边界。

## 2026-09-22：舍入改变 8K 并发输出，但分叉仍存在

- 新候选 8K 前两轮并发各确认 126 次成功双 token 读回，输出
  多重集合不同于改动前，但每轮两个输出仍不同，也未保持
  串行输出。该舍入边界影响 8K 行为，却未消除已测分叉。
- 下一步收齐本轮 HTTP，之后用归档正式父版 7364767 执行
  同配置对照，区分原有行为与候选影响。驱动已准备在
  decode-batch-accepted-parent-20260922，未启动；父版 SHA
  与原已接受五档记录核对一致，无 runtime 修改或前置测试。

## 2026-09-22：通用评分舍入未改变已测 1K 分叉

- 新候选无观察器 1K/8K 串行六请求保持改动前输出。1K 观察器
  下串行也保持；前两轮实际 B=2 均记录 126 次成功双 token
  读回，仍分叉，两个完整输出的多重集合与改动前相同。
- before-rounding-comparison.json 保存逐案例比较。这是该
  独立舍入变更尚未解决 1K 分叉的负结果，不以代码精度边界
  一致替代 HTTP 行为验证；候选仍未接受。
- 下一步等待 PID 1842616 收齐剩余 1K/8K 与正常终态，再决定
  保留或回退并缩小定位。源码/二进制不变，不做底层数值/profile。

## 2026-09-22：精简 decode 当前入口，保留完整过程原文

- STATUS 的 decode 候选段由 268 行过程追加改为当前二进制、
  活跃 HTTP、前置证据及未闭合门禁。旧过程原文完整移入
  DECODE_PATH_CONSISTENCY 的存档段，失败及未启动记录不删除。
- 报告顶部同步当前通用评分舍入候选；旧 GDN float4 五档表
  标为阶段记录，避免把 SHA 2e8d2756 的结果套给 SHA 4752eb4a。
- 为什么：当前状态不应混杂相互过期的进行中表述。下一步
  继续 PID 1842616 的首项 HTTP，本次仅文档，不改运行时。

## 2026-09-22：通用评分舍入候选零警告构建，首项 HTTP 启动

- 必要构建成功、零警告，当前二进制 SHA-256：
  4752eb4a4920aa7219b91b531ed41cb28f08221718a72491af99bba00dde202a。
  构建目标虽包含历史工具，但没有执行 bench 或底层测试。
- 首项 indexer-pooled-dot-round-http-20260922 三十请求定位已
  启动，PID 1842616；仍先真实 HTTP，串行输出严格对照改动前。
- 下一步收齐无观察器串行、观察器串行/并发/串行完整结果，
  判断独立舍入变更影响；不预设通过，正式验收与旧失败仍保留。

## 2026-09-22：无观察器复验结束，应用通用评分舍入候选

- 无观察器 24 请求全部完成、服务及驱动退出 0，十个案例完整
  输出多重集合均匹配观察器版本；两档分叉不依赖观察器加载。
- 确认 PID 1838520 消失、无 q4t 存活、源码/二进制摘要一致后，
  在 indexer-pooled-dot-round-20260922 归档旧二进制与差异，
  仅将通用 pooled indexer 每 head 点积按 BF16 舍入后累加。
- 必要构建进行中；下一步首项运行真实 HTTP 定位三十请求，
  串行输出必须保持改动前，再比较实际 B=2 输出。未运行任何
  前置数值/单测/profile，不预设这一改动能消除全部分叉。

## 2026-09-22：无观察器 1K 复现分叉，准备通用评分舍入候选

- 无观察器 1K 十二请求已结束，五个案例的完整输出多重集合
  与此前观察器版本相同：串行前后相同，三个并发组均分叉。
  该分叉不依赖观察器加载，8K 仍在执行，PID 1838520 存活。
- 静态审查确认 pooled 通用 IndexerLogitsKernel 未在每 head
  点积后做 BF16 舍入，与已修 T=1/GEMM 路径不同。准备单文件
  补丁于 indexer-pooled-dot-round-20260922，只统一此边界，
  不动递推/top-k/长 OnePass；未应用、未构建、未测试。
- 下一步先结束当前 HTTP，再独立验证候选；不能以静态差异
  代替根因证据，首次运行仍用 tools/evalscope，无前置底层测试。

## 2026-09-22：两档真实 B=2 均分叉，开始无观察器对照

- 30 个 HTTP 请求全部完成，两个服务及驱动退出 0，原始数据库
  输入/输出长度、停止原因、文本摘要逐项核对。两档各三轮
  并发都记录 126 次成功双 token 读回，并产生不同输出；两档
  串行前/后均保持无观察器参考。
- 串行控制不能排除观察器对并发时序的影响。确认 PID 1833694
  消失、终态完成且无 q4t 服务后，启动无 LD_PRELOAD 的同配置
  HTTP 24 请求，decode-batch-unobserved-20260922，PID 1838520。
- 下一步比较同案例完整输出多重集合，不以无观察器并发请求
  本身断言实际 B=2；不据时间诊断性能，不推断状态污染或噪声。
  当前候选不接受，正式输出门禁与本轮分叉完整保留。

## 2026-09-22：实际 B=2 的 1K 输出与串行分叉

- 无观察器 1K/8K 六请求及服务已正常结束。1K 观察器串行前/后
  六请求保持无观察器输出；三个并发二请求组每轮均观测到
  126 次双 token 读回，copy/sync 全成功。
- 同 prompt 并发两请求输出不同，三轮分别重复相同的两个摘要；
  与串行共同字符前缀 309/380，仅用于文本差异描述，不能
  当作首次 token/logits 分叉。原始数据库核对长度 1024/128、
  finish=length，证据 one-k-divergence.json/raw-response-review.json。
- 下一步先收齐 8K，再定位实际批大小/位置/slot 与数学路径；
  不先推断状态污染或精度噪声，驱动 PID 1833694 仍在运行，
  不修改当前二进制，正式输出门禁和此次分叉均保留。

## 2026-09-22：44K 复核双侧重叠，启动 B=2 HTTP 定位

- 独立 44K 九个请求全部完成，各版输出保持自身参考，三个服务
  及驱动退出 0；候选 TTFT 30.52213–30.61802 秒、decode
  17.83736–17.85722 tok/s，与双侧父版范围重叠。先前单侧
  分离未复现，原记录保留，不更新基线、不宣称稳定提速。
- 原 PID 1827688 已消失、终态核对完成且无 q4t 存活后启动
  decode-batch-http-20260922 定位，PID 1833694，驱动及日志
  起于 .q4t-work/prepared/decode-batch-http-20260922。
- 必要 host 观察器构建后首项为 tools/evalscope HTTP，候选
  推理二进制不变。下一步核对观察器影响、真实 B=2 事件、
  串行/并发完整输出及复用；正式输出门禁仍保留，无底层测试。

## 2026-09-22：B=2 HTTP 定位驱动准备完成

- 准备的驱动复用 tools/evalscope 请求与原始响应解析，1K/8K
  各自先无观察器串行三次，再观察器下串行三次、并发二请求
  三轮、串行三次，共 30 请求，每请求输出 128。
- 有实际双 token 读回事件才记录 B=2 覆盖；观察器输出必须与
  无观察器对照比较。差异保留到独立 review，不生成正式
  acceptance.json，HTTP 完成和输出一致性分别报告。
- 尚未编译或执行，当前 44K 三侧驱动 PID 1827688 存活；
  前侧父版已正常结束，候选组继续。下一步收齐该组后再定位。

## 2026-09-22：准备真实 B=2 decode HTTP 覆盖观察

- 静态核对普通 SchedulerLoop 以 B*sizeof(int32_t) 读回 GPU
  argmax；prefill 读回词表行。设备端独立 full attention 的
  position 读回是另一路径，定位协议必须明确限定主模型普通
  decode、MTP off，不能将任意 8-byte D2H 都称作 B=2。
- .q4t-work/prepared/decode-batch-http-20260922 保存协议和
  只读观察器源码，未编译、未执行。计划用 serial/parallel-2/
  serial 同 prompt HTTP 检查路径与复用；无实际双行读回不得
  声称合批覆盖，不推断 seq_id 编号或混合上下文正确性。
- 为什么：仅有两并发请求不保证真实 B=2。下一步仍先收齐
  PID 1827688 的 44K 三侧复核，当前运行时和二进制冻结。

## 2026-09-22：TTFT 双侧重叠，44K decode 单侧差异继续复核

- 三侧十八 HTTP 全部完成，服务/驱动退出 0，各版保持自身输出。
  8K 两指标、44K TTFT 与双侧父版范围重叠。
- 44K decode 候选最大 17.856872638、后侧父版最小
  17.857021871 tok/s，单侧不利分离；前侧仍重叠。保留原始
  审查标记，额外全范围通过断言失败如实记录，不能以小差距放行。
- 已确认原 PID 1820700 消失、终态成功且无 q4t 服务后，启动
  gdn-packed-state-vector-44k-confirm-20260922，PID 1827688，
  同二进制、独立父版→候选→父版，每组 44K 三次。
- 下一步检查双侧及启动首请求差异，全部样本保留；正式输出
  门禁仍未闭合，无底层数值/profile，候选未接受。

## 2026-09-22：相邻候选两档完成，与前侧父版范围重叠

- 候选两档六个 HTTP 请求输出保持原候选，长度、停止原因与
  原始摘要核对通过，服务退出 0。8K TTFT 5.16690–5.28093
  秒，44K 30.47325–30.49295 秒；两档 TTFT/decode 均与
  前侧父版重叠，初次 TTFT 分离尚未在本侧复现。
- 下一步等待同一驱动 PID 1820700 的后侧父版完成，再检查
  双侧范围。保留首请求、旧失败和正式输出差异，不将局部
  性能重叠作为精度或生产接受，无底层数值/profile。

## 2026-09-22：TTFT 相邻对照前侧父版完成

- gdn-packed-state-vector-parent-pair 的 parent-before 两档六请求
  完整输出保持正式父版，原始长度、停止原因和摘要核对通过，
  服务退出 0；8K TTFT 5.17338–5.28111 秒，44K
  30.47341–30.52564 秒。
- 本矩阵从 8K 开始，该档首请求含服务加载后首次请求影响；
  不剔除任何样本，不与完整五档顺序混为一次测量。
- 下一步候选及后侧父版按相同顺序完成，比较两侧范围。驱动
  PID 1820700 仍存活，源码/二进制冻结，不启动额外 GPU 工作。

## 2026-09-22：定位报告补齐当前证据边界

- DECODE_PATH_CONSISTENCY 报告顶部新增当前候选二进制摘要、
  五档真实均值、各参考关系和未闭合门禁；明确下面的进行中
  描述属于当时定位阶段，避免旧状态误导后续开发。
- 为什么：质量通过、候选输出保持、正式输出失败和性能范围
  是不同证据，不能混成“E2E 已接受”。原始失败不覆盖。
- 下一步等待 PID 1820700 的相邻 HTTP 对照结束；本次仅文档，
  不触碰运行时或启动额外测试，模型和 reference 保持只读。

## 2026-09-22：状态向量访问五档结束，开始 TTFT 相邻复核

- 五档十五个原始响应核对通过，输出保持上一线程块候选，
  服务/驱动退出 0；200K TTFT 159.30507–159.77333 秒，
  decode 16.95967–17.08949、均值 17.00662 tok/s。
- 五档 decode 均高于上一候选范围，与正式父版/固定参考范围
  重叠；8K/44K TTFT 对正式父版原记录的不利分离仍待复核。
  正式旧调度输出差异保留，不更新参考、不宣布接受。
- 确认上一进程终止、服务退出且无 q4t 存活后，启动
  gdn-packed-state-vector-parent-pair-20260922，PID 1820700。
  8K/44K 各三次，父版→候选→父版；各版检查自身完整输出。
- 为什么：区分可复现的 TTFT 回退与初次范围分离。下一步收齐
  相邻原始响应及终态，决定保留或修正；不运行底层测试/profile。

## 2026-09-22：状态向量访问前四档 decode 改善，TTFT 待复核

- gdn-packed-state-vector-performance-20260922 已完成前四档十二
  个 HTTP 请求，原始响应长度、停止原因、重复输出核对通过，
  完整输出保持上一线程块候选；正式旧调度输出差异仍保留。
- decode 均值 18.74067/18.02965/18.04010/17.84325 tok/s，
  四档均高于上一候选范围，与正式父版范围重叠。
- 8K/44K TTFT 5.17697–5.18491 / 30.51535–30.54670 秒，
  对正式父版原记录不利分离。不能用 decode 改善覆盖 TTFT 风险。
- 下一步等待已确认存活的 PID 1815222 完成 200K，收齐证据后
  进行必要相邻复核；不接受候选，不执行前置数值或 profile。

## 2026-09-22：打包 GDN 状态向量访问质量通过，开始五档性能

- 原十一题质量 HTTP 全部通过，完整输出、输入摘要、答案、
  实际长度和停止原因核对符合原参考，服务退出 0。
- 二进制摘要核对后启动 gdn-packed-state-vector-performance-20260922，
  五档各三次，输出对照刚完成的线程块版，原正式参考性能及
  输出差异继续单列保留，无前置数值/单测/profile。
- 下一步完整收齐性能，尚未认定向量状态访问收益，候选不接受。

## 2026-09-22：线程块五档收尾，打包 GDN 状态向量访问首项 HTTP 启动

- 线程块候选五档十五请求输出保持标量寻址版，服务退出 0。
  200K 两指标与两组标量版重叠；候选间无不利分离不等于原
  正式基线通过，原调度输出与 decode 差距仍保留。
- 完整结束后归档该版源码/二进制，仅改 PackedDecode 且
  ROWS=4/8 的 GDN 状态加载/存储为 float4，递推计算体不变；
  非打包和 ROWS=1/2 保持原标量访问。cuda 分配及固定形状的
  head/列组/池偏移满足 16 字节对齐，无新增分配。
- 为什么：独立验证打包状态访问开销，不预设性能根因或收益。
  必要构建零警告成功，第一项质量 HTTP 已启动，证据
  gdn-packed-state-vector-quality-20260922。下一步核对原十一题，
  通过后五档性能；无前置数值/单测/profile，候选仍未接受。

## 2026-09-22：线程块候选 44K 两指标与标量版范围重叠

- 44K 三请求输出保持标量寻址版，TTFT 30.45311–30.52550
  秒、decode 17.05695–17.19249 tok/s，均与两组标量版
  范围重叠；TTFT 与已接受 7364767 也重叠。
- 四档十二请求原始响应已核对，剩余 200K 继续运行。
  相对原调度的输出与 decode 差距仍保留，不把候选间通过
  当作正式接受。下一步完整收齐最后一档，无前置底层测试。

## 2026-09-22：线程块候选前三档完成，8K TTFT 范围重叠

- 前三档九请求输出保持标量寻址版。1K/4K decode 均值
  18.05034/17.35288 tok/s，4K 三次范围高于两组标量旧版
  记录，1K/4K 均与同输出回退路径范围重叠。
- 8K TTFT 5.17136–5.17968 秒，与两组标量旧版及原接受
  7364767 范围重叠，未复现被拒向量读取版的范围回退。
- 这仍不是正式接受：原调度输出及 decode 差距保留，长档
  仍运行。下一步完整收齐 44K/200K，不执行底层测试/profile。

## 2026-09-22：短评分线程块候选质量通过，开始五档性能

- 十一题质量 HTTP 全部通过，原始响应核对答案、长度、停止
  原因、完整输出与输入摘要均符合原参考，服务退出 0。
- 二进制摘要核对后启动 indexer-short-block-performance-20260922，
  五档各三次，输出对照标量寻址版，性能单列最近旧版与原基线；
  被拒绝的向量读取已不在源码中，不以其作为通过标准。
- 下一步完整收齐性能，当前没有收益结论，不接受候选，
  不执行前置数值/单测/profile，不更新正式参考。

## 2026-09-22：短评分线程块候选构建完成，首项质量 HTTP 启动

- 必要构建零警告成功，未运行测试或 bench 目标。
- indexer-short-block-quality-20260922 已启动原十一题 HTTP，
  输入/输出参考仍为 7364767；命令、摘要及源码差异留证。
- 下一步核对质量结果，通过后完整五档性能；当前未接受候选。

## 2026-09-22：读取候选拒绝，恢复标量并调整短评分线程块

- 三侧五档 45 请求全部完成，三个服务退出 0，完整输出一致。
  后侧 200K 两指标与候选重叠，但 8K 双侧 TTFT 不利分离
  仍在，因此拒绝向量读取。原始记录和候选源码/二进制已归档。
- 正常结束后核对摘要，应用已准备的线程块候选：恢复标量
  读取，相对标量寻址版只改 launch 从 8×256 为 64×32，
  逐线程点积、舍入和评分槽映射覆盖保持原定义。
- 为什么：独立验证短可见前缀只有少量 block 承担评分的问题，
  不预设收益。下一步必要构建后首项原十一题质量 HTTP，
  通过后五档性能，不执行前置底层测试或更新正式参考。

## 2026-09-22：44K 后侧范围重叠，准备短评分线程块候选

- 后侧旧版 44K 完成、输出一致；TTFT 30.45969–30.51290 秒
  与候选重叠，44K 初次不利分离未在后侧复现；8K 双侧回退
  仍不允许接受读取候选。最后 200K 继续运行。
- 静态准备 indexer-short-block-20260922：先丢弃向量读取，
  相对标量寻址版仅改 launch 为 64 blocks×32 threads。
  总计 2048 个独立评分槽不变，无跨线程归约；1K 首步有效
  评分 block 从 1 增至 8，4K 从 4 增至 32。未实测占用率或收益。
- 补丁与前后摘要仅存准备目录，未应用/未构建。下一步收齐
  当前矩阵后实施，再按质量 HTTP→五档性能顺序验证。

## 2026-09-22：后侧旧版复现 8K TTFT 不利分离

- 后侧旧版前三档九请求完成，输出匹配候选。1K/4K 两项指标
  无不利分离，候选 decode 均值较高但范围仍有重叠。
- 8K TTFT 候选 5.17632–5.18727 秒，后侧旧版
  5.16995–5.17580 秒，前后两侧均不利分离；decode 重叠。
  差距小不构成豁免，不能用短档提升抵消，也不替换样本。
- 下一步继续完整收齐 44K/200K，然后修正或回退读取候选；
  当前不接受，不执行底层测试/profile。

## 2026-09-22：向量读取五档结束，启动归档旧版相邻复核

- 候选五档十五请求全部完成，服务退出 0，完整输出保持寻址版。
  200K TTFT 159.15763–159.68756 秒、decode
  16.33799–16.51542 tok/s，两指标均与寻址版范围重叠。
- 8K/44K TTFT 的初次不利分离继续保留；相对固定 fcb5925
  五档 decode 仍不利分离且输出不同，不能以候选间通过覆盖。
- 完整结果核对后启动 indexer-dot-vector-parent-confirm-20260922，
  用归档寻址版二进制跑完整五档，补齐旧版→候选→旧版。
  原摘要与旧版源码证据核对，当前源码/二进制不变，无并发 GPU
  任务。下一步完成相邻范围复核，候选仍不接受。

## 2026-09-22：向量读取候选 8K/44K TTFT 待相邻复核

- 四档十二请求输出保持寻址版；8K/44K TTFT
  5.17632–5.18727 / 30.50588–30.54332 秒，对寻址版
  原五档记录出现不利分离；decode 范围重叠。8K 与寻址版
  另一次重启记录重叠，但不能直接用它覆盖本次不利观察。
- 分析补入固定 fcb5925 对应 short-topk-register-20260921
  原始记录；已完成各档 decode 仍低于固定参考，完整输出
  不同，均单列保存，不以候选间改善冒充正式接受。
- 下一步完成 200K，然后使用归档旧版补相邻复核。当前不接受，
  不改源码/二进制，不执行底层测试，全部失败样本保留。

## 2026-09-22：向量读取候选前两档 decode 接近回退范围

- 1K/4K 六请求输出保持寻址版，TTFT 无不利范围分离。
  decode 均值 18.04992/17.38354 tok/s，范围
  17.86657–18.14442 / 17.19517–17.48174，已与同二进制
  回退记录重叠；原始首请求低值仍保留，不替换样本。
- 均值优于读取改动前，但尚不足以宣称稳定收益或接受候选，
  原调度输出差异及其吞吐差距不被覆盖。下一步完成剩余三档。
- 寻址与三侧路径对照的文档检查点 11d6e40 已推送 main，
  运行时代码未提交，其他任务的文档内容保持未暂存。

## 2026-09-22：评分向量读取候选质量通过，开始五档性能 HTTP

- 十一题质量 HTTP 全部通过，原始响应核对答案、实际长度、
  停止原因、输入摘要和完整输出均符合 7364767 参考，服务退出 0。
- 二进制摘要核对后启动 indexer-dot-vector-performance-20260922，
  五档各三次，输出对照寻址版，同时保留原基线和同二进制路径
  对照的性能范围；原调度输出差异继续保留，不改正式参考。
- 下一步收齐五档，判断向量读取是否改善短档；当前尚无性能
  结论，不接受候选，不执行前置底层测试。

## 2026-09-22：评分向量读取候选构建完成，首项质量 HTTP 启动

- 必要构建成功、零警告；未运行测试目标或 bench。
- 原十一题质量 HTTP 已在 indexer-dot-vector-quality-20260922
  启动，输出参考仍为 7364767；命令、摘要与源码差异留证。
- 下一步等待质量结果，通过后完整五档性能；当前未接受候选。

## 2026-09-22：同二进制三侧复现短档差异，应用评分向量读取候选

- 三侧各九个 HTTP 请求输出一致、三服务退出 0；后侧调度
  1K/4K decode 同样低于回退范围，8K 重叠。原始响应复核
  全部完成；确认同二进制重启可复现，尚未认定具体 kernel 根因。
- 三侧结束后核对源码/二进制摘要并归档，只改专用短评分
  kernel 的 q/k 读取为每次四个 BF16，逐维累加顺序、每 head
  舍入、top-k 与其他路径不变。当前唯一入口固定 hd=128，
  head/group/d 偏移满足 8 字节对齐。
- 下一步必要构建后首先跑原十一题质量 HTTP，再测五档。
  这是独立候选，不预设收益，不执行前置底层测试。

## 2026-09-22：同二进制回退侧完成，短档性能差异仍在

- 回退侧 1K/4K/8K 九请求均完成、服务退出 0，实际启动日志
  确认为回退，完整输出匹配前侧调度。1K/4K decode 范围
  18.03673–18.20607 / 17.36555–17.50058 tok/s，均优于
  前侧调度且不重叠；8K decode 范围重叠。
- 排除了仅由跨版本引起差异的解释。源码 short indexer 在
  调度/回退分别走逐维专用评分/GEMM，而 >8192 共用长分支，
  与现象相符但尚不是因果证明。未运行底层性能分析。
- 后侧重启调度已开始，继续等待完整结果；下一步优先通过
  独立 HTTP 变更验证短评分路径，不改正式参考。

## 2026-09-22：寻址版五档收尾，启动同二进制短档路径对照

- 五档十五请求完成、服务退出 0，输出保持清理版和旧回退。
  200K TTFT 159.19227–159.71833 秒，恢复到旧版范围；
  1K/4K decode 回退仍在，候选整体不接受。
- 为消除跨版本混杂，保持当前源码/二进制，使用已完成的调度
  记录作前侧，依次运行同二进制的强制回退、重启调度。
  1K/4K/8K 各三次实际 HTTP，检查输出和实际运行路径。
- 仅包装原 evalscope 驱动的长度列表，完整包装器/命令/摘要
  保存在 gdn-sequence-base-path-pair-20260922；这是短档排障，
  不替代五档验收。下一步比较各路径性能，继续禁止底层测试。

## 2026-09-22：寻址候选 44K TTFT 回到旧版范围

- 44K 三次输出保持清理版及旧回退；TTFT 30.46071–30.50481
  秒，与两侧旧回退及原调度范围重叠，低于清理版
  30.66260–30.69990 秒。decode 17.02510–17.15035 tok/s
  与两侧旧回退范围重叠，原调度输出差异仍单列保留。
- 原始响应复核已覆盖四档十二请求，200K 继续运行。已有
  1K/4K decode 回退不被长档 TTFT 改善抵消，仍不接受候选。
- 下一步完成 200K，再针对剩余短档路径差异安排 HTTP 验证；
  模板实例不同只作为源码线索，不据此宣称机器级性能根因。

## 2026-09-22：寻址候选短档 TTFT 改善，decode 回退仍在

- 前三档九个 HTTP 请求输出保持清理版。4K TTFT 均值
  2.63551 秒，与两侧旧回退范围重叠；8K 为
  5.16639–5.17282 秒，低于清理版 5.20583–5.21433，
  与原调度参考范围重叠。尚非完整五档接受。
- 1K decode 17.72100–17.88287、4K 17.03735–17.17635
  tok/s，仍低于两侧旧回退；8K decode 对旧回退范围重叠。
  寻址改动没有消除短档 decode 回退，不能用 TTFT 改善抵消。
- 下一步继续收齐 44K/200K；当前不运行底层测试/profile，
  不把源码变化推断为已证明的机器指令或寄存器占用根因。

## 2026-09-22：GDN 寻址候选质量通过，开始五档性能复验

- 原始质量 11/11 答案精确正确，完整输出/输入摘要与 7364767
  一致，实际长度和停止原因正确，服务退出 0，复核记录已保存。
- 摘要核对后启动 gdn-sequence-base-performance-20260922，
  五档各三次，输出对照清理版，性能单列清理前后与原基线，
  原调度输出差异不被覆盖。没有前置底层测试或 profile。
- 下一步收集五档结果，判断独立寻址改动是否改善回退；当前
  仅质量通过，未接受候选，不改变正式参考。

## 2026-09-22：GDN 寻址候选零警告构建，首项质量 HTTP 启动

- 必要构建成功且零警告，未运行构建出的测试或 bench。
- gdn-sequence-base-quality-20260922 保存候选摘要与启动命令，
  使用原 7364767 十一题输入和完整输出，清除实验环境后运行。
- 下一步等待实际 HTTP 结果，通过后再进行性能复验；候选不接受。

## 2026-09-22：清理版五档不接受，应用独立 GDN 寻址改动

- 五档 15 请求均完成，服务退出 0，输出匹配清理前候选和旧
  回退，原调度差异保留。200K TTFT 160.06403–160.47229 秒，
  对两侧旧回退均不利分离；清理前后全部档位两指标范围重叠。
- 诊断清理未解决回退，不接受该版本。驱动正常结束后核对
  源码/二进制摘要并归档，应用事先准备的单文件寻址候选：
  序列池偏移放到 ssm 基指针，head 索引恢复原 int。
- 为什么：把本次复用引入的公共索引变更单独验证，不假定其
  为已证实根因。下一步必要构建后首先运行原质量 HTTP，
  通过后再验证五档；不执行前置数值/单测/profile。

## 2026-09-22：清理版 44K 完成，TTFT 回退仍在

- 44K 三次完整输出保持清理前候选及旧回退，TTFT
  30.66260–30.69990 秒，高于两侧旧回退范围；decode
  17.06863–17.19740 tok/s，与两侧旧回退范围重叠。
- 清理前后 TTFT/decode 范围均重叠，没有证据表明清理解决
  性能回退。原始响应核对已覆盖四档十二请求，200K 仍运行。
- 下一步等待最后一档正常结束并留证，再验证准备好的独立
  寻址候选；当前源码与二进制未改，不运行底层测试。

## 2026-09-22：清理版前三档仍有回退，准备独立寻址候选

- 前三档九请求输出保持清理前候选。1K/4K decode 对两侧旧回退
  仍不利分离；8K TTFT 为 5.20583–5.21433 秒，两侧旧回退
  为 5.17406–5.18077 / 5.18085–5.18576 秒，仍不利分离。
  清理前后范围重叠，不能认为日志清理已解决回退。
- 源码审查发现寄存器 GDN 复用时共同 ss_base 从 int 改为
  size_t，也影响单序列路径。准备仅将池偏移置于基指针、恢复
  原 int head 索引的补丁；保持 recurrence 运算顺序，模型单
  序列状态为 48×128×128 元素，池偏移仍用 size_t。
- 补丁与前后摘要只存 gdn-sequence-base-20260922，未应用、
  未构建、无性能结论。当前五档驱动仍存活，先完成剩余长档，
  再决定下一版；禁止在运行中更换源码/二进制或插入 GPU 任务。

## 2026-09-22：诊断清理版质量 11/11 通过，五档性能复验启动

- 原始响应核对 11 题答案精确正确、输入摘要/完整输出与
  7364767 一致、实际长度与停止原因正确，服务退出 0。
- 必须完成质量和二进制摘要核对才启动性能；五档每档三次
  HTTP 继续对照清理前候选输出，并保留原调度输出门禁失败。
  原调度、清理前候选与两侧旧回退性能均单列，不改正式基线。
- 下一步收齐五档记录，确定清理是否改变回退；候选仍未接受，
  不运行数值/单测/profile。证据 decode-trace-clean-performance-20260922。

## 2026-09-22：诊断清理版构建完成，首项质量 HTTP 启动

- 必要构建成功且零警告；没有运行构建出的测试或 bench。
- 首项真实 HTTP 仍使用原 7364767 的 11 题输入和完整输出
  参考，清除 Q4T_* 与 LD_PRELOAD 环境，独立目录
  decode-trace-clean-quality-20260922 保存命令、源码和二进制摘要。
- 下一步等待质量结果，通过后再做性能 HTTP；当前不接受候选。

## 2026-09-22：三侧五档收尾，移除临时 decode 诊断代码

- 45 个 HTTP 请求全部完成，三服务退出 0，五档完整输出均
  匹配旧回退。200K TTFT 对前后父版均不利分离；短档回退
  同样保留。候选不接受，不更新原调度参考，不开始底层测试。
- 驱动正常退出后核对六文件摘要，归档旧二进制与源码差异，
  应用事先审查的日志/诊断参数清理，保留三项核心修复。
- 原因：去除定位阶段的额外复杂度，随后独立确认清理后行为；
  不预设删除日志就能修复性能。下一步零警告构建后首项质量
  HTTP，质量通过再复验五档性能，全部失败记录继续保留。

## 2026-09-22：44K 三侧对照亦复现 TTFT 回退

- 后侧旧版 44K 三请求结束，三侧输出一致；候选 TTFT
  30.6448–30.6654 秒，后侧 30.4817–30.4988 秒，与前侧
  一样不利分离。decode 对两侧均范围重叠。
- 剩余后侧 200K 继续执行；保留全部原始记录，当前版本
  不接受。下一步完整收尾后进行准备好的去插桩 HTTP 复验。

## 2026-09-22：三侧短档对照复现性能回退

- 后侧旧版 1K/4K/8K 共九请求完成，三侧完整输出一致。
  候选 1K/4K decode 与 4K/8K TTFT 对前后两侧均不利
  范围分离，8K decode 与两侧重叠。
- 这些回退不能按波动接受；raw-response-review.json 保留
  全部范围。后侧长上下文继续运行，当前二进制冻结。
- 下一步完整收尾后清理临时插桩并重新先 HTTP；不预设
  清理能消除回退，不执行底层测试/profile。

## 2026-09-22：候选五档 15 请求输出均匹配旧回退

- 候选 200K 也已完成，五档共 15 请求完整输出均匹配前侧
  旧回退，实际输入/输出长度与停止原因正确，服务退出 0。
- 后侧旧回退已实际启动，raw-response-review.json 更新；
  初次不利性能范围及原旧调度输出失败仍保留，候选未接受。
- 下一步完成后侧相邻复核再清理临时日志，运行时继续冻结，
  不进行前置底层测试或 profile。

## 2026-09-22：44K 输出匹配，准备未应用的定位日志清理补丁

- 候选 44K 三请求完整输出匹配旧回退，decode 范围重叠，
  TTFT 对前侧不利分离；200K 继续执行，后侧待运行。
- 六文件临时日志清理补丁仅保存于 .q4t-work/prepared/
  decode-trace-cleanup-20260922，尚未应用；原始文件摘要和
  变更前后内容留证，linear device 源码前缀未改变。
- 下一步完成当前相邻对照后再应用清理，并重新构建、首项
  HTTP 验证；不预设清理必然消除回退，不做前置底层测试。

## 2026-09-22：候选前三档输出一致，扩展记录初次不利范围

- 候选 1K/4K/8K 共九请求完整输出匹配旧回退，44K 正在执行。
- 前侧范围比较：1K/4K decode 与 4K/8K TTFT 不利分离；
  8K decode 范围重叠。raw-response-review.json 保存原始范围，
  后侧旧版尚未执行，不能据部分样本接受候选。
- 下一步完成剩余长上下文和后侧复核，不穿插其他 GPU 测试。

## 2026-09-22：候选 1K/4K 输出匹配旧回退，1K 速度仍不利

- 候选 1K/4K 各三次完整输出匹配旧回退，长度和停止原因
  通过；8K/44K/200K 及后侧父版尚未完成。
- 相同 1K 输出下，候选 decode 17.737–17.908 tok/s，
  前侧旧回退 18.032–18.282，不利范围分离；该观察不能
  仅由生成内容变化解释，raw-response-review.json 保留。
- 下一步完成后续对照并复核后侧范围，当前不接受候选，
  不做底层测试或 profile。

## 2026-09-22：旧回退前侧五档 15 请求完成，候选调度开始

- 1K/4K/8K/44K/200K 各三次、输出 256，全量原始响应
  长度/停止原因/重复摘要核对通过，旧回退服务退出 0。
- 同一定位驱动已启动候选调度，逐档严格比较前侧旧回退
  完整输出；后侧父版尚未开始，原旧调度门禁失败保留。
- 下一步核对候选全档及相邻范围，整体未接受，无底层测试。

## 2026-09-22：补充五档原始响应核对脚本

- 当前定位目录 review.py 仅读取已完成 HTTP 响应，逐条核对
  3 次重复、实际长度、length 停止原因、prompt 和文本摘要，
  并区分未开始/未完成/失败，不自动标记生产接受。
- raw-response-review.json 确认旧回退前四档 12 请求有效；
  200K 仍运行，候选和后侧父版未开始。
- 下一步沿用同一真实进程，完成后更新核对，不重启采样，
  没有底层测试或 profile。

## 2026-09-22：保留首档 HTTP decode 不利范围观察

- 严格门禁失败的 1K 原始数据中，候选 decode 17.778–17.924
  tok/s，旧调度 18.529–18.616，初次范围不利分离；TTFT 范围
  重叠。initial-range-review.json 留证。
- 两版输出不同，不能由此归因于某个 kernel，候选未接受；
  正在运行的旧回退五档对照保持不变。下一步结合相同输出
  路径和相邻对照核对，不用质量通过覆盖性能风险。

## 2026-09-22：核对真实多序列尚未覆盖的分支

- 当前旧回退 1K/4K/8K 三档 HTTP 完成，长上下文继续执行。
  不改变运行时源码或二进制。
- 静态覆盖表 pending-branch-coverage.json 明确非零/复用 slot、
  实际 B>1、混合上下文跨 8192 的整体评分分支，以及多序列
  prefill/MTP 的独立 GDN 实现尚待验收。
- 下一步完成正在运行的五档对照，单流结果不替代这些覆盖，
  不进行前置数值/profile。

## 2026-09-22：保存 decode 定位文档检查点

- 将已完成定位、严格质量通过和旧调度输出门禁失败保存到
  文档检查点；修正 STATUS 中 build/q4t 仍称已接受版的旧描述。
- 运行时代码仍未接受、未提交；保护其他任务的 MoE 文档改动。
  旧版回退五档 HTTP 对照继续运行，未改变当前二进制。
- 下一步核对完整旧回退与候选行为，不以文档提交表示接受修复。

## 2026-09-22：严格性能首档输出门禁失败，启动父版回退五档因果对照

- 严格性能在 1K 因旧调度输出不同退出；prompt 未变、
  三次稳定、长度正确、服务退出 0，后四档未运行。
  failure-review.json 留证，不以质量通过覆盖失败。
- 已启动旧版回退→候选调度→旧版回退的独立五档 HTTP
  对照，固定 256 输出、每档三次；旧二进制摘要已核对，
  候选严格比较旧回退输出，不改旧调度基线。证据
  decode-path-parent-fallback-matrix-20260922。
- 下一步核对全上下文行为；该实验是失败定位，不是接受
  替代，无底层数值测试或 profile。

## 2026-09-22：严格质量 11/11 通过，启动原五档输出对照门禁

- 11 题答案、实际长度、stop 原因均通过，prompt/完整输出
  与 7364767 相同，服务退出 0，quality-review.json 留证。
- 同二进制启动原五档严格输出对照，证据
  decode-rope-indexer-round-performance-20260922。
- 下一步保存全部失败/成功记录；质量通过不覆盖性能或
  生成变化门禁，无前置数值/profile，候选尚未接受。

## 2026-09-22：合并 decode 定位状态，明确候选未完成范围

- STATUS 将多轮过时的“正在运行”观察合并成三个已验证边界：
  RoPE 坐标、GDN recurrence、indexer 点积舍入；保留原实验和日志。
- 当前严格质量 HTTP 仍运行，原输出参考不变；列明五档、
  多序列/消费者、精度及去插桩复验尚未完成，不误报生产接受。
- 下一步等真实质量进程完成后核对全部响应，无前置数值测试。

## 2026-09-22：8K 舍入三侧对照完成，严格质量 HTTP 启动

- 点积 BF16 舍入候选三侧九请求输出一致，重启日志一致，
  三服务退出 0；decode-rope-indexer-dot-round-20260921/
  complete-review.json 留证。评分摘要仍不全等，不宣称完整精度接受。
- 严格质量依赖条件已满足，decode-rope-indexer-round-quality-20260921
  正在运行原 11 题真实 HTTP，对照 7364767，关闭所有定位环境。
- 下一步核对完整质量结果再推进所需五档/消费者 HTTP；
  没有前置数值或 profile，候选未提交、未接受。
