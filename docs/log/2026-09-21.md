# 2026-09-21 — 开发日志

## 2026-09-21：舍入候选前六请求一致，安排无插桩严格质量 HTTP

- 调度/新回退各三次 8K 输出均保持旧默认回退；重启仍运行。
- 严格质量依赖驱动须等前轮真实进程结束、路径/重启通过、
  二进制不变才运行 11 题；清除日志/实验环境，仍对照
  7364767 原质量输出。证据 decode-rope-indexer-round-quality-20260921。
- 下一步核对重启与质量结果，未运行五档和多序列，
  不以该单形状结果宣称精度或性能接受。

## 2026-09-21：点积舍入后首组 8K 输出及主干边界匹配旧回退

- 调度三次 8K HTTP 完成，完整输出、首步 layer/stage/linear
  摘要均匹配旧回退；首个 full attention 的位置序列对齐，
  评分摘要仍不同。partial-review.json 留证。
- 候选回退/重启继续执行，下一步收尾并核对剩余评分差别；
  该单因素定位不是完整多形状精度和性能接受，无前置数值测试。

## 2026-09-21：启动 indexer 每 head 点积 BF16 舍入的单因素 HTTP 实验

- full attention 内部九请求完成，输出未变、重复/重启
  一致、服务退出 0，complete-review.json 留证。
- 专用 T=1 评分仅增加每 head 点积的 BF16 舍入，保持
  缩放和 top-k 过程。必要构建后首项 8K 三侧 HTTP 已启动，
  证据 decode-rope-indexer-dot-round-20260921。
- 下一步看评分/位置序列及文本是否对齐；当前是因果定位，
  不是完整批量实现或生产精度方案，无前置数值测试。

## 2026-09-21：8K full attention 评分与所选位置序列出现差异

- 前六请求输出不变、日志重复稳定；首个 full attention 的
  query/gate/index query/压缩 key 相同，scores 和所选位置
  序列摘要不同，first-fa-boundary.json 留证。重启仍运行。
- 静态差别为 GEMM 每 head 点积写 BF16 后归约，专用 decode
  保留 FP32 点积；下一步用 HTTP 验证其影响，不先宣称
  选中集合不同或正确精度路径，无前置数值测试。

## 2026-09-21：8K 分叉边界复核结束，启动 full attention 内部 HTTP 日志

- 8K 九请求逐层对照完成，输出不变，重复/重启稳定，
  三服务退出 0；首个不同为 decode 第 3 层 attention 输出。
- 增加默认关闭 Q4T_FA_TRACE_POSITION，记录实际 HTTP 内
  query/gate、索引 query/有效压缩 key、分数和有效 top-k 摘要。
  构建零警告后首项 HTTP 已启动，证据
  decode-rope-full-attention-trace-20260921。
- 下一步验证评分/选择边界；未进行数值测试或性能分析，
  质量门禁未执行，候选未接受。

## 2026-09-21：8K 首步分叉定位至第 3 层 full attention 输出

- 前六个 8K 边界 HTTP 输出未受插桩影响；prefill 全层
  摘要一致，decode 前三层一致，第 3 层 attention 输入
  一致而输出不同。first-boundary.json 保存边界，重启仍运行。
- 下一步检查 full attention 索引打分/选择的实际 HTTP
  边界；当前并未闭合精度或性能，无前置数值测试。

## 2026-09-21：27 请求定位完成，8K 分叉重启可复现

- 三类三侧共 27 请求完成、三服务退出 0；1K/单图路径
  一致，8K 分叉重启仍复现，complete-review.json 记录未接受。
- 质量依赖驱动按断言退出，未创建 quality 目录；
  not-started.json 区分未执行与质量失败。8K 边界 HTTP
  已开始，下一步查首个分叉层，不进行前置数值测试。

## 2026-09-21：新回退 8K 保持旧输出，继续定位调度分叉

- 新回退 8K 输出保持旧版，调度仍不同；质量依赖条件不满足，
  不绕过门禁。三侧重启仍执行，无数值或性能细分析。
- 已排队同二进制 8K prefill/首步边界日志，等待现有真实
  进程结束。静态线索为 full attention 短路径两种 indexer
  打分实现；下一步以 HTTP 日志定位，暂不认定根因。

## 2026-09-21：扩展 8K 首组输出与旧回退不同

- 新调度 1K/单图匹配旧回退，但 8K 不匹配，记录
  prior-fallback-partial.json，不据新路径一致性宣称精度保持。
- 新回退与重启仍运行；下一步检查差异起点并保留失败证据，
  质量及完整性能尚未通过，候选未接受。

## 2026-09-21：整理 decode 路径因果证据及未完成验收边界

- 新增 DECODE_PATH_CONSISTENCY_2026-09-21.md，分开记录 RoPE
  补写与 GDN 计算路径差异，列出九轮实际 HTTP 的范围和结论。
- 当前文本对照仍执行，首组 1K 调度输出保持原回退输出；
  质量门禁依赖完整三侧对照，不更新参考、不做前置数值测试。
- 下一步核对文本与质量结果，报告明确多序列、五档性能、
  参考精度以及最终去插桩复验尚未完成，候选未接受。

## 2026-09-21：默认寄存器单图重启通过，扩展文本并安排严格质量门禁

- 默认寄存器九个单图 HTTP 全部保持旧回退输出，三侧边界
  日志一致、服务退出 0，complete-review.json 留证。
- 同一二进制启动 1K/8K/单图共 27 请求路径对照；依赖驱动
  等其真实进程结束且输出/重启比较全通过后，才运行 11 题
  标准质量 HTTP，严格对照 7364767 原输出。
- 证据 decode-rope-gdn-register-text/quality-20260921；
  下一步核对质量，再处理五档输出变化和多序列覆盖，
  不更新输出参考，不以路径相等替代精度验收。

## 2026-09-21：默认寄存器复用候选单图调度与回退一致

- 前两组六个真实 HTTP 输出一致，均保持此前默认回退文本；
  首步 linear/stage/layer 摘要也全部相同，partial-review.json 留证。
- 重启组继续运行；下一步扩展文本与真实 B>1 HTTP，不能以
  当前单图 B=1 样本代替精度、并发或完整性能验收。

## 2026-09-21：共享顺序对照闭合，默认寄存器批量 decode 候选启动

- 共享顺序九请求输出及 linear/stage/layer 日志全部一致，
  重启确认、三服务退出 0；complete-review.json 留证。
- 批量 decode 复用现有寄存器 recurrence，新增 grid.z token
  和真实 seq_id 池状态选择，同一预归一化与算术体；
  默认配置必要构建后首项 HTTP 已启动，证据
  decode-rope-gdn-register-multi-20260921。
- 下一步核对默认路径一致性，再扩展文本与真实多序列 HTTP；
  暂不接受精度/性能变化，没有前置数值测试。

## 2026-09-21：共享计算顺序下调度与回退 HTTP 输出首次一致

- Q4T_GDN_REG=0 且批量复用共享归约/更新函数的前六请求
  输出一致，去除 packed 标记后 linear/stage/layer 日志全相同。
  双方各三次稳定，partial-review.json 留证，重启仍运行。
- 这支持当前样本的分叉来自 recurrence 计算实现差别；
  下一步收尾重启并处理默认寄存器路径，不能将受控开关
  视为生产精度或性能方案，完整门禁仍未运行。

## 2026-09-21：启动 GDN 共享归约顺序的受控 HTTP 对照

- linear 九请求结束，全部输出与前轮一致、摘要重复/重启
  稳定、三个服务退出 0；complete-review.json 留证。
- 批量 decode norm/点积/更新复用现有单序列共享函数，
  两侧均 Q4T_GDN_REG=0，必要构建后首项三侧 HTTP 已启动。
  证据 decode-rope-gdn-shared-order-20260921，修改前源文件保留。
- 目的为验证不同归约/运算顺序的影响，非生产精度选择；
  下一步核对首个差异与完整输出，不能凭数学同式接受变化。

## 2026-09-21：HTTP 状态与投影一致，首个差异缩小至 GDN recurrence

- 已完成六个 HTTP，输出与前轮一致，三次重复摘要稳定；
  第 0 层初始 SSM/conv、四项投影、卷积输出摘要相同，
  SSM 输出与更新后状态首次不同。first-linear-boundary.json 留证。
- 重启组继续执行；下一步核对两条 GDN 实现及精度语义，
  暂不能判定正确路径，未运行独立数值测试或接受候选。

## 2026-09-21：层内对照完成，补记 linear attention 状态与计算边界

- 上轮九请求、三服务退出及输出无插桩影响核对完成，摘要
  重复/重启稳定，首次差异保持在第 0 层 attention 输出。
- 同一 HTTP 首步追加 linear attention 初始状态、投影、卷积
  与 SSM 边界日志，按实际 seq_id 读取状态；默认关闭。
  必要构建后首项 HTTP 已启动，证据 decode-rope-linear-trace-20260921。
- 下一步区分状态输入和计算路径差异，修复未接受；无前置数值测试。

## 2026-09-21：层内 HTTP 日志定位 attention 输出边界

- 调度/回退六请求完成，输出未受阶段插桩影响，重复摘要稳定。
  第 0 层 PLE 后及 attention 输入相同，attention 输出首次不同。
- 局部证据 first-stage-boundary.json，重启组继续运行；下一步
  核对完整对照，检查 linear attention 内部及持久状态起点，
  暂不判断正确路径或接受精度变化。

## 2026-09-21：逐层 HTTP 对照收尾，启动层内阶段日志定位

- 逐层日志九请求完成，输出与无日志候选一致，三服务退出 0；
  prefill 全层及 decode 输入摘要一致，第 0 层输出首次不同，
  重复和重启对照稳定。完整审查写入 complete-review.json。
- 为缩小内部边界，增加默认关闭阶段摘要，在实际 HTTP 的
  指定首步记录 PLE、attention、MLP 边界。必要构建后首项
  三侧 HTTP 已启动，证据 decode-rope-stage-trace-20260921。
- 下一步核对日志对输出无影响并查明首次不同阶段；修复未接受，
  不将诊断插桩用于性能结论，无前置数值测试。

## 2026-09-21：HTTP 逐层日志定位首步 decode 第 0 层差异

- 单图调度三次输出与无逐层日志候选一致，回退组亦已完成。
  prefill 的输入及 48 层输出摘要一致；首步 decode 输入
  摘要一致，第 0 层输出开始不同，first-layer-boundary.json
  记录当前边界。哈希对照不是完整张量精度证明。
- 调度重启仍在运行；下一步核对全部插桩输出并定位第 0 层
  内部阶段，运行时修复仍未接受，不将 RoPE 缺口当作全部根因。

## 2026-09-21：共享 GDN 反证保留，启动 HTTP 内逐层摘要定位

- Q4T_GDN_REG=0 单图九请求完成，调度重启一致、路径内部
  稳定，但首步 logits 和完整输出仍不同；Q/K 中间 BF16
  精度差异不是充分解释，原实验留证，不作为生产精度选择。
- 新默认关闭 Q4T_LAYER_TRACE_POSITION，仅记录指定请求的
  prefill 和首步 decode 输入/逐层 trunk 摘要。默认 GDN、
  单图三侧 HTTP 在必要构建后启动，证据
  decode-rope-layer-trace-20260921；下一步确认插桩输出保持
  并定位首层差异，修复未接受，无独立数值/profile。

## 2026-09-21：完整 decode HTTP 日志审查结束

- 27 输出与无日志候选完全一致；按每请求实际生成长度分割
  decode 日志，位置连续，三次重复及调度重启日志一致。GPU
  选择与 host top1 全部相同，排除本组请求的 argmax 选择差异。
- 1K/8K/单图 logits 摘要均从首步不同，首个 token 分叉分别
  在第 48/27/14 步，共同输入 token/位置/历史均相同；证据
  trace-review.json。共享 GDN 单图 HTTP 正在运行，首组调度
  重复稳定，待完整三侧结果，不宣称根因已闭合。

## 2026-09-21：视觉首步差异确认，排队共享 GDN 路径 HTTP 对照

- 视觉 pos=84、input=1919、history=248069,271 相同，首步
  top1 都为 2099，但值 23.75/23.5，进一步聚焦 decode。
- 静态发现单序列默认 register GDN 的 Q/K 预归一化写回 BF16，
  批量 decode 直接用 float 归一化。现有 Q4T_GDN_REG=0 开关
  可做更接近路径的 HTTP 对照；decode-rope-shared-gdn-20260921
  已排队，原日志实验结束后运行单图三侧三次，无运行时代码修改。
  开关也影响 prefill，不据此单独认定根因或生产精度方案。

## 2026-09-21：1K 请求日志确认首个 decode 已有分布差异

- 三类首组调度输出与无日志候选完全一致。1K 首步 pos=1024、
  input=271、PLE history=15666,25 相同；top1 同为 248068，
  logits 值分别 22.75/24.5，摘要不同。直到 pos=1071 才首次
  选不同 token（21765/5170），此前输入前缀相同。
- first-1k-difference.json 保留证据，不能用分叉点 near-tie
  直接解释首步差异。HTTP 日志仍在运行；下一步结合视觉同
  prefill 路径证据继续定位内部差异，不跑前置数值/profile。

## 2026-09-21：启动请求内 decode 分歧日志定位

- 增加默认关闭的 Q4T_DECODE_TRACE，调度路径在已有同步后
  读取 logits，回退复用 host logits；记录 token、位置、PLE
  历史、摘要、前四名及实际选择，用于首个分歧定位。
- decode-rope-trace-20260921 必要构建后首项仍为同组真实
  evalscope HTTP，不跑数值微测试或 profile。下一步确认
  插桩输出与无日志候选一致，再比较共同输入下的第一处分歧；
  插桩时间不计性能，候选仍未接受。

## 2026-09-21：最小 RoPE 候选 HTTP 完成，路径差异仍未闭合

- 零警告构建后的首项 27 请求完成、三个服务正常退出，
  各路径内部稳定，调度跨重启一致；三类仍不同于回退，回退
  保持旧版输出不变。候选不接受，不用静态缺口解释全部差异。
- 检查 token/position/PLE history 构造和 CPU/GPU argmax
  tie-break，尚未发现足以闭合差异的第二处错误。保存首个
  不同 SSE 内容块及文本前缀至 path-difference-review.json，
  下一步在真实 HTTP 内记录首个分歧，完整验收和数值未运行。

## 2026-09-21：decode 路径差异跨重启复现，开始最小 RoPE 修复

- 旧版 HTTP 定位 27 请求完成，三类请求各路径内部确定，
  调度重启输出一致，均不同于单序列回退；旧结果完整保留。
- 仅修改 ModelDecodeBatchMulti，按实际 seq_id 在当前位置
  写三行 position+rope_delta，host 数组保持至 forward 完成；
  必要构建后首项运行同组 HTTP，证据 decode-rope-fix-20260921。
  下一步验证因果关系，完整验收尚未运行，未接受、未跑底层测试。

## 2026-09-21：容量阶段已推送，启动 decode 位置表 HTTP 定位

- 7364767 已推送 main，其他任务未提交文件保留。静态核对
  发现单序列 decode 写三行 RoPE 坐标，调度批量 decode 未写；
  pooled 表实际传入 full attention，影响待真实 HTTP 定位。
- 同已接受二进制运行调度→回退→调度重启，1K/8K/单图各
  三次、输出上限 128、MTP 关闭，证据 decode-rope-diagnostic-20260921。
  运行时未改，无前置数值测试。下一步审查输出确定性和路径差异。

## 2026-09-21：serve logits 容量优化最终验收接受

- S=2/4 实际两个 logits 分配各为 993280/1986560 字节，
  CUDA 区间相等、HTTP 输出匹配，两个服务正常退出。最终
  HTTP、文本/视觉精度、64 配置预算、八份时间线与源码/库/
  二进制摘要复核通过，final-audit.json 接受，固定参考不变。
- 接受共享 prefill logits 按 max_seq 行分配及所有消费者
  显式末行策略；S=1 目标分配减少约 3.789 GiB，不称整机峰值
  下降，不宣称 decode 提速。完整重写容量报告，旧失败记录保留。
- 下一步阶段提交推送，随后按 HTTP 先行继续现有 API 模板与
  decode 位置表行为审查，不将本阶段接受当作整体目标完成。

## 2026-09-21：容量候选八份消费者时间线结构核对通过

- 内联、单图、视频、MTP 父候选八份真实 HTTP 输出匹配，
  六次服务正常退出。图像/视频仅 head 六 kernel 从 84 行
  改为单行；MTP 同内联，仅首块 head 删除、末块 head 单行化。
  其余各 stream kernel 名称/grid/block/顺序完全一致，
  structure-review.json 留证，不由此推断指针/标量参数相等。
- max_seq=2/4 分配观察已在采集结束后启动，配实际 HTTP
  输出检查。下一步核对分配、最终审查与阶段提交。

## 2026-09-21：容量候选内联时间线结构核对完成

- 最初局部 kernel 对齐被相邻 forward 同名 mixer 干扰，保存
  原 diff；改用 EmbedLookup forward 边界分段后，只有两处
  差异：首块六个 head kernel 删除，末块六个改为单行。
  其余四个 stream 的 kernel 名称/grid/block/顺序保持一致。
- 视觉父候选捕获已完成，MTP 正在运行。下一步核对剩余
  时间线与并发容量分配，候选仍未最终接受。

## 2026-09-21：容量候选 S=1 实际 logits 分配缩减确认

- 内联 44K 父候选 HTTP 输出匹配、时间线导出、服务正常退出。
  两侧分配观察命中 ChatServer::Start，cudaMalloc 成功且
  cuMemGetAddressRange 成功，大小 4068474880→496640 字节，
  差额 4067978240 字节；这是该分配地址区间，不是整机峰值。
- allocation-s1-review.json 留证；视觉/MTP 时间线仍在运行，
  下一步核对全部执行结构和并发容量分配，候选尚未最终接受。

## 2026-09-21：容量候选开始实际分配与消费者 HTTP 时间线

- 门禁后新增分配观察器，记录指定 logits 分配请求、CUDA
  状态、cuMemGetAddressRange 返回大小和调用位置；回退场景
  仍合成一次 scheduler 分配失败，确认实际走内联长 prefill。
- 同父候选分别捕获回退 44K、单图、视频、MTP 44K 真实
  evalscope HTTP，逐请求核对已接受输出后导出 SQLite；
  consumer-timelines 已启动，记录只用于分配/执行结构审查，
  不替代无插桩性能数据。下一步审查八份时间线及分配记录。

## 2026-09-21：容量候选视觉精度与预算算术完成

- 真实单图/双图/视频完整主模型对照：3317760 个 trunk BF16
  值、MRoPE 表/偏移和固定续算逐位一致，canary 通过。三组
  末行 mixed 全匹配 FP64 参考，最终 head 相对误差均低于全行，
  argmax 与参考一致；接受固定样本，非广泛视觉质量保证。
- 实际父/候选预算源码在 MTP 开关、prefill 1024/8192、
  max_seq 1/2/4/8、固定/自动长度共 64 配置核对通过，自动
  长度符合独立预算公式，零警告。尚未证明旧 workspace 估计
  全面准确，也不等于设备驻留或峰值实测。下一步实际分配及
  内联/视觉/MTP 时间线，候选未最终接受。

## 2026-09-21：文本 head 精度参考完成，启动真实视觉数值对照

- CPU FP64 参考三组完成，末行 mixed 全匹配参考，最终 head
  l2_rel 约 0.0000158/0.00000895/0.0000101，均低于旧全行
  0.000356/0.000116/0.000358，argmax 与参考一致；结合完整
  trunk 和续算，接受这三组文本样本，非全输入精度保证。
- 新视觉工具使用同 HTTP 图片与显式模板，实际 processor、
  视觉塔、多模态展开和完整主模型，比较 trunk/MRoPE/canary/
  固定续算，保存 head 中间值。vision-numerical 已启动，
  下一步审查该结果及容量、时间线，运行时尚未最终接受。

## 2026-09-21：容量候选文本结构对照通过，开始 head 精度参考

- 数值工具零警告构建，1K/8K/44K 共 555745280 个完整 trunk
  BF16 值逐位一致，三组固定 token 后继输出逐位一致，canary
  与序列状态检查通过。三组 logits 有 4016/228/4730 个不同值，
  最大 l2_rel=0.000358、max_abs=0.03125，argmax 均为 271。
- 不由相同 argmax 推导精度通过；CPU FP64 参考工具增加显式
  用例数参数（旧默认 10 不变），已启动三组实际 head 中间值
  对照。下一步审查参考并完成视觉数值、容量和时间线。

## 2026-09-21：容量候选开始门禁后的文本数值对照

- 新工具复现 1K/8K 单块及 44K 六块的旧首末块全行 head 与
  新仅末块末行策略，逐块完整 trunk、canary、固定 token 续算
  对照，并保存实际 head 中间值供精度分析，不设置临时误差阈值。
- 启动前重新验证所有消费者 HTTP 与二进制/源码/库摘要；
  text-numerical 当前正在构建/执行，尚无通过结论。下一步
  审查原始数值，再完成视觉消费者和容量/时间线证据。

## 2026-09-21：容量候选全部消费者 HTTP 门禁完成

- MTP 8K 复核 9 请求输出/路径通过、三个服务退出 0，候选
  decode 27.5777–27.6777 tok/s，两侧父版为
  27.5204–27.5404 / 27.4134–27.5086；初始不利分离未重现，
  不将本次有利分离宣称提速，保留初始失败和联合判断。
- 原始数据库逐组成功数、生命周期四项、源码/二进制/库摘要
  已核对，记录 consumer-http-audit.json。下一步验证消费者
  数值精度、实际分配/预算与受影响路径时间线，尚未最终接受。

## 2026-09-21：容量候选 MTP 输出通过，8K 性能需复核

- MTP 三档三侧 27 请求输出与路径通过，三个服务退出 0。
  8K decode 候选 27.562095–27.595554，前侧父版
  27.519754–27.606396，后侧 27.595954–27.798939 tok/s；
  与后侧不利分离，原始 review 保持未通过，不以差额小豁免。
- HTTP 工具增加受影响文本档位过滤，默认完整覆盖不变；
  启动 consumers-mtp-confirm-8k 三侧复核，保留全部原请求。
  下一步联合判定，容量数值/profile 仍未运行。

## 2026-09-21：容量候选视觉消费者 HTTP 通过

- 原始单图停止、显式模板单图/双图/视频，三侧各三次共 36
  请求输出一致；TTFT/decode 无不利范围分离，三个服务退出 0。
  原始立即停止用例不计性能，显式模板不证明标准模板接入已修复。
- consumers-vision/review.json 与驱动退出记录通过，顺序流水线
  已进入 MTP 三档 HTTP。下一步完成该消费者后联合核对完整
  HTTP 门禁，再开展容量与精度验证；当前运行时仍未最终接受。

## 2026-09-21：容量候选回退相邻复核通过，推进视觉 HTTP

- 同配置复核 18 请求输出一致，三个服务正常退出。44K decode
  候选 17.0389–17.0816 tok/s，两侧父版 17.0108–17.0900 /
  17.0383–17.0586，范围均重叠；首次单侧不利分离未重现。
- 原始失败不替换，联合结论记录 fallback-joint-review.json，
  不宣称 decode 提速或整项接受。视觉 HTTP 已启动，之后 MTP；
  所有消费者 HTTP 完成前仍不运行容量数值或 profile。

## 2026-09-21：容量候选回退 HTTP 性能未通过，启动相邻复核

- 回退 18 请求输出全部一致；44K decode 候选 17.0262–17.0537
  tok/s，前侧父版 17.0812–17.0901，后侧 17.0381–17.0830。
  对前侧不利分离，对后侧重叠；原始失败完整保留，未接受。
- 因两侧对照结论不一致，启动一次同配置父版→候选→父版
  HTTP 复核，证据 consumers-fallback-confirm；不替换原样本。
  下一步联合审查两组结果，视觉/MTP 和数值/profile 尚未运行。

## 2026-09-21：修复后容量候选内联 HTTP 通过

- 内联 1K/8K 三侧各三次共 18 请求，输出一致，无不利
  性能范围分离。8K TTFT 候选 5.146–5.172 秒，两侧父版
  为 5.248–5.262、5.249–5.269 秒。
- 已进入调度器不可用的回退 HTTP，随后视觉与 MTP；
  全部 HTTP 通过前不运行数值/profile，候选尚未最终接受。

## 2026-09-21：修复后容量候选四项生命周期通过

- 轮转复用、停机、复制失败与同步失败四项 HTTP 全通过，
  complete.json 记录当前候选摘要，合成返回错误不等于真实
  硬件故障恢复。
- 内联消费者父版→候选→父版对照已启动，后续调度器回退、
  视觉与 MTP；全部 HTTP 前不运行数值/profile，尚未最终接受。

## 2026-09-21：修复后容量候选交错 HTTP 九轮通过

- 父版→候选→父版共九轮 18 请求，输出对照通过，候选短
  请求均先于长请求首响应，总完成时间无不利范围分离。
- 四项生命周期 HTTP 已启动，后续内联、回退、视觉及 MTP
  仍待完成。未运行数值/profile，不把固定交错场景推广为
  任意并发性能保证，容量候选仍未最终接受。

## 2026-09-21：修复后容量候选真实 B=2 九轮通过

- 父版→候选→父版共九轮 27 请求，全部确认真实 B=2，输出
  一致；TTFT 与总完成时间均和两侧父版范围重叠，按持平处理。
- 候选总完成时间 35.645–35.952 秒，两侧父版为
  35.603–35.937、35.691–35.996 秒。串行进入交错 HTTP，
  之后生命周期与各消费者；未运行数值/profile，尚未最终接受。

## 2026-09-21：修复后容量候选完整五档通过

- 五档 HTTP/输出 15/15、服务退出 0，直接父版 00bddff 与
  固定参考 fcb5925 均无不利范围分离，冻结源码/二进制/库一致。
- 真实 B=2 对照已启动；后续交错、生命周期、内联/回退、
  视觉与 MTP HTTP 仍待完成。未运行数值/profile，候选不据
  标准单流通过直接接受，旧异常和旧候选证据完整保留。

## 2026-09-21：修复后容量候选质量 HTTP 通过

- 新基线首项质量 11/11、输出对照通过、服务退出 0，未运行
  任何前置测试。完整五档性能已串行启动。
- 下一步双参考范围检查，以及真实组批、交错/生命周期和各
  消费者 HTTP；新候选仍未接受，旧候选记录不替代本轮门禁。

## 2026-09-21：修复后容量候选首项质量 HTTP 启动

- 新候选零警告构建，冻结源码、二进制、静态库摘要，首项
  EvalScope 质量 HTTP 已启动，再串行完整五档。
- 双参考范围通过后才运行真实 B=2、交错、生命周期、内联、
  回退、视觉、MTP HTTP；失败停止下游并保留记录，没有
  自动数值/profile。直接父版改为已接受视觉修复，固定参考不变。

## 2026-09-21：00bddff 推送后恢复容量候选

- 视觉修复 00bddff 已提交推送 main。确认无遗留测试进程，
  当前二进制等于已验收版本，三个容量相关文件等于 HEAD 后，
  恢复保存的补丁；视觉 barrier 保持，其他任务内容不改。
- 新证据目录 serve-logits-capacity-v2-20260921 保存父版
  二进制、候选源码与补丁，正在构建。下一步第一项真实质量
  HTTP，再五档与消费者；不沿用旧候选通过记录。

## 2026-09-21：视觉共享分数竞态修复最终接受

- 完整塔对照通过原误差条件：新增大图像/视频相对 L2 为
  0.039954/0.028093，最大绝对差 0.028735/0.011400；
  三次原始 BF16 输出逐位一致，旧小形状参考也通过。
- 最终冻结源码/库/二进制、原始质量 11/性能 15/视觉 48 条
  HTTP、解析 23224320 值及完整塔证据核对通过，接受修复。
  首次数值工具链接失败保留，不宣称性能提速或全面视觉质量。
- 下一步提交本独立修复，再在新基线上恢复容量候选并完整重验；
  其他任务文件及暂存的容量工具改动保持，不混入运行时。

## 2026-09-21：视觉解析数值通过，修正完整塔工具链接

- 实际 AttentionKernel 六种形状各五次，23224320 个 BF16 值
  对解析均匀注意力参考逐位一致，输出 canary 通过，构建零警告。
- 完整塔 CPU 参考生成完成，但工具误用 libq4t_vision.a 链接
  失败；CMake 实际把 vision.cu 纳入 model 库。初轮构建日志
  全保留，修正工具链接列表，在 tower-numerical-v2 新目录
  重新执行。不是运行时回归，候选二进制及源码保持冻结。
- 下一步审查完整塔原误差标准及大空间组重复输出；未最终接受。

## 2026-09-21：视觉 HTTP 四组通过，启动门禁后数值

- 四组 48 请求完成：两侧父版单图/双图/视频均非确定，候选
  连续三次与服务重启后三次均一致；原始停止请求单独保留。
  候选两组对两侧父版均无不利性能范围分离，四服务退出 0。
- 全部 HTTP 门禁通过后，才启动实际 AttentionKernel 解析
  数值工具构建/运行，随后扩展 NumPy 完整视觉塔参考。
  当前只有确定性和 HTTP 证据，不把它们替代精度证明；
  数值尚未出结果，修复仍未最终接受。

## 2026-09-21：视觉 barrier 完整五档通过，进入视觉 HTTP

- 完整五档 HTTP/输出 15/15，服务退出 0，直接父版及固定
  参考均无不利范围分离。TTFT 均值 0.799/2.632/5.179/
  30.514/159.485 秒，decode 18.548/17.857/18.075/17.853/
  17.083 tok/s；按正常文本性能持平理解，不宣称视觉提速。
- 精确二进制/源码/库摘要复核通过，视觉 HTTP 已串行启动；
  候选重复生成、重启、数值参考仍待验证，尚未最终接受。

## 2026-09-21：准备扩大空间组的完整视觉塔参考

- 新工具 verify_vision_tower.py 复用既有 NumPy 完整塔实现与
  vision_forward_test 原误差条件；保留 4×4 参考，增加 16×16
  图像、两个 8×8 时间组的视频，保存大形状三次原始 BF16 输出。
- 参数化旧测试的参考/输出路径，产物仅进入 build/.q4t-work，
  缺模型/设备/参考直接失败，不把 skip 计作通过。共享门禁函数
  要求完整质量/五档及视觉 HTTP；本轮只编写，未运行数值、
  CPU 参考生成或额外 GPU 工作。200K HTTP 仍在运行。

## 2026-09-21：明确旧视觉参考的跨 warp 覆盖缺口

- 只读检查 vision_ref.json：image=[1,4,4]，video=[2,4,4]，
  每时间组只有 16 个分数，覆写者位于同一 warp，不能充分覆盖
  本轮跨 warp 竞态。未运行旧测试或数值/profile。
- 新增在验报告，要求 HTTP 后完整视觉塔参考包含更大空间组，
  保留旧用例，不扩大精度容忍值。五档 HTTP 仍在运行；
  后续不能只凭解析均匀注意力和文本确定性宣称完整精度通过。

## 2026-09-21：准备视觉 attention 精确解析对照

- 新增门禁后工具，直接包含实际 vision.cu 的 AttentionKernel，
  Q/K=0、S=64/256/1024、时间组=1/2、整数 V，构造可精确
  表示的均匀注意力参考；覆盖五次重复、分组隔离与输出 canary。
- 构建/执行入口检查完整质量/五档与视觉 HTTP、候选二进制、
  源码/库摘要。当前仅编写工具，未构建或运行任何数值测试。
  解析用例不替代完整视觉塔参考；五档 HTTP 仍在运行。

## 2026-09-21：视觉 barrier 候选质量 HTTP 通过

- 首项质量 11/11、输出对照通过，服务退出 0，无前置专项。
  驱动已进入完整五档性能；正常文本质量结果不代替视觉确定性
  与精度证据，未接受候选。
- 下一步双参考范围审查及视觉 HTTP，之后才数值/profile；
  暂存容量候选保持，未混入本轮运行时改动。

## 2026-09-21：补齐视觉竞态修复的 HTTP 验收驱动

- 新工具 run_vision_determinism.py 使用保存的相同图像/视频
  请求，父版保留非确定输出，候选要求三次重复及重启后一致；
  原始立即停止请求不计性能。四组完整响应和范围全部留存。
- 性能检查不以父版随机文本作精度参考；正式接受仍需 HTTP
  后独立数值证据。待完整质量/五档双参考通过才串行启动，
  目前首项质量 HTTP 仍在运行，没有前置数值/profile。

## 2026-09-21：视觉 barrier 候选首项质量 HTTP 启动

- 独立候选零警告构建完成，冻结源码、二进制与静态库摘要。
  运行时仅视觉 AttentionKernel 增加一处共享分数读写 barrier。
- 首项 EvalScope 质量 HTTP 已启动，串行后接完整五档；
  后续还需视觉生成的确定性和数值证据，尚未接受或提交。

## 2026-09-21：定位视觉共享分数竞态，独立修复优先

- 父版正常调度/内联 decode 两组 HTTP 均完成，单图、双图、
  视频各三次全部非确定；原始无模板停止行为一致。不是只在
  scheduler decode 才出现的问题。无数值/profile。
- AttentionKernel 的 row_max 扫描后缺少跨 warp barrier，
  可在其他线程仍读 s_score 时覆写 exp；独立候选添加同步。
- 容量候选三个运行时文件先核对冻结快照，再保存补丁并恢复
  HEAD；其他任务内容不动，旧候选与失败证据不删除。新候选
  以 5d560f5 为父版，正在构建；下一步首项真实 EvalScope
  质量/五档及视觉 HTTP，不能凭静态竞态定位宣称修复已验证。

## 2026-09-21：显式模板视觉父版非确定，转 HTTP 定位

- consumers-vision-template 的父版单图 84-token 输入三次都
  输出 32 tokens，但文本哈希各不相同；服务退出 0。原始请求、
  文本和断言失败保留，候选尚未运行，不能归因于容量改动。
- 不放宽正式确定性门禁。启动独立父版 HTTP 定位：相同请求
  在正常调度器与合成初始化失败的内联 decode 两种路径执行，
  诊断保留所有不同输出，明确 accepted=false，不作为验收。
- 原候选源码仍冻结；视觉门禁未通过，MTP 和数值/profile
  不运行。下一步根据两种路径结果缩小根因，再决定修复范围。

## 2026-09-21：视觉父版立即停止，保留证据并补充生成请求

- 首轮单图父版三次均 HTTP success、completion_tokens=1、
  finish_reason=stop、空文本；原断言失败并停止后续 MTP，
  无数值/profile。这不是候选回归证据，也不是视觉性能通过。
- 原始请求在 EvalScope 中未被改写。源码仅拼 role 前缀，未
  应用 checkpoint chat template；本轮不修改已冻结运行时。
- 新目录 consumers-vision-template 保留原请求作停止行为对照，
  并用显式模型模板片段加真实 media parts 覆盖单图/双图/视频
  生成；保存模板配置 SHA，要求生成有文本且超过一个 token。
  原始空响应不参与性能比较，初轮全部证据保留。新 HTTP 已
  启动，之后才继续 MTP；标准 messages 模板问题仍待另行处理。

## 2026-09-21：容量候选调度器回退 HTTP 通过

- 合成 scheduler logits 分配失败并确认未启动调度器，1K/44K
  父版→候选→父版共 18 请求输出一致，decode 无不利范围分离。
- 44K TTFT 候选 30.409–30.416 秒，两侧父版为
  30.582–30.643、30.555–30.588 秒；1K TTFT 范围重叠。
  仅验证合成初始化失败下的内联回退，不宣称真实 OOM 恢复。
- 串行驱动进入单图/双图/视频 HTTP，之后 MTP；全部 HTTP
  通过后才运行数值/profile，候选仍未最终接受。

## 2026-09-21：明确容量候选后续数值证据范围

- 审查现有视觉测试后，在容量合同中明确完整主模型、真实视觉
  特征、完整 trunk、固定续算和 head 参考对照要求，避免把两层
  注入 smoke test 或仅最后一行 trunk 作为完整精度证据。
- 同时区分分配请求、设备分配与整机峰值，预算需核对多序列和
  自动推导配置。以上仅验收设计，未运行任何数值/profile；
  当前回退 HTTP 仍运行，后续先完成视觉和 MTP HTTP。

## 2026-09-21：容量候选内联消费者 HTTP 通过

- Q4T_NO_BATCH_PREFILL=1，1K/8K 各三次，父版→候选→父版
  共 18 请求输出一致，无不利性能范围分离。8K TTFT 候选
  5.151–5.179 秒，两侧父版 5.233–5.269、5.250–5.272 秒；
  decode 与两侧范围重叠。只说明本次固定内联路径测量。
- 串行驱动已进入调度器初始化失败回退 HTTP，覆盖 1K/44K；
  随后视觉、MTP。全部 HTTP 通过前不运行数值/profile，
  容量候选仍未最终接受。

## 2026-09-21：容量候选四项生命周期 HTTP 通过

- 固定两槽的轮转复用、停机、D2H 复制失败及同步失败四场景
  全部通过，complete.json 记录候选二进制 SHA 与 passed=4。
  错误注入仅模拟返回错误，不宣称真实硬件故障恢复。
- 串行驱动已进入内联消费者父版→候选→父版 HTTP 对照，
  随后调度器不可用回退、视觉、MTP。全部 HTTP 通过后才做
  数值、容量和时间线分析；本候选仍未最终接受。

## 2026-09-21：容量候选长短交错 HTTP 通过

- 父版→候选→父版各三轮共 18 请求，输出对照通过；候选
  三轮短请求均先于长请求首响应，总完成时间无不利范围分离。
- 本项验证容量变化未破坏固定双请求的交错行为，不推广为任意
  并发性能保证。串行驱动已进入生命周期 HTTP，随后覆盖内联
  回退、视觉和 MTP；仍未运行数值/profile，候选未最终接受。

## 2026-09-21：容量候选真实 B=2 HTTP 通过

- 父版→候选→父版各三轮共 27 请求，全部确认 B=2 双行读回，
  输出一致，三个服务退出 0。本轮父版/候选均已是所选行布局。
- 各请求 TTFT 与总完成时间均和两侧父版重复范围重叠；候选
  makespan 35.667–36.012 秒，两侧 35.625–35.949、
  35.687–35.994 秒。不认定稳定性能收益。
- 串行驱动已进入长短交错 HTTP，随后生命周期和消费者专项。
  尚未运行数值/profile，容量候选仍未最终接受。

## 2026-09-21：容量候选 4K 相邻复核通过，恢复补充 HTTP

- 父版→候选→父版各三次 4K，输出一致，三个服务退出 0。
  候选 decode 17.834733–17.857702 tok/s，与前侧
  17.837758–17.858961、后侧 17.819459–17.858677 均重叠；
  TTFT 范围也重叠。原始小幅不利分离未重现，完整记录不替换。
- 冻结源码/二进制/库及完整单流门禁重新核对，通过标准验收。
  已恢复真实组批、交错、生命周期及消费者 HTTP 串行驱动。
  仍未运行数值/profile，候选不据局部复核直接接受。

## 2026-09-21：容量候选完整 HTTP 后发现 4K 不利范围

- 五档 HTTP/输出 15/15、服务退出 0；对固定参考无不利分离，
  但 4K decode 对直接父版分离：候选 17.855899–17.856111，
  父版 17.856817–17.858525 tok/s。差异虽小，也不临时放宽门槛。
- 原始整轮结果和范围失败保留；后续驱动因没有 acceptance.json
  停止，没有运行组批/消费者补充、数值或 profile。
- 已启动 4K 父版→候选→父版各三次相邻 HTTP，固定同输入和配置。
  下一步依据两侧重复范围审查，当前仍未接受。

## 2026-09-21：补齐 serve logits 消费者与容量合同

- 新增 dataflow-engine/SERVE_LOGITS_CAPACITY.md，逐项对应普通
  单/多序列、长块、内联回退、视觉、MTP 主模型及回退 decode。
- 明确共享缓冲 4068474880→496640 字节是单流分配请求差额，
  不是整机峰值或驻留页实测。其余旧预算估计不据本轮宣称准确。
- 标准五档仍运行；消费者 HTTP 驱动已就绪并受完整范围门禁
  约束，未运行补充检查、数值或 profile。下一步按合同完成验收。

## 2026-09-21：容量候选质量通过，补充消费者 HTTP 驱动就绪

- 首项质量 HTTP 11/11、输出对照通过，服务退出 0；完整五档
  正在运行，标准 max_len=208896/max_seq=1 未被预算器缩减。
- 已写补充 evalscope 驱动：短内联、调度器分配失败回退（含长块）、
  单/双图像与视频帧、实际 MTP 短/长 prefill。每场景父版→候选→
  父版各三次，检查输出/usage 与 TTFT/decode 范围，尚未执行。
- 组批工具新增显式 parent-selected-rows 模式，本轮父版已有末行
  输出，不能沿用旧全行跨度断言。全部后续 HTTP 按标准门禁串行
  启动，失败即停；没有启动数值/profile。候选仍未接受。

## 2026-09-21：serve logits 容量候选启动首项 E2E

- 所有共享 logits 写入者已核对。内联/视觉/MTP main prefill
  只需最后一行，完整 trunk 独立；改用末行模式与末块需求，
  共享容量改为 max_seq 行，预算相应从固定项移到每序列项。
- 零警告必要构建后第一项测试是标准 tools/evalscope HTTP；
  尚未运行前置数值/单测/profile，证据 serve-logits-capacity-20260921。
- 下一步完整五档/双参考、真实组批及生命周期，并补内联回退、
  视觉与 MTP HTTP，再进行容量/精度细查；候选未接受。

## 2026-09-21：批处理每序列末行输出头最终接受

- 全部 HTTP、17 行 logits/主干/八组续算逐位及 68 对中间值
  文件复核通过。双版本真实 B=2 时间线 22 次 forward，只新增
  一次末行收集并将该 head 从 5120 行变为 2 行，其余每 stream
  kernel 名称/grid/block/顺序一致，两个捕获正常退出与导出。
- 初版审查脚本遇到未采集的 DRIVER 表，错误和原稿保留，修正
  查询后通过；不是推理或捕获失败。局部词表投影 50.738→5.096ms
  不替代正式 HTTP 性能持平结论。冻结源码/库及原始请求终态通过。
- 本阶段接受并准备提交推送；共享 logits 预留未缩减，下一步
  核对所有消费者后优化普通 serve 容量，继续独立 E2E 门禁。

## 2026-09-21：批处理末行数值逐位通过，开始实际组批时间线

- 全部 HTTP 门禁后，数值工具零警告构建、运行退出 0。八种打包
  形状、17 行 logits、356802560 个主干 BF16 值、八组固定 token
  续算逐位一致；收集行与输出 canary 通过，含非连续状态槽。
- 独立读取 68 对原始 logits/mixed/normed/trunk 文件，全部逐位
  一致。不据有限形状宣称任意输入保证，无需新增误差阈值放行。
- 已启动父版与候选实际三请求 HTTP 时间线，先加载模型后开始
  CUDA 捕获；输出与已通过的 B=2 HTTP 对照，另需检查时间线确实
  覆盖组批。诊断耗时不作正式性能收益，最终审查前仍不接受。

## 2026-09-21：批处理末行全部 HTTP 完成，开始数值验证

- 生命周期轮转/复用、停机、复制和同步错误返回四项 HTTP 全部
  通过，串行驱动结束。错误返回是合成注入，不等于真实设备故障。
- 全部单流、真实 B=2、交错与生命周期 HTTP 门禁通过后，才启动
  新批量数值工具构建/运行。参考为同一打包 forward 的全行 head；
  八种形状覆盖 B=1/2/3、不等长、非连续状态槽、8192 满打包。
- 核对全部主干、实际收集行、输出 canary 与固定 token 续算，
  保存 logits/实际 head 中间值供精度归因；未设临时误差阈值。
  数值结果尚待完成，候选未最终接受，时间线尚未运行。

## 2026-09-21：批处理末行交错 HTTP 门禁通过

- 既有长短交错父版→候选→父版各三轮，18 请求输出一致，三个
  服务退出 0。候选短请求均先于长请求首响应，总完成时间相对
  两侧父版无不利范围分离；不将局部 TTFT 波动认定为稳定收益。
- 串行驱动已进入四项生命周期 HTTP；数值/profile 仍未运行。
  证据 interleave/review.json，候选尚未最终接受或提交。

## 2026-09-21：批处理末行真实 B=2 HTTP 对照通过

- 父版→候选→父版各三轮，共 27 请求，所有输出一致，三个服务
  退出 0。每轮同次完成同步前均观察到两行词表读回；候选间距
  496640 字节，父版为 1024/4096 行间距，证明实际进入 B=2。
- 1K/4K 短请求与 44K 长请求 TTFT、总完成时间与两侧父版范围
  均重叠，无不利分离。候选 makespan 35.689–35.975 秒，前侧
  35.749–36.065、后侧 35.761–36.084 秒；不认定稳定收益。
- 完整记录 batch-http/review.json。串行驱动已开始既有长短
  交错 HTTP，随后生命周期；数值/profile 仍未运行，候选未接受。

## 2026-09-21：批处理末行候选完整单流门禁通过

- 质量 11/11、五档性能 HTTP/输出 15/15，服务退出 0；对直接
  父版 61742d4 与固定参考 fcb5925 均无不利范围分离。
- TTFT 秒 / decode tok/s：1K 0.801/18.575、4K 2.637/17.858、
  8K 5.178/18.050、44K 30.518/17.854、200K 159.468/17.048。
  单流路径不使用本轮 B>1 选择，不能据此证明新路径正确或提速。
- 原驱动正常结束后启动真实 B=2 相邻 HTTP 对照，当前为父版。
  后续既有交错及生命周期检查仍需通过；没有运行数值或 profile。
  当前候选尚未接受，证据 prefill-batch-last-rows-20260921。

## 2026-09-21：批处理末行候选质量 HTTP 通过

- 首项质量 E2E 11/11、输出对照通过，服务退出 0；原驱动已进入
  完整五档性能，仍需双参考范围审查，当前候选尚未接受。
- 新增待运行的 B=2 HTTP 驱动与只观察读回的 preload 工具：
  一个 44K 请求期间提交 1K/4K 两请求，父版→候选→父版各三轮，
  交替短请求输入顺序，必须证明同次完成同步前有两行词表读回。
  它不注入错误、不计 kernel 时间；未覆盖 B=2 时不能记为通过。
- 下一步完成单流范围门禁后运行真实组批、既有并发和生命周期
  HTTP，再做数值/时间线；所有运行证据保留在本轮目录。

## 2026-09-21：批处理每序列末行 head 候选开始 E2E

- 新增显式批量输出行模式，复用 decoder 结束后的空闲主干缓冲
  收集各序列末行，head 只处理 B 行；serve 按打包顺序读回。
- 保留默认全行 API、原分配容量与序列状态计算。零警告构建后
  第一项运行验证为 tools/evalscope 完整 HTTP，未做前置测试。
- 尚未接受；下一步双参考范围检查及实际 B>1 并发/生命周期
  HTTP，通过后再进行数值与时间线。证据 prefill-batch-last-rows-20260921。

## 2026-09-21：单序列末行输出头按 E1 接受

- 全部 HTTP、原始证据、冻结源码/库、结构数值与 CPU 参考
  审查通过。4K/44K 时间线仅最终 head 六次调用形状改变，
  其他每 stream kernel 序列完全相同，包含全部 decode。
- 4K/8K TTFT 改善约 1.6%–1.7%；44K 捕获首请求/后续请求
  差异明确保留，不把初始化 API 差别算作优化。固定参考不更新。
- 按 E1 接受并提交推送；不宣称全输入逐位一致或容量下降。
  下一步实施批处理每序列行选择及真实多请求 E2E。

## 2026-09-21：末行输出精度归因完成，进入时间线

- 直接捕获实际 head 工作区：normed 全一致，mixed 四组有
  1–9 个 BF16 差异，其余一致。结构与原 logits 差异均复现。
- 相同 checkpoint BF16 权重的 CPU FP64 参考保留中间 BF16
  舍入边界；末行 mixed 十组均匹配，最终 logits 对共同参考
  L2 八组更小、两组相同，argmax 全一致。按 E1 审查，不称 E0。
- 不设置临时误差阈值，不推广为所有输入保证。冻结源码/库一致，
  已启动父版 4K、候选 4K/44K 时间线；最终核对前仍不接受。

## 2026-09-21：末行候选全部 HTTP 通过，数值非逐位一致

- 四项生命周期通过后才运行数值工具：220416000 个 trunk
  BF16 值及 7 组固定续算逐位一致，输出边界与序列状态通过。
- 10 组 logits 的 8 组有差异，最大 l2_rel=0.000919，
  max_abs=0.0625；argmax/top2 值全相同。近零值存在符号翻转，
  不统称一 ULP，不把结构通过当作精度接受。
- 原始 BF16 与 ULP/margin 分析保留；下一步逐算子/参考精度
  归因，候选未接受，尚未运行 profile。

## 2026-09-21：末行输出头并发相邻对照通过

- 18 请求输出一致、三个服务退出 0；总完成时间与两侧父版
  范围均重叠，按持平理解，不把小幅均值变化认定为稳定收益。
- 四项生命周期 HTTP 已启动，全部通过后才编译执行准备好的
  数值工具；结构成功仍需精度审查，候选尚未接受。

## 2026-09-21：准备末行输出数值证据工具

- 新工具链接同一模型库，覆盖 1/7/1K/4K/8K 与跨块，比较全行
  末行和单行输出，检查全 trunk、canary、游标/历史与固定续算。
- 保存原始 BF16 logits 与误差指标，不自定放宽阈值；结构成功
  不等于精度接受。编译与运行均等待全部并发/生命周期 HTTP 通过。
- 当前并发候选第一轮已完成；下一步继续门禁，尚未执行数值工具。

## 2026-09-21：末行输出头完整单流 HTTP 门禁通过

- 质量 11/11、性能 HTTP/输出 15/15、服务退出 0，父版及
  固定参考均无不利范围分离；4K/8K TTFT 均值改善约 1.6%–1.7%。
- 44K/200K 均值略慢但范围重叠，不宣称稳定长上下文收益。
  已启动并发相邻对照，随后生命周期 HTTP，再核对数值和时间线。
- 候选尚未接受；完整输出一致不能代替 GEMM 形状变化的数值核对。

## 2026-09-21：末行输出头候选质量 HTTP 通过

- 首项 11/11（含 200K）输出对照通过、服务退出 0，二进制
  摘要与冻结源码一致；完整五档性能已串行接续。
- 质量题通过不证明输出头 M=1 与原 GEMM 的数值等价。后续
  全部 HTTP 门禁后仍需数值及时间线核对，候选尚未接受。
- 下一步核对性能双参考；失败时不进入底层专项或 profile。

## 2026-09-21：单序列 prefill 末行输出头候选启动

- 新增显式全行/末行需求，最后 trunk 行执行 T=1 head，logits
  写首行；完整 trunk 输出及默认全行调用保持原语义。普通文本
  单请求短 prefill 和长末块接入，B>1 不误选批次末行。
- 接口中间状态编译失败日志保留，最终零警告构建后首项质量
  HTTP 已启动；没有前置专项，未减少 logits 预留容量。
- 下一步完整双参考 HTTP 及调度场景，再验证形状改变的数值。
  证据 prefill-last-row-20260921，候选尚未接受。

## 2026-09-21：首块无消费者输出头省略接受

- 全部 HTTP、原始响应、工具副本与源码/库最终审查通过；44K
  时间线少首块 head 六次 kernel、两对分配/释放，decode 不变。
- 全局开始时间顺序比较初次失败，核对为既有 MoE 多 stream
  交错；删除六次 head 调用后，261 个 forward 每 stream 的
  kernel 名称/grid/顺序完全一致。失败观察与解释均保留。
- 44K TTFT 改善约 0.46%，其他收益不夸大；固定参考不更新。
  下一步实施末块按需行计算，继续先完整 HTTP。本阶段提交推送。

## 2026-09-21：首块输出头省略生命周期 HTTP 全通过

- 四项轮转复用/停机/copy/sync 场景通过，服务正常退出；停机
  至客户端与服务结束约 4.38 秒，合成错误后槽归还且后续 503。
- 全部 HTTP 后核对冻结源码/二进制/库；非 server 库与父版相同，
  server 唯一改动为块 logits 需求条件。44K 时间线已启动。
- 下一步核对省略 head 的实际调用差异，最终审查前不接受或提交。

## 2026-09-21：首块输出头省略并发对照通过

- 父版→候选→父版各三轮共 18 请求输出一致，服务均退出 0；
  总完成时间无不利分离，候选与后侧父版范围重叠。
- 保留约 5.75 秒最大响应间隔，不宣称稳定并发收益，也不重复
  计入父版已有的 chunk 调度首响应收益。
- 四项生命周期 HTTP 已启动，通过后才采集时间线；候选未接受。

## 2026-09-21：首块输出头省略完整单流 HTTP 通过

- 质量 11/11、性能 HTTP/输出 15/15、服务退出 0；对父版与
  固定参考均无不利范围分离。44K TTFT 均值减少约 0.14 秒；
  200K 均值减少约 0.33 秒但范围重叠，不认定稳定提速。
- 已启动父版→候选→父版各三轮并发 HTTP；随后四项生命周期
  HTTP，全部通过才采集时间线。候选尚未接受或提交。
- 下一步审查并发总完成时间和输出，不以单流结果替代调度门禁。

## 2026-09-21：补齐 P7 输出行需求边界

- 新增 PREFILL_HEAD_DEMAND.md，区分无输出、全部行与每序列末行
  消费者，trunk 输出保持独立；首块省略不等于完整 P7 完成。
- 最终块按需计算将改变 GEMM 形状，必须独立完整 E2E 后再核对
  数值；不能把计算写入减少直接表述为共享容量下降。
- 当前 200K HTTP 进程仍存活，未改冻结代码、未运行底层专项。
  下一步继续完整门禁，再推进实际行选择。

## 2026-09-21：首块输出头省略的 44K 中间结果

- 完整矩阵已完成前四档各三次，44K 输入/输出摘要与父版一致。
  TTFT 均值 30.628→30.489 秒，范围呈有利分离；decode 范围重叠。
- 200K 仍运行，不以局部收益替代完整双参考门禁。已接续带门禁的
  并发、生命周期与最后的时间线驱动，任一前置失败均停止后续。
- 下一步检查 200K 及全矩阵；候选尚未接受或提交。

## 2026-09-21：首块输出头省略候选质量 HTTP 通过

- 首项真实 HTTP 质量 11/11、输出对照通过、服务退出 0，包含
  200K 请求；二进制摘要与当前构建一致。
- 同一驱动串行接续五档性能，完整双参考通过后才运行并发及
  生命周期 HTTP；任一失败停止后续步骤，不提前 profile。
- 候选未接受，下一步审查完整性能范围，保留全部原始结果。

## 2026-09-21：长文本首块无消费者输出头候选

- 源码确认普通文本 chunk 调度只在末块读回 logits，首块输出头
  不更新序列状态，其全部输出未消费。用已有 nullptr 需求跳过。
- 保持末块形状、精度、同步和调度边界，MTP/视觉/回退路径未改。
  必要构建后首项为真实 HTTP 质量及五档性能，不做前置专项。
- 下一步完整双参考门禁及调度 HTTP 对照，全部通过后再做时间线；
  证据 prefill-first-head-skip-20260921，未接受。

## 2026-09-21：普通文本 chunk 调度接受

- 单流完整双参考门禁、18 个长短并发请求、10 个生命周期请求及
  服务退出原始记录复核通过，验收脚本与执行副本一致。
- 短 TTFT 约 24.6→4.75 秒，长 TTFT 增加约 0.9 秒，总完成
  时间范围重叠；保留约 5.75 秒响应间隔与两槽文本验证边界。
- 时间线与源码摘要通过，acceptance/final-audit 记录接受；固定
  参考不更新。下一步核对 prefill 输出头消费者，减少无效计算，
  新候选继续先完整 HTTP；本次整理提交推送 main。

## 2026-09-21：chunk 生命周期 HTTP 完成与时间线关联核对

- 四个生命周期场景通过，服务退出 0；copy/sync 合成错误后执行与
  排队请求失败、slot 归还、健康及后续请求 503。
- 完整 HTTP 后的 44K 时间线 kernel 名称/次数不变，prefill 增加
  五次中间块完成同步。cuLaunchKernel 的差异全部位于无 GPU kernel
  关联记录，关联调用两版同为 576；不宣称减少计算或提速。
- 冻结源码、二进制、全部库摘要一致，证据 trace-source-review.json。
  下一步完成验收工具和原始终态审查，再整理提交；候选尚未接受。

## 2026-09-21：chunk 调度槽复用与停机 HTTP 通过

- 不同长输入、反转顺序后的 slot 复用输出保持，健康且两个 slot
  全部归还；执行/排队时停机请求均终止，服务退出 0、in-flight=0。
- 完整并发对照总完成时间范围重叠；短 TTFT 约 24.6→4.75 秒，
  长 TTFT 约 30.7→31.6 秒。明确记录服务先后次序带来的延迟取舍，
  不宣称所有请求均变快或模型 GPU forward 并行。
- copy/sync 错误 HTTP 仍运行，全部通过后才接续 44K 时间线。
  当前仍不接受或提交。

## 2026-09-21：chunk 调度长短并发对照通过

- 父版→候选→父版各三轮、全部输出一致；候选短请求首内容均早于
  长请求首内容，两侧父版均不提前。总完成时间与两侧重复范围重叠。
- 短 TTFT 约 24.6→4.75 秒，最大响应 chunk 间隔仍约 5.75 秒；
  保留延迟分布边界，不宣称平滑 decode 或普遍并发吞吐提升。
- 依次启动不同长输入/槽复用、停机、copy/sync 错误 HTTP 场景。
  这些完成前不接受，也不提前 profile 或运行底层专项。

## 2026-09-21：chunk 调度并发中间结果与生命周期场景

- 候选三轮短请求 TTFT 4.67–4.87 秒，前侧父版 24.47–24.76 秒；
  候选均在长请求首内容前输出，已完成轮次输出一致。后侧对照仍运行。
- 最大响应 chunk 间隔约 5.75 秒，明确保留大 chunk 停顿限制，不
  将首响应改善等同于平滑 decode；完整并发门禁仍未判定。
- 准备两个不同长输入、反转顺序复用 slot、执行/排队时停机及最终
  读回错误的 HTTP 工具，仅在并发门禁通过后运行，不并行争用 GPU。
  下一步检查完整对照和生命周期终态，尚未接受或提交。

## 2026-09-21：chunk 调度候选单流门禁通过，开始并发 HTTP

- 质量 11/11、五档 HTTP/输出 15/15、服务退出 0，父版与固定参考
  均无不利性能范围分离。候选仍不接受，单流证据不能替代调度验证。
- 新工具用实际 evalscope 双客户端做父版→候选→父版各三轮，比较
  固定长短输入的输出、首内容和总完成时间，记录响应 chunk gap。
  实际服务容量为 65536×2，未被预算器缩减。
- 下一步完成并发对照，再轮转、停机/失败 HTTP；这些通过前不做
  profile 或底层专项，不把队列存在当作公平性证明。

## 2026-09-21：chunk 调度候选首项质量 HTTP 通过

- 11/11 请求及输出对照通过，服务退出 0；候选二进制和四份源码
  仍与冻结快照相同。完整单流五档已串行启动。
- 下一步按父版与固定参考审查性能，再开展长短请求并发、轮转及
  停机/失败 HTTP 场景。尚未接受，也没有运行前置专项或 profile。

## 2026-09-21：普通文本长 prefill 调度候选

- 新增借用请求线程输入/seq/host logits 的 chunk 队列，调度器每轮
  在已选 decode 后最多处理一个 chunk，完成检查后排回队尾或发布
  终态。处理两处提前 continue，停机清空队列，错误后不继续推进。
- 保留块大小、模型路径与首/末块 head 策略。调度结果已完成读回，
  请求线程不再重复同步可能已开始的下一任务；内联路径保持检查。
- 零警告构建，下一步首项质量 HTTP 与完整单流五档，再并发 HTTP；
  不做前置单测/profile，尚未接受，不宣称调度性能收益。

## 2026-09-21：chunk 调度源码审查与实施边界

- 确认长 prefill 仍持有全 prompt 模型锁，共享 scratch/logits 不允许
  无协议交接；mutex 不保证 ready decode 获得下一次执行机会。
- 制定纳入现有调度器的方案：每轮最多一个长 chunk，显式完成检查
  后轮转，修正两处提前 continue 对轮尾工作的影响，明确请求内存、
  slot、停机及失败后禁止重新排队。方案尚未实现。
- 下一步实现候选后先完整单流 HTTP，再长短并发 HTTP（长 output=1
  避免引入 decode batching 数值差异），之后才分析时间线和同步成本。
  本次只有文档与源码审查，没有修改运行时或新增性能结论。

## 2026-09-21：文本分块序列接口 v2 正式接受

- 完整 HTTP 门禁后，链接完整模型库对照原分块链，三组块长、
  两个 slot 的 8,791,040 个 BF16 logits/trunk 逐位一致；逐块位置、
  历史与阶段核对通过。14 项拒绝、2 次 H2D 错误注入与重置恢复通过。
- 44K 双版本 profiled HTTP 的块序列、全部 kernel/CUDA API 次数
  相同；源码/库/二进制冻结核对通过。按状态管理改进、性能持平接受，
  原中止首轮完整保留，固定参考不变，不宣称速度收益。
- 没有真实设备故障、MTP 或并发隔离验证，host 游标不是 GPU 完成。
  下一步继续明确执行计划的完成/调度边界；全 prompt 模型锁仍存在。

## 2026-09-21：分块序列接口 v2 完整 HTTP 门禁通过

- 质量 11/11、五档 HTTP/输出 15/15，服务退出 0；对父版及固定
  参考均无不利范围分离，按持平理解。只使用 v2 完整矩阵。
- 门禁后源码核对：除新增 chunk API 和旧入口拒绝检查外，整个
  model.cu 与父版相同。44K 双版本 HTTP 时间线串行启动。
- 下一步链接实际模型库，对原分块链核对逐块输出与状态，包括
  非四倍数块、非零 slot、错误阶段及 forward 失败后禁止继续。
  尚未接受或提交，不以 HTTP 正常路径替代接口边界验证。

## 2026-09-21：分块序列接口 v2 质量 HTTP 通过

- 修正后的完整首项质量 11/11、输出对照通过、服务退出 0，
  二进制与三份运行时源码仍匹配冻结快照。
- 完整五档性能已串行接续，下一步核对父版与固定参考重复范围。
  尚未执行接口状态专项、数值测试或 profile，不提前接受。

## 2026-09-21：分块接口补齐旧整段入口拒绝检查

- 源码复核发现 partial prefill 仍处于 kPrefill，旧 ModelPrefill
  若被混用会从零位置重跑。补充 position/history 非空时拒绝，
  防止新接口与旧接口混用破坏状态；检查发生在 GPU 工作之前。
- 主动停止尚未完成的首轮质量请求（evalscope 143，服务退出 0），
  原记录及中止原因保留，不计为通过或性能失败。未执行任何专项。
- 修正后零警告重构建，在独立 v2 证据目录重新从质量 HTTP 开始，
  接续完整五档性能；下一步按完整门禁决定专项，旧数据不拼入。

## 2026-09-21：文本 prefill 分块状态接口候选

- 当前 serve 首块后提前标 decode，后续裸 forward，结束后手工
  修正 position/history。引入 ModelPrefillTextChunk 统一逐块位置、
  历史及 slot，最终块才进入 decode；forward 失败则 kFailed。
- 保留原首/末块 logits、trunk 偏移、kernel 与分块大小，不引入
  chunk 间同步或调度让步。不把 host 游标当 GPU 完成或事务回滚。
- 下一步必要构建后先质量 HTTP 和完整五档性能，通过后再核对
  跨 chunk 状态、错误拒绝、调用序列；尚未执行任何前置测试。

## 2026-09-21：serve 结果发布修复正式接受

- 完整 HTTP 双参考门禁后，12 组父版/候选错误返回注入均通过。
  父版复制/同步失败仍保持成功与健康；候选 prefill 返回 500，
  decode 停止后续生成，健康与后续请求 503。全部服务正常退出。
- 正常 4K HTTP 时间线全部 kernel、CUDA API 次数与父版相同；
  二进制/源码/库冻结核对通过，按性能持平接受，不宣称速度收益。
- 验证为非流式单请求、模拟返回错误，不覆盖真实设备故障、强制
  多请求组批、内联 decode 回退或 MTP。下一步继续模型专用执行
  计划及序列状态边界，不能把本修复当完整状态事务或引擎完成。

## 2026-09-21：serve 结果发布候选完整 HTTP 门禁通过

- 质量 11/11、性能 HTTP/输出 15/15、服务退出 0，父版与固定参考
  均无不利分离。按性能持平理解，没有提前运行数值或故障专项。
- 门禁后启动父版/候选各六组实际服务错误返回注入，覆盖调度与
  内联 prefill、调度 decode 的 copy/sync 失败；验证首请求结果、
  健康标记和后续请求拒绝。下一步读取终态记录，未通过前不接受。

## 2026-09-21：serve 结果发布候选质量 HTTP 通过

- 首项质量 11/11、输出对照通过，服务退出 0；二进制与源码仍为
  冻结候选。完整五档性能已串行启动，比较父版与固定参考。
- 下一步先完成范围门禁，再做错误路径验证，不提前故障注入或
  profile；本阶段尚未接受、未提交。

## 2026-09-21：serve 结果发布边界候选

- 源码发现调度器和内联 prefill/decode 忽略 D2H/同步返回值，可能
  发布旧 host 结果；prefill 额外同步只打印错误，未改变请求状态。
- 在原同步位置汇总复制与同步错误，失败阻止结果发布并标记 GPU
  不健康，保留正常路径与现有终止策略。基于 ede4e09 必要构建后
  先完整 HTTP，再验证错误路径；不宣称完整事务或 MTP 恢复已实现。
- 下一步质量 11 题、五档各三次对父版及固定参考验收。

## 2026-09-21：线性 scratch 所有权正式接受

- 完整 HTTP 与相邻旧→新→旧复核通过后，源码核对和 4K HTTP
  时间线确认全部 kernel 及分配/释放次数不变，仍各 266 次/步。
- 集中六块临时缓冲的作用域所有权，删除 29 处手工清理，按性能
  持平接受；不宣称吞吐或容量收益。保留 profile 窗口略慢和初始
  性能异常，没有执行 CUDA 失败注入或新增 MTP/并发运行验证。
- 下一步将资源合同连接到模型专用执行计划与序列状态边界，仍先
  完整 HTTP，再细分析；固定参考 fcb5925 不变。

## 2026-09-21：线性 scratch 所有权相邻复核通过

- 4K decode、44K TTFT 候选与两侧父版范围均重叠，三组 HTTP
  输出检查通过、服务退出 0。初始异常保留，性能门禁现通过。
- 门禁后源码核对证明除 owner/绑定/29 处 cleanup 外，整个源文件
  不变；数学、launch、分配尺寸/次序不变，析构保持六次同 stream
  释放。没有做 CUDA 失败注入，不夸大该边界。下一步完成 4K HTTP
  时间线核对实际调用次数，再决定接受。

## 2026-09-21：线性 scratch 所有权性能范围异常复核

- 质量 11/11、完整 HTTP/输出 15/15。4K decode 对父版约 -0.27%，
  44K TTFT 约 +24 ms，均有不利范围分离；固定参考无不利分离也
  不能覆盖直接父版异常。性能门禁未过，未做所有权专项或 profile。
- 冻结父版/候选在 4K、44K 各三次旧→新→旧 HTTP，下一步按两侧
  范围处理，保留原始异常，不以代码逻辑等价跳过 E2E 门禁。

## 2026-09-21：线性 scratch 所有权质量 HTTP 通过

- 冻结二进制首项质量 11/11，输出检查通过、服务退出 0。完整五档
  性能已串行接续，未运行前置或门禁后单测、数值与 profile。
- 下一步先审查双参考性能范围；通过后才核对所有权退出路径与
  实际 kernel/分配次数，未接受或提交此候选。

## 2026-09-21：线性 scratch 所有权零警告构建并启动 HTTP

- 源码和二进制、静态库摘要冻结，父版 40b7dbb 二进制保留。首项
  质量 HTTP 已启动，随后串行五档；固定参考 fcb5925 不变。
- 未执行任何数值/单测/profile。下一步完整 E2E 与双参考范围通过
  后才核对退出清理、分配序列和时间线，未通过则修复或回退。

## 2026-09-21：线性 scratch 所有权候选，保留物理分配策略

- 最新 decode 时间线 BF16 GEMV 仍占主导，不把分配计数当带宽瓶颈。
  源码发现六块 scratch 的清理分布在 29 个正常/失败出口，候选收拢为
  非复制作用域对象；六次独立分配、尺寸、释放次序与数学保持。
- 不重试此前失败的 slab 合并，不预设提速；收益是生命周期与错误
  清理集中。下一步必要构建后首项质量及完整五档 HTTP。

## 2026-09-21：接受成对 gate+mix，保留 decode 局部略慢证据

- 完整 HTTP 与相邻复核后，25 组/108620800 输出逐位通过、75 组
  实际 GR 链路逐位通过，4K HTTP 时间线完成。kernel/分配次数不变。
- prefill gate+mix 130.558→79.030 ms，正式 4K–200K TTFT 改善
  约 1.5%–2.0%；decode 局部 69.619→71.846 ms，阶段窗口略增，
  完整 E2E/相邻范围重叠，按持平接受，不宣称所有形状加速。
- 初始异常保留，固定参考不变。下一步提交推送，回到整体时间预算
  与资源合同推进模型专用执行计划，保持先完整 E2E 的顺序。

## 2026-09-21：成对 gate+mix 相邻复核通过，开始门禁后检查

- 200K 三组 HTTP 全完成。候选 decode 17.029481–17.059967，
  与前侧 17.031933–17.102361、后侧 17.020079–17.054773 均重叠；
  初始不利分离未重现，原记录保留。TTFT 对两侧仍有明确改善。
- 完整性能门禁现通过，开始提取 kernel 逐位、实际链接 GR 链路对照
  及 4K HTTP 时间线。下一步核对数值和收益后决定接受，尚未提交。

## 2026-09-21：成对 gate+mix 200K decode 门禁未过

- 完整质量 11/11、五档 HTTP/输出 15/15；200K decode 父版
  17.088506→17.045087 tok/s，范围不利分离，对固定参考也不利。
- TTFT 162.122880→159.692799 秒不能抵消 decode 风险，未接受候选；
  原始证据保留，数值、微测试和 profile 均未执行。
- 下一步冻结父版→候选→父版，仅 200K 各三次 HTTP 相邻复核，
  对照两侧范围，不改阈值或挑样本。

## 2026-09-21：准备成对 gate+mix 数值边界工具

- 完整性能运行期间仅准备门禁后工具，未编译执行。计划 25 组
  随机/饱和/正负零/subnormal/抵消，与实际父版标量 kernel 逐位比较，
  同时检查尾部写入边界。运行时候选保持冻结。
- 下一步仍先等待完整 E2E 与范围审查，通过后才执行专项及时间线。

## 2026-09-21：成对 gate+mix 质量通过，五档性能运行中

- 冻结二进制首项质量 HTTP 11/11，输出检查通过、服务退出 0，
  同一二进制已进入完整五档性能；尚未运行数值或 profile。
- 下一步对照父版及固定参考的重复范围，只有有实际收益且无回退
  才值得保留新增分派，不把质量通过作为性能或逐位数学验证。

## 2026-09-21：成对 gate+mix 零警告构建并启动 HTTP

- 候选二进制、源码及静态库摘要冻结，首项质量 HTTP 已启动，随后
  串行完整五档性能。父版 79dd7a6 二进制保留，固定参考 fcb5925。
- 尚未执行数值、微测试或 profile；下一步先审查完整输出和双参考
  重复范围，不因向量访问形式推断实测收益。

## 2026-09-21：准备 GRRead 成对 gate+mix 候选

- 已验收父版 4K prefill 的 MixGateKernel 累计 130.558208 ms。
  候选改为每线程相邻两元素，保持分支次序、BF16 舍入与 CTA 输出数；
  hc=4/hs=2560 且 4 字节对齐才启用，其他路径保留。
- 不减少逻辑字节、kernel 或分配数，新增分派必须有实际收益才能保留。
  下一步构建后首项质量和五档 HTTP，先不运行任何底层测试。

## 2026-09-21：接受 GRWrite→Read 向量读取融合第二版

- 完整质量/五档 HTTP、双参考范围、75 组逐位和资源合同均通过。
  4K 时间线确认每 forward 净少 48 次 kernel，其他 kernel 和异步
  分配/释放次数不变，仍各 266/forward。
- 正式 TTFT 约改善 1.5%–2.0%，decode 按持平理解。带 profile
  decode 窗口 14406.733→14511.677 ms，保留该记录，不宣称提速。
- 首版失败证据保留，固定参考 fcb5925 不变。下一步提交推送，再根据
  已验收时间线核对 GRRead 数据流与生命周期，候选仍先完整 E2E。

## 2026-09-21：融合第二版逐位与资源合同通过

- 完整 E2E 门禁后，75 组、5,865,523,200 个有限 BF16 值逐位一致；
  含未对齐 block 输入的分离路径，frame 消费/丢弃及空 token 检查。
- 252 布局、504 链接容量、1260 错误合同拒绝通过。编译资源报告
  REG=38、SHARED=21504、STACK/LOCAL=0；源码 staging 为 20480 字节，
  与编译总占用区分。下一步完成 4K HTTP 时间线并核对实际调用数。

## 2026-09-21：融合向量读取第二版完整 HTTP 门禁通过

- 质量 11/11、性能 15/15，输出一致、服务退出 0；与直接父版及
  固定参考均无不利范围分离。200K TTFT 162.122880 秒，decode
  17.088506 tok/s。首版全部异常记录保留，不重置正式参考。
- 现开始门禁后逐位、资源合同和 HTTP 时间线检查；下一步确认数值、
  布局及实际调用数后再决定接受，尚未提交运行时。

## 2026-09-21：补充第二版门禁后未对齐输入对照草稿

- 因向量读取新增地址对齐条件，数值工具草稿补充 block 指针偏移
  一个 BF16 的分离路径对照，共计划 75 组；仅写工具、未构建执行。
- 运行时源码和二进制保持冻结，完整性能仍在运行。下一步完整门禁
  通过后才执行这些对照，失败则保留证据并修复或回退。

## 2026-09-21：向量读取第二版质量 HTTP 通过

- 同一冻结二进制质量 11/11，输出检查通过、服务退出 0；完整五档
  性能已接续启动，MTP 关闭、单流、固定容量，未运行门禁后检查。
- 下一步按直接父版和固定参考审查重复范围；首版不利结果仍保留，
  不把质量通过当作性能或逐位数学验证通过。

## 2026-09-21：向量读取第二版启动完整 HTTP

- 必要构建零警告，候选二进制、源码和静态库摘要冻结，首项质量 HTTP
  已启动，随后串行五档性能；父版 dd1da64、固定参考 fcb5925 不变。
- 本轮没有运行数值、资源或 profile；下一步审查完整 HTTP 和双参考
  重复范围，未通过前不提交运行时为已接受版本。

## 2026-09-21：融合首版未接受，准备向量读取第二版

- 相邻旧→新→旧 HTTP 全完成、输出一致。8K 与两侧重叠；200K
  与前侧仍不利分离、与后侧重叠，不能选择后侧对照宣布通过。
- 首版未接受，数值、资源和 profile 均未执行，原始证据完整保留。
- 第二版只改 residual/block 为对齐向量读取，保留 BF16 舍入与归约；
  未对齐走分离路径。下一步必要构建后首项质量、完整五档 HTTP。
  独立目录 grwrite-read-vector-20260921，父版及正式参考均不变。

## 2026-09-21：融合候选 decode 异常，进入相邻 HTTP 复核

- 质量 11/11、五档 HTTP/输出 15/15；8K/200K decode 相对父版
  -0.46%/-0.27%，重复范围不利分离，200K 对固定参考也不利分离。
- TTFT 改善约 1.4%–1.9% 不能抵消 decode 风险。性能门禁未通过，
  数值、资源、profile 全部停止且未执行，完整原始异常保留。
- 冻结 dd1da64 父版与候选，8K/200K 做旧→新→旧各三次 HTTP。
  下一步依据两侧对照判断是否重现，不改阈值、不挑对照、不先 profile。

## 2026-09-21：融合候选质量 HTTP 通过，准备门禁后工具

- 首项质量 11/11、输出一致、服务退出 0；五档性能已启动。
- 已准备边界逐位及资源工具，尚未编译执行：比较不同 next weights/eps
  下 combined/normed/mixed/final，覆盖自有/借用 gate 与 no-combine。
- 报告明确 combined 必须保留、shared 预算不等于实测占用；下一步先
  等待完整 HTTP 与范围审查，再决定是否进入数学及资源检查。

## 2026-09-21：GRWrite→Read 融合候选开始 HTTP

- 源码确认 combined 仍是 MLP Write 的 residual，不能删掉写回。候选
  融合 attention Combine 与 MLP RMSNorm，BF16 舍入后进入原归约顺序，
  20 KiB shared staging 避免归一化重复读取全局 combined。
- WriteAndRead 消费当前帧并准备下一帧，保留通用分离路径；normed
  存活掩码扩到 attention Write，容量及地址不变。
- 构建零警告，保存候选指纹，首项质量及五档 HTTP 开始。尚未运行
  专项或 profile，也未宣称逐位一致或提速。下一步先审查完整 E2E。

## 2026-09-21：接受实际链接的资源合同

- 完整 HTTP 与双参考范围门禁、252 布局/504 容量/1260 错误拒绝通过。
- 4K 时间线逐 kernel 调用数不变；异步分配/释放保持 prefill 各 266、
  255 decode 步各 67830。旧偏移、容量和 forward 实现不变。
- 接受资源可检查性边界，不宣称吞吐/容量收益，不等同于全模型计划
  完成。下一步提交推送，再按 R1 路线核对 GRWrite→下一 GRRead 的
  数值舍入及 residual 最后消费者，为独立候选提供依据。

## 2026-09-21：资源合同完整 HTTP 与检查器通过

- 首项质量 11/11、五档性能 15/15，输出一致、服务退出 0；与父版和
  固定参考均无不利范围分离，性能按持平理解。
- 门禁后实际链接检查通过 252 布局、504 容量、1260 错误合同拒绝，
  导出 3276 个视图。旧 20 个字段及 forward 实现保持一致。
- 工具构建零警告。4K 时间线运行中，下一步核对调用次数并决定接受。

## 2026-09-21：资源合同报告补齐边界

- 报告区分实际链接的 offset/bytes 与仍需代码审查的存活语义；明确
  不覆盖外部状态、权重、缓存驻留或完整执行器。
- 原 HTTP 进程正常，1K/4K 输出检查完成，剩余档位及范围审查待完成；
  专项仍未执行。下一步等待完整结果，不用局部完成替代性能门禁。

## 2026-09-21：资源描述候选质量 HTTP 通过

- 首项质量 11/11，HTTP/输出检查通过，failure=null、服务退出 0。
- 同一候选已开始完整五档性能，对照父版 81cdbf8 与固定参考 fcb5925。
- 合同检查器已准备，尚未执行；下一步等待完整矩阵与范围审查，
  通过后才核对实际链接描述、错误合同拒绝和时间线。

## 2026-09-21：准备链接资源合同检查器

- 仅编写工具，未编译或执行。计划链接实际候选库，覆盖 252 布局、
  504 次公开容量，逐项核对父版 20 个布局字段及 13 视图绑定。
- 同时检查对齐、容量及存活冲突；五类故意错误描述应被拒绝。
  检查器还要求 forward 实现文本与父版一致，不能用新元数据掩盖调度变化。
- 输出可机读 resource-views.json，明确仅覆盖 workspace；质量 HTTP
  仍在运行。下一步完整门禁后执行合同检查及 4K 时间线。

## 2026-09-21：decoder 资源描述候选开始 HTTP

- 导出实际 DecoderWorkspaceLayout，预算与 forward 仍共用同一构造；
  描述视图引用真实偏移/字节，标注七个 caller-stream 阶段的存活范围。
- 原内存位置、数学和调用顺序保留；描述函数仅用于检查，不在热路径
  解析 JSON。它尚不是调度器或状态提交协议。
- 构建零警告，首项质量及五档 HTTP 开始，尚未接受。下一步门禁后
  链接实际库核对旧布局、视图边界、同时存活冲突及错误合同拒绝。

## 2026-09-21：接受 gate 独立工作区

- 首项质量及完整五档、父版/固定参考范围门禁通过，逐位与布局检查通过。
- 4K HTTP 时间线分配/释放各 362→266/步；255 步各 92310→67830。
  每个 kernel 调用数不变，workspace 增加 64 KiB，性能按持平理解。
- 接受此阶段，保留兼容拥有路径与所有权分支的复杂度代价，不宣称
  吞吐或系统峰值改善，固定参考不变。下一步提交推送，然后将已验证
  布局与 GR 执行依赖绑定成可检查资源描述，先不改变内存位置和顺序。

## 2026-09-21：gate 完整 HTTP、逐位与布局检查通过

- 首项质量 11/11、五档 15/15，输入输出一致、服务退出 0；与父版及
  固定参考均无不利范围分离，性能持平，不重置参考。
- 门禁后 30 组、4953108480 个有限 BF16 值逐位一致；所有权切换、
  放弃帧、gate 保护区与错误恢复通过。252 布局/504 容量核对通过。
- workspace 2347958272→2348023808 字节，增加 64 KiB，工具零警告。
- 4K HTTP 时间线进行中，下一步核对分配次数并决定最终接受。

## 2026-09-21：gate 候选质量 HTTP 11/11 通过

- 首项质量 11/11，HTTP/输出一致性检查通过，failure=null，服务退出 0。
- 同一二进制已开始完整五档性能；数值/布局/profile 尚未执行。
- 候选报告和暂存合同已记录所有权与 stream 有效期。下一步审查完整
  五档重复范围，通过后再执行已准备的 gate 专项，不提前接受。

## 2026-09-21：准备 gate 所有权和布局专项

- 仅准备工具，未编译或执行：逐位对照覆盖借用→拥有→借用的同帧
  切换、放弃帧、容量不足后重用、no-combine、gate 两端保护区。
- 布局工具覆盖 252 组布局和 504 次链接容量，检查 gate 在旧 arena
  之后且与所有子层工作区不重叠，旧区域偏移保持。
- 首项质量 HTTP 仍在运行。后续工具仅在完整五档及双参考范围门禁
  通过后串行执行；下一步等待实际结果，不提前声明通过。

## 2026-09-21：gate 候选构建完成，首项 HTTP 启动

- 必要构建完成，零警告；保存二进制、源码与实际链接库指纹。
- 首项真实 HTTP 质量评估已启动，通过后同一二进制串行跑五档性能。
- 完整矩阵后同时审查直接父版 e550a02 与固定参考 fcb5925；
  有不利范围分离则停止，不执行数值/布局/profile。下一步等待质量结果。

## 2026-09-21：GRFrame gate 工作区候选

- 基于 e550a02，将 gate 放到独立尾部区域；跨子层存活，不能借用 MoE。
  两次 Read/Write 按 caller stream 顺序共用 gate 区域。
- frame 增加明确的借用/拥有状态，容量不足在 mix 前拒绝；错误或析构
  不释放调用者内存，原拥有路径保留。模型精度与数学顺序不变。
- 正在必要构建；尚未运行任何测试。下一步首先质量与五档 HTTP，
  通过后才检查逐位结果、放弃帧/复用、区域容量及实际分配次数。

## 2026-09-21：接受 GRRead down/up arena 暂存

- 完整 HTTP 与父版/固定参考范围门禁通过；数值 30 组逐位一致，
  252 布局、504 容量核对通过，工具构建零警告。
- 4K 时间线分配/释放各 554→362/步，255 decode 步各少 48960 次；
  所有 kernel 调用次数保持不变，workspace 2347958272 字节不变。
- 性能按持平理解，正式参考不重置，不宣称整机峰值或稳定提速。
- 下一步提交推送本阶段，再独立处理 gate 跨子层存活期；保留其他任务改动。

## 2026-09-21：GRRead 完整 HTTP、数值与布局检查通过

- 首项质量 11/11、五档性能 15/15，输出一致、服务退出 0；父版和
  固定参考均无不利重复范围分离，性能按持平理解。
- 门禁后 30 组、2867589120 个有限 BF16 值逐位一致；252 布局、
  504 次实际链接容量通过，workspace 保持 2347958272 字节。
- 工具构建零警告。4K HTTP 时间线运行中，下一步核对分配/释放
  与 kernel 次数，再决定接受及提交；尚无整机峰值或稳定提速结论。

## 2026-09-21：准备 GRRead 暂存布局验收，性能继续

- 已准备布局检查：252 组形状、504 次链接容量查询，覆盖 lowrank
  160/320/640、短/分块长度及 linear/full/PLE；尚未编译运行。
- 数值工具覆盖借用路径、旧默认路径、暂存覆写、容量不足及所有权。
  仅准备代码，完整 HTTP 门禁前不执行。
- 质量已过；性能 1K、4K HTTP/输出检查已完成，原进程持续运行，
  剩余档位及范围审查待完成。下一步依据完整结果决定是否进入专项。

## 2026-09-21：GRRead down/up 质量 HTTP 通过

- 首项质量 11/11，HTTP 输出检查通过、failure=null、服务退出 0。
- 同一候选已开始五档性能，未运行数值专项或 profile。
- 补充机读暂存合同与未执行的数值检查工具；下一步等待完整矩阵并
  对比父版 c8e8331 与固定参考 fcb5925，异常则先做 HTTP 复核。

## 2026-09-21：GRRead down/up 借用工作区候选开始 HTTP

- 前一阶段以 c8e8331 提交并推送 main，保留其他任务文件。
- 主层两次 Read 的 down/up 迁移到 MoE 区域中的不重叠视图；按真实
  lowrank 预算，gate 所有权不变，旧调用仍有分配路径。
- 构建零警告，第一项测试为质量及五档 HTTP；未执行前置测试或 profile。
- 下一步审查直接父版和固定参考的重复范围，通过后才做数值与布局检查。

## 2026-09-21：接受 normed/MoE 别名，预算累计减少 320 MiB

- 首项质量 11/11、五档 HTTP 15/15，44K 初始不利分离经旧→新→旧
  9 请求复核未重现，TTFT/decode 同时与两组旧版范围重叠；异常保留。
- 门禁后 84 布局、168 链接容量通过，唯一允许的重叠为 normed/MoE；
  4K 各 kernel 调用及分配数不变。工具构建零警告，全部服务退出正常。
- 8192 分块模型 workspace 2515730432→2347958272，再减 160 MiB，
  相对两份 normed 阶段累计减少 320 MiB。不是整机峰值实测。
- 正式性能参考不变、不宣称稳定提速。下一步独立迁移 down/up 暂存，
  保留 gate 所有权；五文件草稿尚未应用或构建测试。


## 2026-09-21：44K 相邻 HTTP 复核通过，恢复门禁后检查

- 旧→新→旧各 3/3，输出一致、服务退出 0；候选 TTFT/decode 范围
  同时与前后两组旧版重叠，初始不利分离未重现。
- decode 均值前旧 17.863937、新 17.851025、后旧 17.850416；完整
  原始异常与复核保留，不挑对照、不改阈值、不宣称稳定提速。
- 现开始同一候选的 4K 时间线与布局专项；此前因门禁停止的检查均未执行。
  下一步核对唯一允许的别名、容量及实际调用，再决定最终接受。


## 2026-09-21：完整矩阵收尾，开始 44K 旧→新→旧复核

- 五档 15/15 HTTP/输出检查完成、服务退出 0；200K 未新增不利项，
  唯一门禁待复核为 44K decode 相对直接父版的分离范围。
- profile/layout 等待链按门禁停止，未执行低层检查；相邻 HTTP
  仅跑 45056 token，旧版前置组已启动，精确二进制与参数已保存。
- 下一步等前后两组旧版与候选全部结果，不自动接受或挑选有利对照。


## 2026-09-21：normed/MoE 44K decode 进入相邻 HTTP 复核

- 44K 候选 decode 范围 17.854142–17.857208，相对直接父版
  17.857344–17.899210 不利分离；与固定参考范围重叠，不忽略门禁。
- 完整矩阵继续收尾。旧→新→旧精确二进制 HTTP 复核已排队，覆盖
  完整结果中被标记的不利档位；仅在当前 owner 结束后串行执行。
- 不运行 profile/布局专项，不自动接受；保留初始异常与全部相邻结果。
- 等待期间 down/up arena 五文件草稿已保存 .q4t-work/prep/grread-scratch-
  20260921/，保持 gate 所有权与旧调用兼容、传递真实 lowrank，未应用
  或构建测试。下一步优先判断当前别名回退是否重现。


## 2026-09-21：核对 GRRead 暂存的后续 arena 边界

- 当前 normed/MoE 候选正在五档性能中，运行时代码冻结。
- 源码显示主层 down/up 只活在 Read 内，每步共 192 对异步分配/释放；
  若迁移需 normed/down/up 三个独立视图，当前模型 8192 分块合计 325 MiB。
- gate 跨越子层，不能与其混用 MoE 区域；后续先分离 down/up 迁移与
  gate 所有权变化，并用实际 lowrank 核对容量，不预设吞吐或流量收益。
- 仅完成静态范围推演，未实现或测试；下一步仍是当前候选的完整门禁。


## 2026-09-21：normed/MoE 别名质量 E2E 通过

- 首项质量 HTTP 11/11，包含 200K，输入输出检查通过、服务退出 0。
- 完整五档性能已启动；源码与二进制继续冻结，布局专项尚未执行。
- 下一步比较直接父版与固定参考全部重复范围，再决定是否细分析。


## 2026-09-21：准备 normed/MoE 别名边界检查

- 首项质量 HTTP 进行中，运行时冻结；门禁后工具仅编写，未构建执行。
- 计划覆盖 84 布局、168 链接容量，检查唯一允许的重叠、其他物理区
  两两不交叠、对齐/边界与容量差额；另按模型层/head 最大值核对预算。
- 静态确认多 token MoE 默认 4 CUDA stream，评估单流 HTTP 仍覆盖
  辅助 stream 汇合路径；candidate 未修改 stream/event 协议。
- 下一步先完成质量和五档性能，再核对实际布局与时间线。


## 2026-09-21：接入 normed/MoE 工作区别名候选

- 基线 cd1a624；normed 偏移指向 MoE 区域，预留 max(MoE bytes,
  normed bytes)，移除独立 normed 区。输入输出和 HC GEMM scratch 独立。
- 只改变物理布局，GRFrame、子层数学及 stream 协议不变；两次 Read 的
  normed 最后读者在 MoE 覆盖之前，不能因此免除地址变化的性能验收。
- 构建零警告，首项质量 HTTP 已启动，随后完整五档；预计减少的 160 MiB
  仍是推导，布局/容量专项与时间线必须等完整门禁后执行。
- 下一步先审查质量与五档，再核对唯一允许的重叠及各区域容量边界。


## 2026-09-21：接受 decoder workspace 布局单源化

- 首项质量 11/11、五档性能 15/15，输入输出一致，服务退出 0；对父版
  与固定参考无不利分离范围。正式性能参考保留，不宣称稳定提速。
- 完整门禁后 84 个布局的 14 字段、168 个实际链接容量值全部与父版
  相同，工具构建零警告；4K 各 kernel 调用数与分配数不变。
- 三处重复容量/偏移维护收敛到一个值类型构造；父版物理布局保持。
  该结构简化不增加 stream、分配、解释器或热路径 JSON 解析。
- 下一步独立验证 normed/MoE 区域别名；可机读提案和 .q4t-work/prep/
  草稿已准备，但未应用。继续先完整 HTTP，后布局与生命周期核对。


## 2026-09-21：记录 normed/MoE 区域别名的独立提案

- 布局单源化候选仍在完整性能测试，源码与二进制未改变。
- 静态核对 normed 最后消费者、MoE device 单 stream 与多 token 辅助
  stream 汇合，记录三个视图及容量 max 约束；HC GEMM scratch 必须独立。
- plans/normed_moe_alias.json 仅为可机读提案，未应用或测试；预计预算
  差额不当成已实现节省，也不推断 DRAM 流量下降。
- 下一步先完成当前候选验收，再逐阶段验证物理别名。


## 2026-09-21：workspace 布局单源化质量 E2E 通过

- 首项质量 HTTP 11/11，包含 200K，输入输出检查通过、服务退出 0。
- 完整五档性能已启动；运行时源码保持冻结，门禁后的布局专项未执行。
- 下一步审查直接父版和固定参考的重复范围，再决定是否进入细分析。


## 2026-09-21：准备门禁后的新旧 workspace 布局对照

- 当前首项质量 HTTP 仍在运行，源码和二进制冻结。
- 对照工具直接提取父版 carve 算式与新构造，计划覆盖 84 个布局的
  14 字段及 168 个实际链接容量值，含 T<=4 与长 max_len 分支。
- 完整 HTTP 通过且指纹吻合后才构建执行；工具目前仅编写，尚未运行。
  下一步先完成质量与五档性能，不把布局看起来相同当成性能通过。


## 2026-09-21：接入 decoder workspace 布局单源化候选

- 基线 657b2e8，使用 DecoderWorkspaceLayout/MakeDecoderWorkspaceLayout
  同时定义容量与偏移，公开容量查询和 forward 共用；保留子模块区域独立。
- 删除三处重复容量/carve 计算，新增值类型布局描述；没有新分配、同步、
  stream、解释器或每层 JSON 解析。模型形状下目标是物理布局完全不变。
- 同步修正 decoder header 的旧 GR 调用链和 workspace 所有权注释。
- 构建零警告，首项质量 HTTP 已启动；下一步完整五档，全部通过后才
  逐项核对区域偏移/容量及调用次数。尚未接受，也未宣称性能/容量收益。


## 2026-09-21：接受 normed 复用，模型 workspace 预算减少 160 MiB

- 质量 11/11、五档性能 15/15，输入输出一致、服务退出 0；对直接父版
  和固定参考无不利分离范围。正式参考不重置，不宣称稳定吞吐提升。
- 门禁后 4K HTTP 全部 kernel 调用次数与父版一致，异步分配次数不变。
  编译容量核对 21 组层预算、7 组模型最大预算，全部通过、零警告。
- max_prefill=8192 的共享 workspace 为 2683502592→2515730432 字节，
  减少 160 MiB；这是实际编译申请预算，不是整机峰值测量。
- 保留一份明确命名的 normed 区域，删除第二份容量与偏移维护。
  下一步统一 workspace 容量查询和 forward 偏移来源；草稿仅在
  .q4t-work/prep/decoder-workspace-layout-20260921/，未应用或构建。


## 2026-09-21：核对下一阶段 workspace 布局单源化范围

- 当前复用候选前三档 HTTP 已完成，长档继续；源码与二进制未变。
- 静态确认容量函数、forward 容量检查、pointer carve 三处重复维护，
  后续先用共享 offset/bytes 描述消除重复，保持原区域顺序和物理地址。
- 真实 full 参数中的 max_len 影响 T<=4 的 indexer scratch；保留空 full
  兼容预算，不能只凭 T 固定容量。子模块间别名继续独立成下一次实验。
- 布局单源化尚未实现或测试，下一步仍先完成当前五档验收。


## 2026-09-21：normed 复用质量 E2E 通过

- 首项质量 HTTP 11/11，包含 200K，服务退出 0、输入输出检查通过。
- 完整五档性能已启动，仍没有运行前置数值或 profile；运行时源码与
  二进制保持冻结。下一步同时对照直接父版本与正式固定参考。


## 2026-09-21：核对 normed 复用边界并修正可机读合同

- 当前 normed 复用候选仍在首项 HTTP，未运行前置容量/数值测试。
- 静态核对 ModelLoad 取所有 decoder/head 的最大 workspace 才分配，
  单层预算差额不能直接当整机峰值内存下降。
- GRFrame JSON 将原始 baseline 顺序与已接受顺序分开，删除“C++ frame
  未实现”的过期条目；仍明确 JSON 不可执行、arena planner 未实现。
- 下一步先完成本轮 E2E，再验证实际预算与布局；跨子层 arena 复用需
  单独审计 MoE 辅助 stream 汇合，不能凭 host 返回提前覆盖。


## 2026-09-21：启动 GRFrame normed 区域复用候选

- 第一阶段 7a34dd3 已推送 main。下一候选只合并两个 GRRead 的 normed
  区域，并同步缩减 DecoderLayerWorkspaceBytes 与 forward carve。
- normed 不被 Write 保留，两次 Read 位于同一 stream 的顺序链；
  变量从误导的 d_res_a/d_res_m 改成 d_normed，保持子模块 workspace 独立。
- 形状下 decoder 预算减少 20480*T 字节（T=8192 为 160 MiB），不宣称
  系统峰值实测下降；组合/PLE trunk 的地址也随 carve 前移，性能须重验。
- 构建零警告，首项质量 HTTP 启动，下一步完整五档；不做前置测试。


## 2026-09-21：接受 GRFrame read/write 第一阶段

- 质量 11/11、五档 15/15，输入输出相同、服务退出 0；全部性能范围与
  fcb5925 重叠，按持平接受结构步骤，正式性能参考不变。
- 门禁后 30 组、2215864320 个有限 BF16 值逐位相同；覆盖 normed 后
  Write 正确，复用/重复消费拒绝/提前退出路径及旧 Combine API 通过。
- 4K HTTP 全部 96 个 prefill、24480 个 decode GR 子层确认 gate 前移；
  kernel 与分配次数不变。两个大工作区保留，无峰值容量或稳定提速主张。
- 原因：明确 gate 所有权和 normed 最后消费者，为生命周期复用建立边界；
  新增类型与旧 API 并存仍有成本，不声称净复杂度已经下降。
- 同步精简 STATUS 并保留完整历史快照。下一步独立验证两个 normed
  区域合并，仍须先完整质量与五档 HTTP。


## 2026-09-21：精简当前状态入口并保留历史

- 将 fcb5925 的完整 STATUS 正文保存为 HISTORY_STATUS_2026-09-21.md，
  当前入口聚焦候选、接受版、约束和未解决事项，避免多轮“最新”并存。
- 保留全部历史证据与只追加日志；其他任务未提交的 T4000 内容仅留在
  当前工作区，未复制进历史快照，也不纳入本任务提交。
- 运行产物与验收原进程不变；下一步仍完成 GRFrame 200K HTTP 与后续门禁。


## 2026-09-21：明确 GRFrame 第一阶段的结构收益与代价

- 阶段报告明确：移除晚期 normed 依赖是目标，两个物理区域尚未缩减；
  新增 frame 状态和旧 API 并存，不能直接宣称净复杂度或峰值内存下降。
- 记录 stream 生命周期、异常清理与门禁后对照范围，避免将 API 封装
  等同于完整数据流执行器。当前性能原进程继续，运行产物未改。
- 下一步完整五档后判断接受；生命周期检查不能覆盖性能回退。


## 2026-09-21：补齐 GRFrame 对照的旧 API 兼容覆盖

- 新旧 frame 输出对照之外，工具追加当前 HyperConnectionCombine
  与 fcb5925 原路径的直接比较，覆盖共享 helper 提取后的旧入口。
- head/MTP 仍使用该 API，但此数值覆盖不等于完整 MTP E2E；未扩大
  当前验收声明。工具只编写未构建，完整性能门禁仍在前。
- 当前 8K HTTP 已完成输出检查，下一步等待长档与完整范围审阅。


## 2026-09-21：GRFrame 质量通过，准备生命周期对照

- 首项质量 HTTP 11/11，输出一致、服务退出 0，完整五档性能运行中。
- 门禁后工具从 fcb5925 提取原 HC kernels/Mix/Combine，与实际 frame
  API 比较 mixed/normed/最终输出；额外覆盖 normed 后再 Write，以验证
  大张量不再被晚期消费。T=1/3/33/257/8192，含 mixer passthrough。
- 工具还覆盖空帧/重复消费拒绝、复用及提前退出清理；只编写未构建。
  候选库与源码指纹已保存，完整 E2E 与 4K 时间线后才执行。
- 下一步先核对性能，不因生命周期设计合理而免除回退门禁。


## 2026-09-21：接入 GRFrame read/write 第一阶段候选

- 基线 fcb5925；非复制 frame 借用 residual、拥有 BF16 gate，Read
  在子层前完成 inject，Write 后释放；异常退出析构清理，不保留 normed。
- 主层两个子层接入；旧 head/MTP API 保留，共用 inject helper；两个
  normed 区域与 arena 预算/carve 均未改，先隔离执行顺序影响。
- 更正原调用处 silu(down)/hc 注释为源码实际 SiLU(down/hc)，不改该计算。
- 构建零警告，首项质量 HTTP 已启动，无前置测试；下一步完整五档。
  尚未证明精度、性能或峰值容量收益，后续专项须等完整 E2E。


## 2026-09-21：接受短路径 top-k 寄存器网络

- 质量 11/11、五档性能 15/15，输出一致、服务退出 0；4K decode
  +0.76%、8K TTFT -1.08%，其余重复范围重叠，正式五档参考整体更新。
- 完整门禁后 4K 时间线及 352 组、277598448 个槽/长度逐位通过。
  选择累计 decode 226.446→60.105 ms、prefill 25.608→10.869 ms；
  调用数不变，REG 40→44，SHARED 不变、STACK/LOCAL=0，构建零警告。
- 保留 dense/sparse、同分 ID、槽序和组尾，无精度或可见性折中。
- 下一步提交推送，再按 GRFrame 合同独立演进 read/write 生命周期。


## 2026-09-21：记录首份 GR 单子层可机读合同

- JSON 提案列出 residual/normed/mixed/inject_gate 等所有权、形状、
  逻辑字节，当前/提议节点顺序与数值边界，并绑定相关源码指纹。
- 标记 proposal_not_executable；无 schema 验证器、执行器或 arena 别名，
  第一阶段仍保留两个 normed 区域，不宣称峰值内存节省或完整 R0/R1。
- 当前短 top-k 候选的 200K HTTP 继续运行；本次仅设计元数据，未改产物。
- 下一步完成当前验收，再以该合同约束独立 GRFrame 候选。


## 2026-09-21：明确 GRFrame 的 SiLU 数值顺序

- SiluDivKernel 先将 BF16 down 转 FP32 并乘 inv_hc，再 SiLU、舍入
  BF16；调用处注释误写成 silu(down)/hc，不能据此实现新 read 路径。
- 静态合同补齐准确顺序，当前 kernel 和二进制保持冻结；不是新数学修复。
- 下一步继续当前五档 HTTP，GRFrame 中间值对照留到独立候选完整 E2E 后。


## 2026-09-21：整理短路径 top-k 的验收边界

- 阶段报告区分已接受 4K 局部成本、当前 HTTP 与门禁后计划覆盖；
  明确全部位置槽逐位比较，不能以选择集合相同代替注意力累加顺序相同。
- 当前性能原进程继续运行，未改二进制，没有前置数值或 profile。
- 下一步完成五档后核对范围，再执行串行后续检查。


## 2026-09-21：短路径 top-k 首项质量通过

- HTTP 质量 11/11，包含 200K，输出一致、服务退出 0。
- 完整五档性能已启动，未执行数值或 profile；正式参考保持不变。
- 下一步完成各档重复范围核对，门禁通过后再细分析。


## 2026-09-21：核对 runner 到 GRFrame 的生命周期入口

- d_res_a/d_res_m 实为 normed，各跨子层存活以供晚期 inject 投影，
  使用区间不重叠；原始 residual 则为 d_trunk/d_combined。
- 建立静态合同，分开 inject 提前、normed 生命周期合并与 arena 别名；
  每份 160 MiB（T=8192）仅为现有区域容量，不冒充已测峰值节省。
- 识别 head/MTP 旧接口和 use_combine=false 兼容边界，尚未实现。
- 下一步先完成当前 top-k HTTP，再决定架构原型落地，不混入运行产物。


## 2026-09-21：准备短路径选择的门禁后对照

- 候选构建零警告，首项质量 HTTP 继续运行，尚无完整质量/性能结论。
- 已编写 352 组精确选择对照：1/3/33 行的组数边界、四类分数和组尾，
  另含 8192 行 mixed dense/sparse 与上边界；比较全部槽和长度。
- 工具尚未构建运行；完整 E2E 后才串行采集 4K HTTP 并执行专项。
  公共比较模板保持不变，按源码快照与二进制 SHA 校验。
- 下一步先等待真实 HTTP，任何不利回退停止后续细分析。


## 2026-09-21：接入短路径 top-k 寄存器网络候选

- 基线 4f4b8d8 已推送；4K 时间线选择成本约 0.888 ms/decode 步，
  prefill 累计 25.608 ms。独立 CUDA 替换固定窗口的内联排序网络。
- 沿用原 dense 展开、同分 ID、排序槽序、尾位置及 scratch；公共
  比较模板不改，单 token 和多 token 分派共同验收。
- 必要构建进行中，下一步首项质量 HTTP，再完整五档；无前置测试。


## 2026-09-21：接受短上下文索引打分 CTA 映射

- 质量 11/11、五档性能 15/15，输出一致、服务退出 0；4K decode
  +6.20%，其余四档与全部 TTFT 范围重叠，正式五档参考整体更新。
- 完整 E2E 后 288 组、589824 个有限 FP32 分数逐位一致，构建零警告。
  配对 4K HTTP 打分累计 1158.343→280.322 ms（-75.80%），调用数不变，
  REG=37 与 SHARED/STACK/LOCAL=0 不变；时间线和数值工作全部串行。
- 映射分散候选组而保持每分数累加和槽位，无新缓冲或回读。
- 下一步提交推送，再独立评估短路径 TopkSelect 比较网络；不改精度。


## 2026-09-21：索引打分完整 HTTP 门禁通过

- 质量 11/11、五档性能 15/15，输出一致、服务退出 0。4K decode
  +6.20%，其余四档 decode 与全部 TTFT 范围重叠，未见可辨识回退。
- 完整门禁后启动候选 4K 时间线。编译资源旧/新 REG=37，
  SHARED/STACK/LOCAL=0，没有新增寄存器或 shared 用量。
- 下一步串行完成数值和旧版配对采集，再作最终接受与提交。


## 2026-09-21：区分短上下文 top-k 的 dense 与 sparse 成本

- 静态核对可见组数<=512 时直接展开，超过后才排序；已接受 1K
  采集该 kernel 仅 10.867 ms/255 步，不能套用长路径选择成本。
- 下一步等当前候选 E2E 与 4K 配对时间线再确定后续优先级；
  未改动运行产物，也未执行新的低层采集。


## 2026-09-21：索引打分四档性能完成

- 1K/4K/8K/44K 输出与基线一致；4K decode 16.702→17.737 tok/s，
  约 +6.20%，三次范围不重叠。其余三档 decode 及各档 TTFT 范围重叠。
- 200K 原进程继续运行，未启动数值或 profile，正式参考不变。
- 下一步等待最后一档后统一判断；不能将短档收益代替完整性能验收。


## 2026-09-21：更正筹备入口的实现状态

- 筹备 README 仍称尚无实现或实测，与已提交的 MoE、QSA、decode
  top-k 子链不符；改为区分已验收 runner 子链和未实现的完整执行计划。
- 补齐三个执行合同的入口，保留 R0/R1 与 GRFrame 完整闭环未完成的事实。
- 下一步仍完成当前索引打分 E2E，运行产物未修改；不把文档更新当作
  新架构或候选性能已验收。


## 2026-09-21：索引打分候选质量 HTTP 通过

- 首项质量 11/11，包含 200K，输出与接受版一致，服务退出 0。
- 五档性能已启动；新增阶段报告，明确候选映射、容量不变、数值工具
  计划覆盖量与串行配对时间线，不提前宣称性能或逐位通过。
- 下一步等待完整矩阵；门禁通过后才执行候选 4K 时间线、数值及旧版配对。


## 2026-09-21：准备索引打分的 E2E 后数值对照

- 工具提取 fb49668 的原 SIMT 打分 kernel，与候选实际公共入口比较。
  288 组包含组数边界、尾位置、三个序列切片与随机/抵消/正负零输入。
- 工具要求同一二进制质量与完整性能记录及接受标记，当前仅编写，
  没有构建或运行。后续由质量→性能→候选 4K 时间线→数值脚本串行执行。
- 下一步完成当前 HTTP；之后补旧二进制同一 4K 请求时间线，避免拿
  1K 局部数字与 4K 采集直接比较。


## 2026-09-21：短上下文索引打分候选首项 HTTP 启动

- 前一轮 decode top-k 已提交推送 main（fb49668）；独立接入短上下文
  SIMT 打分候选，以八个 CTA 分担 2048 组，保留每分数累加与输出槽。
- 不添加缓冲，不替换空 seq_id 的 BF16 GEMM 路径；旧/新二进制、
  源码快照与 patch 保存于 indexer-decode-cta-20260921。
- 构建零警告，第一项质量 HTTP 已启动，无前置低层测试。
- 下一步完整五档性能；全部 E2E 通过后再做分数逐位及 4K 时间线核对。


## 2026-09-21：接受 decode 多级 top-k 寄存器网络

- 首项质量 11/11、完整五档性能 15/15，输出一致、服务退出 0。
  8K/44K/200K decode +2.23%/+4.03%/+4.98%，短档与 TTFT 范围重叠。
- 完整 E2E 后 200K 时间线完成，选择累计 1050.744→249.847 ms，
  调用数不变；原 streaming merge 基本持平。数值 64 组、2745120
  个分数/ID/位置逐位一致，原 streaming 专项 1 项通过，构建零警告。
- 原因：保留比较语义与 scratch，将线程内交换留在寄存器、warp 内
  用 shuffle；没有减少候选覆盖或改变分数。正式五档参考整体更新。
- 下一步提交推送本轮，再单独接入短上下文 SIMT 索引打分映射候选。


## 2026-09-21：decode top-k 完整 HTTP 门禁通过

- 质量 11/11、五档性能 15/15，输出摘要一致、服务退出 0。
- 8K/44K/200K decode 约 +2.23%/+4.03%/+4.98%，新最低值高于旧最高值；
  短档 decode 与全部 TTFT 范围重叠，未见可辨识回退。
- 完整门禁之后启动 200K HTTP 时间线；下一步待采集完成，串行核对
  decode 精确选择及 streaming 原路径。尚未最终接受、提交或更新正式参考。


## 2026-09-21：decode top-k 四档完成，长档继续验收

- 原始 HTTP 结果确认 1K/4K/8K/44K 输出一致；8K/44K decode 均值
  较接受版约 +2.23%/+4.03%，1K/4K 范围重叠。
- 200K 客户端与服务进程仍在运行，尚未宣布完整性能通过；运行产物冻结。
- 下一步由串行门禁完成 200K、时间线和数值专项，再决定是否接受。


## 2026-09-21：记录短上下文索引打分的候选约束

- 等待当前 200K E2E 时静态审阅短路径：per-sequence SIMT 打分每 token
  一个 CTA，拟按候选组增加 CTA，保留每分数 h/d 累加顺序。
- 明确不替换 seq_id 为空的 GEMM 路径，不新增 scratch，也不声称已有
  4K 局部实测收益。方案未实现，当前运行产物保持冻结。
- 下一步先完成 decode top-k 验收提交，再决定接入该独立候选。


## 2026-09-21：整理 decode top-k 报告与 8K 分支边界

- 新增阶段报告，区分已完成档位、既有 scratch 容量、共享模板影响范围
  及尚未运行的专项；完整性能矩阵继续运行。
- 根据请求位置与分支条件，8K 的 255 次 decode 中前三步仍短路径，
  随后 252 步跨入长上下文选择；这是源码推导，不是 8K trace 实测。
- 下一步完成长档验收，再核对 200K 时间线与精确选择结果。


## 2026-09-21：补充共享比较模板的双路径验收安排

- 完整五档 HTTP 与 200K 时间线通过后，先运行 decode 精确选择对照，
  再构建并只运行 streaming_topk_exact，核对模板移动影响的旧路径。
- 串行脚本有 HTTP、时间线退出及导出门禁，失败停止，无 GPU 工作重叠。
  尚未构建或执行这些专项。
- 已完成 1K/4K E2E 输出一致、重复范围与基线重叠；下一步等待其余档位。


## 2026-09-21：decode top-k 首项质量 HTTP 通过

- 11/11 质量请求通过，包含 200K，输出与接受版一致，服务退出 0。
- 完整五档性能随后启动；尚无性能、数值专项或局部耗时结论。
- 下一步先审阅五档范围，门禁通过后才采集 200K 时间线并执行精确选择对照。


## 2026-09-21：准备 decode top-k 精确选择对照工具

- 编写门禁工具 compare_decode_topk.py 与模板，提取 Git 原比较网络及
  三阶段 kernel，与候选公开选择入口比较分数、ID、展开 token 和长度。
- 覆盖 2049–65536 组、T=1/4、随机、同分/正负零、全无效、尾窗口胜出，
  多层 ping-pong 的已写区域全部检查。尚未构建或运行。
- 当前质量 HTTP 完成 10/11，200K 仍运行；下一步先完成质量及五档性能。


## 2026-09-21：接入 decode 多级 top-k 寄存器网络候选

- 备份接受版 52ca9ef 的精确二进制和五档参考，新增独立 decode_topk.cu，
  固定 2048/512 选择链沿用原分数、scratch、ping-pong 和最终 token 扩展。
- 比较模板提取为私有 bitonic_topk.cuh，streaming 与 decode 共用，
  保留严格比较与同分 ID 次序的设计目标，需实测核对。
- 构建零警告，首项真实 HTTP 质量已启动；随后完整五档性能，
  无前置专项、单测或 profile。下一步先审阅 E2E 结果。


## 2026-09-21：复核当前 200K 时间账并选择多级 top-k 候选

- 已接受 52ca9ef 精确二进制采集 200K HTTP，摘要一致，服务/采集器退出 0，
  完整导出与解析，255 次 decode 前向，counts 回读为 0。
- decode 每步累计 BF16 GEMV 37.045 ms、QSA 4.961 ms、OnePassScore
  2.846 ms、三阶段 top-k 4.121 ms；prefill 主成本仍 attention/GDN/merge。
- 新增 DECODE_TOPK_PLAN：保持原分数、排序网络、同分 ID 与候选范围，
  用已有寄存器网络替换固定窗口选择子链；未实现、无收益承诺。
- 下一步接入独立 CUDA 实现，构建后仍从完整 HTTP E2E 开始。


## 2026-09-21：接受 QSA 单 token 输出列四分并更新基线

- 质量 11/11、五档性能 15/15、输出一致、服务退出 0；decode 提升
  1.31%–3.52%，各档三次范围不重叠，TTFT 范围重叠。
- 完整 E2E 后，HTTP 时间线与数值专项串行完成：34 组、442368 个
  有限 BF16 输出逐位一致，工具构建零警告；1K QSA 累计
  924.018→720.049 ms，3060 次调用不变，寄存器 56→40，无 stack/local。
- 整体更新正式五档参考，记录源码容量与编译/Nsight shared 口径差异；
  未采 DRAM 计数，不把 2.5 倍逻辑读取当作实际物理流量。
- 本次接受并准备提交推送，下一步按新执行预算选择后续候选。


## 2026-09-21：整理 QSA 候选阶段报告与覆盖边界

- 记录已完成 1K/4K/8K/44K HTTP 数据，明确 200K 尚未完成，未接受候选。
- 报告区分逐元素数学设计约束、每 CTA 与整个 grid 的容量、逻辑字节与
  实际 DRAM 流量，以及单流文本与 MTP/多模态/并发的覆盖边界。
- 下一步等待完整 E2E，再由串行门禁流程采集时间线并执行数值对照。


## 2026-09-21：准备 QSA 输出分片的门禁数值工具

- 编写 compare_qsa_decode_split.py 与模板，提取原 Git kernel，
  调用候选公开单 token 入口，覆盖空/尾块/2052 选择、非连续页及 pooled seq。
- 工具检查完整质量和五档性能门禁、二进制 SHA、源码快照；未构建或运行。
  正在进行的长上下文 HTTP 不受影响。
- 下一步先完成五档性能；通过后等 HTTP 时间线采集结束，再执行数值专项。


## 2026-09-21：QSA 四分输出首项质量 HTTP 通过

- 11/11 质量请求通过，包含 200K，输出与接受版一致，服务退出 0。
- 随后启动固定五档性能；完整性能通过及范围核对后才会采集时间线。
- 尚未进行数值专项或 profile，未接受或更新正式参考；下一步核对五档性能。


## 2026-09-21：接入 QSA 单 token 输出维度四分候选

- MoE 98206de 已推送 main；备份精确接受二进制与五档参考后，接入
  独立 qsa_decode.cu，固定单 token 模型形状两 CTA→八 CTA。
- 四分输出列使每 CTA 仅累加 64 列 V，重复 QK/softmax，保留逐元素
  累加次序设计；逻辑 K+V 读取约 2.5 倍，实际收益未知。
- 正在构建，下一步首项真实 HTTP 质量，再完整五档性能；没有前置测试。


## 2026-09-21：接受 MoE 设备执行最终产物并更新正式基线

- 最终质量 11/11、性能 15/15、707520 个值逐位一致，最终 1K 时间线
  255 次前向、counts 回读 0、kernel 442935，分配/释放各 141270，
  与首轮计数一致，服务和采集器退出 0、导出完整。
- 五档 decode 提升 6.65%–7.55%，TTFT 范围重叠；整体更新正式参考，
  不混用首轮数据。报告列明固定形状、内存预算、精度及覆盖限制。
- 本阶段接受并准备提交推送；其他任务路由追踪文件与条目排除。
  下一步接入已准备的 QSA 四分输出候选，再从真实 HTTP 开始验收。


## 2026-09-21：MoE 最终产物完整 HTTP 与数值专项通过

- 最终质量 11/11、性能 15/15，服务退出 0，输入/输出摘要一致；五档
  decode 为 18.266/16.282/17.089/16.645/15.736 tok/s，相对 GDN
  基线提升 6.65%–7.55%，TTFT 范围重叠，且相对首轮候选无明确回退。
- 完整 HTTP 后，最终源码的 12 组、707520 个值逐位一致，无非有限值。
- 最终 1K HTTP 时间线复核已启动；完成后整体更新正式参考并提交，
  首轮与最终产物证据独立保留，不拼接性能数据。


## 2026-09-21：QSA 四分输出草稿保存在独立工作产物目录

- 在 .q4t-work/qsa-decode-split-20260921 准备独立 CUDA/头文件草稿，
  保存原 attention 快照；没有接入 src/include/CMake，也没有构建或测试。
- 静态核对四个 64 列分片、K 全宽/V 局部搬运、尾块清零与原同步边界；
  不改变正在验收的 MoE 二进制。
- 下一步仍先完成 MoE 最终 200K 性能与提交，再接入 QSA 候选首测 HTTP。


## 2026-09-21：静态推演下一项 QSA decode 执行映射

- 等待 MoE 最终 HTTP 期间审阅 QSA：T=1 时 grid 只有两个 CTA，
  记录输出维度四分映射，保持每元素 QK/softmax/PV 次序，避免跨 CTA 归约。
- 明确代价：QK/K 重复四次，逻辑 K+V 读取约 2.5 倍；收益未测、源码未改。
- 合同见 dataflow-engine/QSA_DECODE_PLAN.md；下一步先完成 MoE 验收提交，
  再实施独立候选，仍先完整 HTTP，不并行运行 GPU 实验。


## 2026-09-21：MoE 最终二进制质量 HTTP 通过

- 格式整理重构建后的最终二进制已完成首项质量 HTTP 11/11，包含 200K，
  输出与首轮一致、服务退出 0；随后自动启动固定五档性能复验。
- 证据独立保存在 moe-device-decode-final-20260921，不覆盖首轮记录。
- 下一步核对完整五档范围与原接受版/首轮候选，满足门禁后收尾提交。


## 2026-09-21：同步数据流筹备状态与 MoE 子链事实

- 更新 dataflow-engine/STATUS.md，移除“所有方案未实现”的过时描述，
  区分已有 MoE D5 子链、首轮验收证据与尚未完成的 GRFrame/R0/R1。
- 最终二进制复验仍在运行，未更改正式参考，也未将局部实现当作整个引擎完成。
- 下一步完成复验与提交后，以新时间线重排执行和容量预算。


## 2026-09-21：MoE 最终代码格式整理与二进制重新验收

- 使用仓库 clang-format 整理新增源码、头文件和专项模板；重构建零警告。
- 最终二进制摘要与首轮候选不同，不沿用首轮产物验收作最终证明。
  首项质量 HTTP 已启动，随后完整五档性能，复用同一输入与输出参考。
- 首轮 E2E/数值/时间线证据保留；下一步核对最终复验并更新正式基线、提交。


## 2026-09-21：MoE 配对 HTTP 时间线核对完成

- 精确相邻旧/新二进制各采集一条 1K 请求，输出一致、采集与服务正常退出。
- 255 次 decode 中 counts 回读 12240→0、stream 同步 12496→256、
  kernel 908055→442935，分配与释放仍各 141270 次。
- 因旧专家 kernel 跨流重叠，不将累计耗时差相加作为墙钟因果分解；
  正式速度仍使用无 profiler 的五档 E2E。
- 新增 MoE 执行报告，下一步完成最终源码审查、参考更新与阶段提交。


## 2026-09-21：MoE 设备执行完整 E2E 与数值核对通过

- 完成五档 15/15 请求，输出与输入摘要一致，decode 提升 6.71%–7.65%，
  新旧三次范围均不重叠，TTFT 范围重叠。质量已通过 11/11。
- 完整 HTTP 门禁之后运行数值工具，12 组、707520 个值逐位一致，
  覆盖专家排列、每专家 scale、量化与 BF16/FP32 边界；不是全面业务精度证明。
- 首次数值工具构建因未使用常量警告停止，移除后零警告通过，失败记录保留。
- 为核对真实调度变化，已完成相邻旧二进制 1K HTTP 采集，正在采集新二进制。
  尚未接受或更新正式参考；下一步核对调用与同步，再形成提交边界。


> 本文只追加不修改。全部日志索引见 [README.md](README.md)。

## 2026-09-21 — MoE GPU 描述符首档性能改善，完整矩阵继续

**结果**：1K 三条输入输出摘要与基线一致，decode 16.984→18.282
 tok/s（约 +7.65%），新范围 18.206–18.322，高于旧最高 17.025。
TTFT 0.826 秒与旧范围重叠。服务仍在执行后续档位。

**下一步**：完成 4K/8K/44K/200K；不以短档收益提前接受。候选未
提交，正式参考未更新，数值与 profile 仍等待完整 E2E 门禁。

## 2026-09-21 — MoE GPU 描述符质量 E2E 通过，开始五档性能

**结果**：必要构建零警告，第一项真实 HTTP 11/11 精确正确（含
200K），服务退出 0。日志确认 GU N1280/K2560 与 DN N2560/K640
的 grouped NVFP4 均启用，运行时 cuBLASLt=130501，没有静默回退。

**下一步**：完整五档各三次性能，比较相同输入输出与重复范围。
候选仍未接受；没有前置库探针、数值专项或 profile。

## 2026-09-21 — MoE 单 token GPU 描述符候选接入

**实现**：仅 M=1/E=512/k=10/hs=2560/moe_is=640 分派独立 CUDA
路径。GPU 准备指针/scale/alpha/shape，两个 grouped GEMM，保留
BF16 GU/DN 及原量化表达式，固定 slot 合并；同流临时 scale/描述符
所有权，没有 counts D2H/host 专家循环。host 缓存 shape 算法，每次
重新绑定临时指针；无可用算法时明确失败，不静默回退。

**下一步**：必要构建后第一项真实 HTTP 质量 E2E，通过后五档性能。
尚无兼容性或性能结论，无前置库探针、单测、bench、profile。
证据 `.q4t-work/e2e/moe-device-decode-20260921/`。

## 2026-09-21 — MoE GPU 执行合同与库接口核查

**做了什么/为什么**：从已接受 runner 的 counts 回传及 host 专家循环
核对执行依赖，形成 [单 token MoE 合同](../../dataflow-engine/MOE_DEVICE_PLAN.md)。
本机 Toolkit 13.3 携带 cuBLAS 13.5.1；官方 grouped NVFP4 文档及
NVIDIA 实现支持继续研究，不把 Toolkit 版本误当库版本。固定十个
slot 可以避免 host 活跃专家数，但仍需每专家 scale/alpha、BF16
中间舍入及原 slot 累加。十份独立 scale atom 要按 204800 B 计账，
不是原 SfBufferSize(10,K)=20480 B。记录源码与头文件 SHA、链接路径。

**文档更正**：架构文档去掉“手写 SIMT 数学上无法超越”的泛化结论；
历史实验保留，但不能否决其他 grouped/tensor-core 执行组织方式。

**下一步**：实现独立固定形状入口，首测真实 HTTP；接口存在不等于
本机形状可用，不进行前置能力 probe，不静默回退后宣称通过。
本阶段只读代码/头文件/官方材料并改文档，没有 runtime 改动或新测试。

## 2026-09-21 — GDN 连续读取接受，正式五档参考更新

**证据**：质量 11/11、性能 15/15 后，240 组输出/状态逐位一致。
200K HTTP 确认扫描累计 33.552→31.375 秒（-6.49%），900 次调用
不变；attention/top-k 基本不变。REG=72、STACK/LOCAL=0 不变，
SASS 对应 q/k 八条 U16 load→两条 64-bit load，无额外缓冲或 launch。

**决定/下一步**：接受。正式参考整体更新为本次无 profiler 五档，
200K TTFT 165.127 秒、44K 31.884 秒，decode 范围与旧版重叠。
报告 [GDN 连续读取](../GDN_VECTOR_LOAD_2026-09-21.md)。继续核对模型
执行计划的 host 依赖与资源所有权；不将局部指令收益推广成带宽结论。

## 2026-09-21 — GDN 连续读取完整 E2E 与状态数值专项通过

**结果**：质量 11/11、性能 15/15、输出摘要一致，服务均退出 0。
200K TTFT 167.385→165.127 秒，44K 32.379→31.884 秒，新旧范围
不重叠；五档 decode 范围重叠。没有前置专项或 profile。

**E2E 后专项**：源码提取旧/新扫描，240 组（含实际 head 形状、
ROWS=1/2/4/8、8192 token、非零初始状态及连续两次调用），
1309464576 个 BF16 输出与 100270080 个 FP32 状态逐位一致，
无非有限值，构建无警告。工具强制相同二进制完整 HTTP 门禁。

**下一步**：采集同一候选 200K HTTP 时间线，核对 GDN 耗时和资源。
尚未最终接受或改正式参考；证据 gdn-vector-load-20260921/。

## 2026-09-21 — GDN 连续读取质量通过，开始完整性能矩阵

**结果**：候选构建零警告，首项质量 HTTP 11/11 精确正确，包含
200K，服务退出 0。没有前置专项或 profile。

**下一步**：按固定五档各三次运行性能，核对 TTFT/decode 的重复
范围及输出摘要；通过完整门禁后才检查 FP32 状态/BF16 输出。

## 2026-09-21 — GDN 连续 q/k 读取候选开始

**依据/改动**：已接受 200K 时间线中 GDN 扫描累计约 33.55 秒；
源码每 lane 连续读取四个 BF16。仅将这两组 q/k 读取改为 uint2
再原序解包，调度形状保证 8 字节对齐；状态递归、归约、准备层、
分配与 launch 不变。不预先认定编译器实际减少指令或整体加速。

**下一步**：必要构建后首测质量 HTTP，再五档性能。全部通过才
对照状态/输出与采集，不运行前置专项或 bench。
证据 `.q4t-work/e2e/gdn-vector-load-20260921/`。

## 2026-09-21 — GDN gate 相邻对照确认回退，撤回候选

**结果/决定**：旧/新二进制短矩阵各 9/9，4K TTFT 2.846→2.876 秒，
8K 5.634→5.664 秒，两档三次范围不重叠；输入与输出摘要相同。
候选否决，源码恢复 8ac9820，必要重建无警告；build/q4t 恢复精确
已验收二进制 SHA 55fb505d…276db0，重建不同 SHA 产物另行保留。

**恢复验证**：恢复后 1K HTTP 三条通过，输出摘要一致、服务退出 0；TTFT 0.843 秒，decode 16.970 tok/s。
质量 11/11 与失败矩阵/对照证据均保留，正式五档参考不变。
没有低层专项或 profile；具体性能回退机制未确定。

**下一步**：检查扫描的输入读取与递归依赖，避免直接扩大中间表。
详见 [GDN gate 实验](../GDN_GATES_2026-09-21.md)。

## 2026-09-21 — GDN gate 预计算出现 8K TTFT 回退，转 HTTP 对照

**结果**：质量 11/11 通过，但正式性能 8K TTFT 5.626→5.665 秒，
旧范围 5.623608–5.630682，新范围 5.659057–5.673345 秒，不重叠。
4K 均值也略升但范围重叠；decode 三档范围重叠。

**处理/下一步**：在 44K 阶段终止客户端（143），服务正常退出 0；
前三档共 9 条结果保留，200K 性能未运行，候选不接受。正在用旧
二进制按 1K→4K→8K 相邻复测，排除环境变化。不运行低层专项或
profile，不以重复表达式减少覆盖 E2E 回退。正式参考不变。

## 2026-09-21 — GDN gate 预计算质量通过，开始五档性能

**结果**：必要构建零警告，第一项质量 HTTP 11/11 通过，包括 200K，
输出与参考一致，服务退出 0。没有前置专项或 profile。

**下一步**：完整五档已启动，判断 gate 表准备、分配和读取后的净
收益。通过完整 E2E 后再核对 BF16 输出与 FP32 状态，不提前接受。

## 2026-09-21 — GDN prefill gate 预计算候选开始

**依据/改动**：最新 200K 时间线中 GDN 寄存器扫描约 33.55 秒。
源码 alpha/beta 对状态列不变，候选生成每 (t,h_v) 的 FP32 表供
扫描复用，保持原表达式及状态递归。8K 分块多 3 MiB 临时内存，
仅该路径分配/准备，同流释放，不复用调试仍读取的原始缓冲。

**下一步**：构建后首测真实质量 HTTP，再完整性能；通过后才验证
FP32 状态及 BF16 输出逐位等价和净耗时。候选尚未接受。

## 2026-09-21 — top-k 网络寄存器化接受，正式参考更新

**结果/归因**：完整 E2E 与精确专项通过后，200K HTTP 采集/导出/
服务均正常退出。prefill 合并累计 34.079→19.549 秒（-42.64%），
3888 次调用不变；SparseAttention/GDN 基本保持 35.4/33.6 秒。
寄存器 26→48，cuobjdump 旧/新 STACK、LOCAL 均为 0。资源换取
时间的收益得到实测支持，不把 kernel 累计等同于墙钟因果分解。

**决定**：接受并更新完整五档正式参考：200K TTFT 167.385 秒，
44K 32.379 秒，decode 未见明确回退；旧参考冻结，不拼接最快值。
质量 11/11、性能 15/15，268800 对排序分值/ID 逐位一致，构建零警告。

**下一步**：提交推送，按新时间线关注剩余注意力/GDN 的大项成本。
报告 TOPK_REGISTER_NETWORK_2026-09-21.md，继续 E2E 优先。

## 2026-09-21 — top-k 寄存器网络完整 E2E 与精确专项通过

**结果**：质量 11/11、五档性能 15/15，输出一致、服务退出 0。
200K TTFT 181.853→167.385 秒（-7.96%），44K 33.141→32.379 秒
（-2.30%），新旧三次 TTFT 范围不重叠；decode 未见明确回退。

**验收后专项**：只构建并运行 streaming_topk_exact，1/1 通过，
268800 对分值/ID 逐位一致，含同分与跨块边界，构建零警告。

**下一步**：200K HTTP profile 已启动，核对合并成本与寄存器等
资源再决定最终接受、更新正式参考并提交。尚未重置参考。

## 2026-09-21 — top-k 寄存器网络质量通过，开始完整性能矩阵

**结果**：必要构建零警告，第一项为真实质量 HTTP；11/11 通过，
包含 200K 请求，输出与参考一致、服务退出 0。未运行前置专项或
profile，不用文本一致替代排序分值/ID 的严格等价证明。

**下一步**：五档性能已启动。完整 E2E 通过后才核对精确网络输出
和编译资源；若长上下文或 decode 回退，先按 HTTP 证据处理。

## 2026-09-21 — 回到长上下文预算，top-k 网络寄存器化候选开始

**依据/改动**：已接受 200K 时间线显示流式合并累计约 34 秒，具有
比近期 decode 小特化更大的绝对成本。候选保持比较网络与严格 tie
规则，仅改变中间值存储：线程内部交换和 warp shuffle 留在寄存器，
跨 warp 仍共享内存与显式同步。只改独立 streaming_topk.cu。

**下一步**：构建后首先真实质量 HTTP（含长上下文），再完整五档。
收益、寄存器压力与精确 ID 等价均待验收；不进行前置专项/profile。

## 2026-09-21 — HC gate 融合验收通过，减少真实执行阶段

**结果/决定**：质量 11/11、性能 15/15，五档输出一致、服务退出 0，
对固定参考和上一阶段未见明确回退。均值有升有降且范围重叠，整体
按持平处理，正式速度参考不变。保留固定单 token 形状融合。

**验收后证据**：2048 组、8192 个输出逐位一致，包括旧/新未融合
投影与旧投影+gate/融合投影。构建零警告。1K HTTP 时间线确认
每步少 96 次 gate 启动；投影累计 343.527→346.872 ms 略增，但
与 gate 合计 380.671→346.872 ms（-8.88%，约少 0.133 ms/步）。
prefill gate、分配、counts D2H 次数不变，不把启动减少直接当作提速。

**下一步**：提交推送这一边界，回到整体时间/流量预算选择更有规模
的执行计划改动；继续保持 E2E 优先。报告 HC_GATE_FUSION_2026-09-21.md。

## 2026-09-21 — HC gate 融合质量通过，五档性能启动

**结果**：构建零警告，首测 1K HTTP 三次输出一致、正常退出，
decode 16.994 tok/s（16.963–17.016）。完整质量 11/11 通过，含
200K 请求，服务退出 0。候选尚未接受；没有前置专项/profile。

**下一步**：五档按固定顺序各三次，比较正式参考与上一阶段结果。
融合的逻辑读写减少仅约 1.5 KB/步，主要假设是减少独立启动及
依赖开销，不能把它描述为模型 DRAM 带宽的显著降低。

## 2026-09-21 — HC 投影与 gate 收尾融合候选开始

**改动/理由**：源码确认投影的四个 BF16 输出在 combine 前仅由 gate
消费；候选在固定单 token 形状的投影 lane 0 计算 gate，保持中间
BF16 舍入，避免单独 gate 启动。缓冲所有权与其他路径不变。

**下一步**：必要构建后先真实 HTTP，完整 E2E 后才专项/profile。
预计每步减少 96 次启动只是源码预算，验收前不声明收益或接受。

## 2026-09-21 — GEMV 输入向量搬运完整验收通过，保留有限收益

**结果/决定**：质量 11/11、性能 15/15，五档输出与参考一致、服务
退出 0，对固定参考和上一阶段均未见明确回退。200K TTFT 181.723
秒、decode 14.711 tok/s。保留改动；不将 +0.09%–0.28% 的 decode
均值差声明为稳定收益，正式参考不变。

**验收后证据**：2016 组、105408 个 BF16 输出逐位一致，覆盖实际
K、对齐与偏移输入。首轮发现未覆盖 K=640，补齐再跑，证据保留。
1K HTTP profile 正常退出：普通 GEMV 151215 次，累计 9.599→9.521
秒（约 -0.82%）；寄存器 39→40，输出头基本持平。调用/分配计数
不变，不将局部累计时间当作墙钟速度或 DRAM 带宽。运行时及专项
构建均零警告。

**下一步**：形成独立可回退提交，继续按形状和实际数据流选择目标。
源码循环缩减只是候选依据，不能绕过 E2E。报告 GEMV_STAGING_2026-09-21.md。

## 2026-09-21 — GEMV staging 候选质量通过，开始五档性能

**结果**：必要构建零警告，第一项测试为 1K HTTP 三次，输出与参考
一致、服务退出 0，decode 16.993 tok/s（16.896–17.055）。随后完整
质量 11/11 通过，包含 200K 请求，服务正常退出；未进行专项/profile。

**下一步**：五档性能已启动，检查各档重复范围。质量通过不代表性能
接受，不因短档均值上涨保留候选；进度与 session 记录在证据目录
`gemv-vector-stage-20260921/progress.json`，不重启仍活跃的测试。

## 2026-09-21 — BF16 GEMV 输入 staging 向量化候选开始

**改动/理由**：已接受 HC 阶段时间线中剩余 BF16 GEMV 仍是主要累计
耗时项。源码 shared 输入复制为标量；候选在对齐输入下改为 float4，
显式 shared 对齐，保留未对齐标量分支与原数学次序。不开启 FP8。

**下一步**：构建后首测 1K HTTP，随后质量与五档。未验收不接受，
不进行前置数值测试或 profile。保留基线二进制与 patch 以便回退。

## 2026-09-21 — HC 四输出 GEMV 完整验收通过，保留有限局部改善

**改动/理由**：N=4/K=10240 专用四 CTA GEMV，去 shared staging，
保留逐行数学次序。第一项测试为 HTTP，质量 11/11、性能 15/15，
五档输出一致、未见可辨识回退。decode 均值较固定参考 +0.18%–0.42%，
重复范围重叠，不认定稳定 E2E 增益，也不重置正式速度参考。

**后续验证**：完整 E2E 后 1024 组、4096 个 BF16 输出逐位相同。
专项工具首次因 BF16 memcpy 警告停止，改 bit_cast 后零警告通过；
运行时代码未变。1K HTTP 时间线确认 255 次前向、HC 96 次/步，
累计 380.339→342.936 ms（约 -9.83%，每步少 0.147 ms）；分配与
counts D2H 次数不变。profile 墙钟不替代正式速度。证据全部保留。

**决定/下一步**：范围明确且有局部改善，接受并形成提交边界。继续
核查其余 BF16 投影的输入搬运与归约，不从单一形状外推收益。
用户已授权持续自主验收、回退、提交推送，既定 E2E 约束不变。

## 2026-09-21 — 转向计算路径，HC inject 形状专用 GEMV 开始

**依据/改动**：已接受 MoE 阶段 1K HTTP 时间线中 BF16 GEMV 累计约
9.92 秒；N=4/K=10240 对应 HC inject，24480 次/255 步，累计约
380 ms（1.49 ms/步）。大投影与小输出长归约不应统称为满带宽。
候选将该形状分为四个单 warp CTA，直接读取 x，保留逐行累加和
shuffle 顺序；新 kernel 独立编译，其他形状与原 device 实现保留。

**下一步**：必要构建后第一项为 1K HTTP，再质量/五档完整验收。
通过后才做逐位专项与时间线归因，不使用 bench。不先承诺收益；
证据目录 `.q4t-work/e2e/hc-inject-gemv-20260921/`。

## 2026-09-21 — 线性候选撤回后的运行产物恢复确认

**结果**：恢复构建零警告，build/q4t 使用已接受 c215f74 的旧原件，
SHA-256 2626c015…d631，运行时代码无差异。第一项恢复检查为 1K
HTTP 三次，输出摘要一致、服务退出 0，decode 均值 16.824、范围
16.784–16.846。旧版也会出现该速度，进一步说明 1K 偏低不能单独
归因于候选，不应把全部差异描述为确定的 decode 回退。

**决定/下一步**：4K/8K TTFT 对照仍不满足保持性能的接受要求，候选
不保留。没有重跑完整五档，也没有 profile/低层测试。保留生命周期
分析及全部实验证据，本阶段只提交文档，后续回到主要计算/权重路径。

## 2026-09-21 — 线性分配相邻复测支持 TTFT 回退，候选撤回

**结果**：新旧二进制按 1K→4K→8K 各三次 HTTP，相同 prompt 和输出
摘要。4K 平均 TTFT 2.852835→2.865896 秒，8K 5.640558→5.660398 秒，
范围分别 2.852162–2.854129 vs 2.855792–2.877661、5.637940–5.645494
vs 5.649295–5.670736，均不重叠。decode 范围重叠，原 1K 偏低未稳定
复现。不能把结果表述成稳定 decode 回退或精确长期损失比例。

**决定/原因**：不满足保持性能要求，恢复 c215f74 源码和旧二进制，
正式参考不变。候选未进行低层测试或 profile，180 对调用减少只是
源码估算。质量通过不覆盖未完成的五档性能；44K 中断、200K 未跑。

**下一步**：完成恢复 HTTP 检查后提交实验/生命周期文档。暂不继续
盲目合并大块分配，回到已接受版本的主要计算和权重流量路径。原 MoE
大幅回退与本次小幅 TTFT 回退不能据此认定同源。

## 2026-09-21 — 线性分配候选质量通过，8K TTFT 回退信号待复核

**结果**：首测 1K HTTP 三次均值 16.921 tok/s；随后质量 11/11。
正式性能前三档输出一致，但 1K decode 均值 16.823 偏低，8K TTFT
5.652–5.680 秒，慢于固定参考 5.627–5.637 及上一阶段 5.635–5.641，
重复范围不重叠。故在 44K 阶段主动终止客户端（143），服务退出 0，
完整五档未完成，不接受候选。

**下一步/原因**：保留失败证据，用保存的旧/新二进制按相同
1K→4K→8K 顺序进行 HTTP 相邻复测，区分小幅实现回退与环境波动。
没有前置或失败后的低层专项、bench、profile。证据在本轮目录下
control-short/ 和 candidate-short/，不同二进制以各自原始 patch 为准。

## 2026-09-21 — 线性注意力六块临时缓冲合并分配开始

**改动/原因**：源码核对六块中间量的生产、消费、释放均在 caller
stream，无额外专家流使用；共同生命周期适合合并池分配。保留六个
独立且 256 字节对齐的区域，不复用活跃内容、不侵入 GEMM scratch，
不改状态更新或数学。预计普通 decode 每步少 36×5=180 对调用。

**下一步**：必要构建后第一项测试为 1K HTTP，随后质量及五档完整
E2E；无回退才接受。证据 `.q4t-work/e2e/linear-slab-20260921/`，
保留旧二进制、正式参考和上一阶段结果；不运行前置单测或 bench。

## 2026-09-21 — MoE 合并异步分配完整 E2E 与调用数闭合

**改动/结果**：原 workspace/API 不变，四张路由表共用一次异步池分配
和释放，256 字节对齐各区。必要构建零警告，首测 1K HTTP 后，质量
11/11、五档各三次 15/15 全过，实际长度、256 输出、length、摘要
一致，服务正常退出。无前置专项或 bench。

**性能**：1K/4K/8K/44K/200K decode 16.912/15.108/15.902/15.510/14.682，
TTFT 0.848/2.853/5.638/33.164/181.886 秒。五档新旧重复范围均重叠，
按保持精度且性能持平的复杂度改进接受；不宣称微小提速，不更新
正式速度参考。全范围与原始证据见 moe-routing-slab-20260921 目录。

**E2E 后核对**：1K HTTP 时间线输出一致、退出 0；255 次 decode，
异步分配/释放各 141270，即每步 554；既有 1K 采集各 177990，即
698，每步减少 144 对。计数 D2H 仍 12240（48/步），不声称消除同步。
不使用 profile 耗时作为正式性能，也未另跑数值微测试或全量单测。

**下一步**：按授权提交推送 main，其他任务文档保持不纳入。原四表
持久化回退仍未定位底层机制；继续整理资源前，先限定所有权、分配
来源及跨流依赖，每个可保留改动仍需完整 HTTP 验收。

## 2026-09-21 — 合并异步分配通过 1K 复现，进入完整 E2E

**结果**：必要构建零警告，首测 1K HTTP 三次均值 16.917，范围
16.861–16.952 tok/s，输出摘要一致，服务退出 0。没有复现原候选回退。

**下一步**：质量 11 项与五档各三次完整验收已启动。改动目标为减少
资源管理，保持数学和池来源；不预先宣称速度提升，也不把单档通过
当作接受。完整结果前不运行专项或 profile，不更新正式参考。

## 2026-09-21 — 单表均正常，原候选复现，尝试合并池分配

**结果**：offset/token_list/row_of_flat 单表持久化 1K HTTP 均值
16.890/16.874/16.866 tok/s，输出一致、退出正常。原失败二进制直接
回放仍为 11.434（11.387–11.468），输出一致。支持四表一起变化才
暴露的组合现象，但尚不能确定分配器/提交节奏/缓存等底层原因。

**改动/下一步**：撤回诊断布局/API，保留原 workspace。仅将四张表
合并成一次 cudaMallocAsync/cudaFreeAsync，256 字节对齐各区域，
保留异步池来源与调用生命周期。先构建和 1K HTTP，无回退再质量及
五档完整验收。候选证据 `.q4t-work/e2e/moe-routing-slab-20260921/`。

## 2026-09-21 — counts 单表未复现，继续其余三表 HTTP 隔离

**结果**：counts 持久化对照质量 11/11，1K 三次均值 16.916、范围
16.840–16.965 tok/s，输出一致，服务正常退出。布局单独变化、counts
单独持久化都未复现原先大幅回退。

**下一步**：其余 offset/token_list/row_of_flat 逐表隔离；每个版本
必要构建后的第一项测试为相同 1K 输入三次 HTTP E2E，仅用于失败
定位，未跑完整质量/五档矩阵，不作为接受。产物、patch、命令和结果
位于 `.q4t-work/e2e/moe-table-isolation-20260921/<表名>/`。

## 2026-09-21 — 布局对照未复现回退，开始 counts 单表对照

**结果**：布局对照构建零警告，首测质量 HTTP 11/11，再 1K 三次
16.790–16.931 tok/s，输出摘要一致，服务退出 0。单独预留空间和
移动下游地址未复现原候选约 33% 回退，不代表完整性能验收。

**下一步/原因**：保持同一布局，只将每层 D2H 的 counts 表放入
持久区域；其余三表仍原异步分配/释放。先质量 HTTP 再 1K 三次，
不运行专项或 profile。两个候选 patch 和二进制分别保存在
moe-layout-isolation-20260921 与 moe-counts-isolation-20260921 目录。

## 2026-09-21 — MoE workspace 布局单因素 HTTP 对照开始

**改动/原因**：源码显示扩大 MoE 区域还移动了后续 GEMM/HC workspace
和激活缓冲；此前候选混合布局与生命周期两个因素。新诊断候选保留
原四组分配/释放，只复现 workspace 扩容与后续偏移，新增区域不使用。

**下一步**：必要构建后先质量 HTTP，再 1K 三次复现；不运行前置
专项或 profile，不把单档对照当作五档验收。证据保存在
`.q4t-work/e2e/moe-layout-isolation-20260921/`，诊断代码不作为优化接受。

## 2026-09-21 — MoE 候选撤回后的运行产物恢复确认

**结果**：源码恢复后重新构建零警告，首测 1K HTTP 三次输出一致，
decode 16.823–16.889（均值 16.848）tok/s，服务退出 0。重新构建
摘要不同于旧产物，因此归档该二进制，最终 build/q4t 使用已接受且
本轮 control-1k 复测过的旧原件，SHA-256 394b86c5…79b。
完整记录见实验目录 restoration.json；没有新跑完整五档矩阵。

**原因/下一步**：明确区分恢复检查与新基线验收。保持 main 运行时代码
和正式参考不变，本阶段只提交失败实验及恢复记录，继续以更小实验
定位生命周期与布局耦合，不将性能回退候选纳入基础引擎。

## 2026-09-21 — MoE workspace 候选因 1K decode 回退否决

**结果**：必要构建零警告，首测真实 HTTP 质量 11/11 通过；性能首档
1K 三次输出一致，但 decode 11.312–11.341（均值 11.330）tok/s，
相比正式参考 16.893 下降约 32.93%。主动终止进行中的 4K 客户端
（143），服务退出 0。其余档位未完成，不记为通过。

**复核/决定**：旧二进制同输入 HTTP 三次恢复到 16.816–16.968
（均值 16.914）tok/s，支持实现回退。撤回所有运行时和测试 API
改动，正式性能参考不变。候选 patch/二进制、原始请求、退出记录及
对照保留在 `.q4t-work/e2e/moe-workspace-20260921/`。没有运行单测、
bench 或 profile，不把静态分配调用减少当作性能收益。

**下一步**：确认恢复状态后提交文档。原因尚未定位，下轮先核查
地址布局与生命周期耦合，再设计更小候选，保持先 E2E 的验证顺序。

## 2026-09-21 — MoE 路由元数据 workspace 整理开始

**改动/原因**：四张路由表移入已有 MoEWorkspace，查询与运行时共用
尺寸计算；更新模型层预算及现有测试的 API 调用。减少每层四对临时
分配/释放，保留路由计算、计数 D2H 同步及专家流汇合。没有新增全局缓存。

**验证状态**：尚未测试；旧二进制与参考保存在
`.q4t-work/e2e/moe-workspace-20260921/`。必要构建后先跑真实 HTTP
质量，再五档各三次性能；通过后才进行专项及分配计数核对。

**下一步**：按现有输出摘要、实际 token 长度及重复性能范围判断接受，
不把调用减少等同于已取得吞吐收益。

## 2026-09-21 — shared 布局 HTTP 时间线闭合，阶段提交 main

**结果**：完整 E2E 和数值专项之后的 200K HTTP 采集输出一致、正常退出。
SparseAttentionKernel 同为 300 次 prefill 调用，累计 58.842→35.408 秒
（-39.83%），减少 23.434 秒，top-k 34.085→34.079、GDN 33.557→33.557
秒基本未变。GPU prefill 窗口 207.541→184.185 秒，支持正式 TTFT 减少
23.262 秒的来源；采集时间不进入性能基线，kernel 累计不等于窗口。

**资源**：Nsight/CUPTI 报告注意力 static shared 42688→43968 字节，
寄存器每线程始终 56、local memory 为 0，prefill/decode 一致。未测
bank 冲突硬件计数，不能精确拆分各因素。decode 注意力累计约 1.765
秒，BF16 GEMV 仍占约 53.73%；255 次 decode 前向计数符合已接受收尾。

**下一步**：按授权提交推送 main，其他任务的 MoE 文档不纳入。长 prefill
前三项约 35.4/34.1/33.6 秒，后续不沿用旧注意力占比；下一项先盘点
普通 decode 临时缓冲生命周期及持久 workspace 的整理机会。每项改动
仍先做真实 HTTP E2E，保持精度与五档无回退。

## 2026-09-21 — 稀疏注意力 shared 行 padding 通过 E2E 和逐位专项

**改动/原因**：Q/K/V shared 行距 256→264，尝试减少 MMA 读取不同
行时的 bank 地址重合。保持 cp.async 对齐、全局 KV 和数学顺序，理论
每 CTA 多 1280 字节，未加新 kernel 或运行时分支。9 月 20 日开始构建
及 E2E，原始目录继续使用 sparse-padding-20260920，日期不覆盖重命名。

**结果**：构建无警告，首测质量 HTTP 11/11 精确正确，随后五档各三次
15/15，输出均 256、length、摘要一致。1K/4K/8K/44K/200K TTFT
0.850/2.851/5.631/33.152/181.959 秒，后四档较上一版分别降低
11.70%/13.41%/13.46%/11.34%。decode 16.893/15.089/15.891/15.511/14.670，
均值 +0.87%–1.35%，五档未见回退。正式参考更新，原值与二进制保留。

**细查**：完整 E2E 后才编译运行源码级新旧 CUDA 对照：34 组、442368
个有限 BF16 输出逐位一致，涵盖尾块、双缓冲、非连续页和非平凡 seq_id。
新工具要求已有完整 HTTP 门禁及性能接受记录，无计时或 bench，不能
据此宣称并发调度/多模态等未覆盖分支通过。构建零警告。

**下一步**：改动接受，采一条 200K HTTP 时间线归因并按阶段提交推送。
尚未用硬件计数测量 bank 冲突，不把静态推断写成计数实测。
