# 2026-09-23 — 开发日志

## 2026-09-23：首decode GDN指定FP32算术复现

使用已绑定HTTP快照，设备参数加CPU独立fma递推。声明并保留
两种收缩顺序，v1状态全同；初次诊断常量rsqrt造成14项读出差异，
改为实际运行时kd后28311552状态及221184 BF16输出逐字节相同。
初次警告修正后零警告，所有旧结果留档；未改生产或重跑HTTP。
下一步补linear短卷积及初态来源，原错未修复，见GDN报告后续。

## 2026-09-23：上游合同及GDN状态交接确认

从服务代码确认单流decode仍用pooled路径，梳理GDN/QSA/PLE/
GRRead依赖和缺口。新只读观测先三侧HTTP，原错600440、三服务0。
36层28311552个S状态值交接位同，参数逐层绑定；独立FP64单步
输出20项BF16差异、用实际新S读出17项，未放宽阈值或宣称全通过。
运行时未改，下一步解释FP32递推差异，见交接报告与上游合同。

## 2026-09-23：decode共享GEMV和门控舍入差异解释

使用已接受HTTP快照，零警告构建CPU独立累加工具，96次投影
184320个BF16全部位同，复现8/14项相对FP64差异。门控设备dot
同CPU树，仅替换CUDA exp后合成122880项全同；原3项差异来源
为层20/28的exp相邻FP32舍入。未改生产代码或容忍条件，未重跑
HTTP，不宣称原错修复；下一步梳理上游独立参考合同，见首decode报告。

## 2026-09-23：首decode前向的MoE参考

只读观测零警告构建后先三侧HTTP，原错600440、三服务0；只捕获
完整4K prefill后的首次decode前向。router、480专家GU/down及
量化/路由加权同参考，确认GU20次/down63组高值尺度错误。
共享投影8/14项及合成CPU参考3项差异保留；离线工具复制执行位
遗漏导致首次126，留档后补权限，同二进制重建0，未重跑HTTP。
运行时未改，下一步检查共享GEMV归约及上游参考缺口，见首decode报告。

## 2026-09-23：同token全部选中专家的SwiGLU/down/合成参考

观测工具初次构建变量错误留档，修正后零警告，首项三侧HTTP均
原错600440、服务0。480专家链路身份接通，307200中间FP4及
1228800 down BF16全同参考；独立高精度SwiGLU量化编码亦相同。
设备尺度重建确认71组高值错误，不解释为答案因果；共享142项
投影差异继续保留。运行时未改，下一步梳理首decode路径，详见链路报告。

## 2026-09-23：末token全部选中专家的输入量化与gate/up参考

新观测零警告构建后先三侧HTTP，原错600440保留、三服务0。
48层480专家的实际权重/尺度/输入全部匹配；1228800个FP4码及
614400个gate/up BF16全部同独立参考。设备重建76800组尺度，
确认70次高值提前饱和（6层7组各复用十次），不证明答案因果。
正式运行时未改；下一步补同token SwiGLU/down完整链路，详见GU报告。

## 2026-09-23：共享投影记录算法的独立进程重放

绑定上一轮接受版HTTP证据，独立工具零警告构建；96次实际算法
在同形状/布局/权重、末token复制4096行的输入上重放。184320个
BF16输出全同HTTP，FP64差异仍78/64项，退出0。这些差异无需
模型其余调度也会出现，不等于独立证明cuBLAS算术或修复原错。
运行时不变；下一步补路由专家gate/up及激活量化参考，详见共享报告。

## 2026-09-23：共享GEMM实际权重、输入与描述符绑定

新观测先三侧HTTP，全文均原错600440、三服务0。48层96次共享
GEMM权重全部同checkpoint，输入输出同相邻边界，实际描述符
符合BF16/FP32计算合同。240份边界快照同上一轮，独立投影差异
78/64项复现；排除本样本装载/布局差异但不宣称累加已解释。
运行时不变，下一步用冻结实际算法做同形状重放。证据见共享报告。

## 2026-09-23：共享专家逐阶段参考与未决投影差异

扩展只读观测后先三侧HTTP，原4K均600440、服务0；运行时未改。
48层末token的SwiGLU30720项同参考，gate/up及down分别78/64项
不同，连续共享支路2857项不同。进一步保留9项非相邻编码及
高抵消样本，fsum核对支持FP64参考但未复现cuBLAS累加，不放宽
阈值、不宣称正确性完成。下一步绑定实际GEMM输入/权重/描述符，
明确差异来源；详见MOE_SHARED_REFERENCE报告。

## 2026-09-23：末token全层路由和MoE合成边界参考

新只读观测先真实HTTP三侧，均原错600440、三服务0；采集48层
最后prefill token的logits、槽位、分组索引、十专家输出及共享合成。
HTTP后CPU独立核对480槽、122880路由FP32位同；122880合成BF16
同原顺序重算。FP64合成1项中点差异、router投影15项相邻BF16
差异均保留，后者不在选中top-10。单项中点已用仅替换点积复现，
未放宽阈值或重跑推理；辅助细节脚本标量类型错误修复有记录。
正式源码/二进制未变；下一步承接完整专家/共享支路独立参考，
边界及限制见MOE_TOKEN_COMBINE_REFERENCE报告。

## 2026-09-23：48层96份实际down参考完成

扩展只读观测，首项原4K三侧HTTP均600440、三服务0、原错保留。
每层两个活跃专家的96份实际权重/尺度/alpha匹配checkpoint；
4730240个FP4元素及18920960个BF16输出分别同独立编码/舍入参考。
29层35份有707组高值错误；冻结各层输入的局部修正改变2534个
FP4码及1342031个投影元素，受影响样本重建误差均下降。未做跨层
修正传播，不外推全模型质量；正式运行时不变。下一步沿同一token
补完整MoE加权合成参考，细节及边界见多层报告。

## 2026-09-23：高值单项复现三题变化，拒绝后恢复确认

0730db3b首项三题原始HTTP核对1/3，4K变对、1K/44K新增错误，
全文均同原合并修正；服务0、质量驱动1。无需次正规修改即可
复现此组变化，不等于全部根因定位。按预定规则拒绝，未运行
其余质量题、性能或低层测试；归档候选后恢复源码及同一接受
2392f6b1。恢复首项三题全文同父版2/3、原4K错保留、服务0。
下一步完善可比多层数值参考，停止盲目舍入候选；见高值隔离报告。

## 2026-09-23：启动高值饱和单项HTTP隔离

真实快照已确认高值错误传播到投影，现仅修正exp15有限编码的
提前饱和，保持次正规分支。构建0、零警告，候选0730db3b；首项
测试为原1K/4K/44K三题HTTP，新增错题即拒绝恢复，避免为已退化
质量运行完整性能。若通过初筛仍需完整质量/五档，不以三题代替
验收；低层测试未运行。下一步核对原始响应与父版/合并候选。

## 2026-09-23：实际FP4块/down参考确认缺陷影响传播

扩展只读观测，首项真实HTTP三侧仍原错600440、服务均0；保存
前四个调用实际FP4、权重/尺度、描述符与BF16 down输出。HTTP后
独立重编码核对286720元素、checkpoint第0层权重尺度及alpha，
1146880个BF16位模式与FP64参考舍入一致。离线修正7组尺度，
7个FP4码及16655个投影输出变化，两专家局部重建误差下降。
正式源码和2392f6b1未变，不证明全模型质量改善，不追认旧候选。
下一步扩大可比参考/明确因果实验；完整限制见E4M3_DOWN_REFERENCE。

## 2026-09-23：真实HTTP快照确认高值区格式缺陷命中

保持2392f6b1及运行时源码不变，独立观测库零警告构建后首项真实
HTTP，4K原错题无观测/有观测/无观测均600440、7token、stop，
三服务0，质量驱动因原错均1。首四份prefill SwiGLU只读快照共
17920组，HTTP核对后再重算；旧编码全同实际，CUDA与独立参考
全同但与实际7组不同，均高值提前饱和。采集不是性能验收，不
证明最终答案因果。保存诊断源码及完整证据，下一步建立真实块
和下游参考，不盲目再换舍入实现；详见E4M3_HTTP_CAPTURE报告。

## 2026-09-23：接受基线独立格式参考及checkpoint尺度合同核对

未改运行时；重新绑定原接受版HTTP证据后，仅检查2392f6b1对应
header，不复测拒绝候选。独立枚举33146条E4M3输入，host/device
各147条不一致、CUDA原生0条，退出1，格式缺陷获得直接证据。
只读48层512专家的147456个F32标量，gate/up两类尺度49152对
位同；down输入尺度每层190–301值，排除统一尺度参考的等价假设。
保留工具verify_e4m3_contract.cu、完整原始证据及参考合同文档。
下一步定位冻结HTTP实际量化尺度是否命中；未证明4K错误因果，
未修复格式、未实现完整NVFP4/GEMM参考，不改正式性能基线。

## 2026-09-23：相邻对照拒绝原生舍入候选，恢复基线确认完成

45条原始HTTP核对通过、三服务0、全文同参考；e49bc64f的8K
TTFT完整范围同时差于前后父版，后两次五项同时不利。不追加采样，
归档候选并恢复HEAD源码/同一已接受2392f6b1二进制，无运行时差异。
恢复首项三题HTTP全文同父版，1K/44K正确、4K既有错误，服务0、
驱动1；不声称全题通过或重新验证全部矩阵。格式缺陷未解决，
低层及准备的视觉测试未运行；下一步讨论匹配量化规则的独立参考，
本轮不继续扩展候选。详细证据见E4M3_ROUNDING_2026-09-23.md。

## 2026-09-23：原生舍入五档采集完成，固定一次前后父版复核

e49bc64f五档15条原始核对、全文同三个参考，服务0。全三次范围
无不利分离，后两次仍有7项；为区分同版时间波动与候选影响，启动
一次父版→候选→父版完整45请求，PID2442767。参考与规则冻结，
保留原始差异，下一步核对三服务终态和原始数据；运行时未接受，
低层测试未运行，高端格式缺陷及独立4K错误仍在。

## 2026-09-23：原生舍入版独立14/15同父版，启动自身性能矩阵

e49bc64f独立15条原始核对14/15，全文均同2392f6b1，无新增失败；
原4K错保留，驱动1/服务0，会话63732结束。完整五档HTTP性能
会话83189已启动，固定参考不改；不借用手写版性能结果验收原生版。
下一步检查全部输出和时延范围，低层测试未执行、运行时未接受。

## 2026-09-23：原生舍入候选固定11题通过，进入独立题

e49bc64f首项固定HTTP11/11、全文同正式，原始请求/usage/stop
及二进制摘要已核对，驱动/服务0。会话63732继续原独立15题，
候选冻结；性能及低层测试未运行。下一步核对新增错误及全文差异，
不把手写次正规版旧性能结果当作当前版本验证。

## 2026-09-23：手写次正规五档有不利范围，启动原生舍入候选

43b0e688五档15条原始核对/输出同参考，驱动服务0；完整三次
44K decode对父版1项不利，后两次对父版/正式/固定5/2/2项不利，
未接受。审计器初次completed组数误判保留，修正后逐组3条核对。
归档旧候选后，仅device次正规分支改用原生最近偶数整数转换，
不认定它是时延根因。构建0且零警告，新SHAe49bc64f，首项固定
HTTP会话63732；下一步质量/独立题核对，仍不运行低层测试。

## 2026-09-23：次正规分项独立14/15同父版，启动五档性能

会话39239终态，独立15题原始核对14/15、全文均同2392f6b1、
新增失败0；原4K错误保留，质量驱动1/服务0。合并候选新增错误
未在本单项重现，不能因此断言高端修正的唯一因果。完整五档
HTTP性能会话44477已启动，冻结三个参考结果摘要，不自动接受，
所有输出差异均待审计。下一步核对五档完整三次及后两次范围；
低层测试未运行，当前候选尚未接受或提交。

## 2026-09-23：次正规分项首项固定质量11/11，继续独立题

43b0e688固定11题驱动/服务0，原始HTTP参数/usage/全文/stop
核对通过且全文同正式。会话39239继续原冻结15道独立题；
源码/二进制保持冻结，完整性能与低层测试未运行。下一步检查
是否新增失败，高端编码问题继续明确保留，不宣称完整修复。

## 2026-09-23：隔离次正规舍入候选，首项HTTP启动

仅改a<2^-6半步最近偶数和进位8，正常分支保留原样，明确高端
缺陷未解决。必要构建0、零警告，SHA43b0e688；首项固定11题
HTTP会话39239，成功后才进入原独立15题，未自动安排性能/低层
测试。下一步核对分项是否触发新增错误，不追认被拒绝合并候选。

## 2026-09-23：撤回E4M3候选后的三题恢复核对完成

恢复HTTP三条原始核对、服务0，全文均同已接受2392f6b1：1K与
44K正确，4K仍600440；质量驱动1如实保留。部署二进制摘要一致，
运行时无未提交差异，复用相同已接受产物的旧五档证据、不冒称
新跑完整矩阵。下一步隔离两处格式修正，合并候选仍拒绝。

## 2026-09-23：E4M3合并修正出现两个新增错题，归档撤回

独立15题13/15：原4K答对，1K/44K新增错误，其他12题同父版。
原始请求/usage/全文核对、服务0、质量驱动1；不运行性能或低层
测试。候选与header归档，恢复源码并零警告构建52c1c464（未验收），
另存后部署原已接受2392f6b1。三题恢复HTTP会话49638已启动。
下一步收齐恢复并隔离高端/次正规修正，不豁免新失败、不改参考。

## 2026-09-23：E4M3候选首项固定质量11/11，进入独立答案

会话95491退出0，11条原始请求/usage/全文/stop均核对，答案正确
且全文同正式，服务0。候选a984c284保持冻结，原独立15题已启动
会话75292；下一步记录原4K错误及新增差异，随后仍需完整性能
和门禁后的独立编码验证，不依据源码正确就接受运行时。

## 2026-09-23：参考合同审查发现E4M3边界错误，启动隔离E2E

源码显示exp15被整体饱和，256错误编码448；次正规半步与正常区
进位不符合最近偶数。旧roundtrip缺少这些边界，MoE测试参考复用
同一转换器。仅修改format.h，父版/旧header归档，必要构建0且
零警告，候选a984c284首项固定质量HTTP会话95491。未做数值测试、
未接受运行时，不宣称已定位4K错题根因。下一步收齐E2E终态，
独立答案与完整五档仍需核对。新增编码审查报告保存具体边界。

## 2026-09-23：固定金额长度/布局30条HTTP完成

保持原4K金额、问题及模板，四档两布局各三次加原题前后对照。
分散1K/8K全对、2K/4K全错，集中四档全对；原题前后六次仍错。
实际请求/长度/全文/stop核对、服务0、质量驱动1；没有运行时变化。
等长布局的背景预算差2token已明示，不作单因素根因结论。
下一步停止扩展同类提示词，审查计算合同明确的独立参考可行性，
保留原质量和性能基线。证据filtered-sum-layout-20260923。

## 2026-09-23：4K条件求和HTTP分解24条完成

运行时9a62371/2392f6b1未改。原题前后六次均错；去背景求和、
纯加法、长背景显式加法、首末金额提取及计数六组各三次正确。
实际请求/长度/全文/stop原始核对，服务0、质量驱动1，保持原错题。
此证据用于缩小任务失败范围，不裁定模型或kernel根因。原五档
同时改变金额，下一步固定金额后隔离长度与记录分布，不做性能修改。
证据filtered-sum-decomposition-20260923，详见新分解报告。

## 2026-09-23：真实messages五档闭合，接受模板修复

45条配对HTTP长度/全文一致，三服务与驱动0，源码/二进制绑定核对
通过。完整三次20项0不利，后两次20项2项单侧不利（1K decode对
后参考、200K TTFT对前参考），均未同时差于前后参考；全部保留。
结合既有202条、永久重放63条及21条字节结果接受2392f6b1源码，
不宣称310题全正确或严格零回退。独立14/15及原4K错误不变，
固定性能参考不变，首版失败不追认。更新永久工具说明与阶段报告。
下一步先研究原4K错误来源，再讨论计算路径优化；本轮不扩展实验。

## 2026-09-23：旧范围确认完成，补长 messages 性能覆盖

独立15题14/15且全文同正式，原4K错误保留；旧范围E2E审计后，
模板字节21/21通过，永久HTTP重放63条通过（33条独立答案正确）。
最终审查发现旧五档只走裸prompt，长messages处理尚未覆盖，因此
未接受或提交运行时。冻结新模板负载五档45条，原生前→messages→
原生后，驱动PID2409644；参考/候选2a5813f2/2392f6b1不变。
下一步等待完整HTTP终态，保留各组范围，不与旧负载混算提速。

## 2026-09-23：按需版视觉72条一致，保留单侧时延差异与参考漂移

四服务退出0，六案例各12条token/全文/finish一致且非空；48项
时延比较9项单侧不利、0项同时低于前后参考。复用原始数据检查
同一参考二进制，后组本身3项decode低于前组，记录在reference-drift-review。
未新增采样或容忍百分比，不追认首版两侧均不利结果。会话92721
进入独立15题；下一步收齐并综合审计，字节检查仍未执行，候选未接受。

## 2026-09-23：按需版五档原始审计结束，非法输入通过，进入视觉

2392f6b1质量11题及五档15请求原始核对通过、输出同正式，服务0。
完整三次范围对正式/固定/恢复均0不利；后两次子集对恢复0，对
历史正式4K TTFT、固定8K decode各1项不利，保留不抹除。非法
输入18种400及前后两条健康请求通过。会话92721继续视觉四组，
独立15题随后执行；候选未接受，不运行低层字节验证或profile。
下一步检查视觉是否仍有两侧参考均不利，并完成独立答案回归。

## 2026-09-23：按需版进入200K，准备门禁后的模板字节参考

会话92721持续运行，五档前四档HTTP/输出检查完成，200K进行中，
未改变源码/构建/输入。准备21条字节参考，直接取自已冻结的
原生模板prompt，另保存只调用格式器的检查源码；均未编译或
执行，明确要求完整E2E质量/性能及受影响接口通过后才允许运行。
该准备不能替代HTTP一致性或放宽性能门禁。下一步继续既有序列。

## 2026-09-23：按需存储版扩展45条与固定质量11题完成

2392f6b1扩展15组三路请求长度/全文/finish均一致，预置独立答案
15/15正确；固定质量11/11原始SQLite/SSE核对，全文与正式一致，
配置/源码/二进制绑定有效，服务0。会话92721已进入五档性能。
另准备完整序列汇总审计器，保留视觉48项及五档两种范围比较，
不自动接受候选。下一步继续既有串行序列，源码/构建保持冻结。

## 2026-09-23：按需数字原文候选首项24条HTTP通过

会话8853结束，2392f6b1首项24条原始HTTP核对通过，messages六组
6/6且长度/全文/finish同原生，前后原生6/6、旧拼接0/6，服务0。
会话92721启动后续串行验证：扩展、固定质量/五档及原始审计、
错误输入、视觉、独立答案；任何语义对照失败停止，不自动接受
性能。源码/二进制冻结，下一步逐阶段核对终态，保留旧失败。

## 2026-09-23：首版独立答案14/15无新增失败，数字原文改为按需保留

首版独立15条原始核对完毕：全文同正式、14/15，原4K错误保留；
服务0、质量评估器1、采集0。归档5e6d2ee6二进制后，移除Json
全局新增字段，复用数字类型str并仅HTTP/工具解析显式启用，
默认模型配置不保留数字原文。源码改动范围有据，不宣称性能
根因。2392f6b1构建0且零警告，第一项24条HTTP会话8853启动，
无前置单测/微测/profile。下一步保持输入与参考，重跑完整验证。

## 2026-09-23：视觉72条行为一致，时延失败保留，独立答案回归启动

六视觉案例各12条长度/全文/finish一致，四服务0；另计时延48项
全三次比较中8项不利，部分双图/视频decode低于两侧参考，不能
以功能一致掩盖性能失败，候选不接受。会话71505重跑原冻结15题
独立答案，尚未结束。下一步完成该版本记录，再隔离全局JSON
数字原文存储的额外对象/分配开销；未证明其为性能根因，不改
当前运行中的源码/二进制。仍不运行前置低层测试或profile。

## 2026-09-23：五档原始审计完成，错误输入通过，启动视觉配对

会话75569结束，固定11题及五档15请求的原始输入/输出、配置、
二进制/源码绑定核对通过。全三次范围对正式/固定/恢复均0不利，
后两次子集分别3/2/6项不利；保留疑点，不宣称严格无回退。
错误输入20请求完成：18种HTTP400及对应错误正文准确，前后
健康请求200且正确，服务0。视觉72请求四服务配对会话86212
已启动，旧手工原生模板对新messages；独立15题回归已准备未跑。
下一步收齐视觉原始证据并保持旧失败样本，不运行低层测试。

## 2026-09-23：固定质量原始核对11/11，补齐后续HTTP执行器

模板候选固定质量11/11正确、全文及提示词摘要同正式，配置/二进制/
原始SQLite/SSE已核对，服务0。五档前四档HTTP/输出检查结束，
会话75569继续200K，未判性能通过。准备raw gates审计器，保留
全部三次/后两次与正式、固定、恢复三参考比较；另准备20条非法
输入/健康对照执行器与72条视觉执行器，尚未启动。非法输入通过
evalscope插件结果旁路保存HTTP状态/错误正文，未改已安装包或服务。
下一步等待既有服务终态后原始审计，再串行运行接口HTTP；源码/构建冻结。

## 2026-09-23：扩展模板45条HTTP通过，固定质量/五档门禁运行中

15组三路HTTP在长度/全文/finish上全部一致；仅5组有独立答案，
对应15条正确，其余只计一致性。服务/采集0。固定质量首次驱动
路径误指quality根目录，未启动服务即失败；修正为inputs后v2
会话75569顺序跑11题与五档15请求，尚未结束。准备18非法输入
及72视觉配对请求但未执行。下一步核对门禁终态，继续受影响
接口验证；源码/构建保持冻结，无前置低层测试，候选未接受。

## 2026-09-23：实现模板候选，首项24条HTTP通过基础入口对照

新增模型专用RenderChatPrompt，HTTP负责一次内容/视觉资源解析，
模板负责角色、思考历史、工具JSON、生成前缀；视觉补模型边界。
JSON增加数字原文保留，避免工具整数经double舍入。候选5e6d2ee6
构建0且零警告；第一项测试为evalscope HTTP原24条，messages
六组均与原生长度/全文/finish相同且6/6正确，原生前后各6/6。
扩展45条冻结默认思考/强度/历史/工具/数字HTTP已启动，会话33950。
尚未固定质量、五档、视觉/错误输入验收，不接受候选；下一步
收齐扩展证据后继续HTTP门禁。未运行单测、数值或profile。

## 2026-09-23：真实messages入口配对HTTP定位完成

未改运行时，在恢复确认完成后进行24条HTTP诊断；原准备脚本
保留，启动前提改为有界恢复复核完成，不将性能疑点改成通过。
六组原生prompt前后各6/6，messages和旧拼接各0/6且互相一致；
前五组messages进入思考并length，空白组空stop，不能称为六道
算术错误。实际发送字段、usage、SSE全文已核对，服务/采集0。
新增入口报告及完整模板修复边界。下一步冻结更完整HTTP合同
案例，再实现模板渲染；本轮无前置低层测试或新算子修改。

## 2026-09-23：恢复版正确性确认收尾，保留4K错题与性能疑点

会话1130结束，15个原始HTTP响应核对：独立答案14/15，全文均同
旧正式、无新增失败；1K恢复360010，4K仍600440而非360284。
质量评估器1、服务0、外层采集0分别记录。固定11题及五档15条
输出一致证据保留；相邻45条已结束，不放宽性能门禁。新增恢复
确认报告并更新STATUS，明确本轮仅确认恢复行为，不证明全模型
正确；运行时无新改动，其他任务文档保留。下一步与用户讨论
messages入口合同及验收口径，不提前启动新开发或聊天诊断。

## 2026-09-23：相邻45请求完成，启动恢复版独立答案确认

相邻五服务退出0、原始响应45条核对通过。完整三次范围无不利
分离，后两次对当前/历史参考各3项不利，保留性能疑点；本轮
有界复核结束，不追加采样寻求通过。恢复二进制重跑原冻结15题
独立答案，原4K失败不删除；会话1130。下一步核对答案与旧正式
逐字输出，再汇总正确性边界并讨论后续，不开始新运行时开发。

## 2026-09-23：启动恢复版本双参考相邻45请求

冻结正式→固定→恢复→固定→正式五服务，每个1K/4K/8K各三次，
共45请求，会话1750。三个SHA核对、无其他模型服务；采用冻结
评估器副本，输入相同，各归档比自身正式输出参考。最终并列
全部三次与后两次范围，不更新参考，不重试已拒绝投影候选。
下一步收齐并原始审计，当前恢复性能诊断未闭合，聊天HTTP未跑。

## 2026-09-23：恢复五档原始核对完成，显式区分历史与子集口径

会话41428结束，15条原始响应同正式，服务/驱动0。历史7364767
comparison.json以全部三次范围判断；恢复版按该口径20项无
不利分离，但后两次子集仍正式4K TTFT/decode及固定1K/8K
 decode四项不利。两份比较独立保存，不切换口径隐藏失败，
性能恢复诊断未闭合，聊天入口HTTP仍未运行。下一步一次预定
相邻归档对照复核这三档，不重复试到通过，不更新参考。

## 2026-09-23：冻结聊天入口24请求，未启动推理

六个短文本案例各原生prompt/messages/旧拼接prompt/原生prompt，
共24请求，独立答案、模板摘要与长度全部冻结。使用只读本地
模板和现有tokenizer，准备0；源码核对evalscope完整JSON会保留
messages，后续仍需审计实际载荷。没有HTTP或模型推理。下一步
先完成恢复五档验证，再运行入口对照；不作全面模板正确性结论。

## 2026-09-23：校正筹备文档过期待办，明确标准聊天入口缺口

按ChatServer::Start实际max_seq×vocab分配与7364767验收记录，
修正dataflow-engine/STATUS及runner后续演进中“尚未缩减logits”
待办；完整D/P/S执行器仍未完成。源码确认messages拼接role:
内容，未应用模型模板，而质量题由客户端预渲染后走prompt。
下一步在恢复性能验证后先做真实HTTP配对，避免把模板绕行
结果当标准聊天正确性。当前仅文档治理，不改变运行时或测试。

## 2026-09-23：恢复源码完整运行时路径来源核对

src/include/CMakeLists的HEAD与7364767无差异，工作区与HEAD
亦无差异；Release/g++14/CUDA13.3/110a配置单独记录。该核对
不充当性能或数值测试通过，五档性能会话41428继续，已有1K/
4K输出检查通过。下一步仍是完成全矩阵原始审计与性能比较。

## 2026-09-23：恢复质量11/11原始核对，五档性能启动

新SHA2a5813f2的固定11题原始响应全部正确且全文同正式，驱动/
服务0，会话97219结束。按原门禁启动五档性能15请求，会话
41428，证据accepted-workspace-recovery-performance-20260923。
未做前置低层测试，不以质量通过抹除原有4K求和/位置问题。
下一步完整原始核对与双参考性能比较后确认恢复状态。

## 2026-09-23：恢复构建零警告，首项质量HTTP启动

构建会话48476退出0且零警告，新SHA2a5813f2不同接受归档0c3f，
不直接转移旧二进制验收。首项固定11题质量HTTP启动，会话
97219，原输入/答案/正式输出参考保持，未做低层测试。下一步
核对全部原始响应与正常退出，再完成恢复后的性能验证。

## 2026-09-23：36响应审计完成，拒绝投影候选并恢复接受源码

相邻三服务/驱动0，36条原始响应均同正式；32项比较有10项
不利，4K TTFT/decode及200K decode对当前前后正式均不利。
拒绝独立投影，不继续重复采样找通过。确认无服务存活并复核
九文件摘要后，归档混合源码/补丁/二进制，仅恢复这九个自有
实验至接受源码，运行时diff清空，其他任务内容未纳入。必要
构建会话48476进行中，下一步零警告后首项固定质量HTTP。
恢复不代表已知位置/答题问题解决，原失败证据完整保留。

## 2026-09-23：准备未接受实验的默认工作区恢复边界

只读核对九文件与归档完全一致、HEAD与7364767基准源摘要一致，
冻结恢复清单及再次校验条件。当前后侧正式仍在测试，未恢复/
构建任何运行时。后续仅撤下自有未接受实验，保留所有证据和
其他任务修改；恢复基线不等于解决已知位置/答题问题。下一步
先收完36响应并审计，服务结束后再实施恢复及首项HTTP验证。

## 2026-09-23：4K不利差异在相邻两侧重现，继续收齐证据

后侧正式4K完成；候选TTFT及decode均对前后正式后续范围不利，
部分比较保存partial-4k-comparison.json。当前不接受，不反复
复测寻找通过；后侧剩余请求继续原计划，完整36条原始核对
尚未结束。下一步收齐并记录最终阶段结论，保留所有失败。

## 2026-09-23：相邻候选结束，24响应原始核对通过

前侧正式及独立候选各12条响应全部同正式参考，配置/长度/
结束原因核对，两个服务均0；候选PID2357839退出。后侧正式
PID2361878接续，控制会话24545活跃。尚无完整两侧性能结论，
下一步收完第三组再运行全量审计，不提前验收或改动运行时。

## 2026-09-23：核对公共小行数投影的实际消费者范围

静态源码与只读配置确认indexer非池化分支M=T×4、MTP隐藏投影
M=T×4；草稿专家按compact行数，vision也共用公共入口。不能
将HTTP单流等同所有GEMM的M1，或将当前文本测试转移为MTP/
视觉已覆盖。源码摘要单独保存，没有低层测试/profile。下一步
先完成原相邻复核，再按实际消费者决定后续验证与保留价值。

## 2026-09-23：相邻前侧正式12响应核对，候选接续

前侧正式四档12条原始响应全部同正式参考，配置/长度/结束
原因符合，驱动/服务0，PID2353849退出。独立候选PID2357839
接续运行，会话24545活跃；已结束服务单独审计保存，不将
部分对照当成完整通过。下一步收齐候选和后侧正式再比较范围。

## 2026-09-23：状态入口增加当前决策摘要，区分历史启动记录

STATUS长篇追加过程使旧“运行中”和“最新接受”标题容易被误读。
新增前置摘要，明确正式7364767、固定fcb5925、混合候选与隔离
投影均未接受，以及当前36请求相邻复核；视觉修复标题改为
已完成，历史证据不删除。纯文档修改，没有额外运行时测试。
下一步仍是收完现有相邻HTTP并更新终态，不重启模型服务。

## 2026-09-23：启动独立投影四档相邻36请求复核

完整五档结束并原始核对后，预定正式→候选→正式三个服务，
1K/4K/8K/200K各三次，保留所有不利档位与原始失败；MTP关、
冻结输入/输出目标及容量不变。会话24545运行中，证据
small-m-isolated-adjacent-20260923。固定参考另列，不以正式
复核自动覆盖。下一步逐服务核对结果与两侧范围，不做低层测试。

## 2026-09-23：独立投影五档输出一致，7项性能范围不利

会话65364结束，15条原始响应核对、全部正式输出一致、服务/
驱动0。双参考20项比较有7项后续范围不利：正式1K/8K/200K
 decode与4K/8K TTFT，固定1K/8K decode。保存全部首请求和
后续范围，不以小差异或均值忽略门禁；当前不接受，未做低层
测试。下一步相邻版本HTTP复核不利档位，确认波动或改动影响。

## 2026-09-23：启动独立投影五档性能HTTP

保持冻结输入及正式输出参考，独立SHA41a2a02f版本开始五档各
三次、输出256的单流MTP关闭评估，会话65364。启动前无其他
模型服务，源码补丁/隔离构建参数单独记录，不将共享工作区
快照误认为隔离源码。下一步收完15响应并比较双参考范围；
仍未做低层测试，不以质量题无新增错误代替性能验收。

## 2026-09-23：尾块18响应一致，独立投影正确性阶段收口

8194/8195/8196-token各三题，两版各9/9正确、全文相同，原始
SQLite/SSE核对、驱动与服务均0，会话70463结束。固定质量、
独立答案、并发及尾块已测请求未新增失败；保留原4K求和失败，
不宣称混合候选或全模型正确性已解决，不更新参考或接受运行时。
下一步讨论独立投影五档性能与实际价值，并保持其他改动分离。

## 2026-09-23：并发60响应无新增错误，补充2/3/4行prefill尾块

正式/独立投影各30请求全部正确，十组配对全文集合相同，服务/
驱动0；不将HTTP并发等同实际GPU批次。为补足小行数入口，冻结
8194/8195/8196长度各三题，正式→候选18HTTP已启动，会话70463。
初版长度生成失败保留，v2仅调整中性填充并冻结摘要。下一步
核对尾块原始响应；尚无新的性能或运行时接受结论。

## 2026-09-23：独立投影15题同正式，启动60请求小批次对照

五档独立答案15条原始响应核对，14/15正确、15全文同正式，
新增失败0；原4K求和600440错误保留，质量子驱动1、服务及
采集驱动0，会话66410结束。不将1K恢复正式答案解释为混合
候选的位置问题已解决。按预定条件启动正式→独立候选60个
HTTP小批次检索题，会话6319，尚无性能或真实GPU批次覆盖
结论。下一步核对两版串行前后与并发答案，保持正式门禁。

## 2026-09-23：准备两版本小批次质量HTTP，等待独立矩阵结束

当前独立答案12/15，200K三题进行中，会话66410，未重启。
准备固定1K/8K检索题正式/候选各30HTTP，共60，串行前后及
并发2/3/4，按原独立六位答案评分。脚本只在15条原始核对且
无新增失败后执行，未启动；不将并发参数当实际GPU批次覆盖，
不以质量题延迟声明提速。下一步先收完当前200K并核对结果。

## 2026-09-23：独立投影11条质量通过，冻结15题HTTP启动

small-m-isolated首项质量11条原始响应核对，全部同正式且正确，
服务/驱动0，会话95072结束。未声明实际M2–4覆盖或性能收益。
下一项固定独立答案15题五档HTTP已启动，会话66410，保留正式
4K失败及全部原答案，分别记录正确率、输出变化及新增失败。
新建独立投影报告说明源码边界与仍需批量/性能证据，不把旧
混合候选结果转移为这个版本通过，不进行前置低层测试。

## 2026-09-23：混合候选拆分，独立小批次投影首项HTTP启动

按当前实际九文件改动冻结四组补丁，并从7364767重建逐字节
核对来源；此为源码打包，不是运行时测试。GDN仍为内部多改动
的待进一步拆分组，不冒称每组均是最小修改。先构建仅三个
小批次BF16投影文件，其他组缺席，零警告/退出0。首项固定
正式质量HTTP已启动，会话95072，证据small-m-isolated-20260923。
准确的隔离源码补丁及CMake配置单独保存，说明通用评估器保存
的外层worktree.patch不代表该二进制来源。下一步核对质量，
再验证独立答案及实际M2–4请求；不继承旧混合候选的通过记录。

## 2026-09-23：独立参考有界复核收口，仍不接受运行时候选

v8确定性模式首个HTTP在DeepGEMM因Unsupported architecture
失败；六条SQLite记录均失败、0有效答案，服务-9，全部服务已终止。
不把驱动completed=6当六次成功或0/6答对。独立参考两轮12条
有效响应中1K六次同候选错误、4K均不稳定，不能作为唯一真值。
原14/15→13/15回退不变，不接受/重设参考。下一步区分计算
合同与任务质量判据，拆分候选建立稳定E2E证据，不继续扩展
参考框架kernel开发，不以相同错误替代正确性通过。

## 2026-09-23：无缓存六请求核对，4K不稳定未消失

v7六次均cached-token=0，MTP关、摘要/长度/7token/stop已核对。
1K全360019，4K600430/600430/360284；服务排空后自行-9退出。
关闭前缀缓存不足以消除结果变化，不能据此定位kernel根因。
下一项有界v8仅开独立框架确定性选项，保持原六请求和缓存关，
会话95305。继续不改项目kernel、不调整答案，不扩展为参考
框架kernel开发；下一步核对实际配置及原始响应。

## 2026-09-23：独立模型六次HTTP完成，1K同候选、4K不稳定

v6六条原始响应核对，1K三次360019，4K600430/360284/600430，
1/6答对。MTP关、提示词SHA、长度、输出7tokens/stop核对；
服务清理后自行杀父进程为-9，不算正常0。保留QSA编译竞态
警告，尚未定位与输出的关系。1K相同错误不能证明候选全部
正确，但不能简单将错误归为本项目独有。日志存在前缀缓存
复用；v7仅关闭radix cache复测原六请求，会话22287。下一步
核对无缓存重复性，不更改答案、正式参考或引擎实现。

## 2026-09-23：匹配官方SM110a预编译模块，独立HTTP v6启动

v5两路编译活跃、内存正常。官方0.6.17+cu130 ARM64 wheel
MoE模块按范围下载，CRC及RECORD SHA/长度一致，架构列表包含
SM110a；仅静态元数据检查，未跑kernel。保留Range501及下载
超时后续传记录；完整wheel未下载，不宣称全包SHA已验证。
主动终止v5所属进程后放入独立环境默认AOT目录，0请求；v6
同参数/MAX_JOBS=2启动，会话37283。下一步核对服务和HTTP，
源码未改，不把预编译支持当模型验收通过。

## 2026-09-23：两路JIT限制实际生效，独立服务继续等待编译

v5权重和缓存初始化完成。进程快照确认Ninja -j 2及两个cicc，
当时可用内存27GiB，未再出现120GiB使用及swap耗尽；当前
fused_moe_100模块266对象目标，首次编译仍进行中，会话3212。
尚无HTTP响应，不声明正确性或性能。下一步沿用同一活进程，
等待服务就绪并核对冻结题目，不因观察超时重启。

## 2026-09-23：独立参考完成初始化，限制首次JIT编译内存

v3因自动FlashInfer attention不受Thor混合GDN路径支持而启动
失败，0请求；v4显式Triton attention后完成81.89GB权重、BF16
KV/GDN缓存及QSA初始化。框架启动autotune触发SM110a首次JIT，
并行编译使122GiB内存使用达到120GiB且swap满；主动终止所属
服务与54个进程的树，服务-15、0请求，未等待OOM或污染推理。
FlashInfer cpp_ext.py确认MAX_JOBS控制编译并发。v5仅设2路，
保留缓存，已启动会话3212。下一步确认编译结束及首项HTTP，
不把启动或框架autotune作为性能/正确性证据。

## 2026-09-23：独立服务状态池容量修正，v3启动

v2加载197分片及PLE后在容量检查终止，状态槽1/每请求4导致
最大请求数0；日志和kv_cache_configurator源码一致，0请求。
v3仅增状态槽至5，单请求、冻结题目及精度参数不变，会话38318。
前两次启动失败完整保留；下一步核对真实HTTP，不计启动成功
为精度通过，不运行任何前置低层测试。

## 2026-09-23：独立参考安装完成，首项HTTP v2进入权重加载

依赖与插件安装均0，已冻结依赖；wheel要求的11个源码摘要
全部匹配。初次文件审查把package误作.py，改查__init__.py后
匹配，初始清单保留。首启动发现SGLang专用缓存指向工作区外，
在就绪前主动终止，服务-15、0请求；保存失败不计模型失败。
v2显式设置SGLANG_CACHE_DIR至prepared，已启用PLE SSD读取并
加载权重，会话33661。冻结1K/4K求和各三次作为首项evalscope
HTTP，MTP关闭、不生成预热；下一步核对结果及所有量化差异。

## 2026-09-23：CuteDSL最终也归并scale，开始隔离服务环境准备

追到standard wrapper后确认fc2输入scale仍归并，修正上一阶段
仅检查载入参数的初步判断。独立质量对照仍有价值，但不作为
逐token真值。prepared内独立Python3.12环境205依赖解析成功，
安装会话76503启动；插件wheel已下载，未安装。下一步检查
安装结果并固化依赖，以显式关闭MTP的服务运行首项HTTP，
不执行前置低层测试，不改变现有引擎与参考环境。

## 2026-09-23：独立HTTP参考审查发现真实量化合同差异

下载固定SGLang源码至prepared，未安装、未运行推理。源码显示
CUTLASS/TRTLLM归并全专家down输入scale，当前引擎逐专家保留。
只读checkpoint第0层确认512专家有301个不同down scale，已保存
1536个输入scale及源码归档SHA。该差异阻止直接把该后端输出
作为逐token真值。下一步审查CuteDSL逐专家down路径、Thor支持
及缓存精度，继续保持现有运行时和验收参考，不做性能修改。

## 2026-09-23：正确性确认阶段收口，保留未通过结论

累计54条独立答案和隔离HTTP响应核对。位置补写单独触发1K
答案回退，但源码语义支持补写，不能以旧版单题答对判定正确。
现有ref_dump是默认4层、专家反量化BF16的固定forward工具，
不是完整NVFP4 HTTP参考，未执行。为避免继续叠加无法解释的
性能候选，本阶段仅提交诊断文档，运行时仍未接受。下一步
讨论建立同输入、量化语义明确的完整模型独立HTTP对照。

## 2026-09-23：位置源码合同核对，补写符合纯文本位置推进

沿prefill、scheduler、ModelDecodeBatchMulti、RoPE/indexer消费
检查：prefill只填提示词范围；decode从T开始、完成后加一；
消费持久表[seq,axis,absolute_position]。旧pooled路径缺少新坐标
更新，候选补写与现有ModelDecodeStep一致。读取固定5.16.1及
工具5.17.0源码，两者新位置均past_seen_tokens+arange，未替换
固定参考或运行数值工具。该源码证据不等于独立模型输出证明，
也不能抹去1K答案回退。下一步需独立正确性证据，不贸然撤回
位置补写以迎合单题旧输出，不做新的性能优化。

## 2026-09-23：仅MRoPE九请求完成，本题回退单独重现

正式→仅位置补写→正式各三次全部原始核对，三服务0、总驱动0，
PID2316494退出。正式前后六次均正确360010，隔离版本三次均
360019。无GDN/indexer候选改动也能触发本题回退，因果边界
收敛到位置补写，但不等于证明补写数学错误。下一步审查
位置生成/消费语义，保留独立答案和全部失败，不以此贸然
恢复潜在错误的位置行为，不追加性能优化。

## 2026-09-23：仅MRoPE独立构建完成，首项九请求HTTP启动

构建会话43436退出0、零警告，独立二进制SHA
0266bf52b17f6db5ece1ae349fef7e47dec8a54e1d51dab623171e9010536919。
源码为7364767仅加原位置补写，不含GDN/indexer改动，工作区
运行时不变。首项验证正式→仅MRoPE→正式同题各三次，驱动
PID2316494，证据independent-answer-rope-only-20260923。
下一步核对原始答案判断位置补写单独是否足以改变本题，
不以一题的结果决定完整修复正确性，不做低层测试。

## 2026-09-23：indexer前后均错，独立重建仅MRoPE版本

v2六条原始响应完成核对，d85/d056各三次均360019，两个
服务0、总驱动0，PID2312083退出。说明本题错误在短indexer
BF16舍入之前已存在，不能归因于该后续改动。

未找到旧03ec可执行归档，现从git7364767导出独立源码，只
应用原MRoPE补写patch，逐字匹配旧归档model.cu；工作区源码
不变。初次嵌套目录git apply跳过目标，源码一致断言捕获；
仅configure执行，无编译/推理。改用目录内patch后源码匹配，
开始必要构建（会话43436），输出都在.q4t-work/prepared下。
下一步零警告构建后先同题HTTP，独立记录新二进制SHA，不能
冒称与旧03ec二进制相同，不做低层测试。

## 2026-09-23：修正归档执行权限，边界HTTP v2启动

首驱动2311448已失败终止：归档d85文件无执行权限，serve未启动、
HTTP未发送，原目录和错误记录保留。将d85/d056复制至新prepared
目录后仅设置复制件执行位，内容SHA逐一一致，原归档不改。
v2驱动PID2312083启动，证据independent-answer-rounding-boundary-v2-20260923。
下一步核对原始HTTP，不能将启动失败当模型失败或删除失败记录。

## 2026-09-23：归档indexer舍入边界六请求HTTP启动

核对d85e74e7和d0561816归档SHA及manifest，后者仅增加短
indexer每head BF16舍入，前者已有MRoPE/GDN候选修复。对两版
分别运行冻结1K失败题三次，独立答案360010，PID2311448，
证据independent-answer-rounding-boundary-20260923。运行时不修改、
不构建、不做低层测试。下一步依据同题输出确定此边界是否
改变错误，再向前或向后缩小范围；不以本题通过作全面验收。

## 2026-09-23：1K三侧复核完成，候选错误稳定重现

旧→新→旧九条原始响应核对完成，三服务0、总驱动0，PID2306309
退出。正式前后六次全为360010，候选三次全为360019，输入1024、
finish=stop。候选阶段语义失败保留，无重试替换；只确认该请求
可重复回退，不归因到具体kernel。下一步读取归档中间版本的
源码边界，用相同冻结请求进行HTTP隔离，不做性能优化。

## 2026-09-23：冻结1K新增失败，启动三侧九请求HTTP复核

完成30请求对照并提出后续定位方案后，继续指令下启动最小
重复性核对：正式→候选→正式，每服务同一1024-token求和题
三次，共9次。归档二进制SHA不变，独立答案360010不变，
运行时代码不修改。工具副本只调整质量模式唯一题目ID数量
检查以容许刻意重复，仍逐条检查长度、精确答案和stop结束。

驱动PID2306309，prepared/e2e目录independent-answer-repeat-20260923。
语义失败保留且继续两侧对照，服务/传输失败停止。下一步
判断本请求的错误是否可重复；不能用复核替换原始失败，
不开始性能优化或低层测试。

## 2026-09-23：独立答案对照完成，候选新增1K求和答错

30条原始HTTP响应全部核对，正式14/15、候选13/15。候选新增
1K条件求和错误：应360010，实际360019；正式答对。4K两版
都错，应360284，正式600440、候选600430。其余13题两版
输出相同且正确，全部stop、输入长度正确。两质量驱动语义
失败退出1，两服务0，总采集驱动0，PID2299583终止。

新增独立答案报告，明确仅一次/题/版，不能据此定位引擎数学
错误或证明整体模型正确性。当前不接受、不重设参考。按照
用户要求，本阶段结束后先讨论后续，不追加性能改动或推理
实验。建议固定1K失败旧→新→旧复核，再做HTTP改动隔离，尚未执行。

## 2026-09-23：独立答案正式版14/15，候选同题组继续

正式7364767的15条原始HTTP响应已核对，14题正确，4K条件
求和期望360284、实际600440，finish=stop；服务正常退出0，
质量驱动因语义失败退出1。该失败不自动归因为引擎计算错误，
但说明已接受版输出不能一概充当独立正确答案。所有原始记录
和预先冻结的答案保留，未重试或修改题目。

原对照驱动PID2299583存活，按预定规则继续候选33b11796同15题。
下一步核对候选全部原始响应并逐题比较，不以总分掩盖新增失败。

## 2026-09-23：索引隔离完整五档结束，独立答案HTTP对照启动

33b11796完整五档15条原始响应核对通过，驱动/服务0，原PID2294700
退出。4K/44K后续TTFT对正式参考仍不利，其他档位/指标无不利
范围分离；五档输出同直接父版/正式回退，仍不同正式调度参考。
该结果不证明候选错误，也不解除精度与性能验收条件，当前不接受。

用户同意先完成正确性确认再讨论后续。准备五档×三任务独立答案：
记录最终写入值、两表关联、条件求和，每版15题，同一份原生
关闭思考模板输入。答案由记录规则计算并以独立写法复核，正式
7364767先、候选33b11796后；语义失败保留且继续，传输/服务失败停止。
原固定质量11题只覆盖数字检索，裸prompt性能文本的截断输出
差异不能单独判定质量下降；新增测试仍不等于全面质量证明。

初次fixture长度准备在1024条件求和出现1-token偏差，未发送HTTP；
初始脚本/日志/目录保留，修正背景padding后15题精确长度完成。
驱动PID2299583已启动，prepared/e2e目录均为independent-answer-http-20260923。
下一步原始响应逐题核对并报告新增失败、改善和无法证明的范围；
完成这个阶段后与用户讨论后续，不继续叠加性能实验。

## 2026-09-23：索引宽度隔离质量11响应通过，完整五档启动

33b11796的固定质量HTTP已完成，11条原始响应逐条核对通过，
输出同未变正式7364767参考，驱动/服务退出0，PID2292428终止。
确认质量终态后启动完整五档诊断，PID2294700，证据
`gdn-single-state-offset-full-diagnostic-20260923`。保留全部正式
输入及三次重复，与正式/固定/直接父版4f363cb比较，不因输出
差异提前截断诊断矩阵，不将采集成功当验收。下一步核对五档
原始响应、输出与各参考性能范围；不运行低层测试。

## 2026-09-23：应用单序列状态索引宽度隔离，首项HTTP启动

确认相邻75请求终态、无存活serve后，归档4f363cb二进制和
完整运行时diff。仅将GDN寄存器kernel的非packed局部状态偏移
恢复int，packed仍size_t；不改递推算术或checkpoint宽偏移。
必要构建退出0、零警告，未执行单测、bench、profile或数值工具。
新SHA 33b1179677d642120536aab0c15b126283b5ea0a2e45d9f16437a9100191e22b。
首项固定正式质量HTTP已启动，PID2292428，证据
`gdn-single-state-offset-20260923`。这只是TTFT回退来源的隔离假设，
尚未证明收益。下一步核对11条质量响应，再进行完整五档E2E；
当前不接受，原始失败与正式精度/性能参考保持不变。

## 2026-09-23：相邻75响应完成，TTFT回退在两轮候选重现

预定五服务全部正常退出，75条原始响应核对通过，各版本输出
同自己的旧参考。PID2270980及2286201已退出。ordered-comparison.json
保留全部60项比较，24项后续范围不利：两轮候选对前后正式的
五档TTFT共20项；第一轮1K decode对三个参考、第二轮对后侧
正式共4项。其余decode未出现不利分离，仍不能宣称稳定提速。
候选输出与正式/固定参考不同，不接受，不重设精度或性能目标。

下一步进行已准备的单序列状态索引宽度隔离；保留当前完整
源码和二进制，必要构建后第一项验证仍为固定质量HTTP E2E。

## 2026-09-23：相邻第二轮候选完成，进入正式后侧

服务3五档15条原始响应核对通过，输出同4f363cb旧参考，
驱动/服务退出0，原PID2282914终止。累计60/75条已完成，
原驱动PID2270980继续存活，正式后侧服务PID2286201已启动。
保留前侧不利性能结果和正式输出差异，不把本服务正常结束
视为验收通过。下一步完成最后15条响应，审查全部60项比较，
再决定准备中的单序列寻址隔离实验；测量期间不修改运行时。

## 2026-09-23：相邻固定参考完成，进入第二轮候选

固定参考服务2五档15条原始HTTP响应核对完成，输出与原固定
参考完全一致，驱动/服务退出0；前三服务累计45/75请求。
原驱动PID2270980与新候选服务PID2282914确认存活，继续预定
候选→正式后侧，不重启或更换二进制。完整60项比较尚未完成，
当前候选未接受，不用局部结果重设参考。

同时修正STATUS的版本归属：168条批处理/异长响应属于5cdf9ed3，
MTP三侧27条属于bb74466f，都不能自动证明当前4f363cb通过。
原因是状态摘要的“当前”措辞会误传旧证据。下一步完成剩余
30条相邻HTTP，核对原始响应及完整比较后再决定运行时修改。

## 2026-09-23：相邻固定参考继续，准备单序列寻址隔离草稿

PID2270980存活，固定服务日志确认MTP关闭、max_seq=1，
首个1K已完成，余档继续，未改变顺序或运行时。

检查源码与7364767原GDN寄存器kernel，原单序列ss_base为
int h_v*kd*vd，池化改造后所有实例变size_t。准备独立草稿
prepared/gdn-single-state-offset-20260923，用编译期类型选择
只恢复单序列局部int，池化与checkpoint保持size_t；单序列
指针已由调用方切片，目标48×128×128局部索引在int内。
这只是潜在TTFT来源的源码假设，未证明性能影响；未应用、
未构建或测试，不修改当前4f363cb。下一步先完成预定相邻
75请求，再根据完整结果决定隔离实验，仍以HTTP E2E先行。

## 2026-09-23：相邻首对30响应完成，固定参考服务启动

首轮候选服务1结束，15原始响应核对、驱动/服务0，与正式
服务0合计30响应完成，输出分别同各自版本旧参考。候选五档
TTFT均值0.805027/2.653329/5.214685/30.700903/160.318185秒，
decode18.573573/17.894872/18.154730/17.913463/17.169025 tok/s。
对正式前侧五档后续TTFT均不利，1K后续decode不利，其余
四档decode无不利分离；首对摘要保存first-pair-partial.json，
不把该局部结果当作全部预定60项比较。

原驱动PID2270980存活并进入服务2固定参考，SHA a13e3c核对。
继续固定→候选→正式后侧，不重启/补采、不改二进制、样本
和参考。下一步核对后续服务，保留全部未通过项。

## 2026-09-23：相邻首轮候选1K仍对正式前侧不利

PID2270980确认存活，服务1候选1K/4K六原始响应已核对。
1K后续TTFT0.769056–0.769383秒，对正式前侧0.763316–
0.765038不利；后续decode18.648703–18.649755，对前侧
18.659062–18.686923也不利。首请求单列保留，不用于放宽
后续范围。尚未完成候选五档及固定/正式后侧，不能把该
局部比较推广为完整相邻结论。继续同一驱动和预定75请求，
下一步核对余档，不补采样、不改二进制或输出参考。

## 2026-09-23：相邻正式前侧15响应完成，进入候选服务

PID2270980持续存活，服务0正式版五档15原始响应核对完成，
驱动/服务0，完整输出同既有正式参考。TTFT均值0.798108/
2.639950/5.178728/30.486309/159.453514秒，decode18.631111/
17.886270/18.161252/17.885893/17.120448 tok/s，原样本保留。

原驱动按预定顺序进入服务1候选，二进制SHA4f363cb确认；
尚无该服务完整结果，不能提前形成相邻比较结论。下一步
继续同PID观察候选、固定参考和后侧正式版，不调整顺序、
样本数或输出参考。证据decode-state-staging-adjacent-20260923。

## 2026-09-23：状态搬运完整五档改善，启动预定相邻对照

五档15条原始HTTP响应核对完成，驱动/服务0，PID2268123及
控制器2267781退出。新增200K TTFT均值160.288323秒、decode
17.165730 tok/s（父候选16.603204），输出同父候选/正式回退。
全部五档decode均改善，4K以上对正式/固定参考后续范围无
不利分离，但五档TTFT均对正式不利、1K decode对固定不利。
五档输出仍不同正式调度参考，候选不接受。

完成终态审查后启动此前固定顺序的五服务75请求相邻诊断，
PID2270980确认存活，正式→候选→固定→候选→正式。证据
decode-state-staging-adjacent-20260923。保留旧失败/旧参考，
按各版本既有输出独立核对，全部60项比较不挑样本或合并范围。
本诊断用于判断小幅性能差异的可重复性，不能解除正式输出
精度条件。下一步观察原进程、核对每服务原始结果，不改运行时。

## 2026-09-23：准备预定顺序相邻性能对照，当前200K不受影响

同PID2268123确认存活，200K继续。为判断TTFT及固定参考
小幅差异是否受服务重启/时间影响，准备五服务固定顺序：
正式7364767→候选4f363cb→固定fcb5925→候选→正式，共75
HTTP请求，每服务原五档各三次。不启动、不修改当前服务。

核验三二进制SHA并独立归档候选，脚本要求当前完整15请求
原始核对、输出同直接父候选、质量11原始核对、无其他serve
后才允许执行；每服务原始审查并比对自身版本原输出，异常
停止不重试。全部60个预定候选/参考/档位/指标对照逐一报告，
首请求单列、后续两次范围独立，不拼接宽范围或替换历史失败。
原正式输出差异不会被这种性能对照解除。目录prepared/decode-
state-staging-adjacent-20260923，仅准备。下一步先完成200K，
再决定是否执行此相邻诊断；不将准备方案作为测试通过。

## 2026-09-23：状态搬运44K改善保持，200K继续

同PID2268123确认存活，前四档12响应原始核对完成。44K
TTFT均值30.671036秒，decode17.898155 tok/s，对父候选
17.214510提高，输出同父候选/正式回退，不同正式调度参考。
后续decode对正式/固定参考无不利范围分离；TTFT对正式
参考仍不利，不能接受候选，未以均值抵消门禁未通过项。
200K继续同一服务，无重启/源码修改。下一步核对最后一档
及完整终态，再决定相邻对照或继续修改，保持全部原始样本。

## 2026-09-23：状态搬运前三档decode改善，长档继续

PID2268123存活，1K/4K/8K九条原始响应核对完成，输出均同
直接父候选/正式回退，不同正式调度参考。新增4K/8K TTFT
均值2.655928/5.209116秒，decode17.884083/18.194274 tok/s，
对应父候选17.212833/17.510955。两档后续decode对正式及
固定参考无不利范围分离，1K对固定仍不利；前三档TTFT均
对正式参考不利，4K对直接父候选亦不利，不能接受。

该结果支持状态搬运候选的局部decode改善，不构成完整性能
或正式输出精度通过。44K/200K同一服务继续，无重启、无
源码修改，下一步完成五档后再决定相邻复验或后续修改。

## 2026-09-23：状态搬运候选1K decode改善，门禁仍未通过

同PID2268123存活，1K三条原始响应已核对，完整输出同
bb74466f父候选/正式回退、不同正式调度参考。decode均值
18.572476，对父候选17.898625有改善；后续两次18.609736–
18.625491，对父版17.907959–17.954976明显更高，和正式
参考18.613010–18.615898重叠，但低于固定参考18.641374–
18.656069。TTFT后续0.769402–0.770078秒对正式及直接父版
均不利，不以平均TTFT0.803081秒覆盖有序样本差异。

把直接父候选加入同一有序样本报告，正式/固定参考不变，
没有重采样。余四档继续，候选不接受，不宣称瓶颈因果已
通过profile证实；当前仅有HTTP观测改善。下一步完成五档。

## 2026-09-23：状态搬运候选质量11/11，完整五档启动

PID2266893质量驱动和服务退出0，11条原始响应独立核对
全部匹配未变正式参考，二进制4f363cb不变。衔接PID2267781
按条件启动五档PID2268123并确认存活。证据decode-register-
state-staging-20260923及decode-register-state-staging-full-diagnostic-
20260923。未运行前置单测/bench/profile，没有五档通过结论。
下一步核对五档原始输出及父候选/正式/固定参考有序样本，
不把质量题通过当作性能收益或MTP全路径验收。

## 2026-09-23：状态搬运候选五档条件衔接已准备

质量PID2266893确认存活。衔接PID2267781核对该PID启动时间，
等其退出0并独立核对11条原始响应同正式参考后才启动五档；
任何失败停止。五档保留正式/固定参考，直接父候选改为bb74466f，
目录decode-register-state-staging-full-diagnostic-20260923。
当前质量尚未结束、五档未运行，运行时不变。下一步检查质量
及五档原始结果，不运行其他推理或低层测试。

## 2026-09-23：MTP三侧27响应对齐，状态搬运候选开始首项HTTP

bb74466f的27响应原始核对完成，驱动/三服务0，原PID2261423
及控制器2258976退出。1K/8K/44K均同普通前后完整输出，
各自重复确定；普通参考同修改前。原8K/44K分歧本组消失，
不推广为全部批量MTP/回滚正确，不覆盖五档性能失败。

确认无其他serve、源码父本及SHA匹配后，归档bb74466f二进制
和runtime-before.patch，应用packed非因果ROWS4状态搬运草稿。
128×16 tile经8704字节shared、两次同步组织连续全局读写；
递推、舍入、归约、状态全局布局保持，causal/checkpoint未改。
构建0、零警告，新SHA4f363cb296df9f6447ce2124b7f25650c4dc33c91ec2c1ff4374e6d066608721。

首项固定质量HTTP已启动PID2266893，证据decode-register-state-
staging-20260923。未运行前置单测/bench/profile，尚无收益结论。
下一步核对质量原始响应，再完整五档；旧HTTP通过不转移到新版本。

## 2026-09-23：因果寄存器修正后MTP 8K对齐未变普通参考

同PID2261423确认存活，15次原始响应独立核对：普通前三档
各三次、MTP 1K/8K各三次，全部重复确定。MTP 1K及8K均
与普通前侧完整输出一致，原5cdf9ed3的8K分歧本次未重现。
另核对三档普通前侧完整摘要与修改前相同，未改变比较目标；
证据当前MTP目录raw-response-review.json及parent-plain-comparison.json。

这是本8K案例的直接改善证据，不是全路径或性能验收。44K
MTP与普通后侧仍在运行，未重启、未改运行时；正式输出差异
和五档性能问题尚在。下一步继续同一三侧进程核对剩余结果。

## 2026-09-23：因果寄存器完整五档未接受，当前MTP复验启动

五档15条SQLite/SSE原始响应独立核对完成，驱动/服务0，
PID2257865和控制器2256166退出。输出五档均同父候选5cdf9ed3
及正式回退、不同正式调度参考。200K TTFT均值160.491427秒，
decode16.603204 tok/s。五档后续两样本decode范围均低于正式/
固定参考，TTFT均对正式参考不利，候选未接受，不改变参考。
证据decode-causal-register-full-diagnostic-20260923。

衔接PID2258976按条件启动当前bb74466f的MTP三侧27请求诊断，
PID2261423已确认存活，证据decode-causal-register-mtp-switch-http-
20260923。使用原失败prompt、三个上下文、普通前/MTP/普通后
顺序；尚无修复通过结论。运行时不变，状态搬运草稿仍未应用。
下一步独立核对三侧完整输出，尤其原8K/44K分歧。

## 2026-09-23：因果寄存器44K原始响应完成，200K继续

同PID2257865确认存活，当前四档12条原始响应已核对。
新增44K三次TTFT均值30.725207秒、decode17.214510 tok/s，
完整输出同父候选5cdf9ed3/正式回退，不同正式调度参考。
五档未完成、不接受，原有样本和参考不变。200K继续同一
服务运行，源码/二进制未修改，MTP控制器仍等待完整终态。
下一步核对200K及服务退出，再沿既定条件进入MTP诊断。

## 2026-09-23：前三档九响应核对，准备保持算术的状态搬运草稿

同五档PID2257865及MTP衔接PID2258976确认存活。1K/4K/8K
共九条原始响应核对，输出同父候选及正式回退、不同正式
调度参考。TTFT均值0.804587/2.651664/5.210757秒，decode
17.898625/17.212833/17.510955 tok/s；44K/200K尚未完成。

依据源码packed寄存器kernel的状态加载地址，准备独立草稿
prepared/decode-register-state-staging-20260923：对现有
128×128、ROWS4、128线程非因果packed分支，以8704字节
共享tile组织128×16列状态的连续全局读写，增加两次同步。
保持FP32状态布局、BF16舍入、归约顺序和递推不变，不改
checkpoint/causal路径。访问模式是源码假设，缓存/占用和同步
代价未测，不宣称提速；草稿未应用、未构建、未测试。
下一步先完成当前五档及MTP HTTP再决定是否应用，不并发修改
正在测量的运行时，不运行低层测试或profile。

## 2026-09-23：因果寄存器1K原始响应核对，设置MTP诊断衔接

五档PID2257865存活。1K三条原始响应已核对，TTFT均值
0.804587秒、decode17.898625 tok/s；输出同父候选5cdf9ed3/
正式回退，不同正式调度参考。后续两样本decode范围仍低于
正式/固定参考，TTFT对正式参考亦不利；不接受，不改参考。
最初读取对比时首档results尚未生成，脚本报文件不存在；
未重启进程，待同一进程生成结果后核对，原服务未受影响。

启动MTP衔接PID2258976，监视原控制器2256166及其启动时间。
仅在完整15请求原始核对结束且驱动/服务正常后，启动已准备
27请求MTP三侧HTTP诊断；失败停止不重试。继承质量/SHA/
无其他serve检查，保留完整输出差异，无门禁绕过或低层测试。
下一步完成四个剩余档位，再核对同一组MTP原失败请求。

## 2026-09-23：因果寄存器候选质量11/11，完整五档已启动

首项HTTP质量PID2254283正常结束，服务0、11题完成。
独立原始SQLite/SSE核对11条完整文本、usage、finish和固定
正式参考完全一致；二进制SHA bb74466f不变。
衔接PID2256166通过检查后启动完整五档PID2257865，已确认
存活，证据decode-causal-register-full-diagnostic-20260923。
保留原正式/固定参考，尚无五档通过或MTP修复结论。

准备当前候选MTP三侧27请求脚本，复用上一轮原prompt及
plain-before/MTP/plain-after顺序，前置要求改为当前候选质量
及完整15请求原始核对，尚未启动。目录prepared/decode-causal-
register-mtp-switch-http-20260923。下一步先完成五档，再用
同一组原始失败请求验证因果递推改动，不转用其他样本。

## 2026-09-23：首项质量运行中，设置完整五档条件衔接

PID2254283确认仍存活，同一质量服务继续，无重启和运行时
修改。准备decode-causal-register-full-diagnostic-20260923，
复制既有HTTP五档采集与原始审查工具，直接父候选对照改为
5cdf9ed3，正式7364767与固定fcb5925参考保持不变。

启动衔接PID2256166，核对质量PID启动时间，待其正常终止且
11条原始质量响应与正式参考全部一致后，才启动完整五档
15请求诊断。驱动另核对二进制SHA和无存活serve；结束后审查
原始响应、输出和有序样本范围。任何失败停止，不重试。
当前质量未结束、五档未执行，不以准备脚本作通过证据。
下一步继续核对同一质量进程与衔接状态。

## 2026-09-23：MTP三侧分歧确认，因果寄存器候选首项HTTP启动

27原始响应审查完整，三服务/驱动0，PID2249625退出。普通
三档重启前后相同；MTP 1K相同，8K/44K不同两侧普通，
各自三次确定。原始失败均保留，没有重设输出参考。

确认无存活serve、源码与草稿父本一致后，归档5cdf9ed3二进制
及runtime-before.patch，应用因果寄存器统一。128×128 pooled
causal使用已有BF16归一化和寄存器更新，按uniform/ragged区间
串行推进，检查点按真实seq及局部token保存；单序列checkpoint
复用同一kernel。无checkpoint通过模板裁掉存储分支。旧通用
形状路径保留，本轮尚无精度和性能验收结论。

必要构建退出0、零警告，新SHA bb74466f4c25779731f5051b30d4d31ad245d02ed77a7ca8ee067180e80caac8。
首项运行固定正式参考质量HTTP，PID2254283，证据
decode-causal-register-consistency-20260923。未运行任何前置
单测/bench/profile。下一步原始质量核对，之后五档E2E及
MTP/混合消费者复验；旧168次通过不转移至当前候选。

## 2026-09-23：当前MTP 8K复现分歧，准备因果递推统一草稿

同PID2249625确认存活，已独立核对15原始HTTP响应：普通
前三档各三次、MTP 1K/8K各三次。各组重复确定，1K MTP
同普通，8K MTP不同普通；原始输出差异保存在当前实验目录
8k-output-difference.json，偏移为字符而非token。44K和
plain-after仍待完成，不能提前宣称完整三侧结论。

代码检查确认普通packed BF16寄存器递推和MTP因果共享
FP32递推仍不同。准备独立草稿到prepared/decode-causal-register-
consistency-20260923：扩展已有寄存器kernel处理uniform/ragged
因果区间，并按真实seq槽和局部token写回滚checkpoint；单序列
checkpoint也拟复用该递推。未应用、未构建、未测试，运行中
服务不变。草稿生成首次文本匹配断言失败后收紧为精确整行
匹配，未写工作区源码。下一步先完成当前HTTP对照再审查草稿。

## 2026-09-23：批处理与异长混合168请求对齐，启动当前MTP诊断

核对132请求和36混合请求的原始SQLite/SSE、实际输入输出、
完整文本摘要、批宽读回与终态，所有驱动和三服务退出0。
132请求44案例均同各自串行，22对观察/无观察输出集合一致；
观察组1K/8K实际B2/3/4各三轮每轮126次成功读回。混合组
1K/4K/8K/44K三种排列各121次B4，36输出全同串行前后。
证据decode-pooled-indexer-batches-observed-20260922及
decode-pooled-indexer-mixed-http-20260922，原控制器和驱动
均已退出，无重启/补采样。修正解决本请求集的批处理分歧，
但正式输出与性能门禁仍未通过，不接受候选。

下一步检查尚未闭合的MTP路径：原5cdf9ed3二进制不变，
启动plain-before→MTP→plain-after三个独立服务，1K/8K/44K
每档三请求、最多32输出，27请求仅作HTTP输出诊断。
PID2249625，证据decode-pooled-indexer-mtp-switch-http-20260923。
复用已审查的evalscope驱动，改为核验当前质量/混合诊断前置
证据；每次启动校验SHA及实际MTP模式，不使用旧二进制结果
替代本轮。未运行低层测试、bench或profile。
