# 2026-09-24 — 开发日志

## 2026-09-24：Linear/GDN证据去重释放QSA采集容量

为完整QSA采集留出容量，五个冻结目录的360组1188份BF16候选
全部重读摘要/元数据后，828份重复副本改为只读硬链接，逐操作
fsync journal留档。五个原manifest全部8613条记录复核相同，
原路径/内容保持，元数据变化明确记录。释放39938170880字节，
可用约61.53GB，生产未改、无推理测试，不构成质量验收。
下一步完整QSA选择/attention消费者，逻辑预算2819948544字节，
采集3.2GB+参考1GB+20GiB余量；实际页表超预算即停，禁止截断。
新observer尚未实现，仍先三侧HTTP。详见存储规则后续。

## 2026-09-24：完整PLE投影重放与FP64参考

复用冻结HTTP，零警告构建后两个完整投影52428800个输出同
实际算法重放；FP64舍入差异key52128/value12205全部保留。
12800项末行CPU顺序FP64与Dgemm原始位值/舍入均相同。
每次仅保存一份完整临时参考，基底+delta逐字节/SHA恢复后删除，
末尾再完整重建四份参考，100MB预算内完成。生产未改、原错答
仍在；PLE完整prefill的逐算子条件参考已补齐，未做独立组合
前向。下一步完整QSA各query及证据容量评估。详见PLE投影后续。

## 2026-09-24：完整PLE投影与查表消费者边界

首版控制脚本变量覆盖在HTTP前退出并保留；v2仅修正变量，沿用
零警告观测器。首项三侧HTTP仍600440、4096/7、stop、服务0，
六份元数据通过。完整FP8转换/缩放与10485760个embedding值
接通冻结lookup；52428800个key/value输出接通norm/gated
实际消费者。权重及scale共65536002字节直接匹配checkpoint，
实际两份算法保存。生产未改、算术尚未重算，原错题仍在；下一步
100MB预算内顺序进行完整投影参考。详见PLE投影报告后续。

## 2026-09-24：完整PLE归一化与门值算术参考

复用冻结HTTP，零警告构建并直接核对checkpoint来源后，128块
覆盖三组norm的125829120项及gate的16384项，同CPU指定顺序
加设备数学函数。纯CPU差异225/0、FP64差异1284/2全部保留；
两项gate位差在token840/3473 branch1，不据此推断错答因果。
全部BF16参考以基底+delta逐字节/SHA恢复，末尾完整重建12份
输出并确认连续覆盖，100MB预算内完成。生产未改、原错答仍在；
下一步完整key/value投影与embedding交接。详见PLE报告后续。

## 2026-09-24：完整PLE归一化与门控消费者接通

为追溯卷积来源，新只读观测零警告构建后先三侧HTTP，仍原错、
服务0，六份元数据通过。三组norm权重61440字节同checkpoint；
实际eps/scale及12份旧尾部/参数全同。已有大缓冲全字节比对，
仅保存新增输入/输出，避免重复。41943040个gated输出同CPU
FP32乘法及BF16舍入，完整参考无损恢复通过。norm/gate算术尚未
重算，生产未改、原错题仍在；下一步100MB预算内分块参考，
随后追溯key/value投影。详见PLE卷积报告后续。

## 2026-09-24：完整PLE卷积算术及舍入核对

复用冻结HTTP，零警告构建后绑定12份完整来源及checkpoint权重，
33块覆盖41953280项输出，同CPU四项FP32 fma+设备SiLU参考。
CPU expf/FP64版本差异23/75项，均在prefill且非仅零符号。
三次BF16舍入保留；全部BF16参考从基底+delta无损恢复，末尾
再重建六份完整结果确认连续覆盖。保存CPU累加/设备SiLU及所有
FP64差异处中间值。生产未改，上游门控/归一化尚未重算，原错题
仍在；下一步预算完整PLE上游采集并继续核对。详见PLE卷积报告。

## 2026-09-24：完整PLE卷积输入输出与历史交接

为扩展此前末行参考，新观测零警告构建后首先三侧HTTP，三次
仍600440、4096/7 token、stop、服务0，四份元数据通过。
完整41943040个主干输入/输出各接通HC层0写回与层1读取；
实际卷积权重81920字节匹配checkpoint。初始历史全零，prefill
末九行更新和首decode滑动正确，旧16份证据全部一致。
生产未改、卷积算术尚未运行；下一步有界分块全位置算术参考，
再补门控/归一化/投影上游。原错答仍存在，详见PLE卷积报告后续。

## 2026-09-24：完整共享非线性与最终MoE合并参考

复用冻结HTTP并绑定336份来源，零警告构建后125829120个
SwiGLU和503316480个最终合并输出同CPU指定算术+设备sigmoid。
纯CPU差异17/8890、FP64差异537/17614，均非仅零符号不同，
全部参考以基底+delta逐字节/SHA恢复并二次审计。保存完整门控
FP32/FP64 dot与设备sigmoid；融合内部dot未直接观测，不宣称
内部状态或整模型独立参考。生产未改、原错答/尺度缺陷仍在；
下一步补完整QSA各query与PLE，再评估后续计算改动。

## 2026-09-24：完整共享投影重放与高精度差异保留

复用冻结HTTP证据，零警告构建后96次投影754974720项同记录
算法；FP64经FP32/BF16舍入差异564891项全部保留。96个末行
184320项CPU顺序FP64与Dgemm原始147项不同，最大4.44e-16，
舍入全同；不把它报告成原始FP64全同。全部舍入参考以基底+delta
逐字节/SHA恢复验证，末尾再次审计，末行FP64和全部差异留档。
生产未改、原错答和尺度缺陷仍在；下一步完整SwiGLU与最终合并。

## 2026-09-24：完整共享专家与最终MoE边界采集

为补全路由专家之外的MoE路径，新观测零警告构建后先三侧HTTP，
三次仍600440、4096/7 token、stop、服务0；这是观测保持原错，
不是质量通过。48层240份元数据完整，192组权重472104960字节
直接匹配checkpoint，完整GU/SwiGLU/down及实际算法保存。
实际routed、x和最终HC block全字节接通且不重复存储。
生产未改、算术参考未运行；下一步逐层无损差异保存投影参考，
再补SwiGLU与最终门控合并。见shared报告。

## 2026-09-24：重复清零证据共享，为shared采集留出容量

完整重读48份路由合并前清零缓冲后，将47份重复副本原子替换
为只读硬链接；原manifest全部127818条记录复核相同，原路径
与字节保留，inode/mtime/权限变化留档。释放1971331072字节，
可用约24.88GB。生产未改、无推理测试，不作为正确性或性能验收。
下一步完整shared GU/SwiGLU/down及最终MoECombine观测，已有
routed/x/final block来源全字节比对，避免重复保存，保留20GiB余量。

## 2026-09-24：完整down矩阵乘参考无输出差异

复用冻结HTTP，零警告构建后重建实际中间FP4/SF、行序和权重。
21197次down的5033164800个BF16输出同记录算法及FP64经
FP32/BF16舍入；480末行1228800个CPU顺序FP64同Dgemm逐位
一致。逐层完整临时结果在落盘delta完整恢复、逐字节/SHA校验后
删除，最终再次完整重建审计；末行FP64和全部差异保留，其他一致
位置的未舍入FP64不落盘。生产未改，实际SF错误和原错答未解决；
下一步清零证据去重后补完整shared expert及最终MoECombine。

## 2026-09-24：完整down边界与路由专家合并参考

为接通中间量化到路由输出，新观测先三侧HTTP仍600440、服务0。
21197次down权重/尺度直接匹配checkpoint，19535324776字节
来源事后重读全同；5033164800个BF16输出接通实际合并消费者，
1966080行映射及路由权重完整核对。503316480个合并前值均正零，
合并后同CPU十槽FP32 fma参考；全部参考以可重建delta保存，
临时副本仅在完整恢复及SHA校验后删除。只覆盖CombineGrouped，
不包含shared/最终MoECombine或down算术。生产未改，原错答和
尺度缺陷仍在；下一步完整down矩阵乘参考。详见down报告。

## 2026-09-24：GU重复证据只读共享，释放10.14GB

为后续完整down及合并边界采集保留容量，先完整重读21197组、
63591条相同GU实际/重放/高精度舍入结果，再以只读硬链接原子
替换42394份副本。两个原manifest全部341000条记录复核全同，
原路径/字节不变，inode/mtime/权限变化和原始元数据完整留档。
释放实际分配10143596544字节，可用约38GB；无生产或观测改动，
不运行HTTP，不构成正确性或性能验收。下一步完整down/合并边界
采集，预计14.09GB逻辑输出，参考采用可重建差异并保留容量余量。

## 2026-09-24：完整SwiGLU/中间量化确认尺度缺陷

复用冻结HTTP证据，零警告构建后核对78643200组ratio与
1258291200个FP4值。CPU算术+设备sigmoid的ratio和给定实际SF
的FP4全同，独立RNE同native，实际legacy尺度错误148339项均在
[248,432)，覆盖48层、71131个token×层。纯CPU/FP64参考的
21/131个FP4数值差异及4/22个额外RNE差异完整保留，不能混作
新编码缺陷。生产未改、修正未传播、原错答未修复。下一步先治理
重复GU证据存储，再补完整down/shared/combine。详见中间量化报告。

## 2026-09-24：完整MoE gate/up高精度参考无输出差异

为区分同算法复现与计算偏差，复用冻结HTTP证据，独立解码实际
FP4/E4M3输入及权重，保留实际FP32 alpha，零警告构建后进行
完整FP64参考。21197次GU的2516582400个输出经FP32/BF16
舍入全部同实际；480末行614400个CPU顺序FP64与Dgemm逐位
一致。完整舍入参考及末行FP64落盘，其他一致位置的未舍入
FP64不落盘，边界明确。已知尺度错误/原错答仍在，生产未改；
下一步完整SwiGLU与中间量化。详见GU报告后续。

## 2026-09-24：完整MoE gate/up消费者与记录算法重放

为接通输入量化到专家计算，新观测先三侧HTTP仍600440、服务0。
21197次专家权重/尺度直接比对checkpoint，39070734340字节来源
事后重读摘要相同；1966080行按flat索引重排接通冻结gather。
2516582400个GU输出同记录算法，后续78643200个有效SF布局
字节相同。完整输出和差异保留，旧末行文件历史摘要缺口单列；
不宣称独立算术或整模型正确。生产未改；下一步完整GU高精度
参考与SwiGLU/中间量化。详见MOE_GU_REFERENCE_2026-09-24.md。

## 2026-09-24：完整MoE输入量化确认高区间尺度错误

复用冻结HTTP快照，零警告构建后重建314572800个尺度ratio与
5033164800个FP4值。CPU/device ratio、独立最近偶数/native均
全同；实际legacy错误1208940项全在[248,432)，覆盖48层68119个
token×层，去10槽重复为120894个group。给定实际SF的FP4全同，
但不能宣布量化正确。全值/全差异留存，生产未改、修正未传播、
原错答因果未建。下一步绑定GU消费者及完整专家链。详见输入量化报告。

## 2026-09-24：完整4K MoE mapping/gather边界

为接通完整路由到专家输入，新观测先三侧HTTP仍原错、服务0。
1966080个flat分配及逆映射全同，combine消费映射/权重已绑定；
21197次gather完整采集，49152个gate/up输入尺度同checkpoint，
314572800个有效SF布局字节一致。50.33亿FP4值已保存，但量化
算术未验，已知编码问题仍在。生产未改；下一步冻结快照全行
量化参考。详见MOE_GATHER_REFERENCE_2026-09-24.md。

## 2026-09-24：完整4K MoE路由与高精度选择边界

新最小观测先三侧HTTP仍原错、服务0。完整100663296 logits同
记录算法，FP6451380项舍入差异；1966080个ID及CPU+设备exp
路由权重全同。FP64再舍入后103行专家集合变化，59行实际边界
同分、44行严格分差，完整明细保留，未建立错答因果。192条旧
末行绑定及48条CPU高精度末行对照通过。边界JSON int32序列化
失败留档并修正。生产未改，下一步全行mapping/gather/输入量化。
详见MOE_ROUTER_REFERENCE_2026-09-24.md。

## 2026-09-24：冻结HC证据重复存储治理

为继续全位置MoE/QSA采集恢复容量，先实读624个候选校验SHA，
再将194组相同内容的430份重复副本原子替换为只读硬链接。
释放36.07GB，可用约60.73GB；两个原manifest全部2333条复核
通过，路径/字节保留，inode/权限/mtime变化逐步留档。生产未改，
未新增推理验收。下一步完整4K MoE路由。详见EVIDENCE_STORAGE.md。

## 2026-09-24：完整4K HC投影FP64参考

复用冻结HTTP证据，补全部288个HC投影的Dgemm高精度参考；
4153933824项中1299342项BF16舍入不同，全差异位置/FP64值及
全部舍入结果无损保存。288条末行对照仍旧352项。明确一致位置
非末行的未舍入FP64不落盘，不能称完整FP64中间矩阵留存。
生产未改、原错未修复；下一步先核验约36.07GB重复证据候选，
恢复采集容量后继续全位置MoE/QSA/PLE。详见HC投影报告后续。

## 2026-09-24：完整4K HC实际投影及门值链

为补齐全行低秩/注入来源，新增最小观测，先三侧HTTP仍原错、
服务0，再匹配288组实际权重与checkpoint。完整4153933824项
投影同记录算法重放，125829120 SiLU/1572864 gate同三种参考。
全字节复用已有norm/up/gate，原始增量约1.78GB，参考无损保存。
首次未用NaN编码转换警告留档，修正后完整结果相同。生产未改、
原错未修复；下一步全行投影FP64独立参考。详见HC投影报告后续。

## 2026-09-24：完整4K HC混合与无损参考保存

为接通归一化到混合，复用冻结快照，绑定288份输入及96条完整
norm参考；1006632960项同CPU四分支fma+设备sigmoid表。
纯CPU9962、FP6425801差异完整保存，以实际输出加差异索引/值
无损还原并核对完整SHA后移除本轮临时副本，旧证据保留。
192条末行检查同旧参考。生产未改、原错仍在；下一步全行HC
低秩/inject投影及门值来源。详见HC交接报告2026-09-24后续。

## 2026-09-24：完整4K HC归一化指定参考

为补齐全行主干计算，复用已通过观测一致性检查的冻结快照；先绑定
384份输入并预算24.56GB参考存储，再核对4026531840项输出。
CPU指定归约/乘法+设备rsqrt全部一致，纯CPU13832、FP6429533项
差异保留；末行192条检查同旧参考。工具零警告，生产未改，原错
未修复，未新增HTTP或性能验收。下一步全行混合及剩余存储预算。
详见HC_HANDOFF_REFERENCE_2026-09-23.md的2026-09-24后续。
