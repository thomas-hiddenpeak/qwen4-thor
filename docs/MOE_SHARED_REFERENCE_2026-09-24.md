# 完整4K共享专家与最终MoE合并核对

## 边界采集：通过观测一致性，尚未验证算术

2026-09-24，生产二进制2392f6b1未改。新只读观测器零警告构建后，
首项为tools/evalscope原4K HTTP关闭/开启/关闭观测三次对照。
三次均4096输入、7输出、stop、服务退出0，仍输出错误答案600440，
期望360284；质量驱动退出1是原失败，不是质量验收通过。

48层240份边界元数据全部通过。实际共享GU输入全字节等于已冻结
HC mixed MLP输入；SwiGLU输出接通down，down输出接通最终
MoECombine；routed输入全字节等于此前CombineGrouped输出，
最终BF16输出全字节等于HC write.block。已有大缓冲只比对不复制。

192个实际BF16权重张量、472104960字节直接匹配checkpoint：
每层gate/up/down及shared_expert_gate。保存全部251658240个GU、
125829120个SwiGLU、503316480个down输出以及96份实际算法。
三种算术参考尚未运行，不能用交接一致代替计算正确。

证据：`.q4t-work/e2e/moe-full-shared-20260924/`，含HTTP原始记录、
checkpoint偏移/摘要、上下游来源绑定和完整manifest。复现模板：
`tools/verify/moe_full_shared/`。采集后可用22645972992字节；后续
参考逐层生成，以实际输出加无损差异保存，预留20GiB磁盘容量。

原错答、已知E4M3编码缺陷及因果关系仍未解决。下一步完整共享
GU/down记录算法与FP64参考，再核对SwiGLU和最终门控合并。

## 后续：完整共享投影重放与FP64参考

复用上述冻结HTTP证据，无生产或观测改动；参考程序零警告构建。
96次实际BF16矩阵乘、754974720项输出与本次记录算法重放完全
一致。独立转换输入与权重为FP64，Dgemm后经FP32/BF16舍入：
GU差异322765项、down差异242126项，合计564891项，全部索引、
对应FP64值和舍入值保留。该差异不是新增实现缺陷的判定。

96个末行共184320项另用CPU顺序FP64累加：147项原始FP64
与Dgemm不同，均在layer-0 GU，最大绝对差4.440892098500626e-16；
经FP32/BF16舍入全部一致。不能将末行检查写作原始FP64全同。

全部重放与舍入参考以实际基底+delta保存；每份临时完整输出先
逐字节和SHA重建校验后删除，完成后再次完整恢复审计。另保存
末行完整CPU/GPU FP64；其他一致位置未舍入FP64不落盘。
本轮来源/算法/权重全部绑定，并直接重读checkpoint片段。

证据：`.q4t-work/e2e/moe-full-shared-reference-20260924/`；模板：
`tools/verify/moe_full_shared_reference/`。参考归档后可用约22.63GB，
预算600MB、另保留20GiB。原错答未修复；下一步SwiGLU与最终
门控合并的全行参考，不用本阶段重放一致替代整模型正确性。

## 后续：完整SwiGLU与最终门控合并参考

复用冻结HTTP，无生产/观测改动。CPU和设备辅助程序均零警告；
336份完整实际输入/输出绑定后，48层全部输出与以下参考比较：

| 参考 | SwiGLU差异 / 125829120项 | 最终合并差异 / 503316480项 |
|---|---:|---:|
| CPU指定FP32运算，设备仅提供sigmoid | 0 | 0 |
| 同顺序CPU FP32，使用CPU expf | 17 | 8890 |
| FP64参考，经FP32/BF16舍入 | 537 | 17614 |

差异均为数值差异，不是仅零符号不同。CPU门控dot按每线程十次
fma、256线程加法树重建，设备只计算sigmoid；最终合并为FP32
fma再BF16。SwiGLU按两次FP32乘法再BF16。FP64版本使用顺序
FP64 dot、稳定sigmoid及高精度乘加。融合内部gate dot未直接
观测；本结论是给定实际操作数后的完整输出一致。

三种完整BF16参考均从实际基底+完整delta无损恢复，保存后逐
字节/SHA复核才删除临时结果，完成后再次完整恢复审计。保存全部
CPU FP32/FP64 dot、设备sigmoid、BF16查表及所有差异索引/位值；
逐元素未舍入非线性参考不落盘。证据：
`.q4t-work/e2e/moe-full-shared-nonlinear-20260924/`；模板：
`tools/verify/moe_full_shared_nonlinear/`。

本阶段补齐原4K prefill共享专家与最终MoE合并的条件算术参考，
没有把高精度投影或修正后的尺度向后传播。已知E4M3问题与原题
错答仍未解决；完整QSA各query、完整PLE算术及其他上下文/chunk/
decode覆盖仍未完成。下一步补齐原4K的完整QSA与PLE，避免将
局部/末行一致或可重放现有算法误报为独立整模型正确性通过。
