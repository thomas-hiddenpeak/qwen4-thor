# 冻结证据存储

## 2026-09-24：HC重复内容共享

范围仅为`.q4t-work/e2e/hc-full-boundaries-20260923/`及
`hc-full-norm-20260924/`中两个原始manifest绑定、至少16MiB的
BF16文件。模型目录、reference/、生产二进制和其他任务文件未动。

先按manifest定位候选，再重新读取624个文件计算SHA，检查真实
路径、文件类型、设备/inode/大小/mtime/mode/link count及来源
manifest一致性。全部预检成功后，将194组完全相同内容的430份
重复副本以原子替换改为硬链接，所有共享文件去掉写权限。
每个步骤都记录并fsync journal，原始逐路径元数据保留于preflight。
最后复核两个原始manifest全部2333条记录，原manifest本身未改，
所有原路径及内容完整保留。审计同inode只读取一次，仍逐路径核对。

释放原重复副本占用36071014400字节（约36.07GB），可用空间从
24662679552增至60732952576字节（约60.73GB）。这是证据存储
治理，不是推理性能提升或正确性验收。本次无生产/观测器改动，
未运行HTTP或推理测试；存储校验仅证明文件内容和清单不变。

共享改变了文件的inode、链接数、只读权限；别名mtime使用同一
canonical文件的mtime。不能声称逐路径元数据完全未变，原始值
可在preflight和journal中查到。未修改任何BF16数值或丢弃失败记录。

## 后续操作约束

- 冻结目录只读使用；新实验使用新目录，不在旧目录覆盖输出。
- 共享文件不可原地修改，也不可只对某个别名chmod后写入；那会
  影响所有别名。需要可写文件时，在新实验目录复制为独立文件。
- 若要恢复某路径的独立存储，先创建新的完整临时副本，校验SHA，
  根据preflight恢复该路径的mode/mtime，再原子替换该路径；不要
  向已有硬链接直接复制覆盖。恢复需要相应空闲容量。
- 复用时仍校验原manifest，不能以只读权限或链接关系代替内容校验。

证据目录`.q4t-work/e2e/evidence-storage-20260924/`含候选清单、
preflight、逐操作journal、summary及artifact-binding。执行工具
模板为`tools/verify/evidence_storage/share.py.in`；实际候选列表
保存在证据目录，不把大文件内容或机器inode列表作为通用源码。
模板限定本次两个来源，复用须重新制作范围明确的候选清单和输出
目录；不能拿旧inode清单操作已变化的目录。

该阶段结束时的下一步是回到完整4K MoE路由：从已绑定HC MLP mixed输入核对512路
logits、10个选中专家及softmax路由权重，再扩展专家计算。

## 2026-09-24：GU实际输出、重放与高精度舍入结果共享

范围仅为`moe-full-gu-20260924`中的实际GU输出和同算法重放输出，
以及`moe-full-gu-fp64-20260924`中的FP64经FP32/BF16舍入输出。
21197个专家各对应三个文件，原manifest摘要相同；全部63591条
路径重新读取SHA并完成元数据预检后，才开始只读硬链接替换。
不依据“数值比较通过”直接跳过字节校验。

42394份重复副本释放实际分配空间10143596544字节（约10.14GB）；
对应逻辑内容10066329600字节（约10.07GB），两者因文件块分配
不同而不同。summary记录空闲从27971907584增至38050422784字节；
保存最终工具/日志/清单后约38.03GB可用，不把元数据占用计作内容损失。

两个原始manifest的全部341000条记录最终复核通过，manifest本身
未改。原路径及字节保持完整；所有共享路径去掉写权限，inode、
链接数、mtime的变化遵循前述规则，原始逐路径元数据及操作顺序
保存于preflight和fsync journal。没有丢弃旧失败记录或参考差异。

证据`.q4t-work/e2e/moe-gu-storage-20260924/`；模板
`tools/verify/moe_gu_storage/`。模型目录、reference/、生产二进制
及其他任务内容未动；这是存储治理，未运行推理测试，也不构成
模型正确性或性能验收。原错答与量化尺度缺陷均未解决。

下一步完整down投影采集：根据实际BF16形状，专家输出逻辑内容
10066329600字节；4096×2560 FP32合并前后边界共4026531840字节。
后续参考应采用可完整重建、逐字节验证的差异存储，避免再保存
整份相同矩阵；正式采集前仍需加上元数据、临时结果和20GiB余量
复核容量。新增观测仍必须先三侧真实HTTP，再分析数值。

## 2026-09-24：路由合并前的清零证据共享

范围只包含moe-full-down-20260924的48个`.combine-before.f32`，
每个41943040字节。上一阶段已完整确认正零，本阶段按原manifest
重读每条路径的内容及元数据，全部预检后，将47份重复副本原子
替换为同一只读硬链接。原始manifest全部127818条记录复核相同，
原路径和字节不变；inode/链接数/mtime/权限变化沿用上述留档规则。

释放实际分配1971331072字节（约1.97GB），summary记录可用从
22909816832增至24881049600字节（约24.88GB）。证据位于
`.q4t-work/e2e/moe-routed-zero-storage-20260924/`；模板
`tools/verify/moe_routed_zero_storage/`。生产、模型、reference/
及其他任务内容不变，未运行推理测试，不构成质量或性能验收。

这为完整shared GU/SwiGLU/down观测提供容量：三类大输出逻辑
内容分别503316480、251658240、1006632960字节，权重若独立
保存再需472104960字节。已有routed、输入x、最终HC block可作
全字节来源比较，不重复保存；参考继续逐层无损差异存储并保留
20GiB余量。新增观测必须先三侧HTTP，之后才核对算术。
