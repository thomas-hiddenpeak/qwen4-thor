# 完整4K QSA选择与attention核对

## 已完成：全query边界与KV写入

生产二进制2392f6b1未改。只读观测器零警告构建后，首项为
关闭/开启/关闭tools/evalscope原4K HTTP，三次均600440、
4096/7 token、stop、服务0、质量驱动1；36份元数据通过且
采集均在监听后。原错答保持不代表模型质量通过。

本次12层×4096共49152个query，保存完整评分、选择列表、
长度、Q/gate/attention输出、写入K/V、位置、页表和cache。
实际选择/cache到attention输入的指针及完整字节均接通，没有
复制第二份选择/cache。seq id全为0；指针为空时保存默认零值，
元数据seq_ids_null明确标记。实际页表映射恒等且槽0..4095
完整覆盖，50331648个KV BF16值逐项匹配写入K/V。

100859904个选择槽中75546624个有效索引，全部长度符合分组/
尾部规则、唯一且不指向未来，未使用槽均-1。这不证明选择排序
符合评分，完整排序参考仍待运行。Q、gate和attention输出各
301989888个BF16值完整保留且有限，attention算术尚未重算。

132份旧末query/完整张量逐字节一致。初版分析误将旧manifest
当作直接绑定原始文件而KeyError，analyze-v1.py/log保留；旧
manifest实际绑定capture-binding.json，再由其绑定原始文件。
修正后两层摘要链全部核对，无来源内容变化，无需重跑HTTP。

证据：`.q4t-work/e2e/qsa-full-selection-20260924/`；模板：
`tools/verify/qsa_full_selection/`。含完整HTTP、采集、旧来源
绑定及manifest，归档后可用58702303232字节。采集预算3.2GB、
参考1GB、另保留20GiB；未来超出页表预算必须停止，不能截断。

## 未完成与下一步

先用冻结的完整评分重建所有query的选择顺序，再核对完整
attention。评分计算、Q/gate来源、完整投影和out_proj消费者
仍需逐步接通/重算。此前末query/首decode局部参考不能代替
全query覆盖。本阶段是给定实际输入的边界证据，不是独立整模型
前向；原错答、已知量化尺度缺陷及其因果关系仍未解决。


## 后续：完整选择顺序及最高分集合参考

复用冻结HTTP，零警告构建后绑定48份评分/选择/长度/位置来源。
CPU独立标量比较网络重建12层49152个query的选择顺序，全部
100859904个槽（其中75546624个有效索引）及全部长度均相同。
不调用生产排序代码，按严格比较保留同分网络次序。

另用std::sort排序可见评分，独立检查阈值、入选集合未遗漏更高
评分、候选唯一且选中组按分值非降，全部通过。24540个稀疏行
中5607行的选择边界拆分了同分候选；这些行的精确顺序仍与
实际全同，不以“同分所以任意顺序”掩盖比较差异。其余24612行
走可见位置全选分支，也逐槽核对，包括-1尾部。

每层完整参考以实际基底+索引/值delta保存，先逐字节/SHA恢复
才删除临时文件，最终再完整恢复12层。所有query的长度、分支、
阈值、同分总数/入选数和语义检查标记均保留。证据：
`.q4t-work/e2e/qsa-full-selection-reference-20260924/`；模板：
`tools/verify/qsa_full_selection_reference/`。生产未改、未新增HTTP。

本结论以实际冻结评分为输入，评分算术、完整attention及其上游
仍未验证，原错答未修复。下一步全query attention算术参考，
保留BF16 QK/PV乘法与在线softmax的实际舍入边界，并明确设备
数学函数依赖；随后接通完整评分/投影来源。


## 后续：完整attention指定算术参考

复用原三侧HTTP冻结输入，生产及观测器不变，无新增HTTP。
零警告构建后绑定84份Q/gate/cache/selection/length/page-table/
attention-output来源，以512 query分块完成12层全部49152个query。
75546624个有效KV索引参与计算；301989888个最终BF16输出全部
逐位相同，包含零符号。进程正常退出，完整覆盖与最终恢复审计通过。

参考将QK/PV的BF16 MMA和在线softmax拆成独立设备kernel，CPU
按16位置块顺序执行FP32 FMA累加、归一化除法、门控前BF16舍入，
再用设备sigmoid查表进行CPU乘法和最终BF16舍入。它没有调用
生产模型kernel，但仍依赖相同设备指令与数学函数；不能称为完全
独立的高精度参考，也不能用结果相同证明数学公式或上游输入正确。

保存完整门控前BF16、FP32分母、gate查表、所有query差异计数。
最终BF16参考以实际基底+完整delta保存，每块先逐字节/SHA恢复
才删除临时输出，末尾再次恢复全部12层并保存SHA。所有最终差异
位置的CPU累加/归一化/门控FP32值有保留接口，本次为空。没有
保存全部QK/prob/PV/alpha及一致位置未舍入值，未对这些内部量
逐项核对生产。归档前文件635378965字节，在1GB预算内。

证据：`.q4t-work/e2e/qsa-full-attention-primitives-20260924/`；模板：
`tools/verify/qsa_full_attention_primitives/`。生产二进制仍2392f6b1，
不构成新性能结果。下一步补全query高精度/显式舍入对照及完整
评分、Q/gate投影、out_proj消费者；仍需独立组合前向与错答因果。
原4K的600440错答和E4M3尺度缺陷未解决，整模型正确性未通过。


## 后续：完整FP64公式与显式舍入对照

生产/观测器未改，复用已绑定三侧HTTP的84份实际输入。零警告
构建后完成1536块、49152个query，每个变体比较301989888个
最终BF16值；每个BF16参考均经FP32舍入。进程退出0，覆盖、
逐query差异计数、全部36份完整层级参考恢复及分支统计审计通过。

独立CUDA FP64核按256维顺序FMA计算QK，使用双精度稳定softmax、
PV、sigmoid及门控，不调用生产kernel或Tensor Core。对照三种
精度合同，不用差异比例或临时容忍阈值决定通过：

| 变体 | 最终BF16不同项 | 涉及query | 全选分支不同项 | 稀疏分支不同项 |
|---|---:|---:|---:|---:|
| FP64公式，最后舍入 | 85672536 | 49140 | 43347759 | 42324777 |
| 额外在门控前物化BF16 | 20802125 | 49139 | 11497843 | 9304282 |
| 在线16位置块，概率和门控前均物化BF16 | 175500 | 42079 | 90479 | 85021 |

第三变体的softmax分母仍累加未量化概率，PV使用BF16概率、FP64
块内/块间累加；与实际FP32/Tensor Core数学合同仍不同。三类
差异都不是仅零符号差异。加入中间舍入后差异数减少，不代表
剩余175500项已归因某一个误差源，也不据此判定新增实现缺陷。
上一轮指定设备原语+CPU参考全同，只证明其条件计算一致。

每层末query再用CPU std::fma/std::exp重算：589824个有效QK
分数逐位同设备，590976个评分数组槽含填零尾部也全同。每个
变体73728个最终BF16值均同CPU；未舍入FP64分别有9576/3101/
3106项不同，最大绝对差均2.220446049250313e-16，完整保留。
这不是全query纯CPU参考，不能从末query扩张硬件独立性。

所有最终不同项的索引、BF16值、对应FP64值及所有query差异
计数保留；完整BF16参考从实际基底+delta逐块恢复，删除临时
未舍入全量输出后再次完整恢复12层×3变体。保存全query两种
FP64分母、末query完整CPU/设备输出和评分；一致位置非末query
未舍入值及完整概率/QK/PV不保存。归档前1177911779字节，在
4GB预算内，另保留20GiB。证据：
`.q4t-work/e2e/qsa-full-attention-fp64-20260924/`；模板：
`tools/verify/qsa_full_attention_fp64/`。

### 终态清单修正

上一轮primitives与本轮FP64控制器均在生成初始artifact-binding.json
之后向重定向run.log追加最后一行summary，故该日志的最终摘要
不匹配。原清单分别1066/9275项完整重读，唯一变化都是此日志；
原日志前缀SHA仍匹配，追加行解析后等于summary.json，所有其他
条目不变。没有改写原清单、日志或数值文件。

在确认运行/审计进程已退出后，分别新增finalization审计及
final-artifact-binding.json，最终1069/9283条目全量核对一致。
最终取证须使用终态清单，初始清单保留以解释生成时机错误。
两个run.py模板已把最终打印移至初始清单之前，额外封存工具
仍只允许零变化或能完整证明的旧日志追加，其他差异立即失败。
本次真实执行工具原文仍存证据目录；修正只涉及归档，不改数值。

全量高精度差异已列清，但完整评分/投影/变换与out_proj消费者
尚待补齐，组合前向及错答因果未建立。原600440错答与E4M3尺度
缺陷未解决，不接受新的运行时，不产生新性能结论。


## 后续：完整主投影、变换边界及输出消费者

新增只读观测器零警告构建，首项三侧tools/evalscope原4K HTTP：
全部600440、4096/7 token、stop、服务0、质量驱动1；120份元数据
均通过，采集在监听后。原错答未修复，只确认观测不变性。

绑定96份冻结HC/QSA来源。现场完整输入同HC mixed，qg/k/v
共用输入指针；norm消费qg/k、RoPE消费norm结果、KV写入消费
变换后K及投影V、attention消费Q/gate、out投影消费attention输出、
最终残差消费out，相关指针与完整字节均核对。HC mixed生产者
本轮只作跨运行完整字节绑定，未重新追踪其生产指针。

48次实际BF16 GEMM权重与cuBLASLt算法保存，核对布局、FP32计算、
alpha1/beta0、转置及32MiB workspace。完整qg/k原始投影输出和
q/k norm输出新增保存；已有V/out/RoPE后Q/K/gate现场比较冻结
来源，不重复落盘。norm权重/epsilon、theta、逻辑位置/三行MRoPE
坐标和seqids保存。位置0..4095、三行相同，seqids均0；空seqids
指针物化零数组但未保存null标记，因此不能区分两种传入形式。

72组实际权重（1195388928字节）同checkpoint；276份旧权重/
末行/参数证据一致。301989888个gate拆分值为原始BF16位拷贝；
245366784个RoPE不旋转后缀值与norm输出相同。327155712个完整
q/k norm值已采集且有限，但本阶段未独立重算norm/RoPE算术。

采集3109967284字节，在3.4GB预算内；分析终态后再复制日志并
封存，546项摘要全量复核通过。证据：
`.q4t-work/e2e/qsa-full-main-20260924/`；模板：
`tools/verify/qsa_full_main/`。生产二进制仍2392f6b1。

## 后续：完整48次主投影重放及FP64参考

复用上述冻结HTTP，不新增观测或HTTP。零警告构建后，重新绑定
实际矩阵输入/输出/权重/算法及checkpoint段，完成全部48次、
780140544个BF16输出的记录算法重放，全部逐位一致。

| 投影 | BF16输出数 | FP64经FP32/BF16舍入不同项 |
|---|---:|---:|
| qg | 603979776 | 575041 |
| k | 25165824 | 33789 |
| v | 25165824 | 28772 |
| out | 125829120 | 677529 |
| 合计 | 780140544 | 1315131 |

差异均非仅零符号，全部索引及FP64值保留。48条末行190464个
CPU顺序FP64 FMA与设备Dgemm原始位值/舍入均全同；同旧末行
FP64参考的BF16结果也全同。不能将末行CPU一致推广到所有行的
CPU独立计算，记录算法重放亦不是独立cuBLAS硬件证明。

两种完整参考以实际基底+全部索引/值delta保存，删除临时副本前
逐字节/SHA恢复，末尾再次恢复全部96份。所有FP64不同项及完整
CPU/设备末行保存，一致位置的非末行未舍入值不保存。控制器
真实退出后封存日志，443项清单全部复核，参考约25MB，在1GB
预算内。证据：`.q4t-work/e2e/qsa-full-main-reference-20260924/`；
模板：`tools/verify/qsa_full_main_reference/`。

下一步复用完整主投影快照核对q/k归一化和RoPE算术，再补indexer
完整评分链。当前仍以实际HC/attention输入作条件参考，未做组合
前向或错误尺度修正传播；原错答与E4M3问题未解决，生产未改，
不产生新质量或性能验收结论。


## 后续：完整q/k归一化与RoPE算术

复用已封存三侧HTTP，生产/观测器不变，不新增HTTP。零警告构建
后绑定252份输入与24份norm checkpoint权重段，覆盖12层4096
位置的q/k两侧。每个norm/RoPE变体覆盖327155712个BF16值；
RoPE其中81788928个值参与前64维旋转，245366784个后缀值保持。

归一化由CPU逐个重建八个32-lane XOR归约树及顺序warp累加，
给定相同FP32方差后分别使用设备rsqrt或CPU double sqrt/倒数再
舍入FP32；另计算独立FP64平方和/归一化。RoPE由CPU按当前
分量*cos的显式FMA顺序合成，分别用设备pow/cos/sin或CPU
双精度函数舍入FP32生成频率/三角值；另保留独立FP64旋转公式。
实际epsilon、theta和三行位置用于各参考，最终经FP32/BF16舍入。

| 对象 | 指定CPU顺序+设备数学不同项 | CPU数学不同项 | FP64不同项 |
|---|---:|---:|---:|
| q/k归一化 | 0 | 1186 | 2508 |
| RoPE | 0 | 99253 | 176991 |

差异均非仅零符号。按同层同位置合并q/k后，CPU/FP64 norm差异
涉及1123/2376个query×layer，RoPE涉及34726/42604个；完整
统计分布保存。初始summary的affected_queries是对q/k两侧求和，
实际量纲为query×side×layer，不能当成唯一query数。例如FP64
RoPE的53630个侧别槽对应42604个唯一query×layer。新增覆盖
审计显式分开两种计数，原summary和原始日志保留，不静默改名。

完整144份BF16参考均以实际基底+全部索引/值delta保存，删除
临时输出前逐字节/SHA恢复，最后再次完整恢复。全部差异位置的
未舍入值保留（FP32值精确提升为FP64存储）；完整方差、倒数和
三种参考的频率/角度/cos/sin表均保存，匹配位置最终未舍入值
不保存。控制器/审计终态后封存，931项清单全量复核，约207MB，
在1GB预算内。证据：`.q4t-work/e2e/qsa-full-transform-20260924/`；
模板：`tools/verify/qsa_full_transform/`。

RoPE始终使用实际捕获的归一化输入，未传播高精度norm参考。
因此本结果仍是逐算子条件参考，设备数学依赖明确，不是独立
整层/整模型前向。完整主投影和q/k变换参考已补齐；下一步完整
indexer query/key投影、查询变换、压缩、评分矩阵及汇总链。
原600440错答和E4M3尺度缺陷仍未修复，生产未改，无新性能结论。


## 后续：完整indexer边界、矩阵与评分汇总

新增只读观测零警告构建后，首项tools/evalscope关闭/开启/关闭
原4K HTTP三次保持600440、4096/7、stop、服务0、质量驱动1。
108份元数据、96份跨运行来源全字节相同；36组checkpoint权重
39327744字节、216份旧完整张量/末行绑定一致。相关实际消费者
指针接通；HC mixed生产者仍仅跨运行全字节绑定，非新指针观测。

捕获完整IQ raw/变换后输出、投影权重/算法、norm参数、seqids及
null标记、三行位置坐标、全部2048列压缩key与评分矩阵。IK/raw
历史、压缩前1024组、汇总/选择复用已绑定冻结全张量。完整矩阵
402653184个点积槽，其中实际因果可见100614144；masked点积和
padding key非有限项均0，不能把masked列当作评分实际读取。

随后冻结输入上完成36次矩阵乘参考：

| 阶段 | BF16输出数 | 记录算法重放差异 | FP64经FP32/BF16舍入差异 |
|---|---:|---:|---:|
| IQ投影 | 25165824 | 0 | 28850 |
| IK投影 | 6291456 | 0 | 7770 |
| 四头点积矩阵 | 402653184 | 0 | 7139 |
| 合计 | 434110464 | 0 | 43759 |

评分差异中3585项实际可见、3554项被因果mask、padding列0项。
32256个矩阵末行CPU顺序FP64 FMA与设备FP64原始值/舍入全同；
评分矩阵末行只覆盖末query的head3，不是全部四头。72份完整
BF16参考均以基底+delta无损恢复，全部FP64差异原值和末行保留，
匹配的非末行原始FP64不保存。重放仍依赖cuBLAS、使用实际输入。

评分汇总覆盖100663296个F32输出，可见25153536。CPU四头ReLU/
顺序FP32相加，使用旧已绑定设备rsqrtf(128)常量时逐位一致；
CPU double sqrt倒数舍入FP32变体差异23278560，完整FP64变体
差异24710060，均在可见区域，零符号单独差异0。没有把高精度
矩阵输出传播到汇总。36份完整F32参考无损恢复；全部差异原值及
完整可见四头FP32和保留，匹配最终原值和不可见组和不保存。

压缩key旧完整1024组×12参考的84份操作数/输出已逐字节接通，
复用1572864个BF16值同CPU指定顺序+设备数学的结果，无重复执行。
旧variant5完整输出未被当时清单直接覆盖，本次只证明其当前字节
等于旧已绑定expected摘要；旧计数报告本身由原清单绑定。未把
当前核对冒充历史清单已有覆盖，也未修改任何旧证据。

两个工具初版失败保留：汇总初版把含两个float的device-scale当成
单值，输入检查即退出；v2核对scale.cu明确第0项为rsqrtf，第1项
为1/sqrtf，只使用第0项。复用审计初版因旧清单未包含完整variant
输出而KeyError；v2明确记录历史未绑定与当前expected比较方式。
均发生于离线工具，不改观测/运行时，不用新HTTP替换失败记录。

证据均在`.q4t-work/e2e/`：

- `qsa-full-indexer-20260924`：捕获约954MB，终态559项清单。
- `qsa-full-indexer-reference-20260924`：矩阵参考298项，约1.51MB。
- `qsa-full-indexer-reduce-v2-20260924`：汇总777项，约438MB。
- `qsa-full-indexer-audit-v2-20260924`：覆盖与压缩输入复用绑定。

上述清单均在对应控制器终态后生成并全量复核。模板分别为
`tools/verify/qsa_full_indexer{,_reference,_reduce,_audit}/`。
本阶段仍缺完整indexer查询norm/RoPE算术，旧末query参考不能推广；
补齐后应转向组合前向/已知E4M3修正传播，而非继续累计条件重放
数量。生产二进制未改，原4K错答未修复，没有新的质量或性能验收。
