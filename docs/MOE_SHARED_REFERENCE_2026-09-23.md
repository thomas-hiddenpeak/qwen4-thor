# 同一 token 的共享专家 MLP 参考

## 结论

运行时未改，仍为已接受二进制2392f6b1。原4K错题最后一个
prefill token（4095）在48层的共享支路已建立逐阶段独立参考。
**尚未完成整模型正确性确认，也未修复原4K错误。**

| 比较边界 | BF16元素数 | 不同位模式数 |
|---|---:|---:|
| checkpoint gate/up × 实际MoE输入，FP64→FP32→BF16 | 61440 | 78 |
| 实际gate/up输入上的SwiGLU，FP64参考 | 30720 | 0 |
| 同一SwiGLU，CPU FP32运算参考 | 30720 | 0 |
| checkpoint down × 实际SwiGLU输出，FP64→FP32→BF16 | 122880 | 64 |
| 从实际MoE输入连续重算整个共享MLP | 122880 | 2857 |

连续参考每层都从该层实际输入开始，未把独立结果跨层传播。
2857项差异包含上游gate/up差异的传播，不是2857个独立实现错误。
没有新增误差容忍阈值，全部差异和索引保留。

## HTTP先行与证据

只读观测库零警告构建后，第一项测试是tools/evalscope HTTP。
无观测→有观测→无观测三次原4K请求，均输出600440，应为360284；
4096输入、7输出、stop，三服务退出0，质量驱动均因原错退出1。
观测未改变已测全文；不是质量通过或性能验收。没有改动生产源码。

在前次同token观测基础上，仅增加共享SwiGLU调用的gate/up输入
1280个BF16及中间输出640个BF16，48份元数据均成功。只读checkpoint
逐层读取gate/up/down权重，形状分别640×2560、640×2560、2560×640，
保存权重摘要。层身份沿用共享gate向量逐层匹配，48个摘要互异。

复核路由/合成仍为480槽位正确，122880路由FP32逐位一致；最终
合成同原归约顺序CPU参考，FP64合成仍保留1项差异。router投影
仍15项BF16不同，全部不在选中top-10。

## 投影差异仍待解释

142项逐阶段投影差异中133项相邻，9项跨多个BF16编码，不能统一
解释为靠近舍入中点。最大的编码距离出现在第43层gate分量214：
实际约-8.97794962e-7，FP64参考约-2.26587872e-6，舍入后相隔167码。
该点乘积绝对值和13.90992382，正负抵消后的和约-2.26588e-6，
抵消比约6138865。CPU顺序FP32累加约-1.99675560e-6，仍不等于实际。

用math.fsum重算所有142项，与FP64参考的最大绝对差约1.39e-17。
这支持高精度参考数值本身，但没有复现cuBLAS的内部累加次序，
不能凭抵消现象宣称142项全是正常舍入，也不能据此认定运行时错误。
源码该形状M=4096走BF16 cuBLASLt、CUBLAS_COMPUTE_32F。
下一步需绑定实际GEMM输入/权重/描述符，区分装载、布局和累加差异。

SwiGLU的CPU参考使用高精度exp或舍入到FP32的exp，不是CUDA
__expf的逐位仿真；本次30720项结果一致不外推到所有输入。

## 保存与范围

证据：`.q4t-work/e2e/moe-shared-reference-20260923/`，包括三侧
原始HTTP、48层快照、shared-reference.json、shared-rounding-details.json、
shared-cancellation-details.json及artifact-binding.json。
prepared同名目录保留控制器、分析脚本和构建日志；仓库保存观测
模板和verify_moe_shared_reference.py.in，绑定本模型/ABI/路径。

仅覆盖一个prefill token；路由专家完整gate/up支路、attention/GDN/
PLE/残差、decode尚未独立覆盖。原任务错误的实现/量化/模型来源
仍未定，不能用局部匹配追认已拒绝候选。下一阶段先关闭投影
差异的证据缺口，再决定是否需要运行时修复。

## 后续实际 GEMM 绑定（2026-09-23）

扩展观测库，零警告构建后首项仍为原4K无/有/无观测HTTP，三次
全文600440、三服务0，质量驱动均1。观测不改变已测输出，原错保留。
生产源码和2392f6b1二进制未变。

捕获48层gate/up与down共96次实际cuBLASLt调用的权重、末token
输入和输出。96份权重全部逐字节同checkpoint；输入全部同MoE
输入或实际SwiGLU输出；GEMM输出全部同相邻kernel捕获值。
实际描述符全部满足BF16、列主序连续布局，A转置/B不转置，
M=4096，gate/up N=1280 K=2560、down N=2560 K=640；计算类型
CUBLAS_COMPUTE_32F、FP32主机alpha=1/beta=0、默认epilogue。
每次保存算法对象及workspace字节数，尚未用算法对象做重放。

与上一轮48层五类边界快照共240份逐字节相同，独立参考再次得到
78/0/0/64/2857项差异。因此本样本已排除上述实际权重、输入和
描述符偏离参考的情况；仍没有复现cuBLAS内部归约或证明差异均
符合其累加合同。不能据此将142项未决差异标为零或声称任务已修复。
下一步针对实际算法做形状一致的冻结输入重放，检验差异能否在
脱离模型执行调度后复现；不改生产精度或接受阈值。

新增证据为`.q4t-work/e2e/moe-shared-gemm-binding-20260923/`中的
三侧HTTP、96份GEMM快照、gemm-binding.json、shared-reference.json、
previous-capture-comparison.json和artifact-binding.json；prepared保留
构建、控制器及分析脚本。仓库保存独立GEMM观测与绑定工具模板。
