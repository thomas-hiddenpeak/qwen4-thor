# 4K 条件求和失败分解（2026-09-23）

## 结论与边界

已接受运行时9a62371、二进制2392f6b1未改动，完成24条evalscope
HTTP诊断。原题在前后三次均输出600440而非360284；六种分解或
简化变体各三次全部正确。该证据缩小失败任务范围，但没有定位
模型、量化或引擎实现的根因，也没有修复或豁免原有4K错题。

原始五笔记录重新从冻结prompt解析，与oracle_records逐项一致。
符合group=target且status=accepted的两笔为120140、240144，独立
求和360284；600440不等于全部target金额690438，也不等于全部
accepted金额570430。不能直接把错误解释成只漏了某个筛选条件。

## 冻结设计与实测

沿用原4K冻结请求的原生模板与关闭思考设置，MTP关闭、单流greedy、
输出上限32、max_prefill8192、max_len208896。未修改源码/构建，
没有前置低层测试、bench或profile。三次连续重复每组，原题分别
置于最前和最后；六个变体在推理前冻结，答案来自原始记录与整数
运算，不取任何模型输出作真值。变体实际长度不同，不作性能验收。

| 请求 | 实际输入token | 预置答案 | 三次输出 |
|---|---:|---:|---|
| 原题前组 |4096|360284|600440 ×3|
| 移除背景、保留全部记录与求和问题 |206|360284|360284 ×3|
| 仅询问120140+240144 |61|360284|360284 ×3|
| 保留长背景，问题直接询问上述加法 |4095|360284|360284 ×3|
| 保留长背景，提取首笔符合条件的金额 |4094|120140|120140 ×3|
| 保留长背景，提取末笔符合条件的金额 |4094|240144|240144 ×3|
| 保留长背景，统计符合条件的记录数 |4097|000002|000002 ×3|
| 原题后组 |4096|360284|600440 ×3|

24条实际请求字段、输入usage、全文及stop均从原始SQLite/SSE核对；
服务退出0。质量驱动退出1是因为原题六次错误，18/24不是质量门禁
通过，也不能用六个成功变体替换原题。通用质量工具按prompt摘要
关联元数据，原题前后重复摘要会覆盖其展示ID，因此独立raw-audit
严格按实际请求顺序与冻结manifest重新关联，保留正确的前后分组。

## 推论与下一步

- 同一对数在短输入和约4K背景中显式相加均正确，不支持“这两个
  数普遍算不对”这一解释。
- 长背景中单独提取两笔和计数均正确，不支持“任何提取都失败”；
  改问题也会改变模型的注意力，不能因此证明原题内部检索正确。
- 原题前后六次同错，本序列未观察到请求顺序改变其结果。
- 现有五档独立题随长度同时改变金额（每档偏移137），所以原矩阵
  不能单独证明4K长度造成求和失败。去背景也同时改变长度和位置。

下一项诊断应保持这五笔金额、问题及模板不变，分别改变背景长度
与记录分布，保留原始4K对照。先以HTTP获得有界证据，再判断是否
值得建立精度合同一致的独立数值参考。已有SGLang量化合同不同且
4K输出不稳定，不能用其偶然正确作唯一裁决。无证据时不修改kernel，
不重置独立质量题或性能参考，不把提示词简化当作运行时修复。

## 证据

- `.q4t-work/prepared/filtered-sum-decomposition-20260923/`：prepare.py、
  plan.json、冻结inputs、command.json、driver-exit.json与audit.py。
- `.q4t-work/e2e/filtered-sum-decomposition-20260923/`：原始HTTP数据库、
  服务日志、参数、commit/diff/二进制摘要、exit.json与raw-audit.json。
- 父证据及错误历史见[独立答案对照](INDEPENDENT_ANSWER_E2E_2026-09-23.md)。


## 固定金额的长度/布局对照（30条HTTP）

同一已接受二进制2392f6b1，无源码/构建变化。固定五笔记录金额、
顺序、三个记录块边界、问题、系统指令和关闭思考模板。测试
1024/2048/4096/8192实际token，各比较背景10%/50%/90%处分散
插入记录块与三个记录块全部集中于90%处，每组连续三次；另外
保留原题前后三次。4K分散输入与原始失败prompt逐字相同。

| 实际输入token | 分散记录（三次） | 集中记录（三次） | 正确答案 |
|---:|---|---|---:|
|1024|360284 ×3|360284 ×3|360284|
|2048|600440 ×3|360284 ×3|360284|
|4096|600440 ×3|360284 ×3|360284|
|8192|360284 ×3|360284 ×3|360284|

原题前后另六条仍600440。全部30条HTTP请求/usage/全文/stop原始
核对完成，服务0、质量驱动1；共18条正确、12条错误，不是门禁
通过。每组重复一致不代表跨重启或所有输入确定性。

等长拟合导致集中布局的背景预算比分散多2token：四档分散为
784/1808/3856/7952，集中为786/1810/3858/7954；填充规则和
全部冻结prompt保留。因而不能把结果宣称为背景字节完全不变的
严格单变量位置实验。此诊断不覆盖44K/200K，不是性能矩阵。

证据支持“失败不局限于4K，且不是随长度单调恶化”；在这组
固定内容上，集中布局均正确。它不能证明模型内部只丢失某笔
记录、某个QSA阈值错误或量化是根因。原来五档改变金额的混杂
因素已去除，但长度、位置和填充变化仍需在解释中保留。

到此停止扩展同类提示词变体，原失败案例全部保留。下一步应
审查能否构建计算合同明确的独立参考，先列清权重/激活量化、
位置、GDN状态精度与QSA选择等差异；不能将不等价框架的正确
输出当成唯一真值，也不凭这些诊断修改或豁免kernel。

准备与原始证据前缀均为filtered-sum-layout-20260923，分别位于
`.q4t-work/prepared/`和`.q4t-work/e2e/`。plan.json、prepare.py、
inputs及raw-audit.json记录冻结参数、各组结果与顺序重新关联；
通用工具的重复prompt展示ID覆盖不用于最终前后分组判定。
