# 当前状态

更新：2026-09-21。代码是实现事实来源；验收规则见 [EVALUATION.md](EVALUATION.md)。

## 当前目标

先完善 runner，建立可复现的单流普通 decode 基线，再决定数据流引擎演进顺序。
保持当前精度、不造成性能回退的复杂度降低也属于有效改进。
用户于 2026-09-21 授权在既定约束内持续自主推进、验收、回退及阶段
提交推送，不再就常规推进请示；E2E 优先与保护其他任务改动仍然有效。

主模型位置元数据改动已接受：质量 11/11、性能 15/15、聚焦数值检查通过，
五档 decode 提升约 4.85%–7.17%，此前固定矩阵的历史速度差距已追回。
验收版本 29205f8 已按用户继续指令快进合入并推送 main，工作区已切回 main。
时间线发现的普通生成尾部无用前向现已修复：质量 11/11、边界 6/6、
五档性能 15/15 通过；随后时间线确认 256 输出只需 255 次 decode 前向。
本次改动接受。流式 top-k 的独立 CUDA 实现也已通过完整 E2E 和数值核对：
200K 平均 TTFT 234.40→205.22 秒（-12.45%），44K 39.73→38.31 秒，
decode 五档重复范围重叠，未见可辨识回退。内联首版因 decode 回退否决，
证据保留；第二版接受，200K HTTP 时间线确认合并累计耗时 63.35→34.09 秒。
稀疏注意力 shared 行距 256→264 的布局改动也已接受：质量 11/11、五档
性能 15/15、数值 442368 个 BF16 输出逐位一致。200K TTFT 205.22→181.96
秒，44K 38.31→33.15 秒，decode 五档未回退。HTTP 时间线确认注意力
累计 58.84→35.41 秒，寄存器不变。随后 MoE 临时缓冲持久化候选因
1K decode 明显回退已否决。随后通过布局/单表 HTTP 隔离，改为
四表合并一次异步分配：完整 E2E 无回退、每步减少 144 对调用，
作为复杂度改进接受；正式速度参考不变。随后线性注意力六区合并
候选因 4K/8K TTFT 小幅回退撤回。随后 HC 四输出专用 GEMV 通过完整
E2E 与逐位专项，HTTP 中目标 kernel 累计耗时减少约 9.83%，已接受；
整体小幅均值上升尚不能认定稳定提速，正式速度参考不变。


## 初始基线阶段已完成

- 文档治理：当前状态、评估规范、静态预算与历史材料分离；AGENTS 不再重复
  维护大段状态。旧文档全文归档，日志只追加。
- serve 默认关闭 MTP，--mtp 显式开启，--no-mtp 保留；启动日志标明关闭状态。
- SSE 在 include_usage=true 时返回服务端实际计数，修复客户端输出重分词误差。
- tools/evalscope 五档单流入口：固定技术文本、每档 3 条请求、固定输出上限，
  保留原始请求、响应、版本和配置，并检查失败数、token 长度与结束原因。
- 必要构建完成，构建日志无警告；第一项测试为 HTTP evalscope E2E。
  未运行单测、bench 或 profile。**15/15 请求成功，五档实际长度全部达标**。
- [初始测量报告](BASELINE_2026-09-20.md)：1K/4K/8K/44K/200K 平均 TTFT
  0.878/3.238/6.519/39.741/234.937 秒；decode
  15.51/14.19/14.85/14.45/13.98 tok/s。MTP 明确关闭。
  这是受控文本初始记录，不是完整精度验收或性能无回退证明。

## 当前阶段：BF16 GEMV 输入 staging 向量化已接受

- 对齐输入改用 float4 搬入 shared，未对齐仍标量复制；shared 显式
  16 字节对齐，数学次序与 HC 专用路径不变。
- 第一项测试为 HTTP，质量 11/11、五档性能 15/15、输出一致。
  decode 16.985/15.179/15.985/15.596/14.711 tok/s；对固定参考及
  上一版本未见明确回退，小幅均值变化不视为稳定提速，正式参考不变。
- 完整 E2E 后，2016 组、105408 个 BF16 输出逐位相同。HTTP 时间线
  普通 GEMV 累计 9.599→9.521 秒，寄存器 39→40，输出头基本持平。
  新旧调用与分配次数一致。有限局部收益支持保留此小范围改动。
- 下一步按形状区分权重读取与小输出归约/启动成本，仍一次一个候选。
  报告：[GEMV 输入搬运](GEMV_STAGING_2026-09-21.md)。
  证据 `.q4t-work/e2e/gemv-vector-stage-20260921/`。

## 前一阶段：HC 四输出 decode 投影专用实现已接受

- 仅 N=4/K=10240 且输入/权重对齐时分派四个单 warp CTA，直接读取
  输入，保留每行 FMA、shuffle、alpha 与 BF16 舍入顺序。
- 第一项测试为 HTTP；质量 11/11、五档性能 15/15、输出摘要一致。
  decode 16.937/15.152/15.951/15.572/14.697 tok/s，重复范围与参考
  重叠，未见可辨识回退；不宣称 +0.18%–0.42% 均值差是稳定收益。
- 完整 E2E 后数值专项 4096 个 BF16 输出逐位一致；1K HTTP 时间线
  确认 96 次/步，目标累计 380.339→342.936 ms，约少 0.147 ms/步。
- 正式速度参考不变。下一步核查剩余 BF16 投影输入搬运与归约成本，
  新候选仍先 HTTP；不把大形状与四输出小形状统一按满带宽估算。
- 报告：[HC 形状专用投影](HC_INJECT_GEMV_2026-09-21.md)。
  证据 `.q4t-work/e2e/hc-inject-gemv-20260921/`。

## 前一阶段：线性注意力六区合并分配未接受，已撤回

- 已完成六块缓冲的所有权/消费边界核对，含晚期调试导出；候选保持
  独立区域和数学，只将六对异步分配/释放合为一对。
- 构建零警告，首测 1K HTTP、质量 11/11 通过；正式矩阵因 8K TTFT
  与基线范围不重叠，在 44K 主动停止，200K 性能未运行。
- 新旧二进制按 1K→4K→8K 相邻复测，4K TTFT 2.853→2.866 秒，
  8K 5.641→5.660 秒，范围均不重叠；decode 范围重叠。不设临时
  回退容忍比例，候选不满足保持性能要求，已恢复运行时代码和旧产物。
- 没有进行低层测试或 profile，预计少 180 对调用未采集验证；正式
  速度参考不变。恢复后旧产物 1K 输出一致、退出 0，decode 16.824，
  也说明此前 1K 偏低不能单独归因于候选。详见 [生命周期与实验记录](LINEAR_WORKSPACE_2026-09-21.md)。
- 下一步暂停继续合并大块分配，保留所有权分析，回到已接受版本的
  主要计算与权重读取路径。两次不同回退的机制均未确定，不假定同源。

## 前一阶段：MoE 四表合并异步分配已接受

- 布局扩容单独变化、四表任意一张单独持久化，1K HTTP 均未复现原
  回退；原失败二进制复测仍 11.434 tok/s。最终底层机制尚未定位。
- 已接受方案恢复原 workspace/API，四表共用一次异步池分配及释放，
  保持数学和计数回传/专家流汇合，不新增持久缓存。
- 构建零警告，第一项测试为 1K HTTP 复现；随后质量 11/11、五档各
  三次 15/15 通过，输出摘要一致，正常退出，无前置测试或 bench。
- 1K/4K/8K/44K/200K decode 16.912/15.108/15.902/15.510/14.682
  tok/s，TTFT 0.848/2.853/5.638/33.164/181.886 秒。五档新旧重复
  范围均重叠，按性能持平的复杂度改进接受，不宣称微小提速。
- 完整 E2E 后 1K HTTP 时间线确认：255 次 decode，异步分配及释放
  各 141270 次，即每步 698→554（各减少 144）；计数 D2H 仍 48 次/步。
  速度参考不更新，全部证据见 [分配实验报告](MOE_ROUTING_ALLOC_2026-09-21.md)。
- 下一步先明确剩余临时缓冲的所有权、分配来源与跨流依赖，再选择
  小改动；不把本次结果当作原持久化回退机制已定位。

## 前一阶段：MoE 路由表持久 workspace 候选否决并回退

- 四张路由表纳入 workspace 的候选先通过质量 HTTP 11/11，但 1K
  三次 decode 仅 11.312–11.341 tok/s（均值 11.330），低于基线
  16.825–16.936（均值 16.893），约 -32.93%。输出摘要一致。
- 停止后续性能矩阵，4K 客户端主动终止 143，服务正常退出 0。
  旧二进制同输入 HTTP 三次复测 16.816–16.968（均值 16.914），支持
  候选回退判断；五档未完成，不能宣称完整性能通过。
- 已撤回运行时代码及测试 API 改动，恢复构建 HTTP 1K 均值 16.848；
  最终运行产物恢复为已接受旧二进制原件，正式性能参考不变。未运行
  单测、bench 或 profile；分配调用减少只是源码估算，未作采集验证。
- 原因尚未定位，不能归因于分配器、缓存或流调度。此结果否决的是
  具体实现，不是否定持久 workspace 的方向。下一轮先核查布局与
  生命周期耦合，再制定更小候选；不直接扩大到 HC/线性注意力。
- 详情 [实验记录](MOE_WORKSPACE_2026-09-21.md)，证据目录
  `.q4t-work/e2e/moe-workspace-20260921/`。

## 前一阶段：稀疏注意力 shared 行 padding 已接受

- 仅 Q/K/V shared 行距从 256 改成 264 个 BF16，保持 16 字节异步拷贝
  对齐；全局 KV、候选、分块、数学顺序与舍入不变。每 CTA 理论增加
  1280 字节 shared memory，不增加全局 KV。源码针对不同 MMA 行的
  shared bank 地址重合做布局修正，没有新增运行时分支或替代 kernel。
- 构建后首测质量 11/11，再五档各三次 15/15，实际长度正确、输出均
  256、length、摘要与旧版一致，服务退出 0。无前置低层测试或 bench。
- 1K/4K/8K/44K/200K TTFT 为 0.850/2.851/5.631/33.152/181.959 秒，
  后四档每次均优于旧范围，降幅 11.70%/13.41%/13.46%/11.34%。
  1K 均值降 2.96%、范围重叠；TTFT 不等于纯 prefill。
- decode 为 16.893/15.089/15.891/15.511/14.670 tok/s，均值提升约
  0.87%–1.35%，无可辨识回退；44K 新旧重复范围有小部分重叠。
- 完整 E2E 后的源码级 CUDA 对照 34 组、442368 个有限 BF16 逐位一致，
  覆盖 1/3/12 行、空/尾块/多块/2048 附近长度、非连续页及多序列 ID。
  工具保留源代码/命令/日志并检查 E2E 门禁；不替代模型或调度验收。
- 正式性能参考已更新，详情 [shared 布局报告](SPARSE_LAYOUT_2026-09-21.md)。
  E2E 后 200K HTTP 时间线确认注意力累计 58.84→35.41 秒（-39.83%），
  top-k/GDN 基本不变；static shared 42688→43968 字节，寄存器始终 56、
  local memory 为 0。没有采集 bank 冲突计数，不把静态推断当作计数实测。

## 前一阶段：流式 top-k warp 内交换已接受

- 新 kernel 独立于原注意力 CUDA 编译单元，保持原 bitonic 比较网络、
  严格比较和同分 ID 规则，warp 内用寄存器 shuffle 减少共享内存同步。
  三种打分分支共用一个 host 合并调用；非标准 top-k 保留原路径。
- 首版放在原编译单元时，质量及输出通过却有约 1%–2% decode 回退，
  旧版四档复测支持差异，明确否决。第二版恢复原 device 实现并隔离新
  kernel 后回退未复现；编译布局根因未证实，不能把假设写成结论。
- 第二版构建后首测质量 11/11 精确正确，再五档各三次 15/15 全通过；
  输出 256、length、摘要一致。1K/4K/8K/44K/200K decode 为
  16.747/14.908/15.680/15.361/14.536 tok/s，旧新重复范围全部重叠。
- TTFT 均值 0.876/3.229/6.503/38.307/205.221 秒；短档范围重叠，
  44K/200K 每次均优于旧范围，分别减少约 3.58%/12.45%。TTFT 不是
  纯 prefill；不将 decode 的微小变化宣称为稳定加速。
- 完整 E2E 之后才运行 streaming_topk_exact：1/3/17 行各 25 块，
  268800 对分数/ID 与原网络逐位一致，独立数学 top-k 分数匹配；覆盖
  同分、正负零、全无效、部分可见和末块胜出。构建无警告，无 bench。
- 正式性能参考更新，旧版/首版/第二版证据分开保留，见
  [top-k 报告](TOPK_WARP_2026-09-20.md)。E2E 后 200K HTTP 时间线确认
  同样 3888 次合并累计 63.35→34.09 秒（-46.2%）；稀疏注意力约 58.84 秒、
  GDN 约 33.56 秒均未变。稀疏注意力现占 prefill kernel 累计约 25.77%。

## 前一阶段：输出上限收尾修复已接受

- 当前 token 输出/缓存完成后，达到上限即退出，不再生成无消费者 token；
  EOS 仍优先 stop，上限仍 length，请求移除及调度器唤醒沿用原收尾。
- 构建后首测真实 HTTP。质量 11/11 精确正确；流式/非流式 1/2/8 上限
  六条重采均与旧版逐字一致，原始 request.stream 与用例一致，usage 正确。
  首版非流式标签因 evalscope 默认流式而失效，已修正并保留更正证据。
- 五档各三次 15/15 成功，实际输入达标、输出均 256、length、摘要一致。
  1K/4K/8K/44K/200K decode 为 16.727/14.860/15.691/15.309/14.521 tok/s，
  相对上一版 +0.59%/+0.23%/+0.34%/-0.07%/+0.34%。44K 重复范围重叠，
  未见可辨识回退；不以三次样本保证稳定的微小加速。
- 平均 TTFT 为 0.877/3.225/6.495/39.728/234.401 秒，各档重复范围与对照
  重叠。它不是纯 prefill 时间；正式性能参考更新为本次接受值，旧值保留。
- 全部 E2E 通过后才采一条 1K 时间线：256 输出对应 255 次 decode 前向，
  MoE counts D2H 为 255×48=12240 次。构建无警告，无前置低层测试或 bench。
  结果、边界及证据见 [输出上限报告](OUTPUT_LIMIT_2026-09-20.md)。

## 前一阶段诊断：五档真实 HTTP 时间线

- 已接受运行时未修改，五档各一条 256 输出，实际长度/结束原因/摘要全部
  通过；服务、采集器退出 0。工具要求同一二进制已有完整五档 HTTP 记录。
- 普通路径每档输出 256 token 却做了 256 次 decode 前向（首 token 来自
  prefill，后续本应 255 次）；源码与 embedding/argmax/D2H 三种计数互证。
  该尾部浪费现已修复并通过验收，见本文最新进展。
- decode 的 BF16 GEMV 占 kernel 累计时间约 53%–60%。每步仍有 48 次
  MoE counts D2H，698 次 cudaMallocAsync，提供 workspace/控制依赖整理依据。
- 200K prefill：MergeChunkTopkKernel 63.35 秒、占 kernel 累计 24.59%，
  sparse attention 22.84%。44K merge 仅 7.12%；不能沿用旧短上下文判断。
  精确选择/归并是长上下文后续重点，保持精度及候选覆盖。
- prefill 多余 head 投影存在，但 200K 合计仅约 175.5 ms，非长 TTFT 主因。
- profiler 拉长 decode 时间约 7.8%–9.9%，不作为新性能验收；GPU busy
  为区间并集，不等于带宽饱和。尚无精确 CPU ModelPrefill 计时或 DRAM 字节。
  证据及限制见 [HTTP 时间线报告](HTTP_TIMELINE_2026-09-20.md)。无 bench。

## 最新进展：消除逐层位置回读，五档性能通过

- 主模型七条入口传递已有 host 位置，RunLayers 求一次精确最大逻辑位置，
  供 12 个 full 层使用；packed/ragged 扫描全部位置，MRoPE 坐标不参与。
  device-only/MTP draft 继续用 -1 回读，未改变数学或 QSA 候选选择。
- 首测真实质量 HTTP E2E 11/11 精确正确；随后五档各三次，15/15 成功，
  输入长度准确、输出均 256、正常 length，与上一版输出摘要相同，退出 0。
- 1K/4K/8K/44K/200K 平均 TTFT 为 0.877/3.223/6.495/39.713/234.464 秒；
  decode 为 16.629/14.826/15.637/15.321/14.471 tok/s。相对索引键清理版
  +7.05%/+4.93%/+6.70%/+7.17%/+4.85%，五档新最低值均高于旧最高值。
  相对历史错误数学版本也高 +6.85%/+4.13%/+5.53%/+6.14%/+3.89%。
- E2E 之后才执行 full_attention_forward：1/1 通过，host 元数据与设备
  回读输出逐位一致，主输出/原始键/压缩键误差 0.002964/0/0.002610。
  构建零警告，无前置单测、bench 或 profile，未运行整套测试。
- 固定性能参考更新为本次接受值；历史较慢参考和报告保留。结果、SHA、
  限制和 main 节点见 [位置元数据报告](POSITION_METADATA_2026-09-20.md)。
  单流文本证据不替代并发、MTP、多模态或全面质量验证。

## 前一阶段：索引键等价简化已接受

- 删除原始键无人消费的逐 token norm/RoPE、D2D 副本及临时缓冲；该算子
  在 8192-token 分块下工作区需求少 2 MiB，不等同于实测全模型峰值下降。
- 首测真实质量 HTTP E2E：11/11 精确正确且与清理前文本一致。随后五档各
  三次：15/15 成功、长度达标、输出均 256、与清理前摘要一致、正常退出。
- 当前 1K/4K/8K/44K/200K decode 为 15.533/14.130/14.655/14.296/13.801
  tok/s。相对清理前约 +0.17%/+0.99%/+0.26%/-0.08%/+0.18%；44K 的
  重复范围重叠，未见可分辨的新增回退，按复杂度下降且性能持平保留。
- 相对历史旧版仍低约 0.20%/0.76%/1.09%/0.96%/0.92%，该阶段整体速度目标未
  全部追回，后续已由位置元数据改动追回。三次测量不保证稳定的微小加速。
- E2E 通过后才运行聚焦数值检查：主输出 L2 相对误差 0.002964，原始键
  逐值一致，压缩键误差 0.002610，1/1 通过；构建无警告，无 bench/profile。
- 可复现入口及固定参考已纳入 tools/evalscope，见其 README。结果与范围见
  [索引键清理报告](INDEXER_CLEANUP_2026-09-20.md)。后续位置元数据改动
  的进行中状态见本文开头；尚未采集该项单独耗时占比。

## 数学修复阶段记录（本次简化之前）

- 原生模板检索题 11 条：旧版 6/11，固定顺序+并行归约版 7/11；原先正确的
  六条全部保留，但 8K/44K/200K 仍有失败。实际输入长度全部达标。
- 失败期间暂停数值细分析和最终性能验收。8K 同三题分别用 max_prefill
  8192/4096/2048/1024 重放，输出全部相同，暂不支持大块 prefill 是原因。
- 静态对照官方模型实现和 tokenspeed：索引器 Gemma RMSNorm 漏了权重的
  +1；主注意力及索引器 RoPE 符号与官方相反。另有索引器归一化后跨 warp
  读取缺同步。同步单独修复后短题仍 7/9；仅补 +1 后恢复为 9/9。
  后续分别修正 RoPE 方向、QSA 仅追加未完成组尾部，各自首测均为 9/9。
- serve 已读取 generation_config.json 的停止 token 列表，同时识别 im_end
  和 endoftext；PLE 历史填充仍使用原主 EOS。完整首测 **11/11 精确正确**，
  包含 44K、200K，实际长度匹配、正常 stop、服务退出 0。
- 五档同输入各三次复测完成：15/15 成功、长度达标、每档输出一致、退出 0。
  当前 1K/4K/8K/44K/200K decode 为 15.507/13.992/14.617/14.308/13.776 tok/s，
  比旧版同输入低 0.36%/1.73%/1.35%/0.88%/1.10%。**不声明整体优化完成**。
  旧版数学错误导致输出/路由也不同，尚不能把全部差值归因于 kernel 开销。
- 当时旧 CPU oracle 已修正并新增索引键检查，编译零警告但未执行；性能门禁未通过，
  未用低层检查覆盖差异。所有排障首测均为真实 HTTP E2E，无 bench/profile。
  这组检索题不替代全面质量基准。详见 [精度排障报告](ACCURACY_AUDIT_2026-09-20.md)，
  证据 .q4t-work/e2e/acceptance-20260920/。

## 待处理

1. 随机文本的工具与服务端分词计数差异（1024 目标实际 932/921/915）未解决。
2. 补 messages/真实语料及多轮对照。原生模板渲染后的 prompt 检索题通过
   11/11，不证明 messages 模板转换、多轮、多模态或全面长上下文召回正确。
3. 断连专项已通过六轮恢复 E2E：旧版 SIGPIPE 已修复，FD、请求计数、
   输出与正常退出符合预期，见 [断连报告](DISCONNECT_FIX_2026-09-20.md)。
   后续发现旧版 4K 输出非确定性，固定稀疏索引槽位后五档各三次输出稳定，
   见 [复现报告](REPRODUCIBILITY_2026-09-20.md)。再做质量题发现 QSA 数学
   错误，本轮已修复并恢复 11/11 精确正确。仅固定顺序的中间版曾有约
   1%–2% decode 回退；数学修复阶段和本次等价简化的差异分别见上，历史
   固定矩阵速度目标现已由位置元数据改动追回，见最新进展。
4. 纯 prefill 阶段时间尚未测量；TTFT 不能替代它。下一步在 E2E 已通过的
   请求上进行必要细分析，须先满足现行门禁；不依据历史 bench 决定方向。
5. [静态预算](DATAFLOW_OPTIMIZATION.md) 约 10.24 GB/token，260 GB/s 对应
   25.4 tok/s，是理想参照，不是已测性能。“整个 decode 已无优化空间”没有
   当前 E2E 与完整时间账支持。

## 历史材料

[治理前状态快照](HISTORY_STATUS_2026-09-20.md)、
[数据流分析快照](HISTORY_DATAFLOW_2026-09-20.md)、
[旧入口状态段](HISTORY_AGENT_ENTRY_2026-09-20.md) 不作为当前完成状态或新基线。
[日志](log/README.md) 保留历史，错误以新条目更正。
架构、模型与阶段文档按用途查阅；未在本轮核对的内容仍以源码为准。
