# 小批次BF16投影独立验证（未接受）

混合decode候选包含位置、GDN、indexer和小批次投影改动，不能将
其中任意一项的收益或错误归给全部改动。本阶段从已接受7364767
独立评估M=2/3/4投影，保留正式参考和所有失败案例，不修改共享
工作区中的混合候选，不恢复其余候选作为隐含前提。

## 来源与边界

prepared/candidate-decomposition-20260923包含四组源码补丁及
manifest。四组按7364767重建，全部九个修改文件逐字节匹配当前
工作区。此核对只是来源完整性，不是功能测试。GDN一组仍含
递推/检查点/搬运多项修改，尚未进一步拆分，不冒称最小补丁。

本独立构建仅含三个文件：include/q4t/model/gemv.h、
include/q4t/model/linear.h、src/model/gemv.cu。M2–4、beta=0及
满足对齐/维度条件时走逐行GEMV累加次序的小矩阵kernel；M1和
其他形状保留7364767路径。不包含位置补写、GDN或indexer修改。
是否保持质量及有实际价值须由HTTP证据判断，不由源码推断接受。

构建Release、g++14、CUDA110a、tests OFF，退出0且零警告。
二进制SHA256：
`41a2a02fe1e6dd99e9cbbb59901fc1b77d2fb1ee47adbc7acbef4b3931e15b20`。
原始来源补丁及isolated-CMakeCache.txt随证据保存。通用评估器
记录的worktree.patch属于外层混合工作区，不是这个隔离二进制
的源码；source-provenance.json明确指出该区别。

## 首项HTTP结果

small-m-isolated-20260923固定质量11请求完成：服务/驱动均0，
11条原始SQLite/SSE核对全部正确、长度和stop符合预期、完整
输出同未改正式参考。MTP关闭，先HTTP，没有前置低层测试。
这没有证明实际M2–4被调用，也不能转移旧混合候选批量测试。

## 当前与下一步

冻结独立答案15题五档矩阵已启动，会话66410，证据
small-m-isolated-answers-20260923。每题保留原答案，同时比较
正式7364767原输出和新增失败，正式已有4K求和错误不会删除。
采集驱动0与题目正确率分开，不能将旧失败隐藏为通过。

之后需实际小批次HTTP证据及完整五档性能门禁；若没有对应
工作负载收益或一致性价值，不因单流输出未变而接受新增kernel。
当前没有新的性能收益结论，尚未接受或合入运行时。


## 待运行的小批次HTTP方案

run_batch_quality.py及batch-quality-plan.json已准备，尚未执行。
前置条件为当前独立答案15条原始响应核对且无新增失败；不得
与尚在运行的200K请求并行。正式7364767和独立候选各一个服务，
MTP关、max_seq4、max_prefill8192、max_len16384、输出上限32。
固定检索题1K/8K，各串行前3次、并发2/3/4各一组、串行后3次，
每版本30请求，合计60。按原六位答案评分，同时保存失败/退出。
HTTP并发不是实际GPU B2/3/4覆盖证据，不凭请求参数宣称kernel
已覆盖，更不能代替五档单流性能验收或作为稳定提速结论。


## 独立答案矩阵完成

small-m-isolated-answers-20260923全部15条原始响应已核对，服务0、
采集驱动0，质量子驱动1（保留语义失败）。14/15正确，15条
完整文本均同正式7364767原矩阵，新增失败0。唯一失败仍为
4096-filtered_sum，期望360284、实际600440，正式版亦为600440。
不能称为15题全对，不能将混合候选13/15当本版本的父参考。

1K求和返回正式正确答案360010，符合独立版本未含位置补写的
源码边界；并非解决混合候选或位置更新的正确性问题。没有
改变任何答案或删除失败样本，仍未做数值/性能细分析。

预定两版本60请求小批次HTTP现已启动，会话6319，证据
small-m-isolated-batch-quality-20260923，正式→独立候选依次运行。
脚本仅在15响应核对且新增失败0后启动；仍未接受运行时。

## 并发质量对照完成与尾块补充

small-m-isolated-batch-quality-20260923已完成60个原始HTTP响应，
正式/独立候选各30/30正确，两服务与采集驱动均0。十组对应案例
的prompt/完整输出多重集合相同，串行前后和并发请求均无新增错误。
paired-audit.json保存配对复核。没有观察实际GPU批次，不能声称
B2/3/4 kernel覆盖，也不能以本组短输出延迟推断性能收益。

补充冻结8194/8195/8196-token输入，各三个检索深度；在8192分块
配置下针对2/3/4行尾块，正式→独立候选共18个HTTP。初版输入
生成器无法精确达到8194（实际8193），没有发请求，失败保留；
v2仅调整问题前的中性填充，九条输入精确分词后冻结。原计划保留，
tail-quality-plan-v2.json记录实际生成器及输入摘要。证据
small-m-tail-quality-20260923，尚在运行，不提前计为通过。

## 尾块质量完成及阶段结论

small-m-tail-quality-20260923两版各9/9正确，18条原始SQLite/SSE
核对，输入精确8194/8195/8196、greedy、max_tokens32、stop均符合。
两版九条完整输出全部相同，两质量驱动、两服务、总驱动均0，
会话70463结束。source-provenance.json区分隔离源码与外层工作区
快照；没有运行低层测试或profile，也没有将并发参数当GPU观测。

至此独立投影的固定质量11/11、冻结独立答案14/15且全文同正式、
并发对照60/60、尾块对照18/18已核对。结论仅为已测请求未新增
错误，不是全面正确性证明。原4K求和错误与混合候选1K新增错误
均未解决；独立投影未含位置修复，不能用本结果验收混合候选。
下一阶段建议先单独完成投影候选五档性能，再判断是否有保留
新增kernel的价值；位置/GDN/indexer分别立项，不能一起合入。
本轮正确性确认收口，正式运行时7364767、固定参考fcb5925不变。

## 五档性能评估进行中

small-m-isolated-performance-20260923使用同一隔离二进制，正式
7364767冻结输入各三次、输出256，MTP关、max_seq1、max_prefill8192、
max_len208896。实际源码补丁及隔离CMakeCache单独保存，外层
工作区快照不充当源码证明。会话65364仍运行，200K两条已完成，
末条进行中；数据库完整提交前不提前声明全矩阵原始核对通过。

前四档输出检查通过；初步后续范围比较发现相对正式参考4K/8K
TTFT和1K/8K decode不利分离。完整结果尚未结束，当前不接受。
下一步收完后独立审计15条SQLite/SSE及双参考范围，再决定必要
的相邻版本复核，不以均值抵消单档回退或重置固定参考。

## 完整五档结果：输出相同，性能未通过

会话65364已结束，采集驱动/服务0，15条原始SQLite/SSE独立核对：
输入长度、输出256、greedy、finish=length、MTP关闭、单流与
隔离二进制摘要符合预定配置；全部输出同7364767。证据中的
raw-performance-audit.json保存全部20项双参考比较，包括首请求
和后续两次范围，不只保留不利项。

| 输入token | TTFT均值秒 | decode均值tok/s |
|---:|---:|---:|
| 1024 | 0.808596 | 18.545547 |
| 4096 | 2.640819 | 17.904625 |
| 8192 | 5.175730 | 18.072768 |
| 45056 | 30.478609 | 17.867275 |
| 204800 | 159.371637 | 17.084131 |

后续范围共7项不利分离：相对正式7364767，1K/8K/200K decode
与4K/8K TTFT不利；相对固定fcb5925，1K/8K decode不利。
44K两指标无不利分离。均值仅作概览，不用其覆盖范围门禁；
无新运行时接受或性能收益结论。此阶段未运行低层测试/profile。

下一步在不利档位做预定正式→候选→正式相邻HTTP对照，必要时
对固定参考单独复核，保存全部结果；不扩大容忍区间，不删除
本轮失败。只有性能差异闭合后才考虑底层一致性与实际价值分析。

## 相邻版本复核已启动

证据small-m-isolated-adjacent-20260923，会话24545。预先冻结
正式7364767→独立投影41a2a02f→正式7364767，四档1024/4096/
8192/204800各三次，共36请求。选择全部出现不利范围分离的
上下文，保留原始44K结果；不将该四档复核称为新五档基线。

调用未改tools/evalscope/run_acceptance.py，仅在独立入口明确
替换LENGTHS为四档，源文件及入口、命令、输入参考均保存。
逐服务要求原正式输出相同，MTP关闭，容量/分块/输出长度不变。
候选不改源码或重构建，工作区混合候选也保持原样。只比较相邻
正式前后，固定fcb5925仍单独保留，不能由正式对照自动豁免。
尚未完成，不作性能或运行时接受结论。

相邻复核前侧正式版已完成四档12响应，原始SQLite/SSE核对
通过，输出均同正式参考，驱动/服务0，原PID2353849已退出。
独立候选服务PID2357839已接续，控制会话24545仍活跃。
raw-completed-service-audit.json保存已结束服务审计；候选和
后侧正式未完成，尚无相邻性能结论。

## 源码调用范围补充（不是实测覆盖）

静态核对隔离源码与只读checkpoint配置：Bf16SmallMGemm接在公共
Bf16Gemm入口，不能把它理解为仅作用于HTTP并发2/3/4。主干
GDN/QKV/GR/PLE/router/shared expert/head大多传M=T，但存在：

- 短上下文非池化indexer传M=T×indexer_n_heads，实际头数4；
  因而T=1也可进入M=4。池化T=1另走IndexerDecodeScores分支。
- MTP fc_hidden传M=T×hc，实际hc=4，T=1亦为M=4；本轮MTP关闭。
- 草稿BF16专家按每专家compact后的行数调用，小行数不等于
  HTTP并发数。vision也共用此入口，文本测试不证明视觉覆盖。

来源摘要保存在prepared/candidate-decomposition-20260923/
small-m-static-scope.json。以上只是源码可达形状，不是kernel观察、
数值验证或新增性能结论。即使默认serve性能复核通过，也不能
直接接受为所有消费者已验证；仍需评估相关路径一致性及实际
价值。当前相邻测试继续原配置，不因本次核对改变测试中参数。

相邻候选服务现已完成四档并退出0，前侧正式与候选共24条
原始响应核对通过，输出均同正式，PID2357839已退出。后侧
正式PID2361878已接续，会话24545活跃。尚未完成36条对照，
不提前接受；raw-completed-service-audit.json记录两组原始核对。

后侧正式4K已完成，部分比较确认候选4K TTFT与decode均对
前后两侧正式范围不利：TTFT候选2.635004–2.636991秒，前侧
2.627169–2.629025、后侧2.628614–2.630548；decode候选
17.856552–17.859047，前侧17.917472–17.919776、后侧
17.861554–17.863373 tok/s。partial-4k-comparison.json保留
部分比较，尚非完整36条审计。当前候选不接受，剩余请求仍
按计划完成，不追加反复复测寻找通过结果。

## 后续默认工作区恢复方案（尚未执行）

本轮性能不接受后，不继续把混合实验留在默认运行时。已只读核对
九个源码文件：当前字节与prepared/replay归档相同，HEAD字节与
7364767基准摘要相同。accepted-workspace-recovery-plan.json冻结
范围及保护条件；当前仍在性能采集，没有恢复文件或重新构建。

待36响应全量审计且全部模型服务结束后，再次逐文件校验摘要，
仅恢复这九个自有实验文件；如有新增修改则停止覆盖并重新审查。
保留全部补丁/源码/二进制/失败记录，其他任务文档不纳入。必要
零警告构建后首项仍为HTTP E2E，再完成规定门禁。回到已接受
源码不等于解决位置更新或模型答题正确性问题，已知问题仍保留。

## 最终相邻结果与候选处置

small-m-isolated-adjacent-20260923全部36条原始SQLite/SSE已核对，
三服务及总驱动0，输入/输出长度、greedy、MTP关闭、结束原因及
二进制摘要一致，完整输出均同7364767。会话24545结束，末服务
PID2361878已退出。raw-adjacent-audit.json保存全部32项比较，
共10项后续范围不利，其中6项来自当前前后正式对照：

| 指标 | 候选后两次范围 | 正式前侧 | 正式后侧 |
|---|---|---|---|
| 4K TTFT秒 | 2.635004–2.636991 | 2.627169–2.629025 | 2.628614–2.630548 |
| 4K decode tok/s | 17.856552–17.859047 | 17.917472–17.919776 | 17.861554–17.863373 |
| 200K decode tok/s | 17.103752–17.108347 | 17.118993–17.133148 | 17.161707–17.168848 |

另外4项对历史参考不利：固定1K/8K decode、正式4K TTFT及
200K decode。历史/相邻结论分开保存。1K/8K相对当前两侧未
出现不利分离，不抵消4K/200K失败。**拒绝本独立投影候选**，
不继续相同候选反复采样寻找通过，不转入低层测试/profile。
这些结果不单独证明回退发生在某个kernel，更不证明未测试
MTP/视觉/非池化路径正确或错误。

## 默认工作区已恢复源码，构建与E2E待完成

完整审计后确认无模型服务存活，再次检查九文件与归档摘要匹配，
仅恢复这九个自有实验文件至HEAD/7364767相同源码。全部混合
源码、补丁与build/q4t原二进制归档至prepared/
accepted-workspace-recovery-20260923；未覆盖其他任务文档。
当前运行时git diff已清空，正式/固定参考不变。必要构建正在
进行（会话48476），还没有恢复后二进制/E2E通过结论。构建后
第一项必须是固定质量HTTP，保持原答案与正式输出参考。

恢复是撤下未接受实验，不是修复已知MRoPE位置/答题问题，
也不是接受混合候选其余改动。后续开发从接受源码单项推进，
本轮所有失败与隔离证据保留供定位，不能重新标为通过。

恢复源码的必要构建已退出0且零警告，会话48476结束。新二进制
SHA256为2a5813f2f0877a4671e0415bef8aca42b131cdd24035062f5e87e02858a45853，
与旧接受归档0c3f3914不同，不以源码恢复推断二进制/E2E已通过。
首项固定质量HTTP已启动，会话97219，证据
accepted-workspace-recovery-quality-20260923；仍使用原11题及
正式输出参考，无前置低层测试。

恢复版本首项固定质量HTTP现已11/11原始核对通过，全部答案及
完整输出同正式，驱动/服务0，会话97219结束。新SHA2a5813f2，
没有前置低层测试。五档性能HTTP现已启动，会话41428，证据
accepted-workspace-recovery-performance-20260923，仍按五档各
三次/输出256/MTP关闭/单流及原正式输出参考执行。恢复后的
性能尚待确认，不把固定质量通过解释为已知模型错误已解决。

恢复来源追加核对：src/、include/及CMakeLists.txt的HEAD内容
与7364767无差异，工作区这些路径亦与HEAD无差异。Release、
g++14、CUDA13.3、SM110a配置已记录于source-recovery-audit.json。
这只是源码/构建配置来源核对，不是二进制逐字节一致或性能
验收证明；五档HTTP仍在运行，不进行前置数值/性能细分析。

## 恢复版本五档已完成：两种范围口径必须分开

恢复SHA2a5813f2的完整五档15条原始响应已核对，输出全同正式，
服务/驱动0，会话41428及PID2366201已结束。质量11/11亦已完成。
TTFT均值秒：0.799798/2.635171/5.172710/30.457241/159.342512；
decode均值tok/s：18.599349/17.856566/18.130095/17.859103/17.114677。

来源核对发现历史7364767的comparison.json使用每档全部三次
范围；近期隔离诊断另外使用首请求单列、后两次范围。恢复版本
按历史三次完整范围的20项双参考比较，无不利分离；后两次
子集仍有4项不利：正式4K TTFT/decode、固定1K/8K decode。
两个文件分别为historical-method-comparison.json与
raw-performance-audit.json，均保留，不覆写原失败或扩大容忍比例。

因此源码恢复、固定质量和完整输出一致性已确认；历史范围
口径符合，但更严格后续子集的性能诊断尚未闭合，不能将两者
混成“全部无回退”。recovery-validation.json明确记录此状态，
固定参考不更新，聊天入口24请求仍仅准备、未运行。
下一步对1K/4K/8K按相关正式/固定归档作一次预先固定的相邻
对照，明确各参考与取样口径，保留全部结果；不反复复测已拒绝
的41a2a02f候选，也不通过更换口径追认它。
