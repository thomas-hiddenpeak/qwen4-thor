# 已知尺度缺陷的组合传播对照

## 范围与顺序

原4K仍输出600440，预期360284。已知E4M3高区间编码错误到达
实际量化；先前运行时高值修正候选4K改善、1K/44K退化，已拒绝。
本轮从冻结接受版实际输入建立有界传播，不恢复被拒候选、不改
运行时、不新增性能结论。此前HTTP观测不变性证据允许离线复用，
必要参考构建均零警告。模型目录只读。

## 首层输入尺度 → gate/up

保持首层HC mixed输入、实际路由、checkpoint权重、矩阵alpha、
记录算法和行映射。CPU独立E4M3最近偶数舍入，用冻结device ratio
重编码尺度并重算E2M1输入。CPU ratio逐位同设备、独立RNE同
冻结native参考；尺度变化只有[248,432)高区间，其他变化0。
FP4字节变化只出现在尺度变化组；不修正中间量化或其他层。

| 范围 | 结果 |
|---|---:|
| 输入尺度组（含十路专家复制） | 6553600 |
| 改变的尺度组 | 3800 |
| 改变的FP4元素 | 37180 |
| 涉及唯一token | 378 |
| 改变的专家输入行 | 3780 |
| 完整GU BF16输出 | 52428800 |
| 改变的GU输出值 | 4567449 |
| 改变的GU输出行 | 3780 |
| 输入不变但输出改变的行/元素 | 0 / 0 |

该结果证明：给定这次首层实际输入与路由，尺度修正会传播到
对应专家行的GU输出，未变行仍逐位相同。它不证明输出误差改善，
不证明改变值越少越好，也不说明最终答案的变化原因。矩阵仍用
记录cuBLASLt算法，不是独立高精度整模型前向。

全部新尺度/ratio/FP4 payload、完整GU输出、差异索引及逐行分布
保留，实际输入及基线GU通过原manifest绑定。首层所有活跃专家
九段checkpoint重读核对。控制器终态后封存2013项并全量核对，
约277MB，预算1.5GB及20GiB余量。证据：
`.q4t-work/e2e/moe-scale-propagation-l0-20260924/`；工具：
`tools/verify/moe_scale_propagation_l0/`。

## 后续：中间量化、down与MoE出口

复用已冻结三侧HTTP输入及上节新GU，不新增观测/HTTP。中间
量化先从原GU开始，用完整BF16设备sigmoid表和CPU指定乘法
顺序重算SwiGLU；独立枚举编码保留原E4M3高区间与次正规规则。
原分支1638400组SF、26214400个FP4元素全部逐位恢复，之后
才换入修正输入尺度的新GU。中间尺度编码本身没有修正。

原始down十路合并与shared最终相加也先从冻结实际输入重建，
10485760个F32路由合并值、10485760个最终BF16值均逐位相同。
随后按实际flat/reorder映射新中间FP4至down行序，读取checkpoint
权重并用记录cuBLASLt算法计算；CPU按十槽FMA顺序加权合并，
使用未变实际shared down与已绑定gate sigmoid，FMA后舍入BF16。

| 输入尺度单项修正的传播位置 | 与原结果不同项 | 不同行/token |
|---|---:|---:|
| 中间SF | 43066 | 3780专家行 / 378 token |
| 中间FP4元素 | 359492 | 与SF合计3780专家行 / 378 token |
| down BF16（共104857600） | 9435366 | 3780专家行 |
| 路由合并F32（共10485760） | 967679 | 378 token |
| 最终MoE BF16（共10485760） | 860418 | 378 token |

量化变化未越出GU变化行；down变化未越出中间量化变化行；两个
合并出口的变化均未越出原378个token。新输出全部有限。这里
“最终MoE”是HC write读取的MLP block，不是完成残差写回后的
trunk，更不是下一层或整模型输出。没有据差异数量声称误差改善。

中间量化保存两分支全部FP32激活/ratio、尺度、FP4 payload、
差异与行分布；down/出口保存完整新down、两分支routed F32、
最终舍入前F32/BF16、全部差异索引和token分布。原输入在冻结
来源中绑定，不覆盖旧文件。终态后封存并全量验证：

- `.q4t-work/e2e/moe-scale-inter-l0-20260924/`：5573项，约256MB。
- `.q4t-work/e2e/moe-scale-exit-l0-20260924/`：1632项，约481MB。

模板为`tools/verify/moe_scale_inter_l0/`及`moe_scale_exit_l0/`。
三个矩阵/合并参考与量化参考均零警告构建；生产二进制未变。
这完成了输入尺度单项修正到首层MLP block的有界传播，不是
独立高精度整层前向，也不是运行时候选验收。

## 后续：输入/中间高区间修正四分支

同一冻结原4K请求、首层输入/路由/权重，新增中间单项及双项分支。
中间修正只改变ratio在[248,432)的编码，次正规旧规则保留；不是
所有编码问题一起修正。baseline/input_fixed先逐字节复现前一阶段
完整激活、ratio、量化和差异文件，再计算两组新分支。中间修正
与对应父分支的激活/ratio相同，量化字节变化只在尺度变化组。

| 分支 | 中间SF变化¹ | 中间FP4变化¹ | down BF16变化 | routed F32变化 | 最终MoE BF16变化 | 出口token |
|---|---:|---:|---:|---:|---:|---:|
| 原始 | 0 | 0 | 0 | 0 | 0 | 0 |
| 只修输入 | 43066 | 359492 | 9435366 | 967679 | 860418 | 378 |
| 只修中间 | 1485 | 2588 | 3516199 | 2912104 | 2478211 | 1185 |
| 两处修正 | 44550 | 362068 | 12875098 | 3809990 | 3287910 | 1535 |

¹各项均相对原分支，不能把“双项−输入单项”的计数差当作两组
文件的实际差异数。双项相对输入单项另外改变1484个中间尺度、
2589个FP4元素；新/原GU令部分尺度是否落在高区间发生变化。

两项单独修正的出口token交集28，输入独有350、中间独有1157；
双项影响集合恰为二者并集1535。双项在输入影响集合之外与中间
单项逐位相同，在并集之外与原始逐位相同。两两最终BF16比较：
双项对输入单项2478389项/1185 token，双项对中间单项860475项/
378 token，两个单项之间3287918项/1535 token。全部差异保存。
这不是线性叠加证明，更不是误差大小、质量优劣或答案正确性结论。

每个新出口分支先恢复原routed/final逐位一致，再传播新量化数据。
三个出口参考helper复用已封存的零警告构建，摘要逐项核对；未
重链接服务。旧down可执行文件在归档down/down，初始化复制时
按根目录文件读取曾遇IsADirectoryError，定位后按原manifest的
真实路径复制，尚未运行算术、未覆盖旧证据。inter_fixed计划的
“legacy encoding”旧描述保留，以scope-clarification.json明确
实际输入为高区间修正结果；后续模板已修正文案，数值结果未改。

证据在`.q4t-work/e2e/`，均真实终态后封存并全量复核：

- `moe-scale-factorial-inter-l0-20260924`：11136项，约511MB。
- `moe-scale-exit-inter_fixed-l0-20260924`：1634项，约472MB。
- `moe-scale-exit-both_fixed-l0-20260924`：1633项，约516MB。
- `moe-scale-factorial-audit-l0-20260924`：三出口完整对照及影响集合。

模板：`tools/verify/moe_scale_factorial_l0/`。运行时未改，未新增
HTTP或性能结论。终点仍是HC write读取的首层MLP block。

## 后续：HC残差写回与PLE输入交接

代码顺序确认：mlp_frame在MoE前ReadImpl准备inject gate并保留
residual，MoE返回后Write只读取新block执行gate*block+residual。
本轮干预没有改变这些前置输入，故保持实际门值/残差；不是为了
获得一致结果而固定本应变化的量。相关源码摘要随计划保存。

CPU显式FP32 FMA后BF16舍入，完整4096×4×2560逐项重建。
原分支41943040项与HC捕获输出全同，完整摘要也与既有PLE的
trunk输入相同，接通跨运行数据边界。三分支结果：

| 修正 | HC主干BF16差异 | 受影响token |
|---|---:|---:|
| 输入单项 | 2118970 | 378 |
| 中间单项 | 5747663 | 1185 |
| 双项 | 7747890 | 1535 |

无非有限输出或仅零符号差异；写回变化不越出对应MoE block变化
token。完整四组BF16主干、全部差异及其舍入前FP32、每token
分布保存，匹配位置未舍入值不保存。终态清单全量核对，约461MB，
预算1GB及20GiB余量。证据：
`.q4t-work/e2e/moe-scale-hc-write-l0-20260924/`；模板：
`tools/verify/moe_scale_hc_write_l0/`。生产未改、无新HTTP/性能验收。

PLE在layer1入口执行。代码中key/value投影仅依赖未改变的token
查表embedding，norm_key可复用实际值；query norm依赖新主干，
随后gate、gated value、norm_conv及卷积必须重算。最终还要把新
主干加入PLE结果，不能复用旧残差。卷积K4/dilation3的前向影响
可到t+3、t+6、t+9；实际影响以新旧数据对照为准，不预先推断
全部位置都会变化。当前终点是PLE输入，不是layer1 attention输入。

## 后续：四分支完整PLE传播至layer1 HC读取入口

复用冻结HTTP，两个参考零警告构建。各分支自身trunk重算query
norm、与未变key norm的点积及gate、未变value的广播、conv norm、
K4/dilation3零初态因果卷积、SiLU及两次相加。SiLU先BF16，
gated相加再BF16，最后自身trunk相加再BF16；没有接回旧主干。
CPU按既有指定归约/FMA顺序，设备只执行rsqrt、gate非线性及SiLU。

原分支query norm/gate/gated/conv norm/最终输出、卷积末状态
均逐位恢复。完整最终输出摘要等于原layer1 HC read的trunk，
因此接通下一层读取入口，不只是某个内部孤立张量。

| 修正分支 | PLE最终BF16差异 | 输出受影响token | 超出输入集合的新token | 因果支持集上界 |
|---|---:|---:|---:|---:|
| 输入单项 | 2155162 | 1285 | 907 | 1286 |
| 中间单项 | 5439036 | 2297 | 1112 | 2299 |
| 双项 | 7438628 | 2884 | 1349 | 2885 |

因果支持集由实际conv norm变化位置向后0/3/6/9移位，加新trunk
变化位置组成。所有输出变化都在该集合内；少数支持位置经舍入
没有最终变化，不能直接把结构上界当实测影响。每支输出41943040
项，均有限。更新的92160个卷积状态值，输入单项与原相同，中间/
双项各7819项不同；这仅描述该PLE末状态，不代表整模型decode
状态相同或最终输出不受影响。

所有四支完整中间/最终BF16及更新状态、差异索引和行分布保留。
完整norm/gate数学参数及设备primitive、差异未舍入值、卷积F32
累加和SiLU均保存；匹配最终未舍入值不另存。20项阶段完整覆盖，
终态192项清单全部复核，3180373631字节，在5GB预算内，另留
20GiB。证据`.q4t-work/e2e/moe-scale-ple-chain-20260924/`；模板
`tools/verify/moe_scale_ple_chain/`。无生产修改，无新HTTP或性能结论。

本阶段是一条指定算术的组合传播，仍使用实际前置输入、未变的
查表分支和设备数学；不是独立整模型前向，更不是最终错答解释。

## 后续：layer1 HC读取与新写回门值

从四支自身PLE输出重算HC norm、down、scaled SiLU、up及四路
sigmoid混合；另从自身norm重算inject投影及scaled sigmoid门值。
没有将原支门值用于修正后的第二层。norm CPU指定顺序+设备
rsqrt，GEMM复用记录cuBLASLt算法；非线性使用冻结完整BF16函数
表，混合以CPU四次顺序FMA及除4后BF16执行。

原分支七个阶段全部逐位恢复，4组checkpoint参数重读同摘要。
28项branch×stage覆盖齐全，各阶段变化均未越出其输入影响token，
所有实际使用的值与完整输出有限。结果：

| 修正 | 注意力mixed输入BF16差异（共10485760） | mixed受影响token | 新inject gate差异（共16384） | gate受影响token |
|---|---:|---:|---:|---:|
| 输入单项 | 1048618 | 1284 | 220 | 184 |
| 中间单项 | 2599376 | 2295 | 885 | 576 |
| 双项 | 3453125 | 2884 | 1087 | 742 |

HC混合舍入后，输入单项/中间单项各有少数PLE变化位置不再出现
mixed差异；这不证明那些位置所有后续状态都相同，尤其写回还要
使用各支自身残差和门值。四支新门值完整保存，后续必须配对使用。

完整BF16中间和出口、所有差异索引与token分布、norm数学参数/
primitive和差异未舍入值保留；GEMM累加器与匹配最终未舍入值未
保存。约1.15GB，在2.5GB预算内，另留20GiB；真实终态封存并
全量复核。证据`.q4t-work/e2e/moe-scale-hc-read-l1-20260924/`；
模板`tools/verify/moe_scale_hc_read_l1/`。新增两个CPU helper零
警告构建，未改norm helper按既有封存摘要复用。生产未改。

终点为layer1线性注意力mixed输入及其HC预备门值，尚未执行
layer1注意力，不构成整模型正确性或最终错答因果证明。

## 后续：layer1四组投影与短卷积

复用冻结HTTP，必要helper零警告构建。4组权重重读checkpoint，
四分支各自mixed输入按实际记录算法重算4096行QKV/Z/A/B。
原分支全部输出恢复；修正投影输出均未越出mixed变化token。
每支四组共67502080个BF16输出，变化值数/受影响token如下：

| 投影 | 输入单项 | 中间单项 | 双项 |
|---|---:|---:|---:|
| QKV | 5673188 / 1284 | 12284022 / 2295 | 16069355 / 2884 |
| Z | 1760128 / 1284 | 3857206 / 2295 | 5229453 / 2884 |
| A | 19634 / 1272 | 42044 / 2282 | 55714 / 2869 |
| B | 15164 / 1234 | 36485 / 2271 | 48167 / 2860 |

随后各支自身QKV进入K4/dilation1短卷积，CPU顺序四次FMA，
设备仅SiLU再BF16。权重与零初态绑定，原分支输出同捕获的GDN
输入，更新历史也恢复。每支41943040个卷积输出：

| 修正 | 卷积BF16差异 | 影响token | 相比QKV新增token | 更新历史差异（共30720） |
|---|---:|---:|---:|---:|
| 输入单项 | 9323498 | 3362 | 2078 | 0 |
| 中间单项 | 18034465 | 4027 | 1732 | 18067 |
| 双项 | 22371185 | 4087 | 1203 | 18067 |

变化全部位于实际QKV变化位置前向0/1/2/3步的因果支持集内，
输出及数学中间值有限；本次三支输出影响集合均达到该支持集。
更新历史来自最后3行原始QKV，输入单项历史相同不等于后续GDN
递推状态或整模型decode相同。A/B仍是原始投影，不是已算门值。

完整16份投影输出、全部差异和行分布保留，未保存GEMM累加器。
短卷积保存四支完整F32累加、设备SiLU、BF16、历史、全部差异和
行分布，核对CPU BF16舍入重建。真实终态清单全量验证，证据：

- `.q4t-work/e2e/moe-scale-linear-input-l1-20260924/`：68项，约721MB。
- `.q4t-work/e2e/moe-scale-linear-conv-l1-20260924/`：约1.88GB。

模板分别为`tools/verify/moe_scale_linear_input_l1/`和
`moe_scale_linear_conv_l1/`。运行时未改，没有新HTTP或性能结论。
终点为GDN原始QKV及各支原始Z/A/B，还未进行GDN归一化/递推。

## 四分支 layer1 GDN 归一化、门值与完整递推

冻结HTTP复用，CPU参考helper零警告，dt_bias/A_log两组权重重读。
每支自身短卷积QKV做q/k norm：每lane四次平方FMA、32路XOR、
设备rsqrt，q另乘1/sqrt(128)，BF16写回，v保持自身值。自身A/B
投影经设备softplus/exp/sigmoid产生decay/beta。从原全零初态
CPU递推4096步，状态更新采用`fma(alpha,S,k*delta)`。

原分支完整归一化QKV、门值、25165824个Y和786432个最终状态
均逐位恢复。norm纯CPU/FP64分别仍有170/193个BF16差异，全部
保留，不以设备指定算术相同宣称独立高精度相同。

| 修正 | q/k BF16差异 | 门值F32差异 | Y BF16差异 | Y影响token | 最终状态F32差异 |
|---|---:|---:|---:|---:|---:|
| 输入单项 | 3906011 | 34693 | 11734129 | 4079 | 443655 |
| 中间单项 | 7544545 | 78233 | 15586761 | 4094 | 786430 |
| 双项 | 9267465 | 103495 | 17547025 | 4094 | 786430 |

输出影响位于自身变化输入的前向因果支持集内，三支均达到该集合。
卷积历史相同的输入单项仍改变GDN末态，不能据短卷积历史推断
后续decode不变。输出与状态有限；数字只描述传播，不衡量质量。

保存完整各支norm三种参考、参数/逆平方根、归一化QKV、门值、
Y和末态，以及所有差异/token分布。设备初等函数仍是参考依赖，
没有高精度整条递推或最终答案证明。运行时未改、无新HTTP。
证据`.q4t-work/e2e/moe-scale-gdn-l1-20260924/`，真实终态后
162项清单全量验证，逻辑字节2768312106；工具模板
`tools/verify/moe_scale_gdn_l1/`。终点为layer1 GDN输出/末态。

## 四分支 layer1 NormGate、输出投影与注意力残差写回

各支自身Y与Z进入NormGate：CPU平方树归约，设备rsqrt/sigmoid，
CPU逐步三次F32乘法和BF16。两组checkpoint权重重读，新的NormGate
输出进入捕获算法的out投影，m4096/n2560/k6144、alpha1/beta0。
原分支完整NormGate、out及捕获HC消费者逐位恢复；NormGate纯CPU/
FP64仍有200/270个BF16差异，保留完整对照，不计作整模型精度结论。

随后每支自身attention输出、自身PLE主干与HC attention注入门值，
通过CPU显式FMA+BF16写回四路主干。原分支恢复fused.output及
MLP residual消费者，修正分支不复用旧门值或旧主干。

| 修正 | NormGate BF16差异 | out BF16差异 | HC写回 BF16差异 | 三阶段影响token |
|---|---:|---:|---:|---:|
| 输入单项 | 11728767 | 6176843 | 8765901 | 4079 |
| 中间单项 | 15830531 | 7177847 | 13067963 | 4094 |
| 双项 | 17863770 | 7681784 | 15534688 | 4094 |

所有输出有限，差异支持集未越出自身输入；仅衡量传播，不判断最终
答案改善/退化。生产未改，无新HTTP或性能结论。helper零警告，
真实进程终态后封存并逐项核对：

- `.q4t-work/e2e/moe-scale-linear-output-l1-20260924/`：144项，
  3631648388逻辑字节。完整NormGate三参考/数学中间值/rawFP32、
  out BF16与全部差异/行分布；未保存GEMM累加器。
- `.q4t-work/e2e/moe-scale-hc-attn-write-l1-20260924/`：完整主干
  BF16、全部差异及差异处rawFP32、行分布，未保存匹配处舍入前值。

模板分别在`tools/verify/moe_scale_linear_output_l1/`和
`moe_scale_hc_attn_write_l1/`。终点为注意力后的主干，尚未第二层
MLP HC读取/路由/专家；后续必须重算路由，不固定为旧专家选择。

## 四分支 layer1 MLP HC 读取和新路由

以各支注意力写回主干为输入，重新计算MLP HC norm/down/SiLU/
up/inject/gate/mix七阶段，原支全部逐位恢复。使用设备rsqrt、冻结
完整BF16非线性表与记录cuBLAS算法，四组checkpoint权重重读。

| 修正 | mixed BF16差异 | mixed影响token | MLP gate BF16差异 | gate影响token |
|---|---:|---:|---:|---:|
| 输入单项 | 5539750 | 4079 | 1527 | 1246 |
| 中间单项 | 6751910 | 4094 | 2314 | 1675 |
| 双项 | 7367975 | 4094 | 2792 | 1948 |

各支新mixed进入router投影，m4096/n512/k2560，记录算法、
alpha1/beta0。CPU按分数降序、专家ID升序选择前10；设备exp，
CPU顺序10项求和/倒数/权重。原支2097152个logits、40960个ID
和40960个F32权重全部恢复，checkpoint router权重重读绑定。

| 修正 | logits BF16差异 | ID位置差异 | 权重F32差异 | 顺序变化token | 集合变化token | 替换专家slot |
|---|---:|---:|---:|---:|---:|---:|
| 输入单项 | 136793 | 1382 | 23880 | 643 | 147 | 147 |
| 中间单项 | 179634 | 1737 | 26850 | 803 | 211 | 212 |
| 双项 | 210574 | 2020 | 29850 | 920 | 238 | 238 |

分数变化仍分别覆盖4079/4094/4094 token。所有变化在自身输入
支持集内，数值有限。这证明layer0干预不仅改数值，也改下一层
专家集合；固定旧路由不能继续充当这条传播链。不是最终答案改善
或退化的结论，也不把单层干预等同全模型尺度修正。

两阶段helper零警告、原支恢复后才分析变化支，无生产/观测修改，
无新HTTP。真实终态后清单全量核对，证据：

- `.q4t-work/e2e/moe-scale-hc-mlp-read-l1-20260924/`：163项，
  1671235764逻辑字节，完整阶段输出/差异/token分布与norm参数。
- `.q4t-work/e2e/moe-scale-router-l1-20260924/`：78项，23315503
  字节，完整logits/IDs/deltas/exp/weights及全部差异/逐行替换记录。

模板为`tools/verify/moe_scale_hc_mlp_read_l1/`和`moe_scale_router_l1/`。
终点新路由，尚未按新路由完成layer1量化/GU/down/共享分支。
新的专家行数可能改变矩阵形状，后续不能直接套用旧行数或旧重排；
算法兼容性与原支恢复须分别检查。

## 四分支 layer1 新路由分组、输入量化与 GU

每支新路由按专家、flat slot稳定排序，40960个分配重新建立
counts/offsets/lists。按(token,expert)对齐旧实际gather/GU，而非
沿用旧物理行序；新增选择单列，不填零冒充基线。1024个checkpoint
input_scale及361专家GU权重计划全部重读绑定。

各支新mixed独立重算比例与FP4；设备复现接受版legacy E4M3，
CPU枚举E2M1。CPU/device比例逐位相同，native与独立RNE相同。
此处不额外修正layer1尺度，仍隔离layer0干预；四支各有2950组
已知高区间编码错误，其余RNE差异0。原支SF和packed重新按身份
对齐后全部恢复，不把保留缺陷写成量化规范通过。

| 修正 | 活跃专家 | 行数变化专家 | 新增专家行 | 共有行SF差异 | 共有行packed字节差异 |
|---|---:|---:|---:|---:|---:|
| 输入单项 | 360 | 72 | 147 | 139912 | 1587679 |
| 中间单项 | 360 | 93 | 212 | 201738 | 2259115 |
| 双项 | 360 | 97 | 238 | 245042 | 2764128 |

原支活跃361专家，三支都没有原先未激活的新专家。新行数逐专家
通过记录cuBLASLt算法兼容性和workspace检查，不更换算法绕过。
GU输入直接使用新顺序，权重/alpha不变；原支52428800个BF16
按身份完整恢复。变化分支仍各输出52428800个BF16：

| 修正 | 共有行GU BF16差异 | 变化共有输入/输出行 | 新增行（不计旧值差异） |
|---|---:|---:|---:|
| 输入单项 | 48028445 | 40643 / 40643 | 147 |
| 中间单项 | 48941383 | 40728 / 40728 | 212 |
| 双项 | 49279929 | 40702 / 40702 | 238 |

独立支持集审计逐行确认：共有量化输入未变而GU输出变化的行数为0。
数值有限；以上是指定算法的传播范围，不衡量最终质量。新增行输出
完整保留，后续必须使用新的flat slot回到各支路由权重。

无生产/观测修改，无新HTTP。helper零警告，执行真实终态后，
input量化100项和GU10482项清单全量核对，证据：

- `.q4t-work/e2e/moe-scale-input-quant-l1-20260924/`：614486851
  逻辑字节；完整映射、比例、legacy/native/RNE尺度、packed及
  共有差异/新增删除边，模板`tools/verify/moe_scale_input_quant_l1/`。
- `.q4t-work/e2e/moe-scale-gu-l1-20260924/`：1246430733逻辑
  字节；完整GU、行映射、共有差异、支持审计、权重计划，模板
  `tools/verify/moe_scale_gu_l1/`。不保存GEMM原始F32累加器。

终点layer1 GU，尚未中间量化、down、共享分支及最终MoE合并。

## 待完成

1. 四支新路由已完成输入量化/GU，继续自身GU的中间量化/down，
   重算共享分支并按新flat slot/权重合并，再用自身MLP门值写回。
2. 建立可比多层与最终答案因果证据；不能由局部影响计数或
   格式正确性宣布错答已解释或修复。
3. 保留规范修正候选原HTTP任务退化事实；任何运行时修复仍须
   先完整HTTP质量和五档性能，不能以离线对照追认被拒候选。

原4K600440错答仍未解决，正确性确认尚未完成。
