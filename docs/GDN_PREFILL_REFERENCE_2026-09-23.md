# 完整4K prefill GDN递推参考

2026-09-23。生产运行时未改，仍是2392f6b1。原题期望360284、实际
600440，原错未修复。本阶段从实际归一化q/k和实际v出发，独立CPU
生成完整状态与输出，不代表完整模型前向或完整GDN前处理均已验证。

## 先行HTTP与完整快照

观测器零警告构建后，首项tools/evalscope原4K无观测→观测→无观测。
三侧均输入4096、输出7、600440、stop、服务退出0、质量驱动退出1。
这是观测不改变原请求结果的门禁，不是质量通过，诊断耗时不作性能数据。

实际36个线性层均GdnRegPrepNormKernel后接GatedDeltaNetRegKernel<8>，
每层T4096、nkh16、kd128、每key对应3个value head、vd128、nv48，
扫描grid=(4,48)、block128。不是可选的32-token tensor-core chunk路径。

捕获每层完整归一化前后qkv、a/beta、dt_bias/A_log、状态初值、末值
和全部y；用实际指针将前处理输出绑定扫描输入。保存已有首pooled
decode交接观测，324份历史BF16/FP32快照逐字节不变。

初始状态共28311552个FP32全部为正零。归一化前后905969664个v值
逐字节不变；末状态同首decode初始状态。72组dt_bias/A_log实际参数
逐字节匹配checkpoint各层[48] BF16张量。

## 独立CPU全递推

设备辅助程序只计算alpha/beta的exp/log1p等基础非线性，不调用生产
扫描kernel。CPU从捕获的全零初态出发，顺序处理4096个位置：

1. 每lane连续4个k维度，显式FP32 fma计算旧状态点积。
2. 完整重建32个lane的XOR归约（16/8/4/2/1）。不同lane有不同
   浮点加法顺序，不能只计算lane0再向所有lane广播。
3. delta = fma(-alpha,u,v) * beta。
4. 先将k*delta舍入FP32，再更新S = fma(alpha,S,k*delta)。
5. 每lane显式fma读出，再按lane0的归约树合成，最后BF16 RNE。

36层CPU从初态独立生成的28311552个最终FP32状态，全部逐位一致；
全部4096位置的905969664个BF16输出也逐位一致。不是仅末token，
不是只验证状态交接，也未将中间实际状态灌入参考来消除累计误差。
CPU和设备辅助构建零警告，各进程退出0，独立核查再次比较全量文件。

接受二进制的SASS中，例如0x1910的FMUL先计算注入乘积，0x1960的
FFMA再计算旧状态乘alpha加该乘积；0x19b0/0x1a00对应delta的融合
减法后乘beta。参考依此固定收缩顺序。另保留第0层相反收缩顺序
fma(k,delta,alpha*S)的结果：406928个状态、6102个输出不同，全部
差异索引和输出文件留存。没有在多个候选中只保留更小的差异数。

## 失败记录与边界

初始边界脚本把旧目录里的离线reference.npz也当成新观测文件，
因此缺文件退出，尚未运行递推。保留initial-bind脚本/日志，改为只
比较旧观测的BF16/FP32原始快照；并未忽略数值差异。复用同一轮
通过观测门禁的冻结HTTP，不改运行时，不重选HTTP结果。

SASS首次短函数名查询未取得指令；改用符号表中的完整函数名取得
目标函数。其他fatbin不含该函数的工具提示不属于编译警告或运行故障；
证据包含完整目标函数指令和符号表，不能用空查询输出支持算术结论。

**尚未完成：**完整q/k归一化的独立算术（本轮已保存原始输入供后续
核对）；全token卷积与更早投影的独立来源；全模型高精度前向；
其他上下文、分块边界、多序列、MTP与多模态。基础数学函数仍来自
设备库，不声称独立证明设备exp/log1p精度。已知E4M3尺度编码问题
和原4K错题仍在；本轮没有给出它们与答案之间的因果结论。

## 证据与下一步

`.q4t-work/e2e/gdn-prefill-reference-20260923/`保存完整HTTP、全部
快照、初态/历史绑定、checkpoint参数绑定、每层CPU结果、参考输出、
反向收缩差异、SASS、失败记录与artifact-binding。
永久工具在tools/verify/gdn_prefill/。从模板生成prepared文件，必要
构建后先run.py执行HTTP，再bind.py核对边界并计算基础参数；先第0层
variant1，replay_all.py继续全部层，另保留第0层variant0，最后audit.py。
新实验必须使用新目录，不能覆盖本次记录。

下一步复用冻结快照，独立核对全部prefill q/k归一化和参数生成边界，
继续缩小未验证范围；不能把本报告当成正确性调查结束或性能验收通过。
