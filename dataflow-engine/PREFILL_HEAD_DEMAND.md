# 计划 P7：让输出头由消费者需求驱动

2026-09-21。源码基点 f41adfc。首块省略阶段已接受，过程记录按发生顺序保留；完整
P7 目标是最终 chunk 只计算需要的最后行，不以首块省略代替目标。

## 已核对的消费者

- 普通文本长 prefill 调度：每块推进所有层状态；仅末块在锁内将
  logits 的最后行复制给请求线程。首块输出没有消费者。
- RunLayers 在可选 trunk_out 复制之后检查 logits 是否为空；空
  指针跳过 HeadForward，不跳过任何 decoder 层或状态更新。
- HeadForward 的 mixer 与词表投影读取 trunk，写 workspace 与
  logits；没有 KV、SSM、PLE history 或 ModelSequence 更新。
- 短 prefill 批处理：各序列最后行是消费者，不能只选整个拼接批次
  的最后行。普通单请求同样只消费末行，但当前仍计算整块。
- MTP trunk 输出与 logits 是独立需求。验证接受可能消费多行，
  因此不能全局把 logits 非空解释为“只需要最后一行”。

## 当前候选：省略没有消费者的首块 head

仅将 RunOnePrefillChunk 的 logits 需求从首块或末块改为仅末块。
使用已有 nullptr 路径，最后块形状、D2H 行偏移、stream 完成检查、
调度、scratch 容量和所有序列状态保持原方式。MTP/视觉/内联回退
没有改动。证据 prefill-first-head-skip-20260921。

必要构建零警告，首项质量 HTTP 11/11 通过。单流完整五档仍运行；
44K 三次 TTFT 均值 30.628→30.489 秒，输出一致、decode 范围
重叠。这是中间观察，不能代表候选接受或普遍提速。随后必须完成
双参考范围审查、并发与生命周期 HTTP，最后才采集时间线。

## 后续仍需实现的行需求

接口应明确区分无输出、全部行与指定序列末行，保留完整 logits
消费者的原语义；trunk 输出独立。先从普通文本单序列末行开始
验证，随后扩展到批处理每序列末行，不能将批处理当作单序列。

选择应作用于输出头输入 trunk，使 mixer 和词表投影都只处理
目标行，而不是计算完全部 logits 后再丢弃。共享 logits 容量
是否缩减需核对其他消费者，不能由单路径写入减少推断容量收益。

这一阶段会改变 GEMM 的 M 维与可能的内核分派，不能从当前首块
省略的 E2E 推断数值等价。新候选仍先完整 HTTP；输出或性能失败
先处理门禁，通过后再做需要的 logits/路由数值与时间线核对。
保持模型精度，不以少量质量题代替对改变计算形状的数值证明。

## 完整单流 HTTP 门禁通过

质量 11/11、五档 HTTP/输出 15/15，服务退出 0；对 f41adfc 与
固定参考 fcb5925 均无 TTFT/decode 不利范围分离。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.811122 | 18.544807 |
| 4096 | 2.680828 | 17.851499 |
| 8192 | 5.269453 | 18.069116 |
| 45056 | 30.488916 | 17.855753 |
| 204800 | 159.371614 | 17.068293 |

44K TTFT 均值下降约 0.14 秒，范围有利分离；200K TTFT 均值
159.698→159.372 秒但范围重叠，不作为稳定提速结论。decode 按
持平理解，固定参考不更新。

父版→候选→父版各三轮并发 HTTP 已启动，随后必须完成四项
生命周期 HTTP；这些结束前不采集时间线。候选尚未接受或提交，
不能由单流结果推断调度性能或错误退出语义保持。

## 并发 HTTP 对照通过

父版→候选→父版各三轮，共 18 个请求输出一致、三次服务退出 0。
总完成时间范围依次 45.147–45.268、44.981–45.136、
45.111–45.298 秒；无不利范围分离，候选与后侧父版重叠。
短 TTFT 均值 4.571/4.598/4.765 秒，长 TTFT 均值
31.587/31.456/31.598 秒。短请求每轮均先于长请求首响应返回。
不把原有 chunk 调度收益重复算作本候选收益，也不据此宣称稳定
并发提速。最长响应 chunk 间隔仍约 5.75 秒。

轮转/槽复用、停机、copy/sync 返回错误 HTTP 已串行启动；
全部通过前不运行时间线，候选仍未接受。

## 生命周期 HTTP 全部通过

四项 rotation-reuse/shutdown/copy/sync 均通过、服务正常退出。
停机信号后约 4.38 秒客户端与服务全部结束；copy/sync 合成返回
错误令请求失败、槽归还、健康和后续请求 503。仅为固定两槽文本
与合成错误，不等同于真实设备故障或任意并发验证。

全部 HTTP 门禁后，冻结源码/二进制与库摘要一致，除 server 外
所有库与父版摘要一致，源码唯一变化是调度块 logits 需求条件。
44K 时间线已启动，最终审查未完成，候选仍未接受或提交。

## 最终接受与时间线

44K profiled HTTP 输出匹配、服务及采集器退出 0。块序列保持
[8192,8192,8192,8192,8192,4096]，prefill kernel
537278→537272，正好少去首块 head 的 grouped norm、down GEMM、
SiLU、up GEMM、gate+mix 与词表 GEMM 六次调用；异步分配/释放
各 1588→1586。decode 436815 次 kernel 与全部 API 次数不变。

首轮按全局 GPU 开始时间逐项比较失败，原因已核对为既有 MoE
多 stream 交错排序不同：删除首块尾部六次调用后，全部六个 prefill
块与 255 个 decode forward 的每个 stream 内 kernel 名称、grid
与顺序逐项一致。保留初始失败及解释于 kernel-sequence-audit.json，
不要求并行 stream 的全局开始次序固定。

单次插桩窗口 prefill 31251.028→31071.655 ms，decode
14582.685→14538.440 ms，不以该时间替代正式矩阵。原始 HTTP
记录、执行工具副本、源码/二进制/库摘要均通过最终核对。
按省去不消费的计算、完整门禁通过接受，固定参考 fcb5925 不变。
44K TTFT 改善约 0.46%，200K/decode/并发不宣称稳定提速。
未减少预留 logits 容量，未实现最终块只计算末行；后者仍为下一
实际实施项，MTP/视觉/内联回退也保持原输出头策略。

## 末行输出候选已实现，验收中

基于 46b732b，LogitsRows::kAllRows 为默认，kLastRow 从
最后 trunk 行执行 HeadForward(T=1)，结果写 logits 首行；
trunk_out 保持完整 T 行。单请求短 prefill 与普通文本长末块
调用显式选择，服务器读回相应改为首行。B>1 的短 prefill 保留
原全行语义，不把拼接批次末行冒充每序列末行。

接口编辑中曾有编译失败，保留日志；最终构建零警告，首项质量
HTTP 已启动。证据 prefill-last-row-20260921，未执行前置测试。
完整 HTTP 通过后仍需数值验证，因为 T=1 会改变 GEMM/核函数
分派。未减少预留容量，未接受；每序列行选择仍为后续实施项。

末行候选首项质量 HTTP 完成：11/11、输出与父版一致、服务退出 0，
二进制 SHA-256 `55bd0e1edb9a698bf81384287b4a3f4b918fa9fb690f4f7a1bdd20fcb41347cb`，冻结源码一致。完整五档性能已接续，
尚未进行数值或时间线；质量题通过不等同于计算形状数值等价。

## 末行候选完整单流门禁通过

质量 11/11、五档 HTTP/输出 15/15，服务退出 0；对直接父版
46b732b 与固定参考 fcb5925 均无不利范围分离。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.803008 | 18.591477 |
| 4096 | 2.638076 | 17.863283 |
| 8192 | 5.181467 | 18.115714 |
| 45056 | 30.506348 | 17.861866 |
| 204800 | 159.501478 | 17.120941 |

4K/8K TTFT 均值改善约 1.6%–1.7%；44K/200K 均值略慢但范围
重叠，不宣称长上下文稳定提速，decode 按持平理解。并发父版→
候选→父版对照已启动，随后生命周期 HTTP，再进行数值与时间线。
候选尚未接受；本次改变 GEMM 形状，不能用输出一致替代数值核对。

## 末行候选并发对照通过

父版→候选→父版各三轮、18 请求输出一致，三个服务退出 0。
总完成时间范围分别 45.092–45.221、44.950–45.113、
45.032–45.215 秒，候选与两侧均重叠，按持平理解。短 TTFT
均值 4.739/4.674/4.764 秒，长 TTFT 31.510/31.415/31.486 秒。
不把小幅均值变化视作稳定并发收益。

四项生命周期 HTTP 已启动。数值工具已准备并等待全部 HTTP
通过后才编译执行，检查 logits、完整 trunk、canary、游标历史
和固定 token 续算；保存原始 BF16 差异，不预设放宽阈值。
结构检查成功仍需单独精度审查，候选尚未接受。

## 末行候选生命周期通过，数值审查未闭合

四项生命周期 HTTP 全部通过后，数值工具才编译执行，零警告且
退出 0。七组形状 [1]/[7]/[1024]/[4096]/[8192]/[7,5,1]/
[4096,4096]，交替 slot 0/1，使用正式矩阵文本与其前缀。
完整 trunk 220416000 个 BF16 值逐位相同，7 次固定 token 续算
logits 逐位相同；序列位置/历史/阶段及输出首尾 canary 通过。

10 个末行词表比较中，8 个并非逐位一致，最大 l2_rel 为
0.000918687，max_abs=0.0625。10 组 argmax 相同，父版 top2
的 logits 值和 margin 也均相同，最小 margin=0.3125。全部
原始 BF16 结果保留在 numerical/*-{all,last}.bf16。

不能宣称 E0：除一 ULP 差异外还有近零值的多 ULP/符号翻转。
rounding-review.json 保留完整 ULP 分布与 margin；不预设放宽
阈值，也不把所有差异称为 near-tie。该工具结尾明确仅为
STRUCTURAL_PASS / NUMERICAL_REVIEW_REQUIRED，尚需输出头逐算子
与参考精度归因。候选未接受，未启动 profile，不重复运行已通过
的完整 HTTP；后续专项使用同一冻结二进制与库。

## 末行候选逐阶段与高精度参考审查

numerical-stages 再次使用冻结模型库，通过同一结构检查并复现
全部原始 logits 差异。直接捕获 HeadForward 工作区的实际结果：
trunk/normed 全部逐位一致，mixed 仅四组存在 1/4/1/9 个 BF16
差异，其余六组逐位一致。不是 decoder 状态或行索引错位。

随后 analyze_prefill_head_reference.py 只读 safetensors 中相同
BF16 的 mixer down/up 与 lm_head 权重，从两路径相同的 normed
开始做 CPU FP64 点积/非线性参考，在原中间边界按 FP32→BF16 RNE
舍入。逐算子误差和全部文件摘要保存在 head-reference/。该参考
不重新评估归一化数学，也不是 CUDA 算法的逐指令模拟。

10 组单行 mixed 与参考逐位一致；最终 logits 相对共同舍入参考
的 L2 误差，8 组更小、两组相同。候选范围
4.807e-6–3.305e-5，原全行路径 4.807e-6–9.183e-4；所有
argmax 与参考一致。结合完整 HTTP 输出、完整 trunk/续算逐位
保持，按 E1（dtype/舍入边界保持、归约顺序改变）完成本轮精度
审查，不宣称 E0，也不把近零符号翻转藏入单一 ULP 描述。

这是十个实测行的相对证据，不是所有 prompt 的数学保证，未设置
临时统一误差阈值。precision-review.json 记录审查依据；随后
启动父版 4K、候选 4K/44K 时间线，44K 父版沿用已存同版本捕获。
最终时间线与原始证据审查前仍未接受候选。

## 末行候选最终接受

父版 4K 与候选 4K/44K profiled HTTP 输出匹配，服务/采集器退出
0。4K prefill/decode kernel 87655/430695，44K 为
537272/436815，次数均不变。最终 head 的三次 GEMM 换为
Bf16Gev，norm/SiLU/mix 的 grid 收缩到一行；删除该末尾六次调用
后，每个 forward 的各 stream 内 kernel 名称、grid 和顺序
全部相同，包含全部 decode。

4K 词表投影单次 42.549→4.826 ms，prefill 窗口
2907.305→2854.303 ms；不以单次 profile 代替正式性能矩阵。
44K 候选是在同一服务先跑 4K 后采集，已有父版捕获是首请求，
因此不把初始化 API 次数或插桩窗口差异当作收益。4K 的一个
CUDA library load 从首次 decode 移到 prefill，符合首次调用
GEMV 的位置变化；不宣称库加载被消除。

原始 HTTP、工具副本、冻结源码/库、数值与 CPU 参考以及时间线
最终核对通过，按 E1 接受，固定参考 fcb5925 不更新。4K/8K
TTFT 改善约 1.6%–1.7%；长上下文、decode 与并发按持平理解。
预留 logits 容量没有缩减。当前仅 B=1 普通文本调度使用末行
模式，B>1、MTP/视觉及内联回退保持默认全行语义；下一步仍需
实现批处理每序列行选择及相应多请求门禁。


## 批处理每序列末行输出头：已接受

基于 61742d4。证据 `.q4t-work/e2e/prefill-batch-last-rows-20260921/`。
`ModelPrefillBatch` 新增 `PrefillBatchLogitsRows`，默认全部打包行；
显式末行模式按输入序列顺序输出 `[B,vocab]`，不按状态池 slot 编号。
Decoder 循环结束后，另一主干缓冲已失效，将各 `seq_offset[b+1]-1`
行收集到其中，再执行 B 行 mixer/词表投影，无新增分配。
Serve 的 B>1 短 prefill 接入该模式，删除重复维护的 host 行偏移。
默认全行 API、单序列路径、MTP、视觉、混合 batch 保持原语义。

必要构建零警告后首项真实 HTTP：质量 11/11、五档输出 15/15，
服务退出 0；对直接父版及固定参考 fcb5925 无不利范围分离。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.801 | 18.575 |
| 4096 | 2.637 | 17.858 |
| 8192 | 5.178 | 18.050 |
| 45056 | 30.518 | 17.854 |
| 204800 | 159.468 | 17.048 |

单流不会进入新 B>1 路径，因此另做实际组批 HTTP：44K 长请求期间
同时提交 1K/4K 两个短请求，父版→候选→父版各三轮，共 27 请求。
读回观察器确认每轮同次 stream 完成前有两行词表结果，候选源地址
间距一行 496640 字节，父版间距 1024/4096 行，实际 B=2 覆盖成立。
所有输出一致，三个服务退出 0；TTFT 与总完成时间范围均重叠。
候选 makespan 35.689–35.975 秒，两侧父版分别 35.749–36.065、
35.761–36.084 秒，不宣称稳定提速。观察器所有版本均开启，只记
词表读回，不注入错误、不计 kernel 时间。交错 HTTP 18 请求及
四项生命周期（10 请求，其中六个预期失败）也全部通过。

全部 HTTP 后才运行新数值工具：八种形状、非连续/非顺序槽号，
覆盖 B=1/2/3、不等长及总行数 8192。17 行 logits、356802560 个
主干 BF16 值和八组固定 token 续算逐位一致；实际收集行及输出
canary 通过。原始 logits/mixed/normed/trunk 共 68 对文件全部
逐位一致，没有新增误差容忍阈值。有限形状不是全输入证明。

双版本各一次真实三请求 HTTP 时间线均输出匹配、正常退出并导出：
22 次 forward 的 token 打包相同，包括一次 T=5120 prefill、
15 次 B=2 decode 与六个长 prefill 块。除批量 head 外，按每 stream
核对全部 kernel 名称、grid、block 与顺序完全一致。候选新增一次
GatherSequenceLastRowsKernel，grid=(40,2,1)，head 行数 5120→2；
其余 forward 全部相同。词表投影单次 kernel 50.738→5.096 ms，
仅作局部诊断，不能替代正式并发性能的持平结论。
初版时间线审查脚本假设存在 DRIVER 表而报 SQLite 查询错误，原稿
和错误记录保留；修正为明确标注未采集该表，未重跑/替换捕获。

最终源码/库/二进制摘要、原始 HTTP 数据、正常/预期失败终态及
逐位/时间线核对通过，本阶段接受。共享 logits 仍按 max_prefill
预留，未降低容量；下一步核对所有消费者后缩减普通 serve 的
logits 工作区，不能将写入行减少等同于内存容量已减少。
