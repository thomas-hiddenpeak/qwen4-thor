# 计划 P：把文本 prefill chunk 接入服务调度器

2026-09-21，源码基点 ec64259。本阶段已接受：单流五档、长短并发对照、生命周期 HTTP
及时间线/源码审查通过。下列调用事实描述改动前基点，过程记录按
发生顺序保留，最终结论见文末。代码优先于旧注释中的 PD-ready 描述。

## 已核对的调用事实

- HandleChat 的长 prompt 虽然已调用 ModelPrefillTextChunk，但仍在
  整个循环期间持有 model_mu_。44K 和 200K 正式单流 TTFT 约 31/160
  秒；这不是并发阻塞时间测量，但说明锁内工作可能持续很久。
- Model 的 d_ws、d_trunk、d_ids、d_positions、d_ple_emb 等是共享
  scratch，序列状态才按 seq_id 池化。服务的 d_prefill_logits_ 也共享。
- SchedulerLoop 当前只接收短 prefill 和 decode/MTP。两个提前
  continue（pending.empty、plain_reqs.empty）会跳过轮尾新增工作；
  加 chunk 处理时必须显式处理“只有 chunk、没有 decode”的情况。
- 同一轮先处理短 prefill，再处理已收集的 MTP/plain decode。pending
  集合在执行前冻结；执行过程中到达的请求需在下一轮重新收集。
- ModelPrefillTextChunk 的成功仅表示工作已排队，host position/history
  已推进；它不证明 GPU 完成。现有 FinishHostReadback 才检查结果
  复制和同步返回值，失败标记服务不健康。
- PrefillReq/ActiveRequest 都借用请求线程内存，线程等待 done，调度器
  在 sched_mu_ 下写结果并唤醒。新 chunk 请求必须延续这个生命周期。

因此不能把“每块解锁一下”视为调度实现：mutex 不保证交接给 decode，
未检查的设备工作也不能变成可发布的状态边界。

## 下一候选的执行规则

普通文本长 prompt（scheduler 可用、MTP 未加载、无视觉输入）注册
一个持久到整个 prompt 结束的 chunk 请求。单流也经过此路径，使
44K/200K 正式门禁能覆盖它；不靠仅在并发时启用来绕过正常 E2E。
旧短 prefill 的批量化保留，MTP/视觉路径先保留原驱动。

请求携带完整 prompt 指针、长度、ModelSequence 指针及最终 host
logits 地址。请求线程等待最终完成，不在 chunk 中间同时修改历史、
回收 slot 或销毁请求对象。调度器是这个阶段唯一的 host 状态写者。
每轮：处理已选的短 prefill 和 ready decode，然后最多处理一个长
prefill chunk。未结束的长请求排到队尾；下一轮重新收集 ready 集合。

一次 chunk 的边界：

1. 取得 model_mu_。首次 BeginSequence，后续用 seq 的位置继续。
2. 计算 min(max_prefill, 剩余长度)，调用 ModelPrefillTextChunk。
   保持现有首块/末块 logits 策略，不在本阶段改变 head 或 kernel。
3. 非末块检查 stream 完成；末块在锁内读回最后行并检查完成。
   检查失败后标记序列不可继续、服务不健康，禁止重新排队。
4. 释放 model_mu_ 后，在 sched_mu_ 下排回未完成请求，或标 done 并
   唤醒请求线程。不得在持有 sched_mu_ 时等待 GPU/model_mu_。

这里选择现有 stream 同步作为首个完成边界。没有多个 GPU stream
重叠时，不先增加一套 event 对象；以后若引入真正的异步资源回收，再
根据最后消费者增加 event。增加的 chunk 边界同步需经过单流实测，
不能宣称免费，也不把 host 游标等同于设备提交长度。

## 公平性、生命周期与停止

- 保证的是“每轮最多一个长 chunk，然后重新观察 ready 工作”，不是
  固定毫秒延迟。8192-token chunk 本身可耗时数秒，仍可能造成明显
  decode 间隔。chunk 大小优化是后续独立实验。
- 不先实现设备抢占，不在层中间交接，不并发使用共享 scratch。
- 多个长 prefill 使用轮转队列；有 ready decode 时不得连续排完某个
  长 prompt 的所有 chunk。短 prefill 批次预算仍沿用现有容量限制。
- 停机分支必须唤醒所有排队 chunk 请求并返回失败；正在执行的 chunk
  完成或失败后不得重新排入已停止的队列。正常/失败/停止均只能发布
  一次终态。移出队列的指针在完成通知后不再解引用。
- 沿用当前等待期间的客户端断开行为，不承诺立刻取消 GPU 工作。
  更及时的取消需要独立信号和资源回收协议，不能让请求线程直接释放
  仍被调度器持有的 slot/buffer。
- GPU 不健康时未开始的 chunk 直接终止，不继续对错误设备提交新工作。

## 验收计划（未运行）

必要构建后第一项仍为 tools/evalscope HTTP：质量 11 题，再单流
1K/4K/8K/44K/200K 各三次，保持 MTP off、精度、容量与固定输入。
与直接父版及 fcb5925 双参考比较。异常先配对 HTTP，不先 profile。

新调度行为还必须有并发 HTTP 证据，不能用单流门禁替代：

- max_seq=2、max_len=65536，确认服务实际容量未被预算器缩减。
  长请求使用 44K prompt/output=1，期间提交 1K/output=256 的短请求。
  长请求只输出一个 token，避免它加入 decode batch 而把批量数值
  差异混入本次调度对照。父版/候选相同输入和到达顺序各重复三次。
- 记录每条请求的 TTFT、完整输出和完成时间；确认短请求在长 prompt
  尚未全部完成时取得首 token。记录短请求流式停顿，不能只看平均
  decode tps 掩盖每块期间的数秒停顿。
- 并发可改变单条请求的等待时间，不能要求和串行完全相同，也不能
  用短请求改善掩盖整体吞吐下降；分别报告单流门禁、各请求延迟及
  同一工作负载的总完成时间，不编造容忍阈值。
- 两个长请求轮转、停机时排队/执行中请求终止、slot 重用、错误后不
  重新排队需要专门 HTTP 场景。正式性能环境和故障注入环境分开。

只有这些 HTTP 场景通过后，才分析调度顺序/时间线、同步增加与容量。
若正常单流或输出不保持，先修复或回退，不以设计目标覆盖回退。

## 候选实现状态

2026-09-21 基于 af8fad4，实现 ChunkPrefillReq 队列与
RunOnePrefillChunk，每轮选定 decode 完成后处理至多一块，无 decode
的两处提前分支也会推进一次。单流长 prompt 同样走新队列。
每块在 model_mu_ 下检查 stream 完成，末块读回最终行；再在
sched_mu_ 下决定排队或终态，停机分支唤醒所有未完成 chunk 请求。
请求线程借出的输入、序列与 host 输出一直保留到终态。

调度结果已检查读回，请求线程跳过原额外 stream 诊断同步，避免
等待其收到通知后已经提交的另一个请求；内联路径仍保留原检查。
候选零警告构建，正在 `.q4t-work/e2e/prefill-chunk-scheduler-20260921/`
执行首项质量与完整五档 HTTP；之后必须继续上述并发 HTTP 场景，
在这之前不运行 profile 或底层专项，也不宣称公平性已经验证。

首项质量 HTTP 已完成：11/11、输出与父版一致、服务退出 0；候选
二进制 SHA-256 为 `5ed5dcbf72d9afd85ff7c7b85048197be7351c2bc082464cde45cc0220649a52`。
四份源码与冻结副本一致，完整单流五档已启动，尚未执行并发场景或
profile；不能以该正常质量结果替代公平性与停机/失败行为验证。

## 候选单流门禁结果（并发验收仍未完成）

质量 11/11、五档 HTTP/输出 15/15，服务退出 0；对直接父版运行时
和固定参考 fcb5925 均无 TTFT/decode 不利范围分离。保持原始完整
矩阵，不将微小均值差异视为提速。

| 输入 token | TTFT 秒 | decode tok/s |
|---:|---:|---:|
| 1024 | 0.808516 | 18.570500 |
| 4096 | 2.679658 | 17.867359 |
| 8192 | 5.266469 | 18.103178 |
| 45056 | 30.628435 | 17.856423 |
| 204800 | 159.697788 | 17.084318 |

门禁通过后 run_chunk_interleave.py 已启动父版→候选→父版各三轮
实际 evalscope 双客户端对照，实际容量确认 65536×2，MTP off。
以 metrics 请求计数确认长请求进入服务后延迟两秒启动短客户端，
保存实际到达/首内容/结束时间和输出摘要。除了总完成时间，检查
短请求首内容早于长请求首内容；response chunk gap 不冒充 token
时间戳。尚未完成并发、轮转与停机/失败场景，不能接受生产替换，
也未运行 profile 或底层专项。工具代码属于本候选验证部分。

## 并发中间观察与剩余场景

候选前三轮短请求 TTFT 4.67–4.87 秒，前侧父版 24.47–24.76 秒，
候选均在长请求首内容前输出。最大响应 chunk 间隔约 5.75 秒，说明
8192-token chunk 仍造成长停顿；不能将首响应改善表述为平滑 decode。
后侧父版尚在运行，完整并发门禁未作结论，全部原始数据保留。

run_chunk_lifecycle.py 已准备、仅在完整并发对照 accepted 后执行：
两个不同 44K prompt（性能文本 output=1、检索题 output≤32，预期
实际 7 token、完整答案 711146），第二轮反转顺序验证 slot 重用；
另外覆盖两个请求在执行/排队时停机，以及最终 D2H copy/sync 返回
错误后两个请求都失败、slot 全部归还、健康与后续请求 503。
推理请求由 evalscope 发出，错误返回替身只用于独立验证服务。
这些场景仍未完成，不把准备好的脚本视为通过证据。

## 完整并发对照与生命周期中间结果

父版→候选→父版各三轮已完成，全部输入/输出一致。短请求 TTFT
均值依次 24.587/4.748/24.642 秒；长请求 TTFT 均值
30.641/31.579/30.709 秒。总完成时间范围分别
45.131–45.206、45.098–45.246、45.165–45.260 秒，候选与两侧重叠。
这是同一工作量内让短请求更早获得服务，长请求约增加 0.9 秒等待，
并非所有请求延迟同时下降，也不是 GPU 同时执行多个模型 forward。

rotation-reuse 与 shutdown HTTP 已通过：两份不同长输入正常完成，
第二轮反转到达顺序后输出保持、健康且两个 slot 全部归还；停机时
执行/排队请求均失败终止，服务退出 0 且 in-flight=0。copy/sync
错误场景仍在运行，整个阶段尚未接受。自动验证链只在四场景完成后
运行 44K profile，不在失败或未完成时继续底层分析。

## 生命周期完成与时间线核对

四个生命周期 HTTP 场景全部通过，服务均退出 0。copy/sync 错误返回
注入均令执行与排队请求失败，两个 slot 归还、健康检查及后续请求
返回 503。仅覆盖两个文本 slot 与合成返回错误，不代表真实设备故障、
MTP、多模态或任意并发。

全部 HTTP 门禁后采集的 44K 时间线正常退出、输出匹配。父版与候选
块序列均为 [8192,8192,8192,8192,8192,4096]；prefill/decode
全部 kernel 名称与次数相同，分别 537278/436815。阶段窗口为
31223.792→31251.028 ms、14541.384→14582.685 ms；这是单次
插桩数据，不作为正式性能收益或回退结论。prefill 同步 288→293，
对应新增五个中间块完成检查；decode 窗口同步 256→255。

全捕获 cuLaunchKernel 记录 674→615，但关联 GPU Kernel2 的调用
两版均为 576；无关联记录 98→39，所有返回码为零。额外记录来源
尚未确定，不据此宣称减少 kernel 或计算。阶段 API 计数按 host
调用与 GPU 窗口重叠归属，不等价于 kernel 关联计数。

候选二进制、四份冻结源码和全部库摘要一致，核对证据保存为
trace-source-review.json。剩余为验收工具与原始生命周期结果的
最终审查及提交整理；production_accepted 仍为 false。

## 最终接受

完整单流质量 11/11、性能 15/15 与双参考范围检查通过；父版→候选
→父版各三轮共 18 个并发请求的原始响应复核通过；生命周期 10 个
请求及四个服务退出记录复核通过，验收工具与实际执行副本相同。
候选源码/二进制/库冻结一致，44K 时间线通过。final-audit.json 与
acceptance.json 记录接受，固定参考 fcb5925 不变。

接受范围为 MTP off 普通文本 chunk 调度。单流性能持平；固定双请求
中短 TTFT 约 24.6→4.75 秒、长 TTFT 增加约 0.9 秒，总完成时间
与两侧父版范围重叠。8192-token 块仍有约 5.75 秒响应间隔，不
宣称一般吞吐提升、平滑 decode 或所有请求延迟下降。未覆盖真实
GPU 故障、MTP/视觉 chunk 或任意并发数。

后续应核对输出头的实际消费者，先消除中间计算中不被消费的输出，
再以完整 E2E 评估；更小调度粒度会改变形状及带宽摊销，必须独立
验收，不能直接由本阶段推出。完整模型专用执行器仍未完成。
